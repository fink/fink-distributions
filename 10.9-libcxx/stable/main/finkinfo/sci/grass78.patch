diff -ruN grass-7.8.5-orig/aclocal.m4 grass-7.8.5/aclocal.m4
--- grass-7.8.5-orig/aclocal.m4	2020-12-21 13:40:15.000000000 -0500
+++ grass-7.8.5/aclocal.m4	2021-01-11 03:22:14.000000000 -0500
@@ -510,7 +510,7 @@
 	*-apple-darwin*)
 	    SHLIB_CFLAGS="-fno-common"
 	    SHLIB_SUFFIX=".dylib"
-	    SHLIB_LD="${CC} -dynamiclib -compatibility_version \${GRASS_VERSION_MAJOR}.\${GRASS_VERSION_MINOR} -current_version \${GRASS_VERSION_MAJOR}.\${GRASS_VERSION_MINOR} -install_name \${INST_DIR}/lib/lib\${LIB_NAME}\${SHLIB_SUFFIX}"
+	    SHLIB_LD="${CC} -dynamiclib -compatibility_version \${GRASS_VERSION_MAJOR}.\${GRASS_VERSION_MINOR} -current_version \${GRASS_VERSION_MAJOR}.\${GRASS_VERSION_MINOR} -install_name \${INST_DIR}/lib/lib\${LIB_NAME}\${SHLIB_SUFFIX}":${SHLIB}
 	    LD_LIBRARY_PATH_VAR="DYLD_LIBRARY_PATH"
 	    ;;
 	*-sun-solaris*)
diff -ruN grass-7.8.5-orig/gui/wxpython/core/gcmd.py grass-7.8.5/gui/wxpython/core/gcmd.py
--- grass-7.8.5-orig/gui/wxpython/core/gcmd.py	2020-12-21 13:40:15.000000000 -0500
+++ grass-7.8.5/gui/wxpython/core/gcmd.py	2021-01-11 03:21:54.000000000 -0500
@@ -773,9 +773,7 @@
 
     :return: system encoding (can be None)
     """
-    enc = locale.getdefaultlocale()[1]
-    if forceUTF8 and (enc is None or enc == 'UTF8'):
-        return 'UTF-8'
+    return 'UTF-8'
 
     if enc is None:
         enc = locale.getpreferredencoding()
diff -ruN grass-7.8.5-orig/include/Make/Install.make grass-7.8.5/include/Make/Install.make
--- grass-7.8.5-orig/include/Make/Install.make	2020-12-21 13:40:15.000000000 -0500
+++ grass-7.8.5/include/Make/Install.make	2021-01-11 03:21:54.000000000 -0500
@@ -115,11 +115,6 @@
 
 	-$(CHMOD) -R a+rX $(INST_DIR) 2>/dev/null
 
-ifneq ($(findstring darwin,$(ARCH)),)
-	@# enable OSX Help Viewer
-	@/bin/ln -sfh "$(INST_DIR)/docs/html" /Library/Documentation/Help/GRASS-$(GRASS_VERSION_MAJOR).$(GRASS_VERSION_MINOR)
-endif
-
 $(INST_DIR) $(UNIX_BIN):
 	$(MAKE_DIR_CMD) $@
 
diff -ruN grass-7.8.5-orig/include/Make/Platform.make.in grass-7.8.5/include/Make/Platform.make.in
--- grass-7.8.5-orig/include/Make/Platform.make.in	2020-12-21 13:40:15.000000000 -0500
+++ grass-7.8.5/include/Make/Platform.make.in	2021-01-11 03:21:54.000000000 -0500
@@ -51,6 +51,7 @@
 STRIPFLAG           = @STRIPFLAG@
 LD_SEARCH_FLAGS     = @LD_SEARCH_FLAGS@
 LD_LIBRARY_PATH_VAR = @LD_LIBRARY_PATH_VAR@
+GRASS_LD_LIBRARY_PATH_VAR = @LD_LIBRARY_PATH_VAR@
 
 #generate static (ST) or shared (SH)
 GRASS_LIBRARY_TYPE  = @GRASS_LIBRARY_TYPE@
diff -ruN grass-7.8.5-orig/include/Make/Rules.make grass-7.8.5/include/Make/Rules.make
--- grass-7.8.5-orig/include/Make/Rules.make	2020-12-21 13:40:15.000000000 -0500
+++ grass-7.8.5/include/Make/Rules.make	2021-01-11 03:23:41.000000000 -0500
@@ -43,6 +43,7 @@
 	PATH="$(ARCH_DISTDIR)/bin:$(GISBASE)/bin:$(GISBASE)/scripts:$$PATH" \
 	PYTHONPATH="$(GRASS_PYTHONPATH)" \
 	$(LD_LIBRARY_PATH_VAR)="$(BIN):$(GISBASE)/bin:$(GISBASE)/scripts:$(ARCH_LIBDIR):$(BASE_LIBDIR):$($(LD_LIBRARY_PATH_VAR))" \
+	GRASS_LD_LIBRARY_PATH="$(BIN):$(GISBASE)/bin:$(GISBASE)/scripts:$(ARCH_LIBDIR):$(BASE_LIBDIR):$($(LD_LIBRARY_PATH_VAR))" \
 	LC_ALL=C LANG=C LANGUAGE=C \
 	$(1)
 
diff -ruN grass-7.8.5-orig/lib/init/grass.py grass-7.8.5/lib/init/grass.py
--- grass-7.8.5-orig/lib/init/grass.py	2020-12-21 13:40:15.000000000 -0500
+++ grass-7.8.5/lib/init/grass.py	2021-01-11 03:24:34.000000000 -0500
@@ -1277,8 +1277,7 @@
             # sys.exit("Fix system locale settings and then try again.")
             locale.setlocale(locale.LC_ALL, 'C')
             sys.stderr.write("Default locale settings are missing. GRASS running with C locale.\n")
-
-        language, encoding = locale.getdefaultlocale()
+        
         if not language:
             sys.stderr.write("Default locale settings are missing. GRASS running with C locale.\n")
             install_notranslation()
@@ -1295,7 +1294,6 @@
                 # UTF-8 encoding before giving up as on Linux systems
                 # lang.UTF-8 locales are more common than legacy
                 # ISO-8859 ones.
-                encoding = 'UTF-8'
                 normalized = locale.normalize('%s.%s' % (language, encoding))
                 locale.setlocale(locale.LC_ALL, normalized)
             except locale.Error as e:
diff -ruN grass-7.8.5-orig/lib/python/ctypes/loader.py grass-7.8.5/lib/python/ctypes/loader.py
--- grass-7.8.5-orig/lib/python/ctypes/loader.py	2020-12-21 13:40:15.000000000 -0500
+++ grass-7.8.5/lib/python/ctypes/loader.py	2021-01-11 03:21:54.000000000 -0500
@@ -143,7 +143,7 @@
                 '..',
                 'Frameworks'))
 
-        dirs.extend(dyld_fallback_library_path)
+        dirs.extend(_environ_path("GRASS_LD_LIBRARY_PATH"))
 
         return dirs
 
diff -ruN grass-7.8.5-orig/macosx/Makefile grass-7.8.5/macosx/Makefile
--- grass-7.8.5-orig/macosx/Makefile	2020-12-21 13:40:15.000000000 -0500
+++ grass-7.8.5/macosx/Makefile	2021-02-07 11:33:22.000000000 -0500
@@ -20,7 +20,7 @@
 LN = /bin/ln -sf
 LN_DIR = /bin/ln -sfh
 # get python version - use python in path, should be config'd python
-PYVER = `python -V 2>&1 | cut -d \  -f 2 | cut -d . -f 1,2`
+PYVER = `${PYTHON} -V 2>&1 | cut -d \  -f 2 | cut -d . -f 1,2`
 
 # packagemaker location, options and behaviour changed in xcode 3
 ifeq ($(findstring darwin8,$(ARCH)),darwin8)
diff -ruN grass-7.8.5-orig/macosx/app/Makefile grass-7.8.5/macosx/app/Makefile
--- grass-7.8.5-orig/macosx/app/Makefile	2020-12-21 13:40:15.000000000 -0500
+++ grass-7.8.5/macosx/app/Makefile	2021-02-07 11:37:25.000000000 -0500
@@ -26,6 +26,7 @@
 # - the result of `xcrun --show-sdk-path`
 # - check if Command Line Tools' SDKs is present, and use the default version
 # - use the MacOS version of building machine
+# fink solely uses the last of these
 SDK_CMD = ""
 ifeq ($(strip $(MACOSX_SDK)),)
 SDK_CMD = xcrun --show-sdk-path
@@ -47,6 +48,7 @@
 DEPLOYMENT_TARGET_CMD = sw_vers -productVersion | cut -d . -f 1,2
 endif
 
+DEPLOYMENT_TARGET_CMD = sw_vers -productVersion | cut -d . -f 1,2
 DEPLOYMENT_TARGET := $(shell $(DEPLOYMENT_TARGET_CMD))
 
 # ugly hack - assume gdal prefix starts with -L flag
@@ -58,7 +60,7 @@
 PROJ_BIN = `echo "${NAD2BIN}" | sed -e "s,.=,," -e "s,/nad2bin,:,"`
 endif
 # get python version - use python in path, should be config'd python
-PYVER = `python -V 2>&1 | cut -d \  -f 2 | cut -d . -f 1,2`
+PYVER = `${PYTHON} -V 2>&1 | cut -d \  -f 2 | cut -d . -f 1,2`
 ifneq ($(filter ppc64 x86_64,$(MACOSX_ARCHS_WXPYTHON)),)
 WX64BIT = 1
 else
diff -ruN grass-7.8.5-orig/macosx/app/grass.sh.in grass-7.8.5/macosx/app/grass.sh.in
--- grass-7.8.5-orig/macosx/app/grass.sh.in	2020-12-21 13:40:15.000000000 -0500
+++ grass-7.8.5/macosx/app/grass.sh.in	2021-01-11 03:21:54.000000000 -0500
@@ -20,6 +20,9 @@
 
 trap "echo 'User break!' ; exit" 2 3 9 15
 
+LC_ALL=$LANG
+export LC_ALL
+
 # dummy for now - just saying we're starting GRASS.app on OSX
 GRASS_OS_STARTUP="Mac.app"
 export GRASS_OS_STARTUP
@@ -32,11 +35,12 @@
 grass_ver=`cut -d . -f 1-2 "$GISBASE/etc/VERSIONNUMBER"`
 
 #override config dir.
-GRASS_CONFIG_DIR="Library/Preferences/GRASS/$grass_Ver"
+GRASS_CONFIG_DIR="Library/Preferences/GRASS/$grass_ver"
 
 GISBASE_USER="$HOME/Library/GRASS/$grass_ver"
 export GISBASE_USER
-GISBASE_SYSTEM="/Library/GRASS/$grass_ver"
+#GISBASE_SYSTEM="/Library/GRASS/$grass_ver"
+GISBASE_SYSTEM="@PREFIX@/Applications/GRASS@GUI@-$grass_ver.app/Contents/MacOS/lib"
 export GISBASE_SYSTEM
 
 # for extra utils
@@ -142,65 +146,18 @@
 # change GRASS_WXBUNDLED and pyver_want here from bundle.make, or manually
 GRASS_WXBUNDLED=
 export GRASS_WXBUNDLED
-pyexe="pythonw"
+#pyexe="pythonw"
 GRASS_PYTHONWX="$pyexe"
-pyver_want="@PYVER@"
+#pyver_want="@PYVER@"
 GRASS_WX64BIT="@WX64BIT@"
 export GRASS_WX64BIT
-# make sure python version used matches what wxpython wants
-py=""
-# did user set GRASS_PYTHON already? check it (must have pythonw)
-if [ "$GRASS_PYTHON" ] ; then
-	py=`echo "$GRASS_PYTHON" | grep pythonw`
-	if [ "$py" ] ; then
-		case $GRASS_PYTHON in
-			/*) py="$GRASS_PYTHON" ;;
-			*) py=`type -p $GRASS_PYTHON`
-		esac
-		pyver=`$py -V 2>&1 | grep " $pyver_want"`
-		if [ ! "$pyver" ] ; then
-			py=""
-		fi
-	else
-		py=""
-	fi
-fi # GRASS_PYTHON
-# try in shell path
-if [ ! "$py" ] ; then
-	py=`type -p pythonw$pyver_want`
-	if [ "$py" ] ; then
-		pyver=`$py -V 2>&1 | grep " $pyver_want"`
-		if [ ! "$pyver" ] ; then
-			py=""
-		fi
-	fi
-fi
-# try standard framework
-if [ ! "$py" ] ; then
-	py="/Library/Frameworks/Python.framework/Versions/$pyver_want/bin/python"
-	if [ ! -x "$py" ] ; then
-		py=""
-	fi
-fi
-# try system (may be the same as in shell path)
-if [ ! "$py" ] ; then
-	py="/usr/bin/pythonw$pyver_want"
-	pyver=`$py -V 2>&1 | grep " $pyver_want"`
-	if [ ! "$pyver" ] ; then
-		py=""
-	fi
-fi
 
-if [ "$py" ] ; then
-	echo "$pyver found."
-	GRASS_PYTHONWX="$py"
-else
-	echo "Warning: No Python $pyver_want found, needed by wxPython."
-	echo "         The wxPython GUI may not work properly."
-fi
+
+
+GRASS_PYTHONWX="@PREFIX@/bin/python@PY_VER@"
 export GRASS_PYTHONWX
 # we will execute python scripts from the python wrapper script
-GRASS_PYTHON="python"
+GRASS_PYTHON="@PREFIX@/bin/python@PY_VER@"
 export GRASS_PYTHON
 
 # if grassrc has text startup, switch back to Terminal (gotta duplicate some init.sh stuff)
