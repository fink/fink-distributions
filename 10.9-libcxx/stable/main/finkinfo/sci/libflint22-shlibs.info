Info4: <<
Package: libflint22-shlibs
Version: 3.4.0
Revision: 1
Maintainer: None <fink-devel@lists.sourceforge.net>

BuildDepends: <<
	gmp5 (>= 6.2.1), 
	libmpfr6 (>= 4.0.1),
	libntl44,
	fink-package-precedence
<<
Depends: <<
	gmp5-shlibs (>= 6.2.1),
	libmpfr6-shlibs (>= 4.0.1),
	libntl44-shlibs
<<

Source: https://flintlib.org/download/flint-%v.tar.gz
Source-Checksum: SHA256(9497679804dead926e3affeb8d4c58739d1c7684d60c2c12827550d28e454a33)

ConfigureParams: <<
	--with-gmp=%p \
	--with-mpfr=%p \
	--with-ntl=%p \
	--enable-dependency-tracking
<<
CompileScript: <<
#!/bin/sh -ev
	./configure %c
	make -w V=1
	fink-package-precedence --depfile-ext='\.d' .
<<
InstallScript: <<
#!/bin/sh -ev
	make install DESTDIR=%d
<<

InfoTest: TestScript: make check || exit 2

# yes, i_n has a micro version :(
Shlibs: <<
	%p/%lib/libflint.22.0.dylib 22.0.0 %n (>= 3.4.0-1)
<<

DocFiles: AUTHORS COPYING README.md TODO.md

SplitOff: <<
	Package: libflint22-dev
	Depends: <<
		%N (= %v-%r)
	<<
	BuildDependsOnly: true
	Conflicts: flint2.4, flint2.5, libflint22-dev
	Replaces: flint2.4, flint2.5, libflint22-dev
	Files: <<
		include
		lib/pkgconfig
		lib/libflint.dylib
		lib/libflint.22.dylib
	<<
	DocFiles: AUTHORS COPYING README.md TODO.md
<<

#
Description: Library for doing number theory
DescDetail: <<
FLINT is a C library for doing number theory. It supports arithmetic
with numbers, polynomials, power series and matrices over many base
rings, including:

* Multiprecision integers and rationals
* Integers modulo n
* p-adic numbers
* Finite fields (prime and non-prime order)
* Real and complex numbers (via the Arb extension library)
<<
# TODO: Make an -mpir variant?
DescPackaging: <<
 According to the flint authors, the binary interface *should* not
 change between minor relases, but it still *might*. Hence the name of
 this package includes the full MAJOR.MINOR version (but not the
 patchlevel).
<<
License: GPL
Homepage: https://www.flintlib.org/
<<
