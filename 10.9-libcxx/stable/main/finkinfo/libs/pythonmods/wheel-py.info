# -*- coding: ascii; tab-width: 4 -*-
Info4: <<
Package: wheel-py%type_pkg[python]
# 0.46.3 needs flit-core (>= 3.11) and is py39+
Version: 0.45.1
Revision: 1
Type: python (3.8 3.9 3.10)

Description: Built-package format for Python
Maintainer: Daniel Johnson <daniel@daniel-johnson.org>
License: BSD
Homepage: https://github.com/pypa/wheel

Source: https://files.pythonhosted.org/packages/source/w/wheel/wheel-%v.tar.gz
Source-Checksum: SHA256(661e1abd9198507b1409a20c02106d9670b2576e916d58f520316666abca6729)

Depends: python%type_pkg[python]
BuildDepends: <<
	fink (>= 0.32),
	bootstrap-modules-py%type_pkg[python] (>= 20260125),
	flit-core-py%type_pkg[python] (>= 3.8)
<<
CompileScript: <<
	PYTHONPATH=%p/share/bootstrap-modules-python%type_pkg[python] %p/bin/python%type_raw[python] -m build --wheel --no-isolation --skip-dependency-check
<<

InfoTest: <<
	TestDepends: <<
		pytest-py%type_pkg[python],
		pytest-cov-py%type_pkg[python],
		setuptools-tng-py%type_pkg[python] (>= 65)
	<<
	TestScript: <<
		# TestGetPlatformMacosx checks for system-versioned dylibs that only go up to macOS 11
		PYTHONPATH=%p/share/bootstrap-modules-python%type_pkg[python] %p/bin/python%type_raw[python] -m installer --destdir %b/tempdist dist/*.whl
		PYTHONPATH=%b/tempdist/%p/lib/python%type_raw[python]/site-packages %p/bin/python%type_raw[python] -b -m pytest -k 'not (TestGetPlatformMacosx)' -vv || exit 2
	<<
<<

InstallScript: <<
	PYTHONPATH=%p/share/bootstrap-modules-python%type_pkg[python] %p/bin/python%type_raw[python] -m installer --destdir %d dist/*.whl
	mv %i/bin/wheel %i/bin/wheel-%type_pkg[python]
<<

PostInstScript: <<
	update-alternatives --install %p/bin/wheel wheel %p/bin/wheel-%type_pkg[python] %type_pkg[python]
<<

PreRmScript: <<
	if [ $1 != "upgrade" ]; then
		update-alternatives --remove wheel %p/bin/wheel-%type_pkg[python]
	fi
<<

DocFiles: LICENSE.txt README.rst
DescPackaging: <<
* Explicit BDep on flit-core >= v3.8 (via bootstrap-modules 20241126) to deal with recursive globbing.
<<
# Info4
<<
