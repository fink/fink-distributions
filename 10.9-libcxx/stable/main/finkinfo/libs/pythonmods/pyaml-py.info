Info2: <<
Package: pyaml-py%type_pkg[python]
Version: 25.7.0
Revision: 1
Description: PyYAML-based module to produce YAML data
Type: python (3.8 3.9 3.10)
License: BSD
# Free to update and take over
Maintainer: Hanspeter Niederstrasser <nieder@users.sourceforge.net>

Source: https://files.pythonhosted.org/packages/source/p/pyaml/pyaml-%v.tar.gz
Source-Checksum: SHA256(e113a64ec16881bf2b092e2beb84b7dcf1bd98096ad17f5f14e8fb782a75d99b)

Depends: <<
	python%type_pkg[python],
	unidecode-py%type_pkg[python],
	yaml-py%type_pkg[python]
<<
BuildDepends: <<
	bootstrap-modules-py%type_pkg[python],
	setuptools-tng-py%type_pkg[python] (>= 61)
<<
CompileScript: <<
	PYTHONPATH=%p/share/bootstrap-modules-python%type_pkg[python] %p/bin/python%type_raw[python] -m build --wheel --no-isolation --skip-dependency-check
<<
InfoTest: <<
	TestScript: <<
		%p/bin/python%type_raw[python] -m unittest -vv || exit 2
	<<
<<
InstallScript: <<
	#!/bin/sh -ev
	PYTHONPATH=%p/share/bootstrap-modules-python%type_pkg[python] %p/bin/python%type_raw[python] -m installer --destdir %d dist/*.whl
        for i in pyaml; do
                mv %i/bin/$i %i/bin/$i-py%type_pkg[python]
        done
<<
PostInstScript: <<
  update-alternatives --verbose --install "%p/bin/pyaml" pyaml "%p/bin/pyaml-py%type_pkg[python]" %type_pkg[python]
<<

PreRmScript: <<
  if [ $1 != "upgrade" ]; then
    update-alternatives --verbose --remove pyaml "%p/bin/pyaml-py%type_pkg[python]"
  fi
<<

DocFiles: COPYING README.rst
Homepage: https://github.com/mk-fg/pretty-yaml
DescDetail: <<
PyYAML-based python module to produce pretty and readable 
YAML-serialized data.
This module is for serialization only, see ruamel.yaml module for 
literate YAML parsing (keeping track of comments, spacing, line/column 
numbers of values, etc).
<<
<<
