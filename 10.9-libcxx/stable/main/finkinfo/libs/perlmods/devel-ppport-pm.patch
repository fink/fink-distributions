diff -Nurd Devel-PPPort-3.68.orig/t/ppphtest.t Devel-PPPort-3.68/t/ppphtest.t
--- Devel-PPPort-3.68.orig/t/ppphtest.t	2022-03-18 18:05:13
+++ Devel-PPPort-3.68/t/ppphtest.t	2025-09-30 21:17:16
@@ -126,7 +126,7 @@
 {
   my @args = ('ppport.h', @_);
   unshift @args, $inc if $inc;
-  my $run = $perl =~ m/\s/ ? qq("$perl") : $perl;
+  my $run = $perl;
   $run .= ' -MMac::err=unix' if $isMAC;
   for (@args) {
     $_ = qq("$_") if $isVMS && /^[^"]/;
@@ -218,6 +218,12 @@
 
 sub find_perl
 {
+  # Fink hack: There's lots of logic in this sub to protect against
+  # special chars and whitespace in pathname, but we already can't
+  # cope with whitespace in pathname because we have a multi-word
+  # command. So we just overload it from the .info driver.
+  return $ENV{FULLPERL};
+  # End of fink hack.
   my $perl = $^X;
 
   return $perl if $isVMS;
