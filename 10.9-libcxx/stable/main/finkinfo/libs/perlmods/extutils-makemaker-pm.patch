diff -Nurd ExtUtils-MakeMaker-7.76.orig/lib/ExtUtils/MM_Any.pm ExtUtils-MakeMaker-7.76/lib/ExtUtils/MM_Any.pm
--- ExtUtils-MakeMaker-7.76.orig/lib/ExtUtils/MM_Any.pm	2025-05-23 15:13:48
+++ ExtUtils-MakeMaker-7.76/lib/ExtUtils/MM_Any.pm	2025-12-20 22:36:42
@@ -1915,7 +1915,7 @@
 sub init_INST {
     my($self) = shift;
 
-    $self->{INST_ARCHLIB} ||= $self->catdir($Curdir,"blib","arch");
+    $self->{INST_ARCHLIB} ||= $self->catdir(File::Spec->rel2abs($Curdir),"blib","arch");
     $self->{INST_BIN}     ||= $self->catdir($Curdir,'blib','bin');
 
     # INST_LIB typically pre-set if building an extension after
diff -Nurd ExtUtils-MakeMaker-7.76.orig/lib/ExtUtils/MakeMaker.pm ExtUtils-MakeMaker-7.76/lib/ExtUtils/MakeMaker.pm
--- ExtUtils-MakeMaker-7.76.orig/lib/ExtUtils/MakeMaker.pm	2025-05-23 15:13:48
+++ ExtUtils-MakeMaker-7.76/lib/ExtUtils/MakeMaker.pm	2025-12-20 22:36:42
@@ -234,8 +234,8 @@
 
 sub eval_in_subdirs {
     my($self) = @_;
-    use Cwd qw(cwd abs_path);
-    my $pwd = cwd() || die "Can't figure out your cwd!";
+    use Cwd qw(getcwd abs_path);
+    my $pwd = getcwd() || die "Can't figure out your cwd!";
 
     local @INC = map eval {abs_path($_) if -e} || $_, @INC;
     push @INC, '.';     # '.' has to always be at the end of @INC
@@ -405,6 +405,11 @@
         $Recognized_Att_Keys{$item} = 1;
     }
     foreach my $item (@Get_from_Config) {
+	if ($item eq 'lddlflags' && '@PREFIX@' ne '/usr/local') {
+	    $Config{$item} =~ s,-L/usr/local/lib,,;
+	    print "# removed -L/usr/local/lib from '\U$item\E'\n"
+		if ($Verbose >= 2);
+	}
         $Recognized_Att_Keys{uc $item} = $Config{$item};
         print "Attribute '\U$item\E' => '$Config{$item}'\n"
             if ($Verbose >= 2);
diff -Nurd ExtUtils-MakeMaker-7.76.orig/t/INST.t ExtUtils-MakeMaker-7.76/t/INST.t
--- ExtUtils-MakeMaker-7.76.orig/t/INST.t	2022-12-23 19:09:47
+++ ExtUtils-MakeMaker-7.76/t/INST.t	2025-12-20 22:36:42
@@ -95,7 +95,7 @@
 # INST_*
 is( $mm->{INST_ARCHLIB},
     $mm->{PERL_CORE} ? $mm->{PERL_ARCHLIB}
-                     : File::Spec->catdir($Curdir, 'blib', 'arch'),
+                     : File::Spec->catdir(File::Spec->rel2abs($Curdir), 'blib', 'arch'),
                                      'INST_ARCHLIB');
 is( $mm->{INST_BIN},     File::Spec->catdir($Curdir, 'blib', 'bin'),
                                      'INST_BIN' );
@@ -109,7 +109,7 @@
 my $normalize = $^O =~ /android/ ? \&Cwd::realpath : sub {shift};
 is( $normalize->($c_mm->{INST_ARCHLIB}),
     $normalize->($c_mm->{PERL_CORE} ? $c_mm->{PERL_ARCHLIB}
-                       : File::Spec->catdir($Updir, 'blib', 'arch')),
+                       : File::Spec->catdir(File::Spec->rel2abs($Curdir), 'blib', 'arch')),
                                      'CHILD INST_ARCHLIB');
 is( $c_mm->{INST_BIN},     File::Spec->catdir($Updir, 'blib', 'bin'),
                                      'CHILD INST_BIN' );
diff -Nurd ExtUtils-MakeMaker-7.76.orig/t/lib/MakeMaker/Test/Setup/XS.pm ExtUtils-MakeMaker-7.76/t/lib/MakeMaker/Test/Setup/XS.pm
--- ExtUtils-MakeMaker-7.76.orig/t/lib/MakeMaker/Test/Setup/XS.pm	2025-03-30 06:36:58
+++ ExtUtils-MakeMaker-7.76/t/lib/MakeMaker/Test/Setup/XS.pm	2025-12-22 03:55:26
@@ -244,7 +244,7 @@
     my $t = $PM_TEST; $t =~ s:is_even:is_odd:g;
     $t =~ s/bootstrap/
       use File::Spec::Functions;
-      my \$root; BEGIN {\$root = catdir(updir, qw(XS-Testeud_produce blib));}
+      my \$root; BEGIN {\$root = File::Spec->rel2abs(catdir(updir, qw(XS-Testeud_produce blib)));}
       use lib map catdir(\$root, \$_), qw(lib arch);
       require XS::Other;\n$&
     /g;
