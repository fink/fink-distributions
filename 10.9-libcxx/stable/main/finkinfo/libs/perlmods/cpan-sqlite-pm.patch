diff -Nurd CPAN-SQLite-0.220.orig/lib/CPAN/SQLite/META.pm CPAN-SQLite-0.220/lib/CPAN/SQLite/META.pm
--- CPAN-SQLite-0.220.orig/lib/CPAN/SQLite/META.pm	2022-10-29 02:34:49
+++ CPAN-SQLite-0.220/lib/CPAN/SQLite/META.pm	2025-10-02 00:08:46
@@ -289,7 +289,9 @@
     $CPAN::FrontEnd->mywarn('Database locked - cannot update.');
     return;
   }
-  my @args = ($^X, '-MCPAN::SQLite::META=setup,update,check', '-e');
+  my @interp = split " ", $ENV{FULLPERL};
+  map {s/"//g} @interp;
+  my @args = (@interp, '-MCPAN::SQLite::META=setup,update', '-e');
   if (-e $db && -s _) {
     my $mtime_db    = (stat(_))[9];
     my $time_string = gmtime_string($mtime_db);
@@ -315,7 +317,7 @@
     $CPAN::SQLite::DBI::dbh->disconnect();
     $CPAN::SQLite::DBI::dbh = undef;
   }
-  system(@args) == 0 or die qq{system @args failed: $?};
+  system(@args) == 0 or die "system ",(join " ", @args)," failed: $?";
   $CPAN::FrontEnd->myprint("Done!\n");
   return 1;
 }
