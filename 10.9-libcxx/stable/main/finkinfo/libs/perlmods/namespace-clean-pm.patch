diff -Nurd namespace-clean-0.27.orig/t/10-pure-perl.t namespace-clean-0.27/t/10-pure-perl.t
--- namespace-clean-0.27.orig/t/10-pure-perl.t	2016-04-09 23:38:56
+++ namespace-clean-0.27/t/10-pure-perl.t	2025-09-30 15:40:02
@@ -46,17 +46,18 @@
 
 # for the $^X-es
 $ENV{PERL5LIB} = join ($Config{path_sep}, @INC);
-$ENV{PATH} = '';
 
 
 # rerun the tests under the assumption of pure-perl
 my $this_file = abs_path(__FILE__);
 
+my @interp = split " ", $ENV{FULLPERL};
+map {s/"//g} @interp;
 for my $fn ( bsd_glob("t/*.t") ) {
 
   next if abs_path($fn) eq $this_file;
 
-  my @cmd = map { $_ =~ /(.+)/ } ($^X, $fn);
+  my @cmd = map { $_ =~ /(.+)/ } (@interp, $fn);
 
   # this is cheating, and may even hang here and there (testing on windows passed fine)
   # if it does - will have to fix it somehow (really *REALLY* don't want to pull
