diff -Nurd Class-Accessor-Installer-1.100880.orig/t/00-compile.t Class-Accessor-Installer-1.100880/t/00-compile.t
--- Class-Accessor-Installer-1.100880.orig/t/00-compile.t	2010-03-29 13:46:00
+++ Class-Accessor-Installer-1.100880/t/00-compile.t	2025-09-30 09:26:43
@@ -29,7 +29,7 @@
     # fake home for cpan-testers
     # no fake requested ## local $ENV{HOME} = tempdir( CLEANUP => 1 );
 
-    is( qx{ $^X -Ilib -e "use $_; print '$_ ok'" }, "$_ ok", "$_ loaded ok" )
+    is( qx{ $ENV{FULLPERL} -Ilib -e "use $_; print '$_ ok'" }, "$_ ok", "$_ loaded ok" )
         for sort @modules;
 
     SKIP: {
