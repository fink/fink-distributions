diff -Nurd HTML-Tidy-1.60.orig/Makefile.PL HTML-Tidy-1.60/Makefile.PL
--- HTML-Tidy-1.60.orig/Makefile.PL	2017-09-13 11:29:15
+++ HTML-Tidy-1.60/Makefile.PL	2026-01-07 02:59:05
@@ -9,19 +9,12 @@
 use ExtUtils::Liblist;
 use Config;
 
-my $libs = '-ltidyp';
-my $inc = "-I. -I/usr/include/tidyp -I/usr/local/include/tidyp -I$Config{usrinc}/tidyp";
+my $libs = '-ltidy';
+my $inc = "-I. -I@FINKPREFIX@/include -DWITH_TIDY";
 
-eval { require Alien::Tidyp; };
-
-if ( !$@ ) {
-    print "Using tidyp via Alien::Tidyp\n";
-    $libs = Alien::Tidyp->config('LIBS');
-    $inc = Alien::Tidyp->config('INC');
-}
-else {
+    print "Forcing non-detection of Alien::Tidyp.\n";
     print "Alien::Tidyp not found. Looking for for tidyp on your system.\n";
-    my @vars = ExtUtils::Liblist->ext( '-L/usr/lib -L/usr/local/lib -ltidyp', 0, 1 );
+    my @vars = ExtUtils::Liblist->ext( '-L@FINKPREFIX@/lib -ltidy', 0, 1 );
     $libs = $vars[2];
 
     if ( !$libs ) {
@@ -41,7 +34,6 @@
 
 EOF
     }
-}
 
 eval { require LWP::Simple; };
 
