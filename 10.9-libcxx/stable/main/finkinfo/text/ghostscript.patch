diff -Nurd ghostscript-9.52-orig/base/sjpx_openjpeg.c ghostscript-9.52/base/sjpx_openjpeg.c
--- ghostscript-9.52-orig/base/sjpx_openjpeg.c	2020-03-19 08:21:42.000000000 +0000
+++ ghostscript-9.52/base/sjpx_openjpeg.c	2020-09-07 16:37:45.000000000 +0100
@@ -25,7 +25,6 @@
 #include "gxsync.h"
 #include "assert_.h"
 #if !defined(SHARE_JPX) || (SHARE_JPX == 0)
-#include "opj_malloc.h"
 #endif
 /* Some locking to get around the criminal lack of context
  * in the openjpeg library. */
diff -Nurd ghostscript-9.52-orig/base/stdpre.h ghostscript-9.52/base/stdpre.h
--- ghostscript-9.52-orig/base/stdpre.h	2020-03-19 08:21:42.000000000 +0000
+++ ghostscript-9.52/base/stdpre.h	2020-09-07 16:41:10.000000000 +0100
@@ -314,7 +314,9 @@
  * header file that includes sys/types.h.
  *
  */
-#define bool bool_		/* (maybe not needed) */
+#if !defined(bool)
+#  define bool bool_		/* (maybe not needed) */
+#endif
 #define uchar uchar_
 #define uint uint_
 #define ushort ushort_
diff -Nurd ghostscript-9.52-orig/base/unistd_.h ghostscript-9.52/base/unistd_.h
--- ghostscript-9.52-orig/base/unistd_.h	2020-03-19 08:21:42.000000000 +0000
+++ ghostscript-9.52/base/unistd_.h	2020-09-07 16:40:47.000000000 +0100
@@ -52,7 +52,9 @@
    /* _XOPEN_SOURCE 500 define is needed to get
     * access to pread and pwrite */
 #  define _XOPEN_SOURCE 500
-#  define __USE_UNIX98
+#  if !defined(__USE_UNIX98)
+#    define __USE_UNIX98
+#  endif
 #  include <unistd.h>
 #endif
 
diff -Nurd ghostscript-9.52-orig/configure ghostscript-9.52/configure
--- ghostscript-9.52-orig/configure	2020-03-19 08:22:25.000000000 +0000
+++ ghostscript-9.52/configure	2020-09-07 16:42:13.000000000 +0100
@@ -7568,7 +7568,7 @@
 $as_echo_n "checking for local zlib source... " >&6; }
 # we must define ZLIBDIR regardless because png.mak does a -I$(ZLIBDIR)
 # this seems a harmless default
-ZLIBDIR=src
+ZLIBDIR=$oldincludedir
 AUX_SHARED_ZLIB=
 ZLIBCFLAGS=""
 
diff -Nurd ghostscript-9.52-orig/configure.ac ghostscript-9.52/configure.ac
--- ghostscript-9.52-orig/configure.ac	2020-03-19 08:21:42.000000000 +0000
+++ ghostscript-9.52/configure.ac	2020-09-07 16:42:39.000000000 +0100
@@ -1182,7 +1182,7 @@
 dnl zlib is needed for language level 3, and libpng
 # we must define ZLIBDIR regardless because png.mak does a -I$(ZLIBDIR)
 # this seems a harmless default
-ZLIBDIR=src
+ZLIBDIR=$oldincludedir
 AUX_SHARED_ZLIB=
 ZLIBCFLAGS=""
 
diff -Nurd ghostscript-9.52-orig/devices/gdevpng.c ghostscript-9.52/devices/gdevpng.c
--- ghostscript-9.52-orig/devices/gdevpng.c	2020-03-19 08:21:42.000000000 +0000
+++ ghostscript-9.52/devices/gdevpng.c	2020-09-07 16:48:44.000000000 +0100
@@ -751,6 +751,7 @@
 
 #if PNG_LIBPNG_VER_MINOR < 5
 
+#if 0 /* not required in 1.5 */
 /*
  * Patch around a static reference to a never-used procedure.
  * This could be avoided if we were willing to edit pngconf.h to
@@ -771,6 +772,7 @@
 }
 #endif
 #endif
+#endif
 
 static int
 pngalpha_open(gx_device * pdev)
