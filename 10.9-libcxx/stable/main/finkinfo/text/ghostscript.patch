diff -uNr ghostscript-9.19.orig/base/mkromfs.c ghostscript-9.19/base/mkromfs.c
--- ghostscript-9.19.orig/base/mkromfs.c	2016-03-23 04:22:48.000000000 -0400
+++ ghostscript-9.19/base/mkromfs.c	2016-03-30 19:29:54.000000000 -0400
@@ -2324,7 +2324,7 @@
 #endif
     fprintf(out,"\n#include \"stdint_.h\"\n");
     fprintf(out,"\n#include \"time_.h\"\n\n");
-    fprintf(out,"    time_t gs_romfs_buildtime = %ld;\n\n", time(NULL));
+    fprintf(out,"    time_t gs_romfs_buildtime = %lld;\n\n", time(NULL));
 
     /* process the remaining arguments (options interspersed with paths) */
     for (; atarg < argc; atarg++) {
diff -uNr ghostscript-9.19.orig/base/stdpre.h ghostscript-9.19/base/stdpre.h
--- ghostscript-9.19.orig/base/stdpre.h	2016-03-23 04:22:48.000000000 -0400
+++ ghostscript-9.19/base/stdpre.h	2016-03-30 19:50:15.000000000 -0400
@@ -268,7 +268,9 @@
  * header file that includes sys/types.h.
  *
  */
-#define bool bool_		/* (maybe not needed) */
+#if !defined(bool)
+#    define bool bool_		/* (maybe not needed) */
+#endif
 #define uchar uchar_
 #define uint uint_
 #define ushort ushort_
diff -uNr ghostscript-9.19.orig/base/unistd_.h ghostscript-9.19/base/unistd_.h
--- ghostscript-9.19.orig/base/unistd_.h	2016-03-23 04:22:48.000000000 -0400
+++ ghostscript-9.19/base/unistd_.h	2016-03-30 17:09:01.000000000 -0400
@@ -53,7 +53,9 @@
    /* _XOPEN_SOURCE 500 define is needed to get
     * access to pread and pwrite */
 #  define _XOPEN_SOURCE 500
-#  define __USE_UNIX98
+#if !defined(__USE_UNIX98)
+#    define __USE_UNIX98
+# endif
 #  include <unistd.h>
 #endif
 
diff -uNr ghostscript-9.19.orig/configure ghostscript-9.19/configure
--- ghostscript-9.19.orig/configure	2016-03-23 04:37:09.000000000 -0400
+++ ghostscript-9.19/configure	2016-03-30 19:31:12.000000000 -0400
@@ -6740,7 +6740,7 @@
 $as_echo_n "checking for local zlib source... " >&6; }
 # we must define ZLIBDIR regardless because png.mak does a -I$(ZLIBDIR)
 # this seems a harmless default
-ZLIBDIR=src
+ZLIBDIR=$oldincludedir
 AUX_SHARED_ZLIB=
 
 if test -d $srcdir/zlib; then
diff -uNr ghostscript-9.19.orig/configure.ac ghostscript-9.19/configure.ac
--- ghostscript-9.19.orig/configure.ac	2016-03-23 04:22:48.000000000 -0400
+++ ghostscript-9.19/configure.ac	2016-03-30 19:31:24.000000000 -0400
@@ -892,7 +892,7 @@
 dnl zlib is needed for language level 3, and libpng
 # we must define ZLIBDIR regardless because png.mak does a -I$(ZLIBDIR)
 # this seems a harmless default
-ZLIBDIR=src
+ZLIBDIR=$oldincludedir
 AUX_SHARED_ZLIB=
 
 if test -d $srcdir/zlib; then
diff -uNr ghostscript-9.19.orig/contrib/gdevhl12.c ghostscript-9.19/contrib/gdevhl12.c
--- ghostscript-9.19.orig/contrib/gdevhl12.c	2016-03-23 04:22:48.000000000 -0400
+++ ghostscript-9.19/contrib/gdevhl12.c	2016-03-30 19:32:08.000000000 -0400
@@ -481,7 +481,7 @@
         break;
     }
     if (s->out_count) {
-        fprintf(prn_stream, "\033*b%uW", s->out_count * sizeof(u16) + 9);
+        fprintf(prn_stream, "\033*b%luW", s->out_count * sizeof(u16) + 9);
         put_be16(prn_stream, s->out_count * sizeof(u16) + 7);
         put_be16(prn_stream, s->xl * 16);
         put_be16(prn_stream, band + ytop);
diff -uNr ghostscript-9.19.orig/contrib/japanese/gdevp201.c ghostscript-9.19/contrib/japanese/gdevp201.c
--- ghostscript-9.19.orig/contrib/japanese/gdevp201.c	2016-03-23 04:22:48.000000000 -0400
+++ ghostscript-9.19/contrib/japanese/gdevp201.c	2016-03-30 19:42:38.000000000 -0400
@@ -241,7 +241,7 @@
                 out_beg -= (out_beg - out) % bytes_per_column;
 
                 /* Dot addressing */
-                fprintf(pdev->file, "\033F%04d",
+                fprintf(pdev->file, "\033F%04ld",
                         (out_beg - out) / bytes_per_column);
 
                 /* Dot graphics */
diff -uNr ghostscript-9.19.orig/devices/gdevlp8k.c ghostscript-9.19/devices/gdevlp8k.c
--- ghostscript-9.19.orig/devices/gdevlp8k.c	2016-03-23 04:22:48.000000000 -0400
+++ ghostscript-9.19/devices/gdevlp8k.c	2016-03-30 19:33:04.000000000 -0400
@@ -358,8 +358,8 @@
         fwrite("\035",1,1,prn_stream);
         fprintf(prn_stream,"%d",lnum-60);
         fwrite("Y\035",1,2,prn_stream);
-        fprintf(prn_stream,"%d;",(outp - out));
-        fprintf(prn_stream,"%d;",(in_end - inp) << 3);
+        fprintf(prn_stream,"%ld;",(outp - out));
+        fprintf(prn_stream,"%ld;",(in_end - inp) << 3);
         fwrite("1;0bi{I",1,7,prn_stream);
         fwrite(out,1,(outp - out),prn_stream);
 
diff -uNr ghostscript-9.19.orig/devices/gdevpng.c ghostscript-9.19/devices/gdevpng.c
--- ghostscript-9.19.orig/devices/gdevpng.c	2016-03-23 04:22:48.000000000 -0400
+++ ghostscript-9.19/devices/gdevpng.c	2016-03-30 19:34:22.000000000 -0400
@@ -711,6 +711,7 @@
 
 #if PNG_LIBPNG_VER_MINOR < 5
 
+#if 0 /* not required in 1.5 */
 /*
  * Patch around a static reference to a never-used procedure.
  * This could be avoided if we were willing to edit pngconf.h to
@@ -731,6 +732,7 @@
 }
 #endif
 #endif
+#endif
 
 static int
 pngalpha_open(gx_device * pdev)
