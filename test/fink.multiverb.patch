diff -ruN fink.orig/fink.in fink/fink.in
--- fink.orig/fink.in	Tue Jul 16 10:40:34 2002
+++ fink/fink.in	Tue Jul 16 11:21:59 2002
@@ -38,8 +38,8 @@
 
 Getopt::Long::Configure(qw(bundling ignore_case require_order));
 GetOptions('yes|y' => \$options{dontask},
-	   'quiet|q' => sub { $options{verbosity}-- },
-	   'verbose|v' => sub { $options{verbosity}++ },
+	   'quiet|q' => sub { $options{verbosity} = -1 },
+	   'verbose|v' => sub { $options{verbosity} = 3 },
 #	   'interactive|i' => \$options{interactive},
 	   'version|V' => \&show_version,
 	   'help|h|?' => \&show_help)
diff -ruN fink.orig/perlmod/Fink/Config.pm fink/perlmod/Fink/Config.pm
--- fink.orig/perlmod/Fink/Config.pm	Tue Jul 16 10:40:33 2002
+++ fink/perlmod/Fink/Config.pm	Tue Jul 16 11:26:44 2002
@@ -36,7 +36,7 @@
   @ISA         = qw(Exporter Fink::Base);
   @EXPORT      = qw();
   @EXPORT_OK   = qw($config $basepath $libpath $debarch $darwin_version $macosx_version
-                    &get_option &set_options &is_verbose);
+                    &get_option &set_options &verbose_level);
   %EXPORT_TAGS = ( );   # eg: TAG => [ qw!name1 name2! ],
 }
 our @EXPORT_OK;
@@ -229,17 +229,30 @@
   return $default;
 }
 
-### determine whether verbose mode is on or off
+### determine the current verbosity level. This is affected by the
+### --verbose and --quiet command line options as well as by the
+### "Verbose" setting in fink.conf
 
-sub is_verbose {
-  my $verbosity;
+sub verbose_level {
+  my ($verbosity, $verblevel);
 
+  $verblevel = $config->param("Verbose");
   $verbosity = get_option("verbosity");
-  
-  if ($verbosity != 0) {
-    return ($verbosity>0)
+ 
+  if ($verbosity != -1 && ($verbosity == 3 || $verblevel eq "3" || $verblevel eq "true" || $verblevel eq "high")) {
+    ### Sets Verbose mode to Full
+    $verbosity = 3;
+  } elsif ($verbosity != -1 && $verblevel eq "2" || $verblevel eq "medium") {
+    ### Sets Verbose mode to download and tarballs
+    $verbosity = 2;
+  } elsif ($verbosity != -1 && $verblevel eq "1" || $verblevel eq "low") {
+    ### Sets Verbose mode to download
+    $verbosity = 1;
+  } else {
+    ### Sets Verbose mode to none
+    $verbosity = 0;
   }
-  return $config->param_boolean("Verbose");
+  return $verbosity;
 }
 
 
diff -ruN fink.orig/perlmod/Fink/Configure.pm fink/perlmod/Fink/Configure.pm
--- fink.orig/perlmod/Fink/Configure.pm	Tue Jul 16 10:40:33 2002
+++ fink/perlmod/Fink/Configure.pm	Tue Jul 16 11:24:14 2002
@@ -65,10 +65,13 @@
   }
 
   $verbose =
-    &prompt_boolean("Always print verbose messages?",
-		    $config->param_boolean("Verbose"));
-  $config->set_param("Verbose", $verbose ? "true" : "false");
-
+    &prompt_selection("How verbose should Fink be?", 3, # Default option at index 4 -> "Low"
+                     { 3 => "High (shows everything)",
+                       2 => "Medium (shows almost everything)",
+                       1 => "Low (don't show tarballs being expanded)",
+                       0 => "Quiet (don't show download stats)" },
+                     3, 2, 1, 0);
+  $config->set_param("Verbose", $verbose);
 
   # proxy settings
   print "\n";
diff -ruN fink.orig/perlmod/Fink/Engine.pm fink/perlmod/Fink/Engine.pm
--- fink.orig/perlmod/Fink/Engine.pm	Tue Jul 16 10:40:33 2002
+++ fink/perlmod/Fink/Engine.pm	Tue Jul 16 11:27:10 2002
@@ -179,10 +179,10 @@
   if (Fink::Config::get_option("dontask")) {
     $cmd .= " --yes";
   }
-  if (Fink::Config::get_option("verbosity")>0) {
+  if (Fink::Config::get_option("verbosity") == 3) {
     $cmd .= " --verbose";
   }
-  elsif (Fink::Config::get_option("verbosity")<0) {
+  elsif (Fink::Config::get_option("verbosity") == -1) {
     $cmd .= " --quiet";
   }
   if (Fink::Config::get_option("interactive")) {
@@ -756,7 +756,7 @@
   my ($answer, $s);
   my (%already_rebuilt, %already_activated);
 
-  if (Fink::Config::is_verbose()) {
+  if (Fink::Config::verbose_level() > -1) {
     $showlist = 1;
   }
 
diff -ruN fink.orig/perlmod/Fink/NetAccess.pm fink/perlmod/Fink/NetAccess.pm
--- fink.orig/perlmod/Fink/NetAccess.pm	Tue Jul 16 10:40:33 2002
+++ fink/perlmod/Fink/NetAccess.pm	Tue Jul 16 11:30:03 2002
@@ -214,7 +214,7 @@
   # check if we have curl
   if (-x "$basepath/bin/curl" or -x "/usr/bin/curl") {
     $cmd = "curl -f -L";
-    if (not Fink::Config::is_verbose()) {
+    if (Fink::Config::verbose_level() == 0) {
       $cmd .= " -s -S";
     }
     if (not $config->param_boolean("ProxyPassiveFTP")) {
@@ -234,7 +234,7 @@
   if ((!$cmd or $config->param_default("DownloadMethod") eq "wget") and
       (-x "$basepath/bin/wget" or -x "/usr/bin/wget")) {
     $cmd = "wget";
-    if (Fink::Config::is_verbose()) {
+    if (Fink::Config::verbose_level() >= 1) {
       $cmd .= " --verbose";
     } else {
       $cmd .= " --non-verbose";
@@ -258,7 +258,7 @@
     if ($config->param_default("DownloadMethod") eq "axelautomirror") {
       $cmd = "axel -S 1";
     }
-    if (Fink::Config::is_verbose()) {
+    if (Fink::Config::verbose_level() >= 1) {
       $cmd .= " --verbose";
     }
     if ($file ne &filename($url)) {
diff -ruN fink.orig/perlmod/Fink/PkgVersion.pm fink/perlmod/Fink/PkgVersion.pm
--- fink.orig/perlmod/Fink/PkgVersion.pm	Tue Jul 16 10:40:33 2002
+++ fink/perlmod/Fink/PkgVersion.pm	Tue Jul 16 11:28:49 2002
@@ -764,7 +764,7 @@
   my ($pkgname, $package, $version, $pkgversion);
   my ($found, @parts, $i, @vlist, $v, @rlist);
 
-  if (not Fink::Config::is_verbose()) {
+  if (Fink::Config::verbose_level() < 3) {
     $quiet = 1;
   }
 
@@ -921,7 +921,7 @@
   $bdir = $self->get_fullname();
 
   $verbosity = "";
-  if (Fink::Config::is_verbose()) {
+  if (Fink::Config::verbose_level() > 1) {
     $verbosity = "v";
   }
 
diff -ruN fink.orig/perlmod/Fink/SelfUpdate.pm fink/perlmod/Fink/SelfUpdate.pm
--- fink.orig/perlmod/Fink/SelfUpdate.pm	Tue Jul 16 10:40:33 2002
+++ fink/perlmod/Fink/SelfUpdate.pm	Tue Jul 16 11:27:26 2002
@@ -374,7 +374,7 @@
   }
 
   $verbosity = "";
-  if (Fink::Config::is_verbose()) {
+  if (Fink::Config::verbose_level() > 1) {
     $verbosity = "v";
   }
   $unpack_cmd = "tar -xz${verbosity}f $pkgtarball";
diff -ruN fink.orig/perlmod/Fink/Validation.pm fink/perlmod/Fink/Validation.pm
--- fink.orig/perlmod/Fink/Validation.pm	Tue Jul 16 10:40:33 2002
+++ fink/perlmod/Fink/Validation.pm	Tue Jul 16 11:28:22 2002
@@ -171,7 +171,7 @@
   my $looks_good = 1;
   my $error_found = 0;
 
-  if (Fink::Config::is_verbose()) {
+  if (Fink::Config::verbose_level() == 3) {
     print "Validating package file $filename...\n";
   }
   
@@ -292,7 +292,7 @@
     print "Error: Length of package description exceeds 60 characters. ($filename)\n";
     $looks_good = 0;
   }
-  elsif (length($value) > 45 and Fink::Config::is_verbose()) {
+  elsif (length($value) > 45 and Fink::Config::verbose_level() == 3) {
     print "Warning: Length of package description exceeds 45 characters. ($filename)\n";
     $looks_good = 0;
   }
@@ -338,7 +338,7 @@
     }
   }
   
-  if ($looks_good and Fink::Config::is_verbose()) {
+  if ($looks_good and Fink::Config::verbose_level() == 3) {
     print "Package looks good!\n";
   }
 }
