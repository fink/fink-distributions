diff -ruN gmic-1.5.8.5-orig/src/Makefile gmic-1.5.8.5/src/Makefile
--- gmic-1.5.8.5-orig/src/Makefile	2014-03-21 10:23:10.000000000 -0500
+++ gmic-1.5.8.5/src/Makefile	2014-05-24 11:43:11.000000000 -0500
@@ -47,7 +47,7 @@
 # Set correct variables and paths.
 #----------------------------------
 OS := $(shell uname)
-CC = g++
+CC = g++-fsf-4.9 -MD
 ifeq ($(OS),Linux)
 OS = Unix
 endif
@@ -57,13 +57,8 @@
 ifeq ($(OS),GNU)
 OS = Unix
 endif
-ifeq ($(OS),Darwin)
-PLUGINDIR = ~/Library/Application\ Support/Gimp/plug-ins
-USR = /opt/local
-else
 PLUGINDIR = `gimptool-2.0 --gimpplugindir`/plug-ins
-USR = /usr
-endif
+USR = @FINKPREFIX@
 ifeq ($(OSTYPE),msys)
 EXE = .exe
 endif
@@ -423,8 +423,7 @@
 	mkdir -p $(DESTDIR)$(USR)/share/man/fr/man1/
 	cp -f ../man/gmic.1.gz $(DESTDIR)$(USR)/share/man/man1/gmic.1.gz
 	cp -f ../man/gmic.1.gz $(DESTDIR)$(USR)/share/man/fr/man1/gmic.1.gz
-	if test -d /etc/bash_completion.d/; then mkdir -p $(DESTDIR)/etc/bash_completion.d/; cp -f ../resources/gmic_bashcompletion.sh $(DESTDIR)/etc/bash_completion.d/gmic; fi
-	if test -d /opt/local/etc/bash_completion.d/; then mkdir -p $(DESTDIR)/opt/local/etc/bash_completion.d/; cp -f ../resources/gmic_bashcompletion.sh $(DESTDIR)/opt/local/etc/bash_completion.d/gmic; fi
+	mkdir -p $(DESTDIR)$(USR)/etc/bash_completion.d/; cp -f ../resources/gmic_bashcompletion.sh $(DESTDIR)$(USR)/etc/bash_completion.d/gmic
 
 uninstall:
 	rm -f $(DESTDIR)$(USR)/bin/gmic
diff -ruN gmic-1.5.7.1-orig/zart/zart.pro gmic-1.5.7.1/zart/zart.pro
--- gmic-1.5.7.1-orig/zart/zart.pro	2013-09-18 06:58:08.000000000 -0400
+++ gmic-1.5.7.1/zart/zart.pro	2013-10-13 06:16:12.000000000 -0400
@@ -4,9 +4,9 @@
 CONFIG	+= warn_on release
 
 !macx {
-	INCLUDEPATH	+= .. ./include /usr/include/opencv ../src/
+	INCLUDEPATH	+= .. ./include ../src/ @FINKPREFIX@/include/opencv @FINKPREFIX@/include
 } else {
-	INCLUDEPATH	+= .. ./include /opt/local/include /opt/local/include/opencv ../src/
+	INCLUDEPATH	+= .. ./include ../src/ @FINKPREFIX@/include/opencv
 }
 
 DEPENDPATH += ./include
