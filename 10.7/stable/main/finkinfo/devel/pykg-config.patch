diff -Nurd -x'*~' pykg-config-1.2.0.orig/pykg-config.py pykg-config-1.2.0/pykg-config.py
--- pykg-config-1.2.0.orig/pykg-config.py	2013-12-01 21:53:56.000000000 -0500
+++ pykg-config-1.2.0/pykg-config.py	2014-06-03 13:17:38.000000000 -0400
@@ -39,6 +39,8 @@
 import sys
 import traceback
 
+sys.path[0:0] = ["@PREFIX@/lib/pykg-config/lib"]
+
 from pykg_config.errorprinter import ErrorPrinter
 from pykg_config.result import PkgCfgResult, NoPackagesSpecifiedError
 from pykg_config.options import Options
diff -Nurd -x'*~' pykg-config-1.2.0.orig/pykg_config/pkgsearcher.py pykg-config-1.2.0/pykg_config/pkgsearcher.py
--- pykg-config-1.2.0.orig/pykg_config/pkgsearcher.py	2013-12-01 21:59:38.000000000 -0500
+++ pykg-config-1.2.0/pykg_config/pkgsearcher.py	2014-06-03 22:44:08.000000000 -0400
@@ -210,17 +210,12 @@
                 if not dir or not isdir(dir):
                     continue
                 self._append_packages(dir)
-        # Else append prefix/lib/pkgconfig, prefix/share/pkgconfig
+        # Else append fink's standard default
         else:
-            if Options().get_option('is_64bit'):
-                suffix = '64'
-            else:
-                suffix = ''
-            if isdir(join(sys.prefix, 'lib' + suffix + '/pkgconfig')):
-                self._append_packages(join(sys.prefix,
-                                           'lib' + suffix + '/pkgconfig'))
-            if isdir(join(sys.prefix, 'share/pkgconfig')):
-                self._append_packages(join(sys.prefix, 'share/pkgconfig'))
+            for dir in "@PC_PATH@".split(self._split_char()):
+                if not dir or not isdir(dir):
+                    continue
+                self._append_packages(dir)
         if sys.platform == 'win32':
             key_path = 'Software\\pkg-config\\PKG_CONFIG_PATH'
             for root in ((_winreg.HKEY_CURRENT_USER, 'HKEY_CURRENT_USER'),
