diff --git a/bin/git-cola b/bin/git-cola
index 87a5a00..2d85ced 100755
--- a/bin/git-cola
+++ b/bin/git-cola
@@ -32,6 +32,8 @@ def setup_environment():
     # Try to detect where it is run from and set prefix and the search path.
     # It is assumed that the user installed Cola using the --prefix= option
     prefix = dirname(dirname(realpath(abspath(__file__))))
+    if os.path.islink(__file__):
+        prefix = dirname(dirname(os.readlink(__file__)))
 
     # Look for modules in the source or install trees
     cola_mod = os.path.join(prefix, 'cola', '__init__.py')
@@ -42,6 +44,8 @@ def setup_environment():
         # Install tree
         install_lib = os.path.join(prefix, 'share', 'git-cola', 'lib')
         sys.path.insert(1, install_lib)
+        fink_lib = os.path.join(prefix, 'lib', '@PYTHON@', 'site-packages')
+        sys.path.insert(1, fink_lib)
 setup_environment()
 
 from cola import core
diff --git a/bin/git-dag b/bin/git-dag
index 3c1e29e..ddf9373 100755
--- a/bin/git-dag
+++ b/bin/git-dag
@@ -30,6 +30,8 @@ def setup_environment():
     # Try to detect where it is run from and set prefix and the search path.
     # It is assumed that the user installed Cola using the --prefix= option
     prefix = dirname(dirname(realpath(abspath(__file__))))
+    if os.path.islink(__file__):
+        prefix = dirname(dirname(os.readlink(__file__)))
 
     # Look for modules in the source or install trees
     cola_mod = os.path.join(prefix, 'cola', '__init__.py')
@@ -40,6 +42,8 @@ def setup_environment():
         # Install tree
         install_lib = os.path.join(prefix, 'share', 'git-cola', 'lib')
         sys.path.insert(1, install_lib)
+        fink_lib = os.path.join(prefix, 'lib', '@PYTHON@', 'site-packages')
+        sys.path.insert(1, fink_lib)
 setup_environment()
 
 
