diff -ru cvs-fast-export-cvs-fast-export.orig/export.c cvs-fast-export-cvs-fast-export/export.c
--- cvs-fast-export-cvs-fast-export.orig/export.c	2014-10-08 06:59:08.000000000 -0400
+++ cvs-fast-export-cvs-fast-export/export.c	2014-10-14 17:19:06.000000000 -0400
@@ -27,6 +27,10 @@
 #ifdef THREADS
 #include <pthread.h>
 #endif /* THREADS */
+#if defined(__APPLE__)
+#include <mach/mach_time.h>
+extern mach_timebase_info_data_t timebase;
+#endif
 
 /*
  * Blob compression with zlib is not enabled by default because, (a) in general,
@@ -81,7 +85,13 @@
 	struct rusage rusage;
 	float elapsed;
 
+#if defined(__APPLE__)
+    uint64_t time = mach_absolute_time();
+    now.tv_nsec = ((double)time * (double)timebase.numer)/((double)timebase.denom);
+    now.tv_sec = ((double)time * (double)timebase.numer)/((double)timebase.denom * NSEC_PER_SEC);
+#else
 	clock_gettime(CLOCK_REALTIME, &now);
+#endif
 	(void)getrusage(RUSAGE_SELF, &rusage);
 	elapsed = (nanosec(&now) - nanosec(start_time)) / NANOSCALE;
 	progress_end("100%%, %d commits in %.6fs (%d commits/sec) using %ldKb.",
diff -ru cvs-fast-export-cvs-fast-export.orig/main.c cvs-fast-export-cvs-fast-export/main.c
--- cvs-fast-export-cvs-fast-export.orig/main.c	2014-10-08 06:59:08.000000000 -0400
+++ cvs-fast-export-cvs-fast-export/main.c	2014-10-14 17:05:06.000000000 -0400
@@ -23,6 +23,10 @@
 #if defined(__GLIBC__)
 #include <malloc.h>
 #endif /* __GLIBC__ */
+#if defined(__APPLE__)
+#include <mach/mach_time.h>
+mach_timebase_info_data_t timebase;
+#endif
 
 /* options */
 int commit_time_window = 300;
@@ -206,7 +210,14 @@
      */
     mallopt(M_TOP_PAD,16*1024*1024); /* grab memory in 16MB chunks */
 #endif /* __GLIBC__ */
+#if defined(__APPLE__)
+    mach_timebase_info(&timebase);
+    uint64_t time = mach_absolute_time();
+    start_time.tv_nsec = ((double)time * (double)timebase.numer)/((double)timebase.denom);
+    start_time.tv_sec = ((double)time * (double)timebase.numer)/((double)timebase.denom * NSEC_PER_SEC);
+#else
     clock_gettime(CLOCK_REALTIME, &start_time);
+#endif
 
     /* force times using mktime to be interpreted in UTC */
     setenv("TZ", "UTC", 1);
