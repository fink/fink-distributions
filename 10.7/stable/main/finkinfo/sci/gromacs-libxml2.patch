--- gromacs-5.0.4/CMakeLists.txt.orig	2015-04-26 10:52:02.000000000 -0400
+++ gromacs-5.0.4/CMakeLists.txt	2015-04-26 10:53:42.000000000 -0400
@@ -478,6 +478,7 @@
 # tests. This exports LIBXML2_FOUND, which we should not use because
 # it does not tell us that linking will succeed. Instead, we test that
 # next.
+set(LIBXML2_LIBRARIES @FINKPREFIX@/lib/libxml2.dylib)
 if(DEFINED LIBXML2_LIBRARIES)
   set(LibXml2_FIND_QUIETLY TRUE)
 endif()

--- gromacs-5.0.4/cmake/gmxOptionUtilities.cmake.orig	2015-04-26 11:06:36.000000000 -0400
+++ gromacs-5.0.4/cmake/gmxOptionUtilities.cmake	2015-04-26 11:07:39.000000000 -0400
@@ -166,10 +166,10 @@
 function(GMX_CHECK_IF_CHANGED RESULT)
     set(_result FALSE)
     foreach (_var ${ARGN})
-        if (NOT "${_var}" STREQUAL "${_var}_PREVIOUS_VALUE")
+        if (NOT ${_var} STREQUAL ${_var}_PREVIOUS_VALUE)
             set(_result TRUE)
         endif()
-        set(${_var}_PREVIOUS_VALUE "${${_var}}" CACHE INTERNAL
+        set(${_var}_PREVIOUS_VALUE ${${_var}} CACHE INTERNAL
             "Previous value of ${_var} for change tracking")
     endforeach()
     set(${RESULT} ${_result} PARENT_SCOPE)
