diff -ruNd matplotlib-1.3.1.orig/setup.cfg.template matplotlib-1.3.1/setup.cfg.template
--- matplotlib-1.3.1.orig/setup.cfg.template	2013-10-01 07:18:12.000000000 -0700
+++ matplotlib-1.3.1/setup.cfg.template	2013-11-29 10:53:06.000000000 -0800
@@ -55,17 +55,17 @@
 #           behavior
 #
 #agg = auto
-#cairo = auto
-#gtk = auto
+cairo = True
+gtk = True
 #gtk3agg = auto
 #gtk3cairo = auto
-#gtkagg = auto
-#macosx = auto
+gtkagg = True
+macosx = True
 #pyside = auto
 #qt4agg = auto
-#tkagg = auto
+tkagg = True
 #windowing = auto
-#wxagg = auto
+wxagg = True
 
 [rc_options]
 # User-configurable options
diff -ruNd matplotlib-1.3.1.orig/setup.py matplotlib-1.3.1/setup.py
--- matplotlib-1.3.1.orig/setup.py	2013-10-10 05:42:48.000000000 -0700
+++ matplotlib-1.3.1/setup.py	2013-11-29 10:48:34.000000000 -0800
@@ -203,6 +203,9 @@
     with open('lib/matplotlib/mpl-data/matplotlibrc', 'w') as fd:
         fd.write(template % {'backend': default_backend})
 
+    for mod in ext_modules:
+        mod.include_dirs.append('@PREFIX@/include')
+        mod.library_dirs.append('@PREFIX@/lib')
 
     # Build in verbose mode if requested
     if setupext.options['verbose']:
diff -ruNd matplotlib-1.3.1.orig/setupext.py matplotlib-1.3.1/setupext.py
--- matplotlib-1.3.1.orig/setupext.py	2013-10-10 05:42:48.000000000 -0700
+++ matplotlib-1.3.1/setupext.py	2013-11-29 10:58:07.000000000 -0800
@@ -144,7 +144,8 @@
 
     basedir_map = {
         'win32': ['win32_static',],
-        'darwin': ['/usr/local/', '/usr', '/usr/X11', '/opt/local'],
+        'darwin' : ['@PREFIX@/lib/freetype219', '@PREFIX@', 
+                    '/usr', '/usr/X11', '/opt/X11',],
         'sunos5': [os.getenv('MPLIB_BASE') or '/usr/local',],
         'gnu0': ['/usr'],
         'aix5': ['/usr/local'],
@@ -1337,6 +1338,17 @@
             # this config section lifted directly from Imaging - thanks to
             # the effbot!
 
+            tcl_inc = "@PREFIX@/include"
+            tk_inc = "@PREFIX@/include"
+            tcl_lib = "@PREFIX@/lib"
+            tk_lib = "@PREFIX@/lib"
+            tk_ver = ""
+            # Add final versions of directories and libraries to module lists
+            ext.include_dirs.extend([tcl_inc, tk_inc])
+            ext.library_dirs.extend([tcl_lib, tk_lib])
+            ext.libraries.extend(['tk' + tk_ver, 'tcl' + tk_ver])
+
+        elif False:
             # First test for a MacOSX/darwin framework install
             from os.path import join, exists
             framework_dirs = [
@@ -1706,6 +1718,9 @@
     name = 'macosx'
 
     def check_requirements(self):
+        if True:
+            raise CheckFailed("fink can't use macosc backend - not using frameworks")
+
         if sys.platform != 'darwin':
             raise CheckFailed("Mac OS-X only")
 
