diff -Nurd nginx-1.2.6/auto/lib/pcre/conf nginx-1.2.6-patched/auto/lib/pcre/conf
--- nginx-1.2.6/auto/lib/pcre/conf	2012-03-27 18:44:52.000000000 +0200
+++ nginx-1.2.6-patched/auto/lib/pcre/conf	2012-12-28 17:32:31.000000000 +0100
@@ -105,6 +105,22 @@
 
         if [ $ngx_found = no ]; then
 
+            # Fink port
+
+            ngx_feature="PCRE library in @PREFIX@"
+            ngx_feature_path="@PREFIX@/include"
+
+            if [ $NGX_RPATH = YES ]; then
+                ngx_feature_libs="-R@PREFIX@/lib -L@PREFIX@/lib -lpcre"
+            else
+                ngx_feature_libs="-L@PREFIX@/lib -lpcre"
+            fi
+
+            . auto/feature
+        fi
+
+        if [ $ngx_found = no ]; then
+
             # FreeBSD port
 
             ngx_feature="PCRE library in /usr/local/"
