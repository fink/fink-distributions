diff -Nurd -x'*~' local-lib-1.008026.orig/Makefile.PL local-lib-1.008026/Makefile.PL
--- local-lib-1.008026.orig/Makefile.PL	2013-11-05 13:07:13.000000000 -0500
+++ local-lib-1.008026/Makefile.PL	2013-12-06 16:23:48.000000000 -0500
@@ -57,6 +57,7 @@
 DEATH
   }
   if (my ($x) = grep { /^--bootstrap(?:=.*)?$/ } @ARGV) {
+    die("packaging error: somehow we're in a codeblock that uses un-finkified \$^!;");
     @ARGV = grep { !/^--bootstrap(?:=.*)?$/ } @ARGV;
     $bootstrapping = 1;
     if(my ($x) = grep { /^--no-manpages/ } @ARGV) {
@@ -185,6 +186,7 @@
 requires 'ExtUtils::Install' => '1.43'; # version INSTALL_BASE was added
 requires 'Module::Build' => '0.36'; # PERL_MB_OPT
 
+if (0) {
 # don't bother fixing CPAN.pm if bootstrapped from cpanminus
 unless ($ENV{PERL5_CPANM_IS_RUNNING}) {
     my $required_CPAN = '1.82';
@@ -250,6 +252,7 @@
 } else {
   auto_install;
 }
+	}
 chdir($cwd);
 resources(
   # r/w: p5sagit@git.shadowcat.co.uk:local-lib.git
