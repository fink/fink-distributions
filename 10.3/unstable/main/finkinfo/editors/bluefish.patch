--- bluefish-1.0/configure.ac	Tue Jan  4 07:13:07 2005
+++ bluefish-1.0/configure.ac	Wed Jan  5 04:49:07 2005
@@ -300,7 +300,7 @@
 
 AC_CHECK_HEADER(aspell.h, 
 	AC_CHECK_LIB(aspell, new_aspell_config, 
-		LIBS="$LIBS -laspell"
+		LIBS="$LIBS -laspell -lstdc++"
 		AC_DEFINE(HAVE_LIBASPELL),
 		echo "libaspell not found please install libaspell-dev or similar"), 
 	OLDLIBS=$LIBS

--- bluefish-1.0/data/Makefile.in	Thu Jan  6 18:46:00 2005
+++ bluefish-1.0/data/Makefile-patched.in	Wed Jan 12 09:52:54 2005
@@ -40,7 +40,7 @@
 
 all: $(DEFAULTFILES)
 	sed -e 's!ICONDIR!$(ICONPATH)!' < bluefish.desktop.in > bluefish.desktop
-	if test "x${HAVE_GNOME_VFS}" = "xyes" ; then \
+	if test "${HAVE_GNOME_VFS}" = "1" ; then \
 		sed -e 's!EXPECT_URIS!true!' -e 's!SUPPORTED_URI_SCHEMES!file,http,ftp,sftp,smb!' < bluefish.applications.in > bluefish.applications ;\
 	else \
 		sed -e 's!EXPECT_URIS!false!' -e 's!SUPPORTED_URI_SCHEMES!!' < bluefish.applications.in > bluefish.applications ;\
