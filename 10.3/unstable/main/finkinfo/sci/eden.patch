diff -ruN eden-orig/source/Makefile eden-edited/source/Makefile
--- eden-orig/source/Makefile	2004-07-02 13:58:10.000000000 -0700
+++ eden-edited/source/Makefile	2004-07-14 13:43:45.000000000 -0700
@@ -54,14 +54,14 @@
 #######################################################################
 # 	Directory where the FFTW include and lib are
 #######################################################################
-FFTW		= /sw
+FFTW		= /swprefix
 LIB  		= $(FFTW)/lib
 INC  		= $(FFTW)/include
 
 #######################################################################
 # 	Directory to install the binary, version
 #######################################################################
-BIN 		= $(HOME)/Programs/bin
+# BIN 		= $(HOME)/Programs/bin
 VERSION		=V5.0
 
 #######################################################################
@@ -72,8 +72,8 @@
 # -O3 on HP, -O2 on SGI or DEC.
 #
 #######################################################################
-CFLAGS		= -O2
-ifneq ($(FFTW),/usr/local)
+CFLAGS		= -O3
+ifneq ($(FFTW),/usr)
 CFLAGS		+= -I$(INC)
 endif
 
@@ -118,9 +118,10 @@
 		$(CC) -o $@ $^
 
 install:	all
-		mv fbyteswap $(BIN)
-		mv eden $(BIN)/$(TARGET)
-		chmod 751 $(BIN)/$(TARGET)
+		# mv fbyteswap $(BIN)
+		# mv eden $(BIN)/$(TARGET)
+		# chmod 751 $(BIN)/$(TARGET)
+		chmod 751 *eden
 		chmod +x mkeden.csh
 		./mkeden.csh $(BIN) $(TARGET)
 
