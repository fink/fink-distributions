--- matplotlib/src/_tkagg.cpp.orig	Sun Apr 25 19:37:42 2004
+++ matplotlib/src/_tkagg.cpp	Sun Apr 25 19:38:12 2004
@@ -13,12 +13,7 @@
 #include <stdlib.h>
 
 extern "C" {
-#ifdef __APPLE__
-#  include <Tcl/tcl.h>
-#  include <Tk/tk.h>
-#else
 #  include <tk.h> 
-#endif
 };
 
 #include "_backend_agg.h"
--- matplotlib/setupext.py.orig	Sun Apr 25 19:36:31 2004
+++ matplotlib/setupext.py	Sun Apr 25 19:37:31 2004
@@ -36,7 +36,7 @@
     'win32'  : ['win32_static',],
     'linux2' : ['/usr',],
     'linux'  : ['/usr',],
-    'darwin' : ['/usr/local', '/sw'],
+    'darwin' : ['/usr/X11R6','@PREFIX@'],
     'sunos5' : [os.getenv('MPLIB_BASE') or '/usr/local',],
 }
 
@@ -201,9 +201,6 @@
         module.include_dirs.extend(['win32_static/include/tcl'])
         module.library_dirs.extend(['C:/Python23/dlls'])
         module.libraries.extend(['tk84', 'tcl84'])
-    elif sys.platform == 'darwin':
-        module.extra_link_args.extend(['-framework','Tcl'])
-        module.extra_link_args.extend(['-framework','Tk'])
     else:
 	o = find_tcltk()
 	module.include_dirs.append(o.tcl_inc)
--- matplotlib/setup.py.orig	Sun Apr 25 21:38:37 2004
+++ matplotlib/setup.py	Sun Apr 25 21:39:06 2004
@@ -69,18 +69,18 @@
     
 if BUILD_GTKAGG:
     
-    try: import gtk
-    except ImportError: print 'GTKAgg requires pygtk'
-    else:
-        BUILD_AGG = 1
-        build_gtkagg(ext_modules, packages)
+#   try: import gtk
+#   except ImportError: print 'GTKAgg requires pygtk'
+#   else:
+    BUILD_AGG = 1
+    build_gtkagg(ext_modules, packages)
 
 if BUILD_TKAGG:
-    try: import Tkinter
-    except ImportError: print 'GTKAgg requires pygtk'
-    else:
-        BUILD_AGG = 1
-        build_tkagg(ext_modules, packages)
+#   try: import Tkinter
+#   except ImportError: print 'GTKAgg requires pygtk'
+#   else:
+    BUILD_AGG = 1
+    build_tkagg(ext_modules, packages)
 
 
 if BUILD_AGG:
