diff -ur HiVision2.0.ori/Utilities/get_mem_info.cc HiVision2.0/Utilities/get_mem_info.cc
--- HiVision2.0.ori/Utilities/get_mem_info.cc	Mon Dec  8 16:56:11 2003
+++ HiVision2.0/Utilities/get_mem_info.cc	Tue Dec 30 19:22:14 2003
@@ -25,6 +25,7 @@
 
   char command1[60];
   sprintf(command1,"ps -o rss -p %d | grep -v RSS",PID);
+#ifndef __APPLE__
 
   procbuf buf;
   buf.open(command1,ios::in);
@@ -44,6 +45,19 @@
 
   buf.close();
   */
+#else
+  FILE *pipe;
+  char buf[1024];
+  unsigned long res1;
+
+  sprintf(command1,"ps -o rss -p %d | grep -v RSS",PID);
+  pipe = popen(command1, "r");
+  if (pipe) {
+    fgets(buf, 1024, pipe);
+    res1 = strtoul(buf,NULL,10);
+    pclose(pipe);
+  }
+#endif
 
   return res1;
 }
diff -ur HiVision2.0.ori/Utilities/get_mem_info.h HiVision2.0/Utilities/get_mem_info.h
--- HiVision2.0.ori/Utilities/get_mem_info.h	Mon Dec  8 16:56:04 2003
+++ HiVision2.0/Utilities/get_mem_info.h	Tue Dec 30 19:22:30 2003
@@ -19,9 +19,13 @@
 #include <iostream>
 //#include <pbmplus.h>
 #include <unistd.h>
+#ifndef __APPLE__
 #include <procbuf.h>
 #include <stdio.h>
 #include <sys/procfs.h>
+#else
+#include <unistd.h>
+#endif
 
 #ifndef __GET_MEM_INFO
 #define __GET_MEM_INFO
diff -ur HiVision2.0.ori/Makefile.inc HiVision2.0/Makefile.inc 
--- HiVision2.0.ori/Makefile.inc	Mon Dec  8 17:11:55 2003
+++ HiVision2.0/Makefile.inc	Sun Dec 28 23:57:06 2003
@@ -1,17 +1,17 @@
 
 PREFIX  :=  
 
-ROOTDIR :=  /home/heuvelin/local/src/HiVision2.0
+ROOTDIR := BUILDDIR
 
 
-GL_INCS   :=  /home/heuvelin/local/lib/Mesa-4.0.3/include
-GL_LIBS   :=  /home/heuvelin/local/lib/Mesa-4.0.3/lib
+GL_INCS := /usr/X11R6/include
+GL_LIBS := /usr/X11R6/lib
 
-QT_DIR    := /home/heuvelin/local/src/qt-x11-free-3.2.1/
+QT_DIR := FINKDIR
 
 
-VTK_INCS  := /home/heuvelin/local/lib/vtk-4.2/include/vtk
-VTK_LIBS  := /home/heuvelin/local/lib/vtk-4.2/lib/vtk
+VTK_INCS := FINKDIR/include/vtk
+VTK_LIBS := FINKDIR/lib/vtk
 
 
 ###########################################################################
@@ -22,16 +22,16 @@
 
 
 GL_INCS  := -I$(GL_INCS)
-GL_LIBS  := -L$(GL_LIBS) -lGL -lOSMesa
+GL_LIBS  := -L$(GL_LIBS) -lGL -lOSMesa -framework OpenGL -lobjc
 
 QT_LIBS   := -L$(QT_DIR)/lib -L/usr/X11R6/lib -lqt-mt -lqui -lXext -lX11 -lm -lpthread
-QT_INCS   := -I$(QT_DIR)/include
+QT_INCS   := -I$(QT_DIR)/include/qt
 
 MOC	  := $(QT_DIR)/bin/moc
 UIC	  := $(QT_DIR)/bin/uic
 
 VTK_INCS  := -I$(VTK_INCS)
-VTK_LIBS  := -L$(VTK_LIBS)   -lvtkCommon -lvtkFiltering -lvtkGraphics -lvtkIO -lvtkImaging -lvtkHybrid -lvtkRendering -lvtkjpeg -lvtkpng -lvtkzlib -lvtkIO -lvtkHybrid -lvtktiff -lvtkexpat -lvtkftgl -lvtkfreetype
+VTK_LIBS  := -L$(VTK_LIBS)   -lvtkCommon -lvtkFiltering -lvtkGraphics -lvtkIO -lvtkImaging -lvtkHybrid -lvtkRendering -ljpeg -lpng -lz -lvtkIO -lvtkHybrid -ltiff -lexpat -lvtkftgl -lvtkfreetype
 
 
 ###########################################################################
