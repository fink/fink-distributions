diff -ruN vobcopy-0.5.5.orig/configure vobcopy-0.5.5/configure
--- vobcopy-0.5.5.orig/configure	2002-09-09 21:31:36.000000000 -0600
+++ vobcopy-0.5.5/configure	2002-11-28 16:55:55.000000000 -0700
@@ -11,9 +11,9 @@
 #robos@muon.de
 
 #see if libdvdread is installed
-if !( test -e /usr/local/include/dvdread ) 
+if !( test -e @FINKPREFIX@/include/dvdread ) 
 then
-	if !( test -e /usr/include/dvdread )
+	if !( test -e @FINKPREFIX@/include/dvdread )
 		then
 		echo "Do you have libdvdread installed? I don't think so..."
 		echo "If you have, PLEASE mail me where it is so that I can fix
@@ -31,6 +31,8 @@
 #FreeBSD needs libgnugetopt
 if [ `uname -s` = FreeBSD ]; then
 	LDFLAGS="LDFLAGS += -ldvdread -L/usr/local/lib -lgnugetopt"
+elif [ `uname -s` = Darwin ]; then
+        LDFLAGS="LDFLAGS += -L@FINKPREFIX@/lib -ldvdread -lgnugetopt -ldl"
 else
 	LDFLAGS="LDFLAGS += -ldvdread -L/usr/local/lib"
 fi
@@ -49,11 +51,11 @@
 #below here are variable definitions. They get substituted in the (CC) and 
 #stuff places.
 CC     = gcc
-PREFIX += /usr/local
+PREFIX += @FINKPREFIX@
 BINDIR = \${PREFIX}/bin
-MANDIR = \${PREFIX}/man
+MANDIR = \${PREFIX}/share/man
 $LFS
-CFLAGS += -I/usr/local/include
+CFLAGS += -I@FINKPREFIX@/include
 $LDFLAGS
 
 #This specifies the conversion from .c to .o 
@@ -85,4 +87,4 @@
 uninstall:
 	rm -f \$(BINDIR)/vobcopy
 	rm -f \$(MANDIR)/man1/vobcopy.1
-" > Makefile
\ No newline at end of file
+" > Makefile
diff -ruN vobcopy-0.5.5.orig/dvd.c vobcopy-0.5.5/dvd.c
--- vobcopy-0.5.5.orig/dvd.c	2002-11-01 18:31:14.000000000 -0700
+++ vobcopy-0.5.5/dvd.c	2002-11-28 16:29:51.000000000 -0700
@@ -23,7 +23,7 @@
 #include <unistd.h>
 #include <ctype.h>
 #include <sys/stat.h>
-#if (defined(__unix__) || defined(unix)) && !defined(USG)
+#if ((defined(__unix__) || defined(unix)) && !defined(USG)) || (defined(__APPLE__) && defined(__GNUC__))
 #include <sys/param.h>  
 #include <sys/mount.h>
 #endif
@@ -114,7 +114,7 @@
 #endif
   char  *pointer;
   bool  mounted = FALSE;
-#if (defined(BSD) && (BSD >= 199306))
+#if (defined(BSD) && (BSD >= 199306)) || defined(__APPLE__)
   struct statfs buf;
 #endif
 
@@ -142,7 +142,7 @@
     /*
      *look through /etc/mtab to see if it's actually mounted
      */
-#if (defined(BSD) && (BSD >= 199306)) 
+#if (defined(BSD) && (BSD >= 199306)) || defined(__APPLE__)
     if(!statfs(path,&buf))
       {
        if(!strcmp(path,buf.f_mntonname))
@@ -309,7 +309,7 @@
 /* returns <0 if error                            */
 int get_device_oyo( char *path, char *device )
 { //oyo
-#if (defined(BSD) && (BSD >= 199306))
+#if (defined(BSD) && (BSD >= 199306)) || defined(__APPLE__)
   int i, n, cd_count = 0;
   struct statfs *mntbuf;
 
diff -ruN vobcopy-0.5.5.orig/vobcopy.c vobcopy-0.5.5/vobcopy.c
--- vobcopy-0.5.5.orig/vobcopy.c	2002-11-01 21:17:07.000000000 -0700
+++ vobcopy-0.5.5/vobcopy.c	2002-11-28 16:34:41.000000000 -0700
@@ -63,7 +63,7 @@
 #include <fcntl.h>
 #include <unistd.h>
 #include <dirent.h> /*for readdir*/
-#if (defined(__unix__) || defined(unix)) && !defined(USG)
+#if ((defined(__unix__) || defined(unix)) && !defined(USG)) || (defined(__APPLE__) && defined(__GNUC__))
 #include <sys/param.h>
 #else
 #include <sys/vfs.h>
@@ -82,7 +82,7 @@
 #include <errno.h>
 #define _GNU_SOURCE //for getopt_long
 #ifdef HAVE_GETOPT_LONG
-#include <getopt.h>
+#include <gnugetopt/getopt.h>
 #endif
 
 #include "dvd.h"
@@ -133,7 +133,7 @@
   /**
    * This case here has to be examined for every system vobcopy shall run under
    */
-#if defined( __linux__ ) || ( defined( BSD ) && ( BSD >= 199306 ))
+#if defined( __linux__ ) || ( defined( BSD ) && ( BSD >= 199306 )) || defined(__APPLE__)
   struct statfs     buf1;
 #elif !defined(__sun__)
   struct statvfs    buf1;
@@ -327,7 +327,7 @@
 	  provided_input_dir_flag = TRUE;
 	  break;
 
-#if defined( __USE_FILE_OFFSET64 ) || ( defined( BSD ) && ( BSD >= 199306 ) )
+#if defined( __USE_FILE_OFFSET64 ) || ( defined( BSD ) && ( BSD >= 199306 ) ) || defined (__APPLE__)
 	  case'l': /*large file output*/
 	    max_filesize_in_blocks = 4500000000000000; 
 	  /* 2^63 / 2048 (not exactly) */
@@ -1436,7 +1436,7 @@
 off_t freespace_getter( char *path, int verbosity_level )
 {
 
-#if defined( __linux__ ) || ( defined( BSD ) && ( BSD >= 199306 ) )
+#if defined( __linux__ ) || ( defined( BSD ) && ( BSD >= 199306 ) ) || defined(__APPLE__)
   struct statfs     buf1;
 #else 
   struct statvfs    buf1; 
@@ -1444,7 +1444,7 @@
 /*   ssize_t temp1, temp2; */
   long temp1, temp2;
   off_t sum;
-#if defined( __linux__ ) || ( defined( BSD ) && ( BSD >= 199306 ) )
+#if defined( __linux__ ) || ( defined( BSD ) && ( BSD >= 199306 ) ) || defined(__APPLE__)
   statfs( path, &buf1 );
   if( verbosity_level >= 1 )
     fprintf( stderr, "Used the linux statfs\n" );
@@ -1473,7 +1473,7 @@
 off_t usedspace_getter( char *path, int verbosity_level )
 {
 
-#if defined( __linux__ ) || ( defined( BSD ) && ( BSD >= 199306 ) )
+#if defined( __linux__ ) || ( defined( BSD ) && ( BSD >= 199306 ) ) || defined(__APPLE__)
   struct statfs     buf2;
 #else 
   struct statvfs    buf2; 
@@ -1481,7 +1481,7 @@
 /*   ssize_t temp1, temp2; */
   long temp1, temp2;
   off_t sum;
-#if defined( __linux__ ) || ( defined( BSD ) && ( BSD >= 199306 ) )
+#if defined( __linux__ ) || ( defined( BSD ) && ( BSD >= 199306 ) ) || defined(__APPLE__)
   statfs( path, &buf2 );
   if( verbosity_level >= 1 )
     fprintf( stderr, "Used the linux statfs\n" );
@@ -1540,7 +1540,7 @@
 	   "[-1/path/to/second/output/dir/] [-2/.../third/..] [-3/../] [-4 /../]\n"
 	   "[-b <skip-size-at-beginning[bkmg]>] [-e <skip-size-at-end[bkmg]>]\n" 
 	   "[-t <name (can be \"stdout\")>] [-m (mirror)] \n", program_name );
-#if defined( __USE_FILE_OFFSET64 ) || ( defined( BSD ) && ( BSD >= 199306 ) )
+#if defined( __USE_FILE_OFFSET64 ) || ( defined( BSD ) && ( BSD >= 199306 ) ) || defined(__APPLE__)
   fprintf( stderr, "[-l (large-file support for files > 2GB)] \n" );
 #endif
   exit( 1 );
diff -ruN vobcopy-0.5.5.orig/vobcopy.h vobcopy-0.5.5/vobcopy.h
--- vobcopy-0.5.5.orig/vobcopy.h	2002-10-29 18:45:39.000000000 -0700
+++ vobcopy-0.5.5/vobcopy.h	2002-11-28 17:06:09.000000000 -0700
@@ -4,7 +4,14 @@
 #define MAX_STRING  81
 #define MAX_DIFFER  2000
 
+#if defined(__APPLE__) && defined(__GNUC__)
+typedef int bool;
+
+#define FALSE 0
+#define TRUE 1
+#else
 typedef enum  { FALSE=0, TRUE=1 }  bool;
+#endif
 
 void usage(char *);
 int end_slash_adder( char * );
