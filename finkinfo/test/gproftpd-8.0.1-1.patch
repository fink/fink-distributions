diff -ruN gproftpd-8.0.1.orig/Makefile.in gproftpd-8.0.1/Makefile.in
--- gproftpd-8.0.1.orig/Makefile.in	2002-10-13 10:32:21.000000000 -0600
+++ gproftpd-8.0.1/Makefile.in	2002-10-23 18:44:17.000000000 -0600
@@ -435,14 +435,24 @@
 	    fi \
 	  done \
 	fi
-	mv $(DESTDIR)$(datadir)/pixmaps/$(PACKAGE)/net-gproftpd.desktop /usr/share/applications/
-	if test ! -d /etc/gproftpd; then \
-	  $(mkinstalldirs) /etc/gproftpd; \
+
+	if test ! -d $(DESTDIR)$(prefix)/share/gnome/apps/Internet; then \
+	  $(mkinstalldirs) $(DESTDIR)$(prefix)/share/gnome/apps/Internet; \
+	fi
+
+	mv $(DESTDIR)$(datadir)/pixmaps/$(PACKAGE)/net-gproftpd.desktop $(DESTDIR)$(prefix)/share/gnome/apps/Internet/
+
+	if test ! -d $(DESTDIR)$(prefix)/etc/gproftpd; then \
+	  $(mkinstalldirs) $(DESTDIR)$(prefix)/etc/gproftpd; \
+	fi
+
+	if test ! -d $(DESTDIR)$(prefix)/share/pixmaps; then \
+	  $(mkinstalldirs) $(DESTDIR)$(prefix)/share/pixmaps; \
 	fi
 
-	cp support/useradd-profile /etc/gproftpd
-	cp support/gproftpd.pem /etc/gproftpd
-	cp pixmaps/gproftpd.png /usr/share/pixmaps
+	cp support/useradd-profile $(DESTDIR)$(prefix)/etc/gproftpd
+	cp support/gproftpd.pem $(DESTDIR)$(prefix)/etc/gproftpd
+	cp pixmaps/gproftpd.png $(DESTDIR)$(prefix)/share/pixmaps
 	@echo ""
 	@echo "Start GProFTPD by typing gproftpd"
 	@echo "or start it from the menu panels internet section where i placed an application launcher."
