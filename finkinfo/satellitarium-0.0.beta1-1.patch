diff -Nurd -x'*~' satellitarium-0.0.beta1.orig/configure satellitarium-0.0.beta1/configure
--- satellitarium-0.0.beta1.orig/configure	2003-05-01 17:16:10.000000000 -0400
+++ satellitarium-0.0.beta1/configure	2003-07-06 23:24:23.000000000 -0400
@@ -5364,16 +5364,19 @@
 
   if test "x$kde_use_qt_mt" = "xyes"; then
 
-      case $host in
-      *-*-linux-*)
         if test "x$GCC" = "xyes"; then
-          LIBQT="-lqt-mt"
-          kde_int_qt="-lqt-mt"
-          LIBQT_GLOB="libqt-mt.*"
-          USING_QT_MT="using -mt"
+	    case $host in
+		*-*-linux-*)
+		;;
+		*)
+		echo "warning: qt-mt is experimental"
+		;;
+	    esac
+	    LIBQT="-lqt-mt"
+	    kde_int_qt="-lqt-mt"
+	    LIBQT_GLOB="libqt-mt.*"
+	    USING_QT_MT="using -mt"
         fi
-      ;;
-      esac
   fi
 
   kde_qt_was_given=no
