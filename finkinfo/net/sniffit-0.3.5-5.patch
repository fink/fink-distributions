diff -ru sniffit.0.3.5/Makefile.in sniffit.0.3.5-patched/Makefile.in
--- sniffit.0.3.5/Makefile.in	Fri Apr 18 03:33:57 1997
+++ sniffit.0.3.5-patched/Makefile.in	Sun Oct  7 13:50:49 2001
@@ -4,6 +4,10 @@
 
 .PHONY : clean
 
+# Install variables
+prefix=@prefix@
+manpath=@mandir@
+
 #Some Vars
 SNIFFIT    = sniffit.0.3.5.c
 CC         = @CC@
@@ -11,9 +15,9 @@
 DEFS	   = @DEFS@
 OS_OPT     = @OS_OPT@
 OBJ_FLAG   = -w -O2 -c
-OBJ_OPT    = -I./libpcap-0.3 -L./libpcap-0.3
+OBJ_OPT    = -I@includedir@ -I@FINKPREFIX@/include -L@libdir@ -L@FINKPREFIX@/lib
 EXE_FLAG   = -w -O2 -o sniffit
-EXE_OPT    = -I./libpcap-0.3 -L./libpcap-0.3 -lpcap
+EXE_OPT    = -I@includedir@ -I@FINKPREFIX@/include -L@libdir@ -L@FINKPREFIX@/lib -lpcap
 EXE_OBJ    = sn_packets.o sn_generation.o sn_interface.o sn_cfgfile.o \
              sn_logfile.o
 DEP_FILES  = sn_config.h pcap.h sn_data.h sn_defines.h sn_plugins.h \
@@ -21,7 +25,7 @@
              sn_generation.o sn_interface.o sn_cfgfile.o sn_logfile.o
 
 sniffit: $(SNIFFIT) $(DEP_FILES)
-	cd libpcap-0.3; make; cd ..
+#	cd libpcap-0.3; make; cd ..
 	$(CC) $(EXE_FLAG) $(SNIFFIT) $(EXE_OBJ) $(EXE_OPT) $(LIBS) $(DEFS) $(OS_OPT) 
 	strip sniffit
 
@@ -52,3 +56,9 @@
 	rm -f ./libpcap-0.3/config.status
 	rm -f ./libpcap-0.3/config.log
 	rm -f *.o sniffit
+
+#Install   
+install:   
+	./install-sh sniffit $(prefix)/bin/sniffit
+	./install-sh sniffit.5 $(manpath)/man5/sniffit.5          
+	./install-sh sniffit.8 $(manpath)/man8/sniffit.8          
Only in sniffit.0.3.5-patched/: config.cache
Only in sniffit.0.3.5/: config.sub
diff -ru sniffit.0.3.5/configure sniffit.0.3.5-patched/configure
--- sniffit.0.3.5/configure	Fri Apr 18 03:33:57 1997
+++ sniffit.0.3.5-patched/configure	Sun Oct  7 13:47:40 2001
@@ -712,7 +712,7 @@
 fi
 
 
-NCURSES_PATH=/usr/include:/usr/include/ncurses:/usr/include/curses:/usr/local/include:/usr/local/include/ncurses:/usr/local/include/curses
+NCURSES_PATH=@FINKPREFIX@/include:/usr/include:/usr/include/ncurses:/usr/include/curses:/usr/local/include:/usr/local/include/ncurses:/usr/local/include/curses
 echo $ac_n "checking for main in -lncurses""... $ac_c" 1>&6
 echo "configure:718: checking for main in -lncurses" >&5
 ac_lib_var=`echo ncurses'_'main | sed 'y%./+-%__p_%'`
@@ -1301,6 +1301,14 @@
 #define FREEBSD 1
 EOF
 
+        OS_OPT=
+    
+        ;;  
+darwin*)   
+        cat >> confdefs.h <<\EOF
+#define FREEBSD 1
+EOF
+
 	OS_OPT=
 	
         ;;
@@ -1612,7 +1620,7 @@
       continue
     fi
 
-    echo configuring in $ac_config_dir
+#    echo configuring in $ac_config_dir
 
     case "$srcdir" in
     .) ;;
@@ -1659,13 +1667,13 @@
         ac_sub_cache_file="$ac_dots$cache_file" ;;
       esac
 
-      echo "running ${CONFIG_SHELL-/bin/sh} $ac_sub_configure $ac_sub_configure_args --cache-file=$ac_sub_cache_file --srcdir=$ac_sub_srcdir"
+#      echo "running ${CONFIG_SHELL-/bin/sh} $ac_sub_configure $ac_sub_configure_args --cache-file=$ac_sub_cache_file --srcdir=$ac_sub_srcdir"
       # The eval makes quoting arguments work.
-      if eval ${CONFIG_SHELL-/bin/sh} $ac_sub_configure $ac_sub_configure_args --cache-file=$ac_sub_cache_file --srcdir=$ac_sub_srcdir
-      then :
-      else
-        { echo "configure: error: $ac_sub_configure failed for $ac_config_dir" 1>&2; exit 1; }
-      fi
+#      if eval ${CONFIG_SHELL-/bin/sh} $ac_sub_configure $ac_sub_configure_args --cache-file=$ac_sub_cache_file --srcdir=$ac_sub_srcdir
+#      then :
+#      else
+#        { echo "configure: error: $ac_sub_configure failed for $ac_config_dir" 1>&2; exit 1; }
+#      fi
     fi
 
     cd $ac_popdir
