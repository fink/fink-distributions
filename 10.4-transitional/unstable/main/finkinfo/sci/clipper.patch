diff -ur clipper-orig/configure clipper/configure
--- clipper-orig/configure	2003-11-14 03:19:05.000000000 -0800
+++ clipper/configure	2004-04-20 10:36:29.000000000 -0700
@@ -19811,8 +19811,8 @@
     if test "`basename $CXX`" = gcc || test "`basename $CXX`" = g++; then
     case "$CXXFLAGS" in
       *-O0* ) ;;
-      * )
-        CXXFLAGS="$CXXFLAGS -O0" # problem with PIC relocation tables for 3.1
+      # * )
+      #  CXXFLAGS="$CXXFLAGS -O0" # problem with PIC relocation tables for 3.1
       esac
     fi
   ;;
@@ -19938,7 +19938,7 @@
  # SGI compiler CC (CXX=CC) needs -lm to link maths library, but
  # GCC c++ does not.
  #
- ac_FFTW_LDOPTS="-L$fftw_prefix/lib -lrfftw -lfftw"
+ ac_FFTW_LDOPTS="-L$fftw_prefix/lib -lsrfftw -lsfftw"
 else
  # the compiler looks in the "standard" places for FFTW.  In real life,
  # it would be quite possible that FFTW would not be installed in
@@ -19970,7 +19970,7 @@
 cat confdefs.h >>conftest.$ac_ext
 cat >>conftest.$ac_ext <<_ACEOF
 /* end confdefs.h.  */
-#include "fftw.h"
+#include "sfftw.h"
 int
 main ()
 {
@@ -20007,7 +20007,7 @@
 cat confdefs.h >>conftest.$ac_ext
 cat >>conftest.$ac_ext <<_ACEOF
 /* end confdefs.h.  */
-#include <fftw.h>
+#include <sfftw.h>
 int
 main ()
 {
