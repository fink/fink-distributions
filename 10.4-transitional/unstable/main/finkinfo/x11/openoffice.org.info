Info2: <<
Package: openoffice.org
Description: Integrated office productivity suite
Version: 2.0.0
Revision: 22
License: LGPL
Maintainer: Todai Fink Team <fink@sodan.ecc.u-tokyo.ac.jp>

BuildDepends: <<
  x11-dev, ant, bison, fileutils, system-java14-dev,
  archive-zip-pm586,
  libjpeg, expat, freetype219, libwpd-0.8-dev, libxml2,
  db42-ssl | db42,
  libsablot-dev, libsablot, unixodbc2 | unixodbc2-nox,
  sane-backends, libcurl3-ssl | libcurl3,
  libsndfile1-dev,
  portaudio (>= 18.1-1), neon24-ssl | neon24,
  mozilla-dev, libart2, startup-notification-dev,
  atk1, audiofile, esound, gconf2-dev, libgettext3-dev, glib2-dev,
  gnome-keyring-dev (>= 0.4.3-1), gnome-vfs2-ssl-dev | gnome-vfs2-dev,
  gtk+2-dev, libbonobo2-dev, libbonoboui2-dev, libglade2,
  libgnome2-dev, libgnomecanvas2-dev, libgnomeprint2.2-dev,
  libgnomeprintui2.2-dev, libgnomeui2-dev, libiconv-dev, 
  orbit2-dev, pango1-xft2-dev, pcre, pkgconfig, popt, vte-dev, 
  python24, 
  boost1.32-py23 | boost1.32-py24,
  autoconf2.5, gcc3.3
<<

Depends: <<
  x11, system-java14, system-perl,
  libjpeg-shlibs, expat-shlibs, libwpd-0.8-shlibs, libxml2-shlibs,
  db42-ssl-shlibs | db42-shlibs, db42-ssl-java,
  libsablot-shlibs, unixodbc2-shlibs | unixodbc2-nox-shlibs,
  sane-backends-shlibs, libcurl3-ssl-shlibs | libcurl3-shlibs,
  libsndfile1-shlibs,
  portaudio-shlibs (>= 18.1-1), neon24-ssl-shlibs | neon24-shlibs,
  python24-shlibs, 
  boost1.32-py23-shlibs | boost1.32-py24-shlibs, 
  mozilla-shlibs, mozilla-mailnews, libart2-shlibs,
  fondu, startup-notification-shlibs
<<

# FIXME -- workaround for IssueZilla #54110 and #54113 (duplicities)
Replaces: <<
  openoffice.org-bg,
  openoffice.org-en-gb,
  openoffice.org
<<

CustomMirror: <<
aus-AU: http://openoffice.mirrors.ilisys.com.au/
aus-AU: ftp://mirror.pacific.net.au/OpenOffice/
aus-AU: http://mirror.pacific.net.au/openoffice/
aus-AU: ftp://ftp.planetmirror.com/pub/openoffice/
aus-AU: http://public.planetmirror.com/pub/openoffice/
eur-AT: http://gd.tuwien.ac.at/office/openoffice/
eur-AT: ftp://gd.tuwien.ac.at/office/openoffice/
eur-BE: http://ftp.belnet.be/pub/mirror/ftp.openoffice.org/
#eur-BE: ftp://ftp.scarlet.be/pub/openoffice/
#eur-BE: http://ftp.scarlet.be/pub/openoffice/
eur-BE: ftp://ftp.openoffice.skynet.be/pub/ftp.openoffice.org/
sam-BR: http://linorg.usp.br/OpenOffice.org/
sam-BR: ftp://ftp.pucpr.br/openoffice/
#nam-CA: ftp://openoffice.mirror.cygnal.ca/openoffice/
#nam-CA: http://openoffice.mirror.cygnal.ca/
nam-CA: http://gulus.USherbrooke.ca/pub/appl/openoffice/
eur-CZ: http://ftp.sh.cvut.cz/MIRRORS/OpenOffice/
eur-CZ: ftp://ftp.sh.cvut.cz/MIRRORS/OpenOffice/
eur-DK: http://mirrors.sunsite.dk/openoffice/
eur-DK: ftp://sunsite.dk/mirrors/openoffice/
eur-FR: ftp://openoffice.cict.fr/openoffice/
eur-FR: ftp://ftp.free.fr/mirrors/ftp.openoffice.org/
eur-DE: ftp://ftp.tu-chemnitz.de/pub/openoffice/
#eur-DE: ftp://sunsite.informatik.rwth-aachen.de/pub/mirror/OpenOffice/
#eur-DE: ftp://ftp.leo.org/pub/comp/general/office/openoffice/
eur-DE: http://ftp.leo.org/pub/comp/general/office/openoffice/
#eur-DE: ftp://ftp.join.uni-muenster.de/pub/software/OpenOffice/
eur-DE: ftp://openoffice.tu-bs.de/OpenOffice.org/
#eur-DE: http://ftp.stardiv.de/pub/OpenOffice.org/
eur-DE: ftp://ftp-stud.fht-esslingen.de/pub/Mirrors/ftp.openoffice.org/
eur-DE: http://mirror.xaranet.de/openoffice/
#eur-GR: http://www.ellak.gr/pub/openoffice/
#eur-GR: http://ftp.ntua.gr/pub/openoffice/
#eur-GR: ftp://ftp.ntua.gr/pub/openoffice/
eur-HU: http://ftp.fsf.hu/OpenOffice.org/
#eur-HU: ftp://ftp.fsf.hu/OpenOffice.org/
eur-IE: ftp://ftp.heanet.ie/mirrors/openoffice.org/
eur-IE: http://ftp.heanet.ie/mirrors/openoffice.org/
asi-JP: ftp://ftp.kddlabs.co.jp/office/openoffice/
#asi-JP: http://www.ring.gr.jp/archives/misc/openoffice/
#asi-JP: ftp://ftp.ring.gr.jp/pub/misc/openoffice/
eur-LT: http://files.akl.lt/OpenOffice.org/
#asi-MY: http://mymirror.asiaosc.org/openoffice/
eur-NL: http://ftp.nluug.nl/pub/office/openoffice/
eur-NL: ftp://ftp.snt.utwente.nl/pub/software/openoffice/
eur-NL: http://vlaai.snt.utwente.nl/pub/software/openoffice/
eur-PL: ftp://ftp.man.poznan.pl/pub/openoffice/
eur-PL: ftp://ftp.openoffice.pl/OpenOffice.ORG/
#eur-PT: http://ftpdem.ubi.pt/OpenOffice
#eur-PT: http://mirrors.oninet.pt/openoffice/
#eur-PT: http://tux.cprm.net/pub/openoffice.org/
#eur-PT: ftp://tux.cprm.net/pub/openoffice.org/
eur-RO: ftp://ftp.iasi.roedu.net/pub/mirrors/openoffice.org/
eur-RO: http://ftp.iasi.roedu.net/mirrors/openoffice.org/
eur-YU: ftp://mirror.etf.bg.ac.yu/openoffice.org/
eur-YU: http://mirror.etf.bg.ac.yu/openoffice/
asi-SG: ftp://mirror.averse.net/pub/openoffice/
asi-SG: http://mirror.averse.net/openoffice/
eur-SI: ftp://ftp.bevc.net/mirrors/openoffice/
eur-SI: http://mirrors.bevc.net/openoffice/
#eur-SI: http://www.wsection.com/openoffice/
asi-KR: ftp://ftp.kr.freebsd.org/pub/openoffice/
eur-ES: http://ftp.rediris.es/ftp/mirror/openoffice.org/
eur-ES: ftp://ftp.rediris.es/mirror/openoffice.org/
eur-SE: http://ftp.sunet.se/pub/Office/OpenOffice.org/
#eur-CH: ftp://sunsite.cnlab-switch.ch/mirror/OpenOffice/
#eur-CH: http://mirror.switch.ch/ftp/mirror/openoffice/
#eur-CH: ftp://mirror.switch.ch/mirror/openoffice/
eur-UK: http://www.mirror.ac.uk/mirror/sunsite.dk/openoffice/
#eur-UK: ftp://mirrors.blueyonder.co.uk/sites/openoffice.org
#eur-UK: http://openoffice.blueyonder.co.uk
eur-UK: ftp://ftp.mirror.ac.uk/mirror/sunsite.dk/openoffice/
eur-UK: http://www.mirror.ac.uk/mirror/sunsite.dk/openoffice/
#nam-US: http://www.binarycode.org/openoffice/
#nam-US: http://mirrors.ibiblio.org/pub/mirrors/openoffice/
#nam-US: ftp://ftp.ibiblio.org/pub/mirrors/openoffice/
#nam-US: ftp://ftp.ussg.iu.edu/pub/openoffice/
nam-US: http://mirrors.isc.org/pub/openoffice/
#nam-US: http://www.online-mirror.org/openoffice/
nam-US: ftp://openoffice.mirrors.pair.com/
#nam-US: http://openoffice.mirrors.pair.com/
nam-US: ftp://openofficeorg.secsup.org/pub/software/openoffice/
nam-US: http://openofficeorg.secsup.org/
nam-US: http://openoffice.mirror.wrpn.net/
#asi-TW: ftp://ftp.nctu.edu.tw/UNIX/OpenOffice/
<<
Source: mirror:custom:stable/%v/OOo_%v_src.tar.gz
SourceDirectory: OOo_%vrc3_src
Source-MD5: a68933afc2bf432d11b2043ac99ba0aa

PatchScript: <<
  /usr/bin/patch -p0 < %a/%n.patch
<<

SetCC: gcc-3.3
SetCXX: g++-3.3
SetCPPFLAGS: -I%p/include/db4 -I%p/include/boost
ConfigureParams: <<
  --with-gnu-cp=%p/bin/cp \
  --disable-epm \
  --with-lang=ALL \
  --with-x --x-includes=/usr/X11R6/include --x-libraries=/usr/X11R6/lib \
  --with-jdk-home=/Library/Java/Home --with-ant-home=%p/lib/ant \
  --disable-crashdump \
  --with-build-version="%v-%r; Built with Fink <http://fink.sourceforge.net>" \
  --enable-libart \
  --enable-pasf \
  --with-system-db \
  --with-db-jar=%p/share/java/db42-ssl-java/db.jar \
  --with-system-libwpd \
  --with-system-sablot \
  --with-system-odbc-headers \
  --with-system-sane-header \
  --with-system-xrender-headers \
  --with-system-curl \
  --with-system-sndfile \
  --with-system-portaudio \
  --with-system-neon \
  --with-system-stdlibs \
  --with-system-zlib \
  --with-system-jpeg \
  --with-system-expat \
  --with-system-mozilla \
  --enable-gtk \
  --with-system-freetype \
  --with-system-boost \
  --without-nas \
  --enable-libsn
<<
#  --with-system-python
#  --with-system-libxml \

GCC: 3.3
CompileScript: <<
  #!/bin/bash

  set -e

  echo "[ Message from OpenOffice.org package maintainer ] ================="
  echo
  echo "Welcome to OpenOffice.org build script!"
  echo
  echo "Notice for builders:"
  echo
  echo "1. This building process may take a couple of day or more,"
  echo "   and often fails with errors."
  echo "2. This building process may consume much of your disk space,"
  echo "   possibly up to 10GB."
  echo "3. This building process needs your own WindowServer process."
  echo "   i.e, it is not possible to build through simple SSH connection."
  echo "   This is because the building process uses /usr/bin/osacompile ."
  echo "   (This limitation would be removed in the future release.)"
  echo
  echo "If you faced a building problem, feel free to mail the maintainer,"
  echo "preferably with your environment (gcc-3.3's version, etc.) and"
  echo "the build log."
  echo "You can find the logs here:"
  echo "   %b/%n-%v-%r.buildlog"
  echo
  echo "The mail address is: Todai Fink Team <fink@sodan.ecc.u-tokyo.ac.jp>"
  echo
  echo "Please understand this Fink package is still unstable."
  echo
  echo "===================================================================="
  echo
  echo "If you are ready, press RETURN/ENTER to proceed..."

  read -t 60 || true

  echo

  # Record the time
  STARTTIME=`/bin/date +"%%F %%T %%Z(%%z)"`

  set -v

  # $X_LDFLAGS is needed to configure with X correctly.  
  export X_LDFLAGS=$LDFLAGS

  # $PKG_CONFIG_PATH is needed to configure with freetype219
  export PKG_CONFIG_PATH=%p/lib/freetype219/lib/pkgconfig

  /usr/bin/printf "[ Phase 1: Configure ]\n\n" >> %n-%v-%r.buildlog
  (cd config_office && autoconf && ./configure %c) 2>&1 |
    /usr/bin/tee -a %n-%v-%r.buildlog

  /usr/bin/printf "\n\n[ Phase 2: Bootstrap ]\n\n" >> %n-%v-%r.buildlog
  ./bootstrap 2>&1 |
    /usr/bin/tee -a %n-%v-%r.buildlog

  # Because we are using %p, $SOLARINC and $SOLARLIB need modified  
  # Include libdb_java-4.2.jnilib to DYLD_LIBRARY_PATH so that Java can find it
  /usr/bin/sed -i.bak \
    -e"/^SOLARLIB/s|-L/usr/lib|-L%p/lib/freetype219/lib -L%p/lib -L/usr/lib|" \
    -e"/^SOLARINC/s|-I/usr/X11R6/include -I/usr/X11R6/include/freetype2|-I%p/include/db4 -I%p/lib/freetype219/include/freetype2 -I%p/lib/freetype219/include -I%p/include -I/usr/X1186/include|" \
    -e"/^DYLD_LIBRARY_PATH/s|/lib|/lib:%b/FINKLIBS|" \
    MacosxEnv.Set.sh

  /bin/mkdir FINKLIBS
  /bin/ln -s %p/lib/libdb_java-4.2.jnilib FINKLIBS

  # Retry forever to build OOo until success!
  until [ -n "$succeeded" ]; do
    /usr/bin/printf "\n\n[ Phase 3: Make ]\n\n" >> %n-%v-%r.buildlog
    . MacosxEnv.Set.sh
    dmake 2>&1 | /usr/bin/tee -a %n-%v-%r.buildlog
    if [ ${PIPESTATUS[0]} == 0 ]; then
      succeeded=TRUE
      continue
    fi
    echo
    echo "[ Message from OpenOffice.org package maintainer ] ================="
    echo
    echo "Building OpenOffice.org faild with an error."
    echo "Now you can get rid of the cause by hand, or call for help."
    echo "The build directory is:"
    echo "  %b"
    echo "and the full log file is available here:"
    echo "  %b/%n-%v-%r.buildlog"
    echo
    echo "===================================================================="
    echo
    echo "Press RETURN/ENTER to exit or input any key to restart dmake..."
    read input
    [ -n "$input" ] || false
  done

  set +v

  /usr/bin/printf "\n\n[ Phase 4: Statistics ]\n\n" >> %n-%v-%r.buildlog
  echo
  echo "[ Message from OpenOffice.org package maintainer ] ================="
  echo
  echo "Congratulations!"
  echo "The building process of OpenOffice.org has completed!"
  echo "   Started:   $STARTTIME" | /usr/bin/tee -a %n-%v-%r.buildlog

  ENDTIME=`/bin/date +"%%F %%T %%Z(%%z)"`

  echo "   Completed: $ENDTIME"   | /usr/bin/tee -a %n-%v-%r.buildlog

  DISKUSAGE=`/usr/bin/du -sh %b | /usr/bin/cut -f1`

  echo "   $DISKUSAGE is used for this building process (not including tarball)." | /usr/bin/tee -a %n-%v-%r.buildlog
  echo
  echo "===================================================================="
  echo
<<

InstallScript: <<
  #!/bin/bash -e

  # Mainly, this InstallScript is derived from
  # "ooinstall" in ooo-build and instsetoo_native/util/makefile.mk

  # Setting up environment
  . MacosxEnv.Set.sh
  export OUT="../$INPATH"
  export LOCAL_OUT="$OUT"
  export LOCAL_COMMON_OUT="$OUT"

  build=`sed -n 's/^BUILD=\(.*\)$/\1/p' $SOLARENVINC/minor.mk`

  set -v

  # Some dirty hacks to reduce disk consumption

  # Force make_installer.pl to use ln instead of cp
  /usr/bin/sed -i .bak 's/cp -af/ln -f/' \
    $SOLARENV/bin/modules/installer/worker.pm
  # Disable making download installation set  
  /usr/bin/sed -i .bak 's/$makedownload = .;/$makedownload = 0;/' \
    $SOLARENV/bin/modules/installer/globals.pm

  # Install
  pushd instsetoo_native/util
  /usr/bin/perl -w $SOLARENV/bin/make_installer.pl \
    -f openoffice.lst -l en-US -p OpenOffice \
    -packagelist ../inc_openoffice/unix/packagelist.txt \
    -buildid $build -destdir %i \
    -simple /lib/openoffice.org
  popd

  # Remove the list files
  /bin/rm %i/gid_Module*

  # Install pyuno
  # -- derived from the post-install script of openoffice.org-pyuno.install
  /bin/ln -s python-core-2.3.4 %i/lib/%n/program/python-core
  /bin/ln -s python.sh %i/lib/%n/program/python
  /bin/chmod +x %i/lib/%n/program/python-core-2.3.4/bin/python

  # Convenience symlinks
  /usr/bin/install -m 755 -d %i/bin
  for bin in base calc draw impress math writer; do
    /bin/ln -s %p/lib/%n/program/s${bin} %i/bin
    /bin/ln -s s${bin} %i/bin/oo${bin}
  done
  /bin/ln -s %p/lib/%n/program/soffice %i/bin
  /usr/bin/sed -e "s/ -calc//" %i/bin/scalc > %i/bin/ooffice
  /bin/chmod 755 %i/bin/ooffice

  # Symlink libdb_java-4.2.jnilib so that Java can find it
  /bin/ln -s %p/lib/libdb_java-4.2.jnilib %i/lib/%n/program

  # Install DocFiles
  /usr/bin/install -m 755 -d %i/share/doc/%n
  /bin/ln -s %i/lib/%n/LICENSE %i/share/doc/%n/LICENSE

  # Currently, Fink cannot handle "OpenOffice.org 2.0.app"
  # (which contains space character) with AppBundles field.
  /usr/bin/install -d -m 755 %i/Applications
  /usr/bin/tar -xf $STAR_RESOURCEPATH/OpenOffice.org.app.tar -C %i/Applications
  /bin/mv %i/Applications/OpenOffice.org.app "%i/Applications/OpenOffice.org 2.0.app"
  /bin/ln -s %p/lib/%n "%i/Applications/OpenOffice.org 2.0.app/Contents"
  /bin/chmod -R o-w '%i/Applications/'
  [ -x /Developer/Tools/SplitForks ] && /Developer/Tools/SplitForks '%i/Applications/'
<<
#AppBundles: instsetoo_native/unxmacxp.pro/OpenOffice/install/en-US_temp/OpenOffice.org*.app
PostInstScript: <<
  cd %p/lib/openoffice.org/share/fonts/truetype
  echo "Converting Mac OS X fonts..."
  /usr/bin/touch .fondudone
  for font in /System/Library/Fonts/* /Library/Fonts/*; do
    %p/bin/fondu --force "$font" || true
  done
  
  [ ! -e /Applications/Fink ] && /usr/bin/install -d -m 755 /Applications/Fink
  /bin/ln -s '%p/Applications/OpenOffice.org 2.0.app' /Applications/Fink/
<<
PostRmScript: /bin/rm -f '/Applications/Fink/OpenOffice.org 2.0.app'

Homepage: http://www.openoffice.org/

DescDetail: <<
OpenOffice.org is an Open Source, community-developed, multi-platform office
productivity suite. It includes the key desktop applications, such as a
word processor, spreadsheet, presentation manager, and drawing program,
with a user interface and feature set similar to other office suites.

Components include:
  * A universal word processing application for creating business
    letters, extensive text documents, professional layouts, and HTML
    documents.
  * A sophisticated application for performing advanced spreadsheet
    functions, such as analyzing figures, creating lists, and viewing data.
  * A tool for creating effective eye-catching presentations.
  * A vector-oriented draw module that enables the creation of 3D
    illustrations.
<<
DescPort: <<
This package is advised by NAKATA Maho <maho@FreeBSD.org>.
And furthermore, much advice from _rene_, ericb2, paveljanik, pmladek, shres
and ssa @ irc://irc.freenode.net/OpenOffice.org . Thanks to all OOo persons!

You can join discussions on porting here:
http://porting.openoffice.org/servlets/SummarizeList?listName=dev

It is reported that:
  * Mac OS X SDK (which comes with Xcode) is needed.
  * Sometimes checking getaddrinfo() fails on configuring python (on Panther?).
  * Some files in openoffice.org-en-gb and -br interfere with others.

Because DYLD_LIBRARY_PATH does not contain %p/lib by default,
Java cannot find %p/lib/libdb_java-4.2.jnilib at compile time and runtime.
This script tries to make a symlink of it in %p/lib/openoffice.org/programs.
<<
DescPackaging: <<
To Do 0: Report build failues on enabling system Python and libxml2
  ( --with-system-python and --with-system-libxml2 )
Fails on building instsetooo_native... 

To Do 1: Report build failures on building nas (Network Audio System)
  ( --without-nas or --with-system-nas )
with internal nas, build fails on Panther. 

"boost" is required on compile time, but not needed on runtime.

These --with-system flags are not used because they are not in Fink:
   --with-system-mspack
   --with-system-myspell
   --with-system-mythes
   --with-system-altlinuxhyph
   --enable-evolution2

I have no idea about openldap. Maybe mozilla is enough?
   --enable-openldap

Some comments about configure logs:
# checking whether to enable fontconfig support... no
Using fontconfig is not supported on Mac OS X.

# checking whether to build Mozilla addressbook connectivity...
#  no, not possible with system-mozilla
# checking whether to use Mozilla or Firefox... Mozilla
Shall I use Firefox instead of Mozilla?
<<
DescUsage: <<
To start OpenOffice.org, type "soffice" on your terminal,
or double-click "OpenOffice.org 2.0.app" icon located at /Applications/Fink .

To use GTK+ look and feel,
set SAL_USE_VCLPLUGIN environmental variable to "gtk".
normally this is done by rewriting %p/bin/soffice startup script.

To update fonts, execute these lines:
  cd %p/lib/openoffice.org/share/fonts/truetype
  for font in /System/Library/Fonts/* /Library/Fonts/*; do
    %p/bin/fondu --force "$font"
  done
or just one line:
  fink reinstall openoffice.org
<<

SplitOff: <<
  Package: %N-af
  Description: Language Pack(af) for OpenOffice.org
  Depends: %N (= %v-%r)
  InstallScript: <<
    #!/bin/bash -e

    # Setting up environment
    . MacosxEnv.Set.sh
    export OUT="../$INPATH"
    export LOCAL_OUT="$OUT"
    export LOCAL_COMMON_OUT="$OUT"

    build=`sed -n 's/^BUILD=\(.*\)$/\1/p' $SOLARENVINC/minor.mk`

    set -v

    # Install language pack af
    pushd instsetoo_native/util
    /usr/bin/perl -w $SOLARENV/bin/make_installer.pl \
      -f openoffice.lst -l af -p OpenOffice \
      -packagelist ../inc_openoffice/unix/packagelist.txt \
      -buildid $build -destdir %i \
      -languagepack \
      -simple /lib/openoffice.org
    popd

    # Remove the list files
    /bin/rm -vf %i/gid_Module*

    /usr/bin/install -m 755 -d %i/share/doc/%n
    /bin/ln -sf %i/lib/%n/LICENSE_af %i/share/doc/%n/LICENSE
  <<
<<

SplitOff2: <<
  Package: %N-ar
  Description: Language Pack(ar) for OpenOffice.org
  Depends: %N (= %v-%r)
  InstallScript: <<
    #!/bin/bash -e

    # Setting up environment
    . MacosxEnv.Set.sh
    export OUT="../$INPATH"
    export LOCAL_OUT="$OUT"
    export LOCAL_COMMON_OUT="$OUT"

    build=`sed -n 's/^BUILD=\(.*\)$/\1/p' $SOLARENVINC/minor.mk`

    set -v

    # Install language pack ar
    pushd instsetoo_native/util
    /usr/bin/perl -w $SOLARENV/bin/make_installer.pl \
      -f openoffice.lst -l ar -p OpenOffice \
      -packagelist ../inc_openoffice/unix/packagelist.txt \
      -buildid $build -destdir %i \
      -languagepack \
      -simple /lib/openoffice.org
    popd

    # Remove the list files
    /bin/rm -vf %i/gid_Module*

    /usr/bin/install -m 755 -d %i/share/doc/%n
    /bin/ln -sf %i/lib/%n/LICENSE_ar %i/share/doc/%n/LICENSE
  <<
<<

SplitOff3: <<
  Package: %N-bg
  Description: Language Pack(bg) for OpenOffice.org
  Depends: %N (= %v-%r)
  InstallScript: <<
    #!/bin/bash -e

    # Setting up environment
    . MacosxEnv.Set.sh
    export OUT="../$INPATH"
    export LOCAL_OUT="$OUT"
    export LOCAL_COMMON_OUT="$OUT"

    build=`sed -n 's/^BUILD=\(.*\)$/\1/p' $SOLARENVINC/minor.mk`

    set -v

    # Install language pack bg
    pushd instsetoo_native/util
    /usr/bin/perl -w $SOLARENV/bin/make_installer.pl \
      -f openoffice.lst -l bg -p OpenOffice \
      -packagelist ../inc_openoffice/unix/packagelist.txt \
      -buildid $build -destdir %i \
      -languagepack \
      -simple /lib/openoffice.org
    popd

    # Remove the list files
    /bin/rm -vf %i/gid_Module*

    /usr/bin/install -m 755 -d %i/share/doc/%n
    /bin/ln -sf %i/lib/%n/LICENSE_bg %i/share/doc/%n/LICENSE
  <<
<<

SplitOff4: <<
  Package: %N-ca
  Description: Language Pack(ca) for OpenOffice.org
  Depends: %N (= %v-%r)
  InstallScript: <<
    #!/bin/bash -e

    # Setting up environment
    . MacosxEnv.Set.sh
    export OUT="../$INPATH"
    export LOCAL_OUT="$OUT"
    export LOCAL_COMMON_OUT="$OUT"

    build=`sed -n 's/^BUILD=\(.*\)$/\1/p' $SOLARENVINC/minor.mk`

    set -v

    # Install language pack ca
    pushd instsetoo_native/util
    /usr/bin/perl -w $SOLARENV/bin/make_installer.pl \
      -f openoffice.lst -l ca -p OpenOffice \
      -packagelist ../inc_openoffice/unix/packagelist.txt \
      -buildid $build -destdir %i \
      -languagepack \
      -simple /lib/openoffice.org
    popd

    # Remove the list files
    /bin/rm -vf %i/gid_Module*

    /usr/bin/install -m 755 -d %i/share/doc/%n
    /bin/ln -sf %i/lib/%n/LICENSE_ca %i/share/doc/%n/LICENSE
  <<
<<

SplitOff5: <<
  Package: %N-cs
  Description: Language Pack(cs) for OpenOffice.org
  Depends: %N (= %v-%r)
  InstallScript: <<
    #!/bin/bash -e

    # Setting up environment
    . MacosxEnv.Set.sh
    export OUT="../$INPATH"
    export LOCAL_OUT="$OUT"
    export LOCAL_COMMON_OUT="$OUT"

    build=`sed -n 's/^BUILD=\(.*\)$/\1/p' $SOLARENVINC/minor.mk`

    set -v

    # Install language pack cs
    pushd instsetoo_native/util
    /usr/bin/perl -w $SOLARENV/bin/make_installer.pl \
      -f openoffice.lst -l cs -p OpenOffice \
      -packagelist ../inc_openoffice/unix/packagelist.txt \
      -buildid $build -destdir %i \
      -languagepack \
      -simple /lib/openoffice.org
    popd

    # Remove the list files
    /bin/rm -vf %i/gid_Module*

    /usr/bin/install -m 755 -d %i/share/doc/%n
    /bin/ln -sf %i/lib/%n/LICENSE_cs %i/share/doc/%n/LICENSE
  <<
<<

SplitOff6: <<
  Package: %N-cy
  Description: Language Pack(cy) for OpenOffice.org
  Depends: %N (= %v-%r)
  InstallScript: <<
    #!/bin/bash -e

    # Setting up environment
    . MacosxEnv.Set.sh
    export OUT="../$INPATH"
    export LOCAL_OUT="$OUT"
    export LOCAL_COMMON_OUT="$OUT"

    build=`sed -n 's/^BUILD=\(.*\)$/\1/p' $SOLARENVINC/minor.mk`

    set -v

    # Install language pack cy
    pushd instsetoo_native/util
    /usr/bin/perl -w $SOLARENV/bin/make_installer.pl \
      -f openoffice.lst -l cy -p OpenOffice \
      -packagelist ../inc_openoffice/unix/packagelist.txt \
      -buildid $build -destdir %i \
      -languagepack \
      -simple /lib/openoffice.org
    popd

    # Remove the list files
    /bin/rm -vf %i/gid_Module*

    /usr/bin/install -m 755 -d %i/share/doc/%n
    /bin/ln -sf %i/lib/%n/LICENSE_cy %i/share/doc/%n/LICENSE
  <<
<<

SplitOff7: <<
  Package: %N-da
  Description: Language Pack(da) for OpenOffice.org
  Depends: %N (= %v-%r)
  InstallScript: <<
    #!/bin/bash -e

    # Setting up environment
    . MacosxEnv.Set.sh
    export OUT="../$INPATH"
    export LOCAL_OUT="$OUT"
    export LOCAL_COMMON_OUT="$OUT"

    build=`sed -n 's/^BUILD=\(.*\)$/\1/p' $SOLARENVINC/minor.mk`

    set -v

    # Install language pack da
    pushd instsetoo_native/util
    /usr/bin/perl -w $SOLARENV/bin/make_installer.pl \
      -f openoffice.lst -l da -p OpenOffice \
      -packagelist ../inc_openoffice/unix/packagelist.txt \
      -buildid $build -destdir %i \
      -languagepack \
      -simple /lib/openoffice.org
    popd

    # Remove the list files
    /bin/rm -vf %i/gid_Module*

    /usr/bin/install -m 755 -d %i/share/doc/%n
    /bin/ln -sf %i/lib/%n/LICENSE_da %i/share/doc/%n/LICENSE
  <<
<<

SplitOff8: <<
  Package: %N-de
  Description: Language Pack(de) for OpenOffice.org
  Depends: %N (= %v-%r)
  InstallScript: <<
    #!/bin/bash -e

    # Setting up environment
    . MacosxEnv.Set.sh
    export OUT="../$INPATH"
    export LOCAL_OUT="$OUT"
    export LOCAL_COMMON_OUT="$OUT"

    build=`sed -n 's/^BUILD=\(.*\)$/\1/p' $SOLARENVINC/minor.mk`

    set -v

    # Install language pack de
    pushd instsetoo_native/util
    /usr/bin/perl -w $SOLARENV/bin/make_installer.pl \
      -f openoffice.lst -l de -p OpenOffice \
      -packagelist ../inc_openoffice/unix/packagelist.txt \
      -buildid $build -destdir %i \
      -languagepack \
      -simple /lib/openoffice.org
    popd

    # Remove the list files
    /bin/rm -vf %i/gid_Module*

    /usr/bin/install -m 755 -d %i/share/doc/%n
    /bin/ln -sf %i/lib/%n/LICENSE_de %i/share/doc/%n/LICENSE
  <<
<<

SplitOff9: <<
  Package: %N-el
  Description: Language Pack(el) for OpenOffice.org
  Depends: %N (= %v-%r)
  InstallScript: <<
    #!/bin/bash -e

    # Setting up environment
    . MacosxEnv.Set.sh
    export OUT="../$INPATH"
    export LOCAL_OUT="$OUT"
    export LOCAL_COMMON_OUT="$OUT"

    build=`sed -n 's/^BUILD=\(.*\)$/\1/p' $SOLARENVINC/minor.mk`

    set -v

    # Install language pack el
    pushd instsetoo_native/util
    /usr/bin/perl -w $SOLARENV/bin/make_installer.pl \
      -f openoffice.lst -l el -p OpenOffice \
      -packagelist ../inc_openoffice/unix/packagelist.txt \
      -buildid $build -destdir %i \
      -languagepack \
      -simple /lib/openoffice.org
    popd

    # Remove the list files
    /bin/rm -vf %i/gid_Module*

    /usr/bin/install -m 755 -d %i/share/doc/%n
    /bin/ln -sf %i/lib/%n/LICENSE_el %i/share/doc/%n/LICENSE
  <<
<<

SplitOff10: <<
  Package: %N-en-gb
  Description: Language Pack(en-GB) for OpenOffice.org
  Depends: %N (= %v-%r)
  InstallScript: <<
    #!/bin/bash -e

    # Setting up environment
    . MacosxEnv.Set.sh
    export OUT="../$INPATH"
    export LOCAL_OUT="$OUT"
    export LOCAL_COMMON_OUT="$OUT"

    build=`sed -n 's/^BUILD=\(.*\)$/\1/p' $SOLARENVINC/minor.mk`

    set -v

    # Install language pack en-GB
    pushd instsetoo_native/util
    /usr/bin/perl -w $SOLARENV/bin/make_installer.pl \
      -f openoffice.lst -l en-GB -p OpenOffice \
      -packagelist ../inc_openoffice/unix/packagelist.txt \
      -buildid $build -destdir %i \
      -languagepack \
      -simple /lib/openoffice.org
    popd

    # Remove the list files
    /bin/rm -vf %i/gid_Module*

    /usr/bin/install -m 755 -d %i/share/doc/%n
    /bin/ln -sf %i/lib/%n/LICENSE_en-GB %i/share/doc/%n/LICENSE
  <<
<<

SplitOff11: <<
  Package: %N-eo
  Description: Language Pack(eo) for OpenOffice.org
  Depends: %N (= %v-%r)
  InstallScript: <<
    #!/bin/bash -e

    # Setting up environment
    . MacosxEnv.Set.sh
    export OUT="../$INPATH"
    export LOCAL_OUT="$OUT"
    export LOCAL_COMMON_OUT="$OUT"

    build=`sed -n 's/^BUILD=\(.*\)$/\1/p' $SOLARENVINC/minor.mk`

    set -v

    # Install language pack eo
    pushd instsetoo_native/util
    /usr/bin/perl -w $SOLARENV/bin/make_installer.pl \
      -f openoffice.lst -l eo -p OpenOffice \
      -packagelist ../inc_openoffice/unix/packagelist.txt \
      -buildid $build -destdir %i \
      -languagepack \
      -simple /lib/openoffice.org
    popd

    # Remove the list files
    /bin/rm -vf %i/gid_Module*

    /usr/bin/install -m 755 -d %i/share/doc/%n
    /bin/ln -sf %i/lib/%n/LICENSE_eo %i/share/doc/%n/LICENSE
  <<
<<

SplitOff12: <<
  Package: %N-es
  Description: Language Pack(es) for OpenOffice.org
  Depends: %N (= %v-%r)
  InstallScript: <<
    #!/bin/bash -e

    # Setting up environment
    . MacosxEnv.Set.sh
    export OUT="../$INPATH"
    export LOCAL_OUT="$OUT"
    export LOCAL_COMMON_OUT="$OUT"

    build=`sed -n 's/^BUILD=\(.*\)$/\1/p' $SOLARENVINC/minor.mk`

    set -v

    # Install language pack es
    pushd instsetoo_native/util
    /usr/bin/perl -w $SOLARENV/bin/make_installer.pl \
      -f openoffice.lst -l es -p OpenOffice \
      -packagelist ../inc_openoffice/unix/packagelist.txt \
      -buildid $build -destdir %i \
      -languagepack \
      -simple /lib/openoffice.org
    popd

    # Remove the list files
    /bin/rm -vf %i/gid_Module*

    /usr/bin/install -m 755 -d %i/share/doc/%n
    /bin/ln -sf %i/lib/%n/LICENSE_es %i/share/doc/%n/LICENSE
  <<
<<

SplitOff13: <<
  Package: %N-et
  Description: Language Pack(et) for OpenOffice.org
  Depends: %N (= %v-%r)
  InstallScript: <<
    #!/bin/bash -e

    # Setting up environment
    . MacosxEnv.Set.sh
    export OUT="../$INPATH"
    export LOCAL_OUT="$OUT"
    export LOCAL_COMMON_OUT="$OUT"

    build=`sed -n 's/^BUILD=\(.*\)$/\1/p' $SOLARENVINC/minor.mk`

    set -v

    # Install language pack et
    pushd instsetoo_native/util
    /usr/bin/perl -w $SOLARENV/bin/make_installer.pl \
      -f openoffice.lst -l et -p OpenOffice \
      -packagelist ../inc_openoffice/unix/packagelist.txt \
      -buildid $build -destdir %i \
      -languagepack \
      -simple /lib/openoffice.org
    popd

    # Remove the list files
    /bin/rm -vf %i/gid_Module*

    /usr/bin/install -m 755 -d %i/share/doc/%n
    /bin/ln -sf %i/lib/%n/LICENSE_et %i/share/doc/%n/LICENSE
  <<
<<

SplitOff14: <<
  Package: %N-eu
  Description: Language Pack(eu) for OpenOffice.org
  Depends: %N (= %v-%r)
  InstallScript: <<
    #!/bin/bash -e

    # Setting up environment
    . MacosxEnv.Set.sh
    export OUT="../$INPATH"
    export LOCAL_OUT="$OUT"
    export LOCAL_COMMON_OUT="$OUT"

    build=`sed -n 's/^BUILD=\(.*\)$/\1/p' $SOLARENVINC/minor.mk`

    set -v

    # Install language pack eu
    pushd instsetoo_native/util
    /usr/bin/perl -w $SOLARENV/bin/make_installer.pl \
      -f openoffice.lst -l eu -p OpenOffice \
      -packagelist ../inc_openoffice/unix/packagelist.txt \
      -buildid $build -destdir %i \
      -languagepack \
      -simple /lib/openoffice.org
    popd

    # Remove the list files
    /bin/rm -vf %i/gid_Module*

    /usr/bin/install -m 755 -d %i/share/doc/%n
    /bin/ln -sf %i/lib/%n/LICENSE_eu %i/share/doc/%n/LICENSE
  <<
<<

SplitOff15: <<
  Package: %N-fi
  Description: Language Pack(fi) for OpenOffice.org
  Depends: %N (= %v-%r)
  InstallScript: <<
    #!/bin/bash -e

    # Setting up environment
    . MacosxEnv.Set.sh
    export OUT="../$INPATH"
    export LOCAL_OUT="$OUT"
    export LOCAL_COMMON_OUT="$OUT"

    build=`sed -n 's/^BUILD=\(.*\)$/\1/p' $SOLARENVINC/minor.mk`

    set -v

    # Install language pack fi
    pushd instsetoo_native/util
    /usr/bin/perl -w $SOLARENV/bin/make_installer.pl \
      -f openoffice.lst -l fi -p OpenOffice \
      -packagelist ../inc_openoffice/unix/packagelist.txt \
      -buildid $build -destdir %i \
      -languagepack \
      -simple /lib/openoffice.org
    popd

    # Remove the list files
    /bin/rm -vf %i/gid_Module*

    /usr/bin/install -m 755 -d %i/share/doc/%n
    /bin/ln -sf %i/lib/%n/LICENSE_fi %i/share/doc/%n/LICENSE
  <<
<<

SplitOff16: <<
  Package: %N-fr
  Description: Language Pack(fr) for OpenOffice.org
  Depends: %N (= %v-%r)
  InstallScript: <<
    #!/bin/bash -e

    # Setting up environment
    . MacosxEnv.Set.sh
    export OUT="../$INPATH"
    export LOCAL_OUT="$OUT"
    export LOCAL_COMMON_OUT="$OUT"

    build=`sed -n 's/^BUILD=\(.*\)$/\1/p' $SOLARENVINC/minor.mk`

    set -v

    # Install language pack fr
    pushd instsetoo_native/util
    /usr/bin/perl -w $SOLARENV/bin/make_installer.pl \
      -f openoffice.lst -l fr -p OpenOffice \
      -packagelist ../inc_openoffice/unix/packagelist.txt \
      -buildid $build -destdir %i \
      -languagepack \
      -simple /lib/openoffice.org
    popd

    # Remove the list files
    /bin/rm -vf %i/gid_Module*

    /usr/bin/install -m 755 -d %i/share/doc/%n
    /bin/ln -sf %i/lib/%n/LICENSE_fr %i/share/doc/%n/LICENSE
  <<
<<

SplitOff17: <<
  Package: %N-gl
  Description: Language Pack(gl) for OpenOffice.org
  Depends: %N (= %v-%r)
  InstallScript: <<
    #!/bin/bash -e

    # Setting up environment
    . MacosxEnv.Set.sh
    export OUT="../$INPATH"
    export LOCAL_OUT="$OUT"
    export LOCAL_COMMON_OUT="$OUT"

    build=`sed -n 's/^BUILD=\(.*\)$/\1/p' $SOLARENVINC/minor.mk`

    set -v

    # Install language pack gl
    pushd instsetoo_native/util
    /usr/bin/perl -w $SOLARENV/bin/make_installer.pl \
      -f openoffice.lst -l gl -p OpenOffice \
      -packagelist ../inc_openoffice/unix/packagelist.txt \
      -buildid $build -destdir %i \
      -languagepack \
      -simple /lib/openoffice.org
    popd

    # Remove the list files
    /bin/rm -vf %i/gid_Module*

    /usr/bin/install -m 755 -d %i/share/doc/%n
    /bin/ln -sf %i/lib/%n/LICENSE_gl %i/share/doc/%n/LICENSE
  <<
<<

SplitOff18: <<
  Package: %N-he
  Description: Language Pack(he) for OpenOffice.org
  Depends: %N (= %v-%r)
  InstallScript: <<
    #!/bin/bash -e

    # Setting up environment
    . MacosxEnv.Set.sh
    export OUT="../$INPATH"
    export LOCAL_OUT="$OUT"
    export LOCAL_COMMON_OUT="$OUT"

    build=`sed -n 's/^BUILD=\(.*\)$/\1/p' $SOLARENVINC/minor.mk`

    set -v

    # Install language pack he
    pushd instsetoo_native/util
    /usr/bin/perl -w $SOLARENV/bin/make_installer.pl \
      -f openoffice.lst -l he -p OpenOffice \
      -packagelist ../inc_openoffice/unix/packagelist.txt \
      -buildid $build -destdir %i \
      -languagepack \
      -simple /lib/openoffice.org
    popd

    # Remove the list files
    /bin/rm -vf %i/gid_Module*

    /usr/bin/install -m 755 -d %i/share/doc/%n
    /bin/ln -sf %i/lib/%n/LICENSE_he %i/share/doc/%n/LICENSE
  <<
<<

SplitOff19: <<
  Package: %N-hi-in
  Description: Language Pack(hi-IN) for OpenOffice.org
  Depends: %N (= %v-%r)
  InstallScript: <<
    #!/bin/bash -e

    # Setting up environment
    . MacosxEnv.Set.sh
    export OUT="../$INPATH"
    export LOCAL_OUT="$OUT"
    export LOCAL_COMMON_OUT="$OUT"

    build=`sed -n 's/^BUILD=\(.*\)$/\1/p' $SOLARENVINC/minor.mk`

    set -v

    # Install language pack hi-IN
    pushd instsetoo_native/util
    /usr/bin/perl -w $SOLARENV/bin/make_installer.pl \
      -f openoffice.lst -l hi-IN -p OpenOffice \
      -packagelist ../inc_openoffice/unix/packagelist.txt \
      -buildid $build -destdir %i \
      -languagepack \
      -simple /lib/openoffice.org
    popd

    # Remove the list files
    /bin/rm -vf %i/gid_Module*

    /usr/bin/install -m 755 -d %i/share/doc/%n
    /bin/ln -sf %i/lib/%n/LICENSE_hi-IN %i/share/doc/%n/LICENSE
  <<
<<

SplitOff20: <<
  Package: %N-hu
  Description: Language Pack(hu) for OpenOffice.org
  Depends: %N (= %v-%r)
  InstallScript: <<
    #!/bin/bash -e

    # Setting up environment
    . MacosxEnv.Set.sh
    export OUT="../$INPATH"
    export LOCAL_OUT="$OUT"
    export LOCAL_COMMON_OUT="$OUT"

    build=`sed -n 's/^BUILD=\(.*\)$/\1/p' $SOLARENVINC/minor.mk`

    set -v

    # Install language pack hu
    pushd instsetoo_native/util
    /usr/bin/perl -w $SOLARENV/bin/make_installer.pl \
      -f openoffice.lst -l hu -p OpenOffice \
      -packagelist ../inc_openoffice/unix/packagelist.txt \
      -buildid $build -destdir %i \
      -languagepack \
      -simple /lib/openoffice.org
    popd

    # Remove the list files
    /bin/rm -vf %i/gid_Module*

    /usr/bin/install -m 755 -d %i/share/doc/%n
    /bin/ln -sf %i/lib/%n/LICENSE_hu %i/share/doc/%n/LICENSE
  <<
<<

SplitOff21: <<
  Package: %N-it
  Description: Language Pack(it) for OpenOffice.org
  Depends: %N (= %v-%r)
  InstallScript: <<
    #!/bin/bash -e

    # Setting up environment
    . MacosxEnv.Set.sh
    export OUT="../$INPATH"
    export LOCAL_OUT="$OUT"
    export LOCAL_COMMON_OUT="$OUT"

    build=`sed -n 's/^BUILD=\(.*\)$/\1/p' $SOLARENVINC/minor.mk`

    set -v

    # Install language pack it
    pushd instsetoo_native/util
    /usr/bin/perl -w $SOLARENV/bin/make_installer.pl \
      -f openoffice.lst -l it -p OpenOffice \
      -packagelist ../inc_openoffice/unix/packagelist.txt \
      -buildid $build -destdir %i \
      -languagepack \
      -simple /lib/openoffice.org
    popd

    # Remove the list files
    /bin/rm -vf %i/gid_Module*

    /usr/bin/install -m 755 -d %i/share/doc/%n
    /bin/ln -sf %i/lib/%n/LICENSE_it %i/share/doc/%n/LICENSE
  <<
<<

SplitOff22: <<
  Package: %N-ja
  Description: Language Pack(ja) for OpenOffice.org
  Depends: %N (= %v-%r)
  InstallScript: <<
    #!/bin/bash -e

    # Setting up environment
    . MacosxEnv.Set.sh
    export OUT="../$INPATH"
    export LOCAL_OUT="$OUT"
    export LOCAL_COMMON_OUT="$OUT"

    build=`sed -n 's/^BUILD=\(.*\)$/\1/p' $SOLARENVINC/minor.mk`

    set -v

    # Install language pack ja
    pushd instsetoo_native/util
    /usr/bin/perl -w $SOLARENV/bin/make_installer.pl \
      -f openoffice.lst -l ja -p OpenOffice \
      -packagelist ../inc_openoffice/unix/packagelist.txt \
      -buildid $build -destdir %i \
      -languagepack \
      -simple /lib/openoffice.org
    popd

    # Remove the list files
    /bin/rm -vf %i/gid_Module*

    /usr/bin/install -m 755 -d %i/share/doc/%n
    /bin/ln -sf %i/lib/%n/LICENSE_ja %i/share/doc/%n/LICENSE
  <<
<<

SplitOff23: <<
  Package: %N-km
  Description: Language Pack(km) for OpenOffice.org
  Depends: %N (= %v-%r)
  InstallScript: <<
    #!/bin/bash -e

    # Setting up environment
    . MacosxEnv.Set.sh
    export OUT="../$INPATH"
    export LOCAL_OUT="$OUT"
    export LOCAL_COMMON_OUT="$OUT"

    build=`sed -n 's/^BUILD=\(.*\)$/\1/p' $SOLARENVINC/minor.mk`

    set -v

    # Install language pack km
    pushd instsetoo_native/util
    /usr/bin/perl -w $SOLARENV/bin/make_installer.pl \
      -f openoffice.lst -l km -p OpenOffice \
      -packagelist ../inc_openoffice/unix/packagelist.txt \
      -buildid $build -destdir %i \
      -languagepack \
      -simple /lib/openoffice.org
    popd

    # Remove the list files
    /bin/rm -vf %i/gid_Module*

    /usr/bin/install -m 755 -d %i/share/doc/%n
    /bin/ln -sf %i/lib/%n/LICENSE_km %i/share/doc/%n/LICENSE
  <<
<<

SplitOff24: <<
  Package: %N-kn-in
  Description: Language Pack(kn-IN) for OpenOffice.org
  Depends: %N (= %v-%r)
  InstallScript: <<
    #!/bin/bash -e

    # Setting up environment
    . MacosxEnv.Set.sh
    export OUT="../$INPATH"
    export LOCAL_OUT="$OUT"
    export LOCAL_COMMON_OUT="$OUT"

    build=`sed -n 's/^BUILD=\(.*\)$/\1/p' $SOLARENVINC/minor.mk`

    set -v

    # Install language pack kn-IN
    pushd instsetoo_native/util
    /usr/bin/perl -w $SOLARENV/bin/make_installer.pl \
      -f openoffice.lst -l kn-IN -p OpenOffice \
      -packagelist ../inc_openoffice/unix/packagelist.txt \
      -buildid $build -destdir %i \
      -languagepack \
      -simple /lib/openoffice.org
    popd

    # Remove the list files
    /bin/rm -vf %i/gid_Module*

    /usr/bin/install -m 755 -d %i/share/doc/%n
    /bin/ln -sf %i/lib/%n/LICENSE_kn-IN %i/share/doc/%n/LICENSE
  <<
<<

SplitOff25: <<
  Package: %N-ko
  Description: Language Pack(ko) for OpenOffice.org
  Depends: %N (= %v-%r)
  InstallScript: <<
    #!/bin/bash -e

    # Setting up environment
    . MacosxEnv.Set.sh
    export OUT="../$INPATH"
    export LOCAL_OUT="$OUT"
    export LOCAL_COMMON_OUT="$OUT"

    build=`sed -n 's/^BUILD=\(.*\)$/\1/p' $SOLARENVINC/minor.mk`

    set -v

    # Install language pack ko
    pushd instsetoo_native/util
    /usr/bin/perl -w $SOLARENV/bin/make_installer.pl \
      -f openoffice.lst -l ko -p OpenOffice \
      -packagelist ../inc_openoffice/unix/packagelist.txt \
      -buildid $build -destdir %i \
      -languagepack \
      -simple /lib/openoffice.org
    popd

    # Remove the list files
    /bin/rm -vf %i/gid_Module*

    /usr/bin/install -m 755 -d %i/share/doc/%n
    /bin/ln -sf %i/lib/%n/LICENSE_ko %i/share/doc/%n/LICENSE
  <<
<<

SplitOff26: <<
  Package: %N-lt
  Description: Language Pack(lt) for OpenOffice.org
  Depends: %N (= %v-%r)
  InstallScript: <<
    #!/bin/bash -e

    # Setting up environment
    . MacosxEnv.Set.sh
    export OUT="../$INPATH"
    export LOCAL_OUT="$OUT"
    export LOCAL_COMMON_OUT="$OUT"

    build=`sed -n 's/^BUILD=\(.*\)$/\1/p' $SOLARENVINC/minor.mk`

    set -v

    # Install language pack lt
    pushd instsetoo_native/util
    /usr/bin/perl -w $SOLARENV/bin/make_installer.pl \
      -f openoffice.lst -l lt -p OpenOffice \
      -packagelist ../inc_openoffice/unix/packagelist.txt \
      -buildid $build -destdir %i \
      -languagepack \
      -simple /lib/openoffice.org
    popd

    # Remove the list files
    /bin/rm -vf %i/gid_Module*

    /usr/bin/install -m 755 -d %i/share/doc/%n
    /bin/ln -sf %i/lib/%n/LICENSE_lt %i/share/doc/%n/LICENSE
  <<
<<

SplitOff27: <<
  Package: %N-ms
  Description: Language Pack(ms) for OpenOffice.org
  Depends: %N (= %v-%r)
  InstallScript: <<
    #!/bin/bash -e

    # Setting up environment
    . MacosxEnv.Set.sh
    export OUT="../$INPATH"
    export LOCAL_OUT="$OUT"
    export LOCAL_COMMON_OUT="$OUT"

    build=`sed -n 's/^BUILD=\(.*\)$/\1/p' $SOLARENVINC/minor.mk`

    set -v

    # Install language pack ms
    pushd instsetoo_native/util
    /usr/bin/perl -w $SOLARENV/bin/make_installer.pl \
      -f openoffice.lst -l ms -p OpenOffice \
      -packagelist ../inc_openoffice/unix/packagelist.txt \
      -buildid $build -destdir %i \
      -languagepack \
      -simple /lib/openoffice.org
    popd

    # Remove the list files
    /bin/rm -vf %i/gid_Module*

    /usr/bin/install -m 755 -d %i/share/doc/%n
    /bin/ln -sf %i/lib/%n/LICENSE_ms %i/share/doc/%n/LICENSE
  <<
<<

SplitOff28: <<
  Package: %N-nb
  Description: Language Pack(nb) for OpenOffice.org
  Depends: %N (= %v-%r)
  InstallScript: <<
    #!/bin/bash -e

    # Setting up environment
    . MacosxEnv.Set.sh
    export OUT="../$INPATH"
    export LOCAL_OUT="$OUT"
    export LOCAL_COMMON_OUT="$OUT"

    build=`sed -n 's/^BUILD=\(.*\)$/\1/p' $SOLARENVINC/minor.mk`

    set -v

    # Install language pack nb
    pushd instsetoo_native/util
    /usr/bin/perl -w $SOLARENV/bin/make_installer.pl \
      -f openoffice.lst -l nb -p OpenOffice \
      -packagelist ../inc_openoffice/unix/packagelist.txt \
      -buildid $build -destdir %i \
      -languagepack \
      -simple /lib/openoffice.org
    popd

    # Remove the list files
    /bin/rm -vf %i/gid_Module*

    /usr/bin/install -m 755 -d %i/share/doc/%n
    /bin/ln -sf %i/lib/%n/LICENSE_nb %i/share/doc/%n/LICENSE
  <<
<<

SplitOff29: <<
  Package: %N-nl
  Description: Language Pack(nl) for OpenOffice.org
  Depends: %N (= %v-%r)
  InstallScript: <<
    #!/bin/bash -e

    # Setting up environment
    . MacosxEnv.Set.sh
    export OUT="../$INPATH"
    export LOCAL_OUT="$OUT"
    export LOCAL_COMMON_OUT="$OUT"

    build=`sed -n 's/^BUILD=\(.*\)$/\1/p' $SOLARENVINC/minor.mk`

    set -v

    # Install language pack nl
    pushd instsetoo_native/util
    /usr/bin/perl -w $SOLARENV/bin/make_installer.pl \
      -f openoffice.lst -l nl -p OpenOffice \
      -packagelist ../inc_openoffice/unix/packagelist.txt \
      -buildid $build -destdir %i \
      -languagepack \
      -simple /lib/openoffice.org
    popd

    # Remove the list files
    /bin/rm -vf %i/gid_Module*

    /usr/bin/install -m 755 -d %i/share/doc/%n
    /bin/ln -sf %i/lib/%n/LICENSE_nl %i/share/doc/%n/LICENSE
  <<
<<

SplitOff30: <<
  Package: %N-nn
  Description: Language Pack(nn) for OpenOffice.org
  Depends: %N (= %v-%r)
  InstallScript: <<
    #!/bin/bash -e

    # Setting up environment
    . MacosxEnv.Set.sh
    export OUT="../$INPATH"
    export LOCAL_OUT="$OUT"
    export LOCAL_COMMON_OUT="$OUT"

    build=`sed -n 's/^BUILD=\(.*\)$/\1/p' $SOLARENVINC/minor.mk`

    set -v

    # Install language pack nn
    pushd instsetoo_native/util
    /usr/bin/perl -w $SOLARENV/bin/make_installer.pl \
      -f openoffice.lst -l nn -p OpenOffice \
      -packagelist ../inc_openoffice/unix/packagelist.txt \
      -buildid $build -destdir %i \
      -languagepack \
      -simple /lib/openoffice.org
    popd

    # Remove the list files
    /bin/rm -vf %i/gid_Module*

    /usr/bin/install -m 755 -d %i/share/doc/%n
    /bin/ln -sf %i/lib/%n/LICENSE_nn %i/share/doc/%n/LICENSE
  <<
<<

SplitOff31: <<
  Package: %N-ns
  Description: Language Pack(ns) for OpenOffice.org
  Depends: %N (= %v-%r)
  InstallScript: <<
    #!/bin/bash -e

    # Setting up environment
    . MacosxEnv.Set.sh
    export OUT="../$INPATH"
    export LOCAL_OUT="$OUT"
    export LOCAL_COMMON_OUT="$OUT"

    build=`sed -n 's/^BUILD=\(.*\)$/\1/p' $SOLARENVINC/minor.mk`

    set -v

    # Install language pack ns
    pushd instsetoo_native/util
    /usr/bin/perl -w $SOLARENV/bin/make_installer.pl \
      -f openoffice.lst -l ns -p OpenOffice \
      -packagelist ../inc_openoffice/unix/packagelist.txt \
      -buildid $build -destdir %i \
      -languagepack \
      -simple /lib/openoffice.org
    popd

    # Remove the list files
    /bin/rm -vf %i/gid_Module*

    /usr/bin/install -m 755 -d %i/share/doc/%n
    /bin/ln -sf %i/lib/%n/LICENSE_ns %i/share/doc/%n/LICENSE
  <<
<<

SplitOff32: <<
  Package: %N-pl
  Description: Language Pack(pl) for OpenOffice.org
  Depends: %N (= %v-%r)
  InstallScript: <<
    #!/bin/bash -e

    # Setting up environment
    . MacosxEnv.Set.sh
    export OUT="../$INPATH"
    export LOCAL_OUT="$OUT"
    export LOCAL_COMMON_OUT="$OUT"

    build=`sed -n 's/^BUILD=\(.*\)$/\1/p' $SOLARENVINC/minor.mk`

    set -v

    # Install language pack pl
    pushd instsetoo_native/util
    /usr/bin/perl -w $SOLARENV/bin/make_installer.pl \
      -f openoffice.lst -l pl -p OpenOffice \
      -packagelist ../inc_openoffice/unix/packagelist.txt \
      -buildid $build -destdir %i \
      -languagepack \
      -simple /lib/openoffice.org
    popd

    # Remove the list files
    /bin/rm -vf %i/gid_Module*

    /usr/bin/install -m 755 -d %i/share/doc/%n
    /bin/ln -sf %i/lib/%n/LICENSE_pl %i/share/doc/%n/LICENSE
  <<
<<

SplitOff33: <<
  Package: %N-pt
  Description: Language Pack(pt) for OpenOffice.org
  Depends: %N (= %v-%r)
  InstallScript: <<
    #!/bin/bash -e

    # Setting up environment
    . MacosxEnv.Set.sh
    export OUT="../$INPATH"
    export LOCAL_OUT="$OUT"
    export LOCAL_COMMON_OUT="$OUT"

    build=`sed -n 's/^BUILD=\(.*\)$/\1/p' $SOLARENVINC/minor.mk`

    set -v

    # Install language pack pt
    pushd instsetoo_native/util
    /usr/bin/perl -w $SOLARENV/bin/make_installer.pl \
      -f openoffice.lst -l pt -p OpenOffice \
      -packagelist ../inc_openoffice/unix/packagelist.txt \
      -buildid $build -destdir %i \
      -languagepack \
      -simple /lib/openoffice.org
    popd

    # Remove the list files
    /bin/rm -vf %i/gid_Module*

    /usr/bin/install -m 755 -d %i/share/doc/%n
    /bin/ln -sf %i/lib/%n/LICENSE_pt %i/share/doc/%n/LICENSE
  <<
<<

SplitOff34: <<
  Package: %N-pt-br
  Description: Language Pack(pt-BR) for OpenOffice.org
  Depends: %N (= %v-%r)
  InstallScript: <<
    #!/bin/bash -e

    # Setting up environment
    . MacosxEnv.Set.sh
    export OUT="../$INPATH"
    export LOCAL_OUT="$OUT"
    export LOCAL_COMMON_OUT="$OUT"

    build=`sed -n 's/^BUILD=\(.*\)$/\1/p' $SOLARENVINC/minor.mk`

    set -v

    # Install language pack pt-BR
    pushd instsetoo_native/util
    /usr/bin/perl -w $SOLARENV/bin/make_installer.pl \
      -f openoffice.lst -l pt-BR -p OpenOffice \
      -packagelist ../inc_openoffice/unix/packagelist.txt \
      -buildid $build -destdir %i \
      -languagepack \
      -simple /lib/openoffice.org
    popd

    # Remove the list files
    /bin/rm -vf %i/gid_Module*

    /usr/bin/install -m 755 -d %i/share/doc/%n
    /bin/ln -sf %i/lib/%n/LICENSE_pt-BR %i/share/doc/%n/LICENSE
  <<
<<

SplitOff35: <<
  Package: %N-ru
  Description: Language Pack(ru) for OpenOffice.org
  Depends: %N (= %v-%r)
  InstallScript: <<
    #!/bin/bash -e

    # Setting up environment
    . MacosxEnv.Set.sh
    export OUT="../$INPATH"
    export LOCAL_OUT="$OUT"
    export LOCAL_COMMON_OUT="$OUT"

    build=`sed -n 's/^BUILD=\(.*\)$/\1/p' $SOLARENVINC/minor.mk`

    set -v

    # Install language pack ru
    pushd instsetoo_native/util
    /usr/bin/perl -w $SOLARENV/bin/make_installer.pl \
      -f openoffice.lst -l ru -p OpenOffice \
      -packagelist ../inc_openoffice/unix/packagelist.txt \
      -buildid $build -destdir %i \
      -languagepack \
      -simple /lib/openoffice.org
    popd

    # Remove the list files
    /bin/rm -vf %i/gid_Module*

    /usr/bin/install -m 755 -d %i/share/doc/%n
    /bin/ln -sf %i/lib/%n/LICENSE_ru %i/share/doc/%n/LICENSE
  <<
<<

SplitOff36: <<
  Package: %N-sk
  Description: Language Pack(sk) for OpenOffice.org
  Depends: %N (= %v-%r)
  InstallScript: <<
    #!/bin/bash -e

    # Setting up environment
    . MacosxEnv.Set.sh
    export OUT="../$INPATH"
    export LOCAL_OUT="$OUT"
    export LOCAL_COMMON_OUT="$OUT"

    build=`sed -n 's/^BUILD=\(.*\)$/\1/p' $SOLARENVINC/minor.mk`

    set -v

    # Install language pack sk
    pushd instsetoo_native/util
    /usr/bin/perl -w $SOLARENV/bin/make_installer.pl \
      -f openoffice.lst -l sk -p OpenOffice \
      -packagelist ../inc_openoffice/unix/packagelist.txt \
      -buildid $build -destdir %i \
      -languagepack \
      -simple /lib/openoffice.org
    popd

    # Remove the list files
    /bin/rm -vf %i/gid_Module*

    /usr/bin/install -m 755 -d %i/share/doc/%n
    /bin/ln -sf %i/lib/%n/LICENSE_sk %i/share/doc/%n/LICENSE
  <<
<<

SplitOff37: <<
  Package: %N-sl
  Description: Language Pack(sl) for OpenOffice.org
  Depends: %N (= %v-%r)
  InstallScript: <<
    #!/bin/bash -e

    # Setting up environment
    . MacosxEnv.Set.sh
    export OUT="../$INPATH"
    export LOCAL_OUT="$OUT"
    export LOCAL_COMMON_OUT="$OUT"

    build=`sed -n 's/^BUILD=\(.*\)$/\1/p' $SOLARENVINC/minor.mk`

    set -v

    # Install language pack sl
    pushd instsetoo_native/util
    /usr/bin/perl -w $SOLARENV/bin/make_installer.pl \
      -f openoffice.lst -l sl -p OpenOffice \
      -packagelist ../inc_openoffice/unix/packagelist.txt \
      -buildid $build -destdir %i \
      -languagepack \
      -simple /lib/openoffice.org
    popd

    # Remove the list files
    /bin/rm -vf %i/gid_Module*

    /usr/bin/install -m 755 -d %i/share/doc/%n
    /bin/ln -sf %i/lib/%n/LICENSE_sl %i/share/doc/%n/LICENSE
  <<
<<

SplitOff38: <<
  Package: %N-sv
  Description: Language Pack(sv) for OpenOffice.org
  Depends: %N (= %v-%r)
  InstallScript: <<
    #!/bin/bash -e

    # Setting up environment
    . MacosxEnv.Set.sh
    export OUT="../$INPATH"
    export LOCAL_OUT="$OUT"
    export LOCAL_COMMON_OUT="$OUT"

    build=`sed -n 's/^BUILD=\(.*\)$/\1/p' $SOLARENVINC/minor.mk`

    set -v

    # Install language pack sv
    pushd instsetoo_native/util
    /usr/bin/perl -w $SOLARENV/bin/make_installer.pl \
      -f openoffice.lst -l sv -p OpenOffice \
      -packagelist ../inc_openoffice/unix/packagelist.txt \
      -buildid $build -destdir %i \
      -languagepack \
      -simple /lib/openoffice.org
    popd

    # Remove the list files
    /bin/rm -vf %i/gid_Module*

    /usr/bin/install -m 755 -d %i/share/doc/%n
    /bin/ln -sf %i/lib/%n/LICENSE_sv %i/share/doc/%n/LICENSE
  <<
<<

SplitOff39: <<
  Package: %N-th
  Description: Language Pack(th) for OpenOffice.org
  Depends: %N (= %v-%r)
  InstallScript: <<
    #!/bin/bash -e

    # Setting up environment
    . MacosxEnv.Set.sh
    export OUT="../$INPATH"
    export LOCAL_OUT="$OUT"
    export LOCAL_COMMON_OUT="$OUT"

    build=`sed -n 's/^BUILD=\(.*\)$/\1/p' $SOLARENVINC/minor.mk`

    set -v

    # Install language pack th
    pushd instsetoo_native/util
    /usr/bin/perl -w $SOLARENV/bin/make_installer.pl \
      -f openoffice.lst -l th -p OpenOffice \
      -packagelist ../inc_openoffice/unix/packagelist.txt \
      -buildid $build -destdir %i \
      -languagepack \
      -simple /lib/openoffice.org
    popd

    # Remove the list files
    /bin/rm -vf %i/gid_Module*

    /usr/bin/install -m 755 -d %i/share/doc/%n
    /bin/ln -sf %i/lib/%n/LICENSE_th %i/share/doc/%n/LICENSE
  <<
<<

SplitOff40: <<
  Package: %N-tn
  Description: Language Pack(tn) for OpenOffice.org
  Depends: %N (= %v-%r)
  InstallScript: <<
    #!/bin/bash -e

    # Setting up environment
    . MacosxEnv.Set.sh
    export OUT="../$INPATH"
    export LOCAL_OUT="$OUT"
    export LOCAL_COMMON_OUT="$OUT"

    build=`sed -n 's/^BUILD=\(.*\)$/\1/p' $SOLARENVINC/minor.mk`

    set -v

    # Install language pack tn
    pushd instsetoo_native/util
    /usr/bin/perl -w $SOLARENV/bin/make_installer.pl \
      -f openoffice.lst -l tn -p OpenOffice \
      -packagelist ../inc_openoffice/unix/packagelist.txt \
      -buildid $build -destdir %i \
      -languagepack \
      -simple /lib/openoffice.org
    popd

    # Remove the list files
    /bin/rm -vf %i/gid_Module*

    /usr/bin/install -m 755 -d %i/share/doc/%n
    /bin/ln -sf %i/lib/%n/LICENSE_tn %i/share/doc/%n/LICENSE
  <<
<<

SplitOff41: <<
  Package: %N-tr
  Description: Language Pack(tr) for OpenOffice.org
  Depends: %N (= %v-%r)
  InstallScript: <<
    #!/bin/bash -e

    # Setting up environment
    . MacosxEnv.Set.sh
    export OUT="../$INPATH"
    export LOCAL_OUT="$OUT"
    export LOCAL_COMMON_OUT="$OUT"

    build=`sed -n 's/^BUILD=\(.*\)$/\1/p' $SOLARENVINC/minor.mk`

    set -v

    # Install language pack tr
    pushd instsetoo_native/util
    /usr/bin/perl -w $SOLARENV/bin/make_installer.pl \
      -f openoffice.lst -l tr -p OpenOffice \
      -packagelist ../inc_openoffice/unix/packagelist.txt \
      -buildid $build -destdir %i \
      -languagepack \
      -simple /lib/openoffice.org
    popd

    # Remove the list files
    /bin/rm -vf %i/gid_Module*

    /usr/bin/install -m 755 -d %i/share/doc/%n
    /bin/ln -sf %i/lib/%n/LICENSE_tr %i/share/doc/%n/LICENSE
  <<
<<

SplitOff42: <<
  Package: %N-xh
  Description: Language Pack(xh) for OpenOffice.org
  Depends: %N (= %v-%r)
  InstallScript: <<
    #!/bin/bash -e

    # Setting up environment
    . MacosxEnv.Set.sh
    export OUT="../$INPATH"
    export LOCAL_OUT="$OUT"
    export LOCAL_COMMON_OUT="$OUT"

    build=`sed -n 's/^BUILD=\(.*\)$/\1/p' $SOLARENVINC/minor.mk`

    set -v

    # Install language pack xh
    pushd instsetoo_native/util
    /usr/bin/perl -w $SOLARENV/bin/make_installer.pl \
      -f openoffice.lst -l xh -p OpenOffice \
      -packagelist ../inc_openoffice/unix/packagelist.txt \
      -buildid $build -destdir %i \
      -languagepack \
      -simple /lib/openoffice.org
    popd

    # Remove the list files
    /bin/rm -vf %i/gid_Module*

    /usr/bin/install -m 755 -d %i/share/doc/%n
    /bin/ln -sf %i/lib/%n/LICENSE_xh %i/share/doc/%n/LICENSE
  <<
<<

SplitOff43: <<
  Package: %N-zh-cn
  Description: Language Pack(zh-CN) for OpenOffice.org
  Depends: %N (= %v-%r)
  InstallScript: <<
    #!/bin/bash -e

    # Setting up environment
    . MacosxEnv.Set.sh
    export OUT="../$INPATH"
    export LOCAL_OUT="$OUT"
    export LOCAL_COMMON_OUT="$OUT"

    build=`sed -n 's/^BUILD=\(.*\)$/\1/p' $SOLARENVINC/minor.mk`

    set -v

    # Install language pack zh-CN
    pushd instsetoo_native/util
    /usr/bin/perl -w $SOLARENV/bin/make_installer.pl \
      -f openoffice.lst -l zh-CN -p OpenOffice \
      -packagelist ../inc_openoffice/unix/packagelist.txt \
      -buildid $build -destdir %i \
      -languagepack \
      -simple /lib/openoffice.org
    popd

    # Remove the list files
    /bin/rm -vf %i/gid_Module*

    /usr/bin/install -m 755 -d %i/share/doc/%n
    /bin/ln -sf %i/lib/%n/LICENSE_zh-CN %i/share/doc/%n/LICENSE
  <<
<<

SplitOff44: <<
  Package: %N-zh-tw
  Description: Language Pack(zh-TW) for OpenOffice.org
  Depends: %N (= %v-%r)
  InstallScript: <<
    #!/bin/bash -e

    # Setting up environment
    . MacosxEnv.Set.sh
    export OUT="../$INPATH"
    export LOCAL_OUT="$OUT"
    export LOCAL_COMMON_OUT="$OUT"

    build=`sed -n 's/^BUILD=\(.*\)$/\1/p' $SOLARENVINC/minor.mk`

    set -v

    # Install language pack zh-TW
    pushd instsetoo_native/util
    /usr/bin/perl -w $SOLARENV/bin/make_installer.pl \
      -f openoffice.lst -l zh-TW -p OpenOffice \
      -packagelist ../inc_openoffice/unix/packagelist.txt \
      -buildid $build -destdir %i \
      -languagepack \
      -simple /lib/openoffice.org
    popd

    # Remove the list files
    /bin/rm -vf %i/gid_Module*

    /usr/bin/install -m 755 -d %i/share/doc/%n
    /bin/ln -sf %i/lib/%n/LICENSE_zh-TW %i/share/doc/%n/LICENSE
  <<
<<

SplitOff45: <<
  Package: %N-zu
  Description: Language Pack(zu) for OpenOffice.org
  Depends: %N (= %v-%r)
  InstallScript: <<
    #!/bin/bash -e

    # Setting up environment
    . MacosxEnv.Set.sh
    export OUT="../$INPATH"
    export LOCAL_OUT="$OUT"
    export LOCAL_COMMON_OUT="$OUT"

    build=`sed -n 's/^BUILD=\(.*\)$/\1/p' $SOLARENVINC/minor.mk`

    set -v

    # Install language pack zu
    pushd instsetoo_native/util
    /usr/bin/perl -w $SOLARENV/bin/make_installer.pl \
      -f openoffice.lst -l zu -p OpenOffice \
      -packagelist ../inc_openoffice/unix/packagelist.txt \
      -buildid $build -destdir %i \
      -languagepack \
      -simple /lib/openoffice.org
    popd

    # Remove the list files
    /bin/rm -vf %i/gid_Module*

    /usr/bin/install -m 755 -d %i/share/doc/%n
    /bin/ln -sf %i/lib/%n/LICENSE_zu %i/share/doc/%n/LICENSE
  <<
<<


<<
