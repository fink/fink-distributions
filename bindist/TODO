- Finish the bdrm script to fulle remove the deb and source tar balls from the local bindist.

- Add a list of default selections, i.e. xfree86-base, since -y would select system-xfree86 by default, also make it choose all none ssl if we are building an none ssl pkg as the parent, and all ssl if the parent pkg is ssl.
