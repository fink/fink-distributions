diff -Nur clusterssh-3.19.1.orig/src/Makefile.in clusterssh-3.19.1/src/Makefile.in
--- clusterssh-3.19.1.orig/src/Makefile.in	2006-07-24 14:13:27.000000000 -0500
+++ clusterssh-3.19.1/src/Makefile.in	2007-05-13 00:52:02.000000000 -0500
@@ -353,13 +353,13 @@
 cssh: $(srcdir)/cssh.pl
 	rm -f cssh
 	echo "#!"$(PERL) > cssh
-	cat $(srcdir)/cssh.pl >> cssh
+	cat $(srcdir)/cssh.pl | sed 's#SYSCONFDIR#$(sysconfdir)#'>> cssh
 	chmod ugo+x cssh
 
-cssh.1: $(srcdir)/cssh.pl
+cssh.1: $(srcdir)/cssh
 	rm -f cssh.1
-	podchecker $(srcdir)/cssh.pl
-	pod2man $(srcdir)/cssh.pl > cssh.1
+	podchecker $(srcdir)/cssh
+	pod2man $(srcdir)/cssh > cssh.1
 	chmod 444 cssh.1
 
 #
diff -Nur clusterssh-3.19.1.orig/src/cssh.pl clusterssh-3.19.1/src/cssh.pl
--- clusterssh-3.19.1.orig/src/cssh.pl	2006-07-24 14:12:54.000000000 -0500
+++ clusterssh-3.19.1/src/cssh.pl	2007-05-13 01:32:05.000000000 -0500
@@ -316,7 +316,7 @@
 
 sub load_configfile()
 {
-  parse_config_file('/etc/csshrc');
+  parse_config_file('SYSCONFDIR/csshrc');
   parse_config_file( $ENV{HOME} . '/.csshrc' );
   check_config();
 }
@@ -469,7 +469,7 @@
 {
 
   # first, read in global file
-  my $cluster_file = '/etc/clusters';
+  my $cluster_file = 'SYSCONFDIR/clusters';
 
   logmsg( 3, "Logging for $cluster_file" );
 
@@ -1886,7 +1886,7 @@
 
 =item <tag> ...
 
-Open a series of xterms defined by <tag> within either /etc/clusters or
+Open a series of xterms defined by <tag> within either SYSCONFDIR/clusters or
 F<$HOME/.csshrc> (see L<"FILES">).
 
 =back
@@ -1924,11 +1924,11 @@
 
 =over
 
-=item /etc/clusters
+=item SYSCONFDIR/clusters
 
 This file contains a list of tags to server names mappings.  When any name
 is used on the command line it is checked to see if it is a tag in
-/etc/clusters (or the .csshrc file, or any additional cluster file specified 
+SYSCONFDIR/clusters (or the .csshrc file, or any additional cluster file specified 
 by -c).  If it is a tag, then the tag is replaced with the list of servers 
 from the file.  The file is formated as follows:
 
@@ -1945,7 +1945,7 @@
 Clusters may also be specified within the user's .csshrc file, as documented
 below.
 
-=item F</etc/csshrc> & F<$HOME/.csshrc>
+=item F<SYSCONFDIR/csshrc> & F<$HOME/.csshrc>
 
 This file contains configuration overrides - the defaults are as marked.
 Default options are overwritten first by the global file, and then by the
@@ -1970,14 +1970,14 @@
 =item clusters = <blank>
 
 Define a number of cluster tags in addition to (or to replace) tags defined
-in the F</etc/clusters> file.  The format is:
+in the F<SYSCONFDIR/clusters> file.  The format is:
 
  clusters = <tag1> <tag2> <tag3>
  <tag1> = host1 host2 host3
  <tag2> = user@host4 user@host5 host6
  <tag3> = <tag1> <tag2>
 
-As with the F</etc/clusters> file, be sure not to create recursivly nested tags.
+As with the F<SYSCONFDIR/clusters> file, be sure not to create recursivly nested tags.
 
 =item console_position = <null>
 
@@ -2175,7 +2175,7 @@
 
 C<< perl -MX11::Protocol -e 'print $X11::Protocol::VERSION,$/' >>
 
-C<< cat /etc/csshrc $HOME/.csshrc >>
+C<< cat SYSCONFDIR/csshrc $HOME/.csshrc >>
 
 =item *
 
