diff -ru XorCurses-0.0.9.orig/Makefile XorCurses-0.0.9/Makefile
--- XorCurses-0.0.9.orig/Makefile	2009-09-15 18:57:49.000000000 -0400
+++ XorCurses-0.0.9/Makefile	2009-09-16 21:52:51.000000000 -0400
@@ -9,7 +9,7 @@
 MAPDIR=$(SHAREDIR)maps
 #------------------standard options------------------
 # probably fine for most people:
-CFLAGS:= -O2 -std=gnu99 -Wall -pedantic -DDATADIR=\"$(SHAREDIR)\"
+CFLAGS:= -O2 -std=gnu99 -Wall -pedantic -DDATADIR=\"$(SHAREDIR)\" -I@PREFIX@/include
 
 # -DDATA_DIR is an easy way of giving the src files
 # the default install location. options code relies
@@ -36,7 +36,7 @@
 PROG:=xorcurses
 CC:=gcc
 
-LIBS   :=-lcurses
+LIBS   :=-L@PREFIX@/lib -lcurses
 SRC    :=$(wildcard *.c)
 OBJS   :=$(patsubst %.c, %.o, $(SRC))
 HEADERS:=$(wildcard *.h)
@@ -53,10 +53,11 @@
 	rm -f $(PROG) $(OBJS)
 
 install: $(PROG)
+	install -d $(BINDIR)
 	install $(PROG) $(BINDIR)
 	install -d $(MAPDIR)
-	install -t $(MAPDIR) maps/[1-9]*
-	install -t $(SHAREDIR) xorcurses*.txt
+	install maps/[1-9]* $(MAPDIR)
+	install xorcurses*.txt $(SHAREDIR)
 
 uninstall:
 	rm -f $(BINDIR)$(PROG)
