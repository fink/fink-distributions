diff -Naur cvs2svn-1.5.0_orig/svntest/main.py cvs2svn-1.5.0/svntest/main.py
--- cvs2svn-1.5.0_orig/svntest/main.py  2006-12-09 16:27:00.000000000 -0600
+++ cvs2svn-1.5.0/svntest/main.py       2006-12-09 16:31:55.000000000 -0600
@@ -288,14 +288,17 @@
   outfile.close()
   errfile.close()
 
-  if platform_with_os_wait:
-    pid, wait_code = os.wait()
+  if platform_with_os_wait:
+    try:
+      pid, wait_code = os.wait()
 
-    exit_code = int(wait_code / 256)
-    exit_signal = wait_code % 256
+      exit_code = int(wait_code / 256)
+      exit_signal = wait_code % 256
 
-    if exit_signal != 0:
-      raise SVNProcessTerminatedBySignal
+      if exit_signal != 0:
+        raise SVNProcessTerminatedBySignal
+    except OSError:
+      pass
 
   if verbose_mode:
     stop = time.time()
