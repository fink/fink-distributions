diff -Nurd -x'*~' Test-Harness-3.21.orig/lib/TAP/Parser/SourceHandler/Perl.pm Test-Harness-3.21/lib/TAP/Parser/SourceHandler/Perl.pm
--- Test-Harness-3.21.orig/lib/TAP/Parser/SourceHandler/Perl.pm	2010-01-30 07:24:55.000000000 -0500
+++ Test-Harness-3.21/lib/TAP/Parser/SourceHandler/Perl.pm	2010-07-27 19:49:01.000000000 -0400
@@ -203,7 +203,7 @@
 
    # XXX don't need to quote if we treat the parts as atoms (except maybe vms)
    #$file = qq["$file"] if ( $file =~ /\s/ ) && ( $file !~ /^".*"$/ );
-    my @command = ( $command, @switches, $file, @args );
+    my @command = ( '/usr/bin/arch', '-i386', $command, @switches, $file, @args );
     return @command;
 }
 
