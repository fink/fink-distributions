diff -ru vorbis-tools-1.1.1/ogg123/http_transport.c vorbis-tools-1.1.1-patched/ogg123/http_transport.c
--- vorbis-tools-1.1.1/ogg123/http_transport.c	2005-06-13 15:11:44.000000000 +0200
+++ vorbis-tools-1.1.1-patched/ogg123/http_transport.c	2007-02-20 23:42:05.000000000 +0100
@@ -116,7 +116,6 @@
   if (inputOpts.ProxyTunnel)
     curl_easy_setopt (handle, CURLOPT_HTTPPROXYTUNNEL, inputOpts.ProxyTunnel);
   */
-  curl_easy_setopt(handle, CURLOPT_MUTE, 1);
   curl_easy_setopt(handle, CURLOPT_ERRORBUFFER, private->error);
   curl_easy_setopt(handle, CURLOPT_PROGRESSFUNCTION, progress_callback);
   curl_easy_setopt(handle, CURLOPT_PROGRESSDATA, private);
diff -ru vorbis-tools-1.1.1/ogg123/ogg123.c vorbis-tools-1.1.1-patched/ogg123/ogg123.c
--- vorbis-tools-1.1.1/ogg123/ogg123.c	2005-06-03 12:15:09.000000000 +0200
+++ vorbis-tools-1.1.1-patched/ogg123/ogg123.c	2007-02-20 23:40:22.000000000 +0100
@@ -381,7 +381,6 @@
 
   /* Setup signal handlers and callbacks */
 
-  ATEXIT (exit_cleanup);
   signal (SIGINT, signal_handler);
   signal (SIGTSTP, signal_handler);
   signal (SIGCONT, signal_handler);
@@ -396,7 +395,8 @@
 
   playlist_array_destroy(playlist_array, items);
 
+  exit_cleanup();
   exit (0);
 }
 
 
@@ -613,9 +612,6 @@
   status_reset_output_lock();  /* In case we were killed mid-output */
 
   status_message(1, _("Done."));
-  
-  if (sig_request.exit)
-    exit (0);
 }
 
 
