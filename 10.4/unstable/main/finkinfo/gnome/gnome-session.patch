diff -Nurd -x'*~' gnome-session-2.24.3.orig/capplet/Makefile.in gnome-session-2.24.3/capplet/Makefile.in
--- gnome-session-2.24.3.orig/capplet/Makefile.in	2009-01-12 18:37:57.000000000 -0500
+++ gnome-session-2.24.3/capplet/Makefile.in	2009-01-20 01:28:18.000000000 -0500
@@ -254,11 +254,11 @@
 top_srcdir = @top_srcdir@
 NULL = 
 INCLUDES = \
+	-I$(top_srcdir)/egg				\
+	-I$(top_srcdir)/gnome-session			\
 	$(WARN_CFLAGS)					\
 	$(SESSION_PROPERTIES_CFLAGS)			\
 	$(GCONF_CFLAGS)					\
-	-I$(top_srcdir)/egg				\
-	-I$(top_srcdir)/gnome-session			\
 	-DLOCALE_DIR=\""$(datadir)/locale"\"		\
 	-DGLADEDIR=\""$(pkgdatadir)"\"		\
 	$(NULL)
diff -Nurd -x'*~' gnome-session-2.24.3.orig/compat/Makefile.in gnome-session-2.24.3/compat/Makefile.in
--- gnome-session-2.24.3.orig/compat/Makefile.in	2009-01-12 18:37:57.000000000 -0500
+++ gnome-session-2.24.3/compat/Makefile.in	2009-01-20 01:28:18.000000000 -0500
@@ -285,12 +285,12 @@
 top_builddir = @top_builddir@
 top_srcdir = @top_srcdir@
 INCLUDES = \
+	-I$(top_srcdir)/egg			\
 	$(WARN_CFLAGS)				\
 	$(DBUS_GLIB_CFLAGS)			\
 	$(GCONF_CFLAGS)				\
 	$(GNOME_KEYRING_CFLAGS)			\
 	$(GTK_CFLAGS)				\
-	-I$(top_srcdir)/egg			\
 	-DAT_SPI_REGISTRYD_DIR=\""$(AT_SPI_REGISTRYD_DIR)"\" \
 	-DDEFAULT_SESSION_DIR=\""$(default_sessiondir)"\" \
 	-DGNOME_KEYRING_DAEMON=\""$(GNOME_KEYRING_DAEMON)"\" \
diff -Nurd -x'*~' gnome-session-2.24.3.orig/gnome-session/Makefile.in gnome-session-2.24.3/gnome-session/Makefile.in
--- gnome-session-2.24.3.orig/gnome-session/Makefile.in	2009-01-12 18:37:58.000000000 -0500
+++ gnome-session-2.24.3/gnome-session/Makefile.in	2009-01-20 01:28:18.000000000 -0500
@@ -280,13 +280,13 @@
 NULL = 
 noinst_LTLIBRARIES = libgsmutil.la
 INCLUDES = \
+	-I$(top_srcdir)/egg			\
 	$(WARN_CFLAGS)				\
 	$(GNOME_SESSION_CFLAGS)			\
 	$(DBUS_GLIB_CFLAGS)			\
 	$(GCONF_CFLAGS)				\
 	$(GTK_CFLAGS)				\
 	$(POLKIT_GNOME_CFLAGS)			\
-	-I$(top_srcdir)/egg			\
 	-DLOCALE_DIR=\""$(datadir)/locale"\"	\
 	-DDATA_DIR=\""$(datadir)/gnome-session"\" \
 	-DDBUS_LAUNCH=\"dbus-launch\"		\
diff -Nurd -x'*~' gnome-session-2.24.3.orig/gnome-session/gdm-signal-handler.c gnome-session-2.24.3/gnome-session/gdm-signal-handler.c
--- gnome-session-2.24.3.orig/gnome-session/gdm-signal-handler.c	2009-01-05 20:41:53.000000000 -0500
+++ gnome-session-2.24.3/gnome-session/gdm-signal-handler.c	2009-01-20 01:28:18.000000000 -0500
@@ -27,7 +27,6 @@
 #include <unistd.h>
 #include <string.h>
 #include <signal.h>
-#include <execinfo.h>
 #include <syslog.h>
 #include <sys/wait.h>
 #include <sys/stat.h>
@@ -160,22 +159,7 @@
 static void
 fallback_get_backtrace (void)
 {
-        void *  frames[64];
-        size_t  size;
-        char ** strings;
-        size_t  i;
-
-        size = backtrace (frames, G_N_ELEMENTS (frames));
-        if ((strings = backtrace_symbols (frames, size))) {
-                syslog (LOG_CRIT, "******************* START ********************************");
-                for (i = 0; i < size; i++) {
-                        syslog (LOG_CRIT, "Frame %zd: %s", i, strings[i]);
-                }
-                free (strings);
-                syslog (LOG_CRIT, "******************* END **********************************");
-        } else {
                 g_warning ("GDM crashed, but symbols couldn't be retrieved.");
-        }
 }
 
 
