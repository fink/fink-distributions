diff -uNr gnopernicus-0.10.9/configure gnopernicus-0.10.9-new/configure
--- gnopernicus-0.10.9/configure	2005-05-04 06:14:06.000000000 -0400
+++ gnopernicus-0.10.9-new/configure	2006-12-30 10:15:40.000000000 -0500
@@ -23681,6 +23681,7 @@
             LIBKE_CFLAGS=`$PKG_CONFIG --cflags "\
 		  cspi-1.0 >= $CSPI_REQUIRED \
 		  gconf-2.0 >= $GCONF_REQUIRED \
+		  gtk+-2.0 >= $GTK_REQUIRED \
 		  glib-2.0 >= $GLIB_REQUIRED "`
             echo "$as_me:$LINENO: result: $LIBKE_CFLAGS" >&5
 echo "${ECHO_T}$LIBKE_CFLAGS" >&6
@@ -23690,6 +23691,7 @@
             LIBKE_LIBS=`$PKG_CONFIG --libs "\
 		  cspi-1.0 >= $CSPI_REQUIRED \
 		  gconf-2.0 >= $GCONF_REQUIRED \
+		  gtk+-2.0 >= $GTK_REQUIRED \
 		  glib-2.0 >= $GLIB_REQUIRED "`
             echo "$as_me:$LINENO: result: $LIBKE_LIBS" >&5
 echo "${ECHO_T}$LIBKE_LIBS" >&6
