diff -Nurd -x'*~' libgksu-2.0.7.orig/libgksu/Makefile.in libgksu-2.0.7/libgksu/Makefile.in
--- libgksu-2.0.7.orig/libgksu/Makefile.in	2008-05-27 20:24:09.000000000 -0400
+++ libgksu-2.0.7/libgksu/Makefile.in	2008-11-13 13:43:13.000000000 -0500
@@ -275,7 +275,7 @@
 # minor -> keeps compatibility (additions to the API)
 # micro -> no change to the API/ABI
 libgksu2_la_LIBADD = ../libgksuui/libgksuui1.0.la
-libgksu2_la_LDFLAGS = -version-info 0:2:0 -Wl,-O1 -lutil \
+libgksu2_la_LDFLAGS = -version-info 0:2:0 \
 	${LIBGKSU_LIBS} $(am__append_1)
 noinst_HEADERS = defines.h
 include_HEADERS = gksu.h libgksu.h
diff -Nurd -x'*~' libgksu-2.0.7.orig/libgksu/libgksu.c libgksu-2.0.7/libgksu/libgksu.c
--- libgksu-2.0.7.orig/libgksu/libgksu.c	2008-05-27 20:00:44.000000000 -0400
+++ libgksu-2.0.7/libgksu/libgksu.c	2008-11-13 13:19:53.000000000 -0500
@@ -23,7 +23,7 @@
 #include <unistd.h>
 #include <string.h>
 #include <fcntl.h>
-#include <pty.h>
+#include <util.h>
 #include <pwd.h>
 #include <sys/types.h>
 #include <sys/wait.h>
@@ -1930,7 +1930,7 @@
       gchar **cmd = g_malloc (sizeof(gchar*)*7);
 
       setsid();   // make us session leader
-      cmd[i] = g_strdup ("/bin/su"); i++;
+      cmd[i] = g_strdup ("/usr/bin/su"); i++;
       if (context->login_shell)
 	{
 	  cmd[i] = g_strdup ("-"); i++;
@@ -1952,7 +1952,7 @@
       if (execv (cmd[0], cmd) == -1)
 	{
 	  fprintf (stderr,
-		   "Unable to run /bin/su: %s",
+		   "Unable to run /usr/bin/su: %s",
 		   strerror(errno));
 	}
 
diff -Nurd -x'*~' libgksu-2.0.7.orig/libgksuui/Makefile.in libgksu-2.0.7/libgksuui/Makefile.in
--- libgksu-2.0.7.orig/libgksuui/Makefile.in	2008-05-27 20:24:09.000000000 -0400
+++ libgksu-2.0.7/libgksuui/Makefile.in	2008-11-13 13:34:55.000000000 -0500
@@ -245,7 +245,7 @@
 AM_CPPFLAGS = -DLOCALEDIR=\"$(datadir)/locale\" -DDATA_DIR=\"$(datadir)\" -DPREFIX=\"$(prefix)\"
 noinst_LTLIBRARIES = libgksuui1.0.la
 libgksuui1_0_la_SOURCES = gksuui-dialog.c
-libgksuui1_0_la_LDFLAGS = -Wl,-O1 `pkg-config --libs gtk+-2.0 gconf-2.0`
+libgksuui1_0_la_LDFLAGS = `pkg-config --libs gtk+-2.0 gconf-2.0`
 noinst_HEADERS = defines.h gksuui.h gksuui-dialog.h
 test_gksuui_SOURCES = test-gksuui.c
 test_gksuui_LDADD = libgksuui1.0.la
