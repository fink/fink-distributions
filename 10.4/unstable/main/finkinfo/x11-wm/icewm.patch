diff -Nurd -x'*~' icewm-1.2.33.orig/Makefile.in icewm-1.2.33/Makefile.in
--- icewm-1.2.33.orig/Makefile.in	2007-11-04 09:13:53.000000000 -0500
+++ icewm-1.2.33/Makefile.in	2007-11-20 00:09:06.000000000 -0500
@@ -84,19 +84,19 @@
 install-base: base
 	@echo ------------------------------------------
 	@echo "Installing binaries in $(DESTDIR)$(BINDIR)"
-	@$(INSTALLDIR) "$(DESTDIR)$(BINDIR)"
-	@for bin in $(BINFILES); do \
+	$(INSTALLDIR) "$(DESTDIR)$(BINDIR)"
+	for bin in $(BINFILES); do \
              $(INSTALLBIN) "$${bin}" "$(DESTDIR)$(BINDIR)"; \
          done
 	
 	@echo "Installing presets and icons in $(DESTDIR)$(LIBDIR)"
-	@$(INSTALLDIR) "$(DESTDIR)$(LIBDIR)"
+	$(INSTALLDIR) "$(DESTDIR)$(LIBDIR)"
 	#-@$(INSTALLDIR) "$(DESTDIR)$(CFGDIR)"
-	@for lib in $(LIBFILES); do \
+	for lib in $(LIBFILES); do \
              $(INSTALLLIB) "$${lib}" "$(DESTDIR)$(LIBDIR)"; \
          done
 
-	@for xpmdir in $(XPMDIRS); do \
+	for xpmdir in $(XPMDIRS); do \
 	     if test -d "lib/$${xpmdir}"; then \
 		$(INSTALLDIR) "$(DESTDIR)$(LIBDIR)/$${xpmdir}"; \
 		for pixmap in "lib/$${xpmdir}/"*.xpm; do \
@@ -106,7 +106,7 @@
         done
 	
 	@echo ------------------------------------------
-	@for theme in $(THEMES); do \
+	for theme in $(THEMES); do \
 	     SRCDIR="$(top_srcdir)" \
 	     DESTDIR="$(DESTDIR)" \
 	     LIBDIR="$(LIBDIR)" \
@@ -121,27 +121,28 @@
 
 install-docs: docs
 	@echo ------------------------------------------
-	@rm -fr "$(DESTDIR)$(DOCDIR)/icewm-$(VERSION)"
-	@$(INSTALLDIR) "$(DESTDIR)$(DOCDIR)/icewm-$(VERSION)"
+	rm -fr "$(DESTDIR)$(DOCDIR)/icewm-$(VERSION)"
+	$(INSTALLDIR) "$(DESTDIR)$(DOCDIR)/icewm-$(VERSION)"
 	@echo "Installing documentation in $(DESTDIR)$(DOCDIR)"
-	@$(INSTALLLIB) $(DOCFILES) "$(DESTDIR)$(DOCDIR)/icewm-$(VERSION)"
-	@$(INSTALLLIB) "$(top_srcdir)/doc/"*.sgml "$(DESTDIR)$(DOCDIR)/icewm-$(VERSION)"
-	@$(INSTALLLIB) "$(top_srcdir)/doc/"*.html "$(DESTDIR)$(DOCDIR)/icewm-$(VERSION)"
+	$(INSTALLLIB) $(DOCFILES) "$(DESTDIR)$(DOCDIR)/icewm-$(VERSION)"
+	$(INSTALLLIB) "$(top_srcdir)/doc/"*.sgml "$(DESTDIR)$(DOCDIR)/icewm-$(VERSION)"
+	$(INSTALLLIB) "$(top_srcdir)/doc/"*.html "$(DESTDIR)$(DOCDIR)/icewm-$(VERSION)"
 	@echo ------------------------------------------
 
 install-nls: nls
 	@echo ------------------------------------------
-	@cd po; $(MAKE) install
+	cd po; $(MAKE) install
 	@echo ------------------------------------------
 
 install-man:
-	@for man in $(MANFILES); do \
+	$(INSTALLDIR) "$(DESTDIR)$(MANDIR)/man1"
+	for man in $(MANFILES); do \
 		$(INSTALLMAN) doc/$$man.man $(DESTDIR)$(MANDIR)/man1/$$man; \
 	done
 
 install-gnome: gnome
 	@echo ------------------------------------------
 	@echo Installing icewm-gnome in $(DESTDIR)$(GWMDIR)
-	@$(INSTALLDIR) "$(DESTDIR)$(GWMDIR)"
-	@$(INSTALLLIB) "$(top_srcdir)/lib/IceWM.desktop" "$(DESTDIR)$(GWMDIR)"
+	$(INSTALLDIR) "$(DESTDIR)$(GWMDIR)"
+	$(INSTALLLIB) "$(top_srcdir)/lib/IceWM.desktop" "$(DESTDIR)$(GWMDIR)"
 	@echo ------------------------------------------
diff -Nurd -x'*~' icewm-1.2.33.orig/configure icewm-1.2.33/configure
--- icewm-1.2.33.orig/configure	2007-11-04 09:14:01.000000000 -0500
+++ icewm-1.2.33/configure	2007-11-20 00:09:06.000000000 -0500
@@ -12611,7 +12611,7 @@
 
 VERSION=`sed -ne 's/VERSION=//p' VERSION`
 
-HOSTOS=`uname -sr || echo unknown`
+HOSTOS=`uname -s || echo unknown`
 
 HOSTCPU=`uname -m || echo unknown`
 
diff -Nurd -x'*~' icewm-1.2.33.orig/po/Makefile.in icewm-1.2.33/po/Makefile.in
--- icewm-1.2.33.orig/po/Makefile.in	2007-11-04 09:13:53.000000000 -0500
+++ icewm-1.2.33/po/Makefile.in	2007-11-20 00:09:06.000000000 -0500
@@ -28,7 +28,7 @@
 
 install: all
 	@echo "Installing message catalogues in $(DESTDIR)$(LOCDIR)"
-	@for catalog in $(MOFILES); do \
+	for catalog in $(MOFILES); do \
 	     lang=`echo $${catalog} | sed -e 's/\.mo//'` ; \
 	     msgdir="$(DESTDIR)$(LOCDIR)/$${lang}/LC_MESSAGES"; \
 	     echo "Installing language: $${lang}" ; \
diff -Nurd -x'*~' icewm-1.2.33.orig/src/Makefile.in icewm-1.2.33/src/Makefile.in
--- icewm-1.2.33.orig/src/Makefile.in	2007-11-04 09:13:53.000000000 -0500
+++ icewm-1.2.33/src/Makefile.in	2007-11-20 00:17:27.000000000 -0500
@@ -205,30 +205,24 @@
 ################################################################################
 
 %.o: %.cc
-	@echo "  CXX     " $@
-	@$(CXX) $(CXXFLAGS) $(GCCDEP) -c $<
+	$(CXX) $(CXXFLAGS) $(GCCDEP) -c $<
 
 $(BINARIES):
-	@echo "  LD      " $@
-	@$(LD) -o $@ $($(@:$(EXEEXT)=)_OBJS) $(LFLAGS) $($(@:$(EXEEXT)=)_LFLAGS) $(LIBS) $($(@:$(EXEEXT)=)_LIBS)
+	$(LD) -o $@ $($(@:$(EXEEXT)=)_OBJS) $(LFLAGS) $($(@:$(EXEEXT)=)_LFLAGS) $(LIBS) $($(@:$(EXEEXT)=)_LIBS)
 
 genpref.o: genpref.cc
-	@echo "  HOSTCXX " $@
-	@$(HOSTCXX) $(CXXFLAGS) $(GCCDEP) -c $<
+	$(HOSTCXX) $(CXXFLAGS) $(GCCDEP) -c $<
 	
 genpref$(EXEEXT):
-	@echo "  HOSTLD  " $@
-	@$(HOSTLD) -o $@ $(genpref_OBJS)
+	$(HOSTLD) -o $@ $(genpref_OBJS)
 
 ################################################################################
 
 gnome.o: gnome.cc
-	@echo "  CXX     " $@
-	@$(CXX) $(CXXFLAGS)  @GNOME1_CFLAGS@ $(GCCDEP) -c $<
+	$(CXX) $(CXXFLAGS)  @GNOME1_CFLAGS@ $(GCCDEP) -c $<
 
 gnome2.o: gnome2.cc
-	@echo "  CXX     " $@
-	@$(CXX) $(CXXFLAGS)  @GNOME2_CFLAGS@ $(GCCDEP) -c $<
+	$(CXX) $(CXXFLAGS)  @GNOME2_CFLAGS@ $(GCCDEP) -c $<
 
 ################################################################################
 
@@ -239,8 +233,7 @@
 wmabout.o: ../VERSION
 
 ../lib/preferences: genpref$(EXEEXT)
-	@echo "  GENPREF " $@
-	@./genpref$(EXEEXT) >../lib/preferences
+	./genpref$(EXEEXT) >../lib/preferences
 
 genpref$(EXEEXT): $(genpref_OBJS)
 
--- icewm-1.2.33/src/base.h~	2007-11-04 15:13:53.000000000 +0100
+++ icewm-1.2.33/src/base.h	2008-07-24 08:11:28.000000000 +0200
@@ -1,11 +1,11 @@
 #ifndef __BASE_H
 #define __BASE_H
 
-#if ( __GNUC__ == 3 && __GNUC_MINOR__ > 0 ) || __GNUC__ > 3
-#define deprecated __attribute__((deprecated))
-#else
-#define deprecated
-#endif
+//#if ( __GNUC__ == 3 && __GNUC_MINOR__ > 0 ) || __GNUC__ > 3
+//#define deprecated __attribute__((deprecated))
+//#else
+//#define deprecated
+//#endif
 
 /*** Atomar Data Types ********************************************************/
 
