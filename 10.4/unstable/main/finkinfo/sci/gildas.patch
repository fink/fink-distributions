diff -ruN gildas-src-oct07a/admin/Makefile gildas-src-oct07a.patched/admin/Makefile
--- gildas-src-oct07a/admin/Makefile	2007-07-26 11:46:43.000000000 -0400
+++ gildas-src-oct07a.patched/admin/Makefile	2007-11-18 17:42:22.000000000 -0500
@@ -42,6 +42,5 @@
 	@(echo "Bye." >> final-message.txt)
 	@(echo "***************************************************************************" >> final-message.txt)
 	@(echo >> final-message.txt)
-	-(cat final-message.txt)
 
 ###########################################################################
diff -ruN gildas-src-oct07a/admin/Makefile.def gildas-src-oct07a.patched/admin/Makefile.def
--- gildas-src-oct07a/admin/Makefile.def	2007-07-26 11:46:43.000000000 -0400
+++ gildas-src-oct07a.patched/admin/Makefile.def	2007-11-18 17:42:22.000000000 -0500
@@ -256,7 +256,7 @@
 
 # Generic flags
 GLOBAL_CFLAGS = $(GAG_INC_FLAGS) -c
-OPTION_CFLAGS = -O
+OPTION_CFLAGS = -O3
 
 # GCC flags
 ifeq ($(CC),gcc)
@@ -328,7 +328,7 @@
 
 # Generic flags
 GLOBAL_FFLAGS = -I$(incdir)
-OPTION_FFLAGS = -O
+OPTION_FFLAGS = -O3 -fno-range-check
 
 # G95 flags
 ifeq ($(FC),g95)
@@ -442,7 +442,7 @@
 
 GAG_LIBS = -lgio -lgsys
 
-SIC_LIBS = -lsic -lgio -lgsys -lslatec -llapack -lblas 
+SIC_LIBS = -lsic -lgio -lgsys ${gagsrcdir}/legacy/slatec/libslatec.a  ${gagsrcdir}/legacy/lapack/liblapack.a ${gagsrcdir}/legacy/blas/libblas.a 
 
 GREG_LIBS = -lgreg -lgtv -lgcont -lgchar -lX11
 
@@ -473,7 +473,7 @@
   ADD_LIBS += -lpng -lz
 endif
 ifeq ($(FFTW3_PRESENT),yes)
-  ADD_LIBS += -lfftw3
+  ADD_LIBS += -lfftw3f
 endif
 
 # Mandatory system libraries
diff -ruN gildas-src-oct07a/admin/gildas-env.sh gildas-src-oct07a.patched/admin/gildas-env.sh
--- gildas-src-oct07a/admin/gildas-env.sh	2007-07-20 11:51:33.000000000 -0400
+++ gildas-src-oct07a.patched/admin/gildas-env.sh	2007-11-18 17:42:22.000000000 -0500
@@ -361,14 +361,14 @@
 	    NEWATM_PRESENT=yes
 	    NEWATM_LIB_DIR=$DIR
 	fi
-	if file_present "liblapack." "${DIR}"; then
-	    LAPACK_PRESENT=yes
-	    LAPACK_LIB_DIR=$DIR
-	fi
-	if file_present "libblas." "${DIR}"; then
-	    BLAS_PRESENT=yes
-	    BLAS_LIB_DIR=$DIR
-	fi
+	#if file_present "liblapack." "${DIR}"; then
+	#    LAPACK_PRESENT=yes
+	#    LAPACK_LIB_DIR=$DIR
+	#fi
+	#if file_present "libblas." "${DIR}"; then
+	#    BLAS_PRESENT=yes
+	#    BLAS_LIB_DIR=$DIR
+	#fi
 	if file_present "libslatec." "${DIR}"; then
 	    SLATEC_PRESENT=yes
 	    SLATEC_LIB_DIR=$DIR
@@ -424,10 +424,10 @@
 	    PNG_INC=yes
 	    PNG_INC_DIR=$DIR
 	fi
-        if file_present "Python.h" "${DIR}/python${PY_VERS}"; then
-            PYTHON_INC=yes
-            PYTHON_INC_DIR=$DIR/python$PY_VERS
-        fi
+        #if file_present "Python.h" "${DIR}/python${PY_VERS}"; then
+        #    PYTHON_INC=yes
+        #    PYTHON_INC_DIR=$DIR/python$PY_VERS
+        #fi
     done
     for DIR in $LIB_PATH; do
 	if file_present "libX11." "${DIR}"; then
@@ -446,10 +446,10 @@
 	    PNG_LIB=yes
 	    PNG_LIB_DIR=$DIR
 	fi
-        if file_present "libpython" "${DIR}/python${PY_VERS}/config"; then
-            PYTHON_LIB=yes
-            PYTHON_LIB_DIR=$DIR/python$PY_VERS/config
-        fi
+        #if file_present "libpython" "${DIR}/python${PY_VERS}/config"; then
+        #    PYTHON_LIB=yes
+        #    PYTHON_LIB_DIR=$DIR/python$PY_VERS/config
+        #fi
 	if [ "$SDM_PRESENT" = "yes" ]; then
 	    if file_present "libstdc++." "${DIR}"; then
 		STDCpp_LIB=yes
