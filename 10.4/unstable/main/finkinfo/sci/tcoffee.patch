diff -Naur T-COFFEE_distribution_Version_7.43/install T-COFFEE_distribution_Version_7.43-patched/install
--- T-COFFEE_distribution_Version_7.43/install	2008-12-10 10:59:02.000000000 -0500
+++ T-COFFEE_distribution_Version_7.43-patched/install	2008-12-11 16:35:28.000000000 -0500
@@ -250,11 +250,11 @@
       }
   }
 print "------- Finishing The installation\n";
-if ( &input_yes("??????? Do you want to finalize the installation and copy the executables to their final destination ? ([y]/n):"))
-  {
-    $installation_directory=&input_installation_directory($installation_directory);
-    $final_report=&install ($installation_directory);
-  }
+#if ( &input_yes("??????? Do you want to finalize the installation and copy the executables to their final destination ? ([y]/n):"))
+#  {
+#    $installation_directory=&input_installation_directory($installation_directory);
+#    $final_report=&install ($installation_directory);
+#  }
 
 print "\n";
 print "*********************************************************************\n";
@@ -487,6 +487,7 @@
 
 sub sign_license 
   {
+  	return 0;
     my ($signature_file,$signature_flag, $email)=(@_);
  
     if (!-e $signature_file || !-e $signature_flag)
@@ -533,6 +534,7 @@
   }     
 sub get_email
   {
+    return 0;
     my ($env_file, $email)=(@_);
     my $answer;
     
