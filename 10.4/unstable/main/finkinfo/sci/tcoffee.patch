diff -Naur T-COFFEE_distribution_Version_6.92/install T-COFFEE_distribution_Version_6.92-patched/install
--- T-COFFEE_distribution_Version_6.92/install	2008-09-12 05:58:43.000000000 -0400
+++ T-COFFEE_distribution_Version_6.92-patched/install	2008-09-27 16:15:15.000000000 -0400
@@ -233,11 +233,11 @@
       }
   }
 print "------- Finishing The installation\n";
-if ( &input_yes("??????? Do you want to finalize the installation and copy the executables to their final destination ? ([y]/n):"))
-  {
-    $installation_directory=&input_installation_directory($installation_directory);
-    $final_report=&install ($installation_directory);
-  }
+#if ( &input_yes("??????? Do you want to finalize the installation and copy the executables to their final destination ? ([y]/n):"))
+#  {
+#    $installation_directory=&input_installation_directory($installation_directory);
+#    $final_report=&install ($installation_directory);
+#  }
 
 print "\n";
 print "*********************************************************************\n";
@@ -458,6 +458,7 @@
 
 sub sign_license 
   {
+  	return 0;
     my ($signature_file,$signature_flag, $email)=(@_);
  
     if (!-e $signature_file || !-e $signature_flag)
@@ -500,6 +501,7 @@
       
 sub get_email
   {
+  	return 0;
     my ($tcdir, $email)=(@_);
     my $answer;
     
