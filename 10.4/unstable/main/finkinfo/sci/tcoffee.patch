diff -Naur T-COFFEE_distribution_Version_7.54/install T-COFFEE_distribution_Version_7.54_patched/install
--- T-COFFEE_distribution_Version_7.54/install	2009-01-13 12:16:51.000000000 -0500
+++ T-COFFEE_distribution_Version_7.54_patched/install	2009-01-18 12:25:39.000000000 -0500
@@ -255,11 +255,11 @@
       }
   }
 print "------- Finishing The installation\n";
-if ( &input_yes("??????? Do you want to finalize the installation and copy the executables to their final destination ? ([y]/n):"))
-  {
-    $installation_directory=&input_installation_directory($installation_directory);
-    $final_report=&install ($installation_directory);
-  }
+#if ( &input_yes("??????? Do you want to finalize the installation and copy the executables to their final destination ? ([y]/n):"))
+#  {
+#    $installation_directory=&input_installation_directory($installation_directory);
+#    $final_report=&install ($installation_directory);
+#  }
 
 print "\n";
 print "*********************************************************************\n";
@@ -492,6 +492,7 @@
 
 sub sign_license 
   {
+    return 0;
     my ($signature_file,$signature_flag, $email)=(@_);
  
     if (!-e $signature_file || !-e $signature_flag)
@@ -538,6 +539,7 @@
   }     
 sub get_email
   {
+    return 0;
     my ($env_file, $email)=(@_);
     my $answer;
     
