diff -Nurd pixman-0.12.0/configure pixman-0.12.0-new/configure
--- pixman-0.12.0/configure	2008-09-17 13:14:40.000000000 -0400
+++ pixman-0.12.0-new/configure	2008-09-17 17:15:28.000000000 -0400
@@ -20404,11 +20404,17 @@
 
 cat >conftest.$ac_ext <<_ACEOF
 
+#ifdef __APPLE__
+#ifndef __SSE2__
+#error "Need __SSE2__ definition on Mac OS X"
+#endif
+#else
 #if defined(__GNUC__) && (__GNUC__ < 4 || (__GNUC__ == 4 && __GNUC_MINOR__ < 2))
 #   if !defined(__amd64__) && !defined(__x86_64__)
 #      error "Need GCC >= 4.2 for SSE2 intrinsics on x86"
 #   endif
 #endif
+#endif
 #include <mmintrin.h>
 #include <xmmintrin.h>
 #include <emmintrin.h>
