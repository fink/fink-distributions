diff -Nurd -x'*~' gnome-session-2.26.2.orig/capplet/Makefile.in gnome-session-2.26.2/capplet/Makefile.in
--- gnome-session-2.26.2.orig/capplet/Makefile.in	2009-07-01 08:39:02.000000000 -0400
+++ gnome-session-2.26.2/capplet/Makefile.in	2009-07-01 15:17:04.000000000 -0400
@@ -288,12 +288,12 @@
 top_srcdir = @top_srcdir@
 NULL = 
 INCLUDES = \
+	-I$(top_srcdir)/egg				\
+	-I$(top_srcdir)/gnome-session			\
 	$(WARN_CFLAGS)					\
 	$(DISABLE_DEPRECATED_CFLAGS)			\
 	$(SESSION_PROPERTIES_CFLAGS)			\
 	$(GCONF_CFLAGS)					\
-	-I$(top_srcdir)/egg				\
-	-I$(top_srcdir)/gnome-session			\
 	-DLOCALE_DIR=\""$(datadir)/locale"\"		\
 	-DGLADEDIR=\""$(pkgdatadir)"\"		\
 	$(NULL)
diff -Nurd -x'*~' gnome-session-2.26.2.orig/compat/Makefile.in gnome-session-2.26.2/compat/Makefile.in
--- gnome-session-2.26.2.orig/compat/Makefile.in	2009-07-01 08:39:02.000000000 -0400
+++ gnome-session-2.26.2/compat/Makefile.in	2010-02-07 14:38:02.000000000 -0500
@@ -307,17 +307,17 @@
 top_srcdir = @top_srcdir@
 QUIET_GEN = $(Q:@=@echo '  GEN   '$@;)
 INCLUDES = \
+	-I$(top_srcdir)/egg			\
 	$(WARN_CFLAGS)				\
 	$(DISABLE_DEPRECATED_CFLAGS)		\
 	$(COMPAT_CFLAGS)			\
 	$(GCONF_CFLAGS)				\
-	-I$(top_srcdir)/egg			\
 	-DAT_SPI_REGISTRYD_DIR=\""$(AT_SPI_REGISTRYD_DIR)"\" \
 	-DDEFAULT_SESSION_DIR=\""$(default_sessiondir)"\" \
 	-DSYSCONFDIR=\""$(sysconfdir)"\"
 
 LDADD = \
-	$(COMPAT_LIBS)
+	$(COMPAT_LIBS) $(INTLLIBS) $(X_LIBS)
 
 helperdir = $(pkglibdir)/helpers
 default_sessiondir = $(sysconfdir)/xdg/autostart
diff -Nurd -x'*~' gnome-session-2.26.2.orig/configure gnome-session-2.26.2/configure
--- gnome-session-2.26.2.orig/configure	2009-07-01 08:39:01.000000000 -0400
+++ gnome-session-2.26.2/configure	2010-02-07 15:06:25.000000000 -0500
@@ -17899,6 +17899,7 @@
    sed -e "/^#/d" -e "/^\$/d" -e "s,.*,	$posrcprefix& \\\\," -e "\$s/\(.*\) \\\\/\1/" \
 	< $srcdir/po/POTFILES.in > po/POTFILES
 
+EGG_SMCLIENT_LIBS="$EGG_SMCLIENT_LIBS $INTLLIBS"
 
 { $as_echo "$as_me:$LINENO: checking for ANSI C header files" >&5
 $as_echo_n "checking for ANSI C header files... " >&6; }
diff -Nurd -x'*~' gnome-session-2.26.2.orig/gnome-session/Makefile.in gnome-session-2.26.2/gnome-session/Makefile.in
--- gnome-session-2.26.2.orig/gnome-session/Makefile.in	2009-07-01 08:39:04.000000000 -0400
+++ gnome-session-2.26.2/gnome-session/Makefile.in	2009-07-01 15:18:08.000000000 -0400
@@ -318,6 +318,7 @@
 QUIET_GEN = $(Q:@=@echo '  GEN   '$@;)
 noinst_LTLIBRARIES = libgsmutil.la
 INCLUDES = \
+	-I$(top_srcdir)/egg			\
 	$(WARN_CFLAGS)				\
 	$(DISABLE_DEPRECATED_CFLAGS)		\
 	$(SM_CFLAGS)				\
@@ -325,11 +326,11 @@
 	$(GNOME_SESSION_CFLAGS)			\
 	$(GCONF_CFLAGS)				\
 	$(POLKIT_GNOME_CFLAGS)			\
-	-I$(top_srcdir)/egg			\
 	-DLOCALE_DIR=\""$(datadir)/locale"\"	\
 	-DDATA_DIR=\""$(datadir)/gnome-session"\" \
 	-DDBUS_LAUNCH=\"dbus-launch\"		\
 	-DLIBEXECDIR=\"$(libexecdir)\"		\
+	-DPREFIX=\"$(prefix)\"	\
 	-DGLADEDIR=\""$(pkgdatadir)"\"		\
 	-DGCONF_SANITY_CHECK=\""$(GCONF_SANITY_CHECK)"\" \
 	-DGCONFTOOL_CMD=\"$(GCONFTOOL)\"
@@ -359,7 +360,7 @@
 	$(GCONF_LIBS)				\
 	$(POLKIT_GNOME_LIBS)			\
 	$(XRENDER_LIBS)				\
-	$(XTEST_LIBS)				\
+	$(XTEST_LIBS) $(X_LIBS) $(INTLLIBS)	\
 	$(EXECINFO_LIBS)			\
 	$(NULL)
 
@@ -410,7 +411,7 @@
 	$(NULL)
 
 libgsmutil_la_LIBADD = \
-	$(GNOME_SESSION_LIBS)
+	$(GNOME_SESSION_LIBS) -lXext -L/usr/X11/lib -L/usr/X11R6/lib
 
 libgsmutil_la_SOURCES = \
 	gsm-util.c				\
diff -Nurd -x'*~' gnome-session-2.26.2.orig/gnome-session/gdm-signal-handler.c gnome-session-2.26.2/gnome-session/gdm-signal-handler.c
--- gnome-session-2.26.2.orig/gnome-session/gdm-signal-handler.c	2009-06-29 18:36:53.000000000 -0400
+++ gnome-session-2.26.2/gnome-session/gdm-signal-handler.c	2009-07-01 15:23:17.000000000 -0400
@@ -203,8 +203,8 @@
                 }
         } else if (pid == 0) {
                 /* Child process */
-                execl (LIBEXECDIR "/gdm-crash-logger",
-                       LIBEXECDIR "/gdm-crash-logger", NULL);
+                execl (PREFIX "/lib/gdm/gdm-crash-logger",
+                       PREFIX "/lib/gdm/gdm-crash-logger", NULL);
         }
 
         return success;
@@ -221,7 +221,7 @@
          * we get much better information out of it.  Otherwise
          * fall back to execinfo.
          */
-        if (g_stat (LIBEXECDIR "/gdm-crash-logger", &s) == 0) {
+        if (g_stat (PREFIX "/lib/gdm/gdm-crash-logger", &s) == 0) {
                 fallback = crashlogger_get_backtrace () ? FALSE : TRUE;
         }
 
diff -Nurd -x'*~' gnome-session-2.26.2.orig/gnome-session/gdm.c gnome-session-2.26.2/gnome-session/gdm.c
--- gnome-session-2.26.2.orig/gnome-session/gdm.c	2009-04-19 14:26:52.000000000 -0400
+++ gnome-session-2.26.2/gnome-session/gdm.c	2009-07-01 15:18:33.000000000 -0400
@@ -254,10 +254,12 @@
 
         g_assert (data->fd <= 0);
 
-        if (g_file_test (GDM_PROTOCOL_SOCKET_PATH, G_FILE_TEST_EXISTS)) {
-                strcpy (addr.sun_path, GDM_PROTOCOL_SOCKET_PATH);
+        if (g_file_test (PREFIX GDM_PROTOCOL_SOCKET_PATH, G_FILE_TEST_EXISTS)) {
+                strcpy (addr.sun_path, PREFIX GDM_PROTOCOL_SOCKET_PATH);
         } else if (g_file_test ("/tmp/.gdm_socket", G_FILE_TEST_EXISTS)) {
                 strcpy (addr.sun_path, "/tmp/.gdm_socket");
+        } else if (g_file_test (GDM_PROTOCOL_SOCKET_PATH, G_FILE_TEST_EXISTS)) {
+                strcpy (addr.sun_path, GDM_PROTOCOL_SOCKET_PATH);
         } else {
                 return FALSE;
         }
diff -Nurd -x'*~' gnome-session-2.26.2.orig/gnome-session/main.c gnome-session-2.26.2/gnome-session/main.c
--- gnome-session-2.26.2.orig/gnome-session/main.c	2009-07-01 06:25:30.000000000 -0400
+++ gnome-session-2.26.2/gnome-session/main.c	2009-07-01 15:23:59.000000000 -0400
@@ -385,6 +385,7 @@
                       char   **argv,
                       GError **error)
 {
+#ifndef __APPLE__
         char **new_argv;
         int    i;
 
@@ -415,6 +416,7 @@
                              g_strerror (errno));
                 return FALSE;
         }
+#endif
 
         /* Should not be reached */
         return TRUE;
diff -Nurd -x'*~' gnome-session-2.26.2.orig/tools/Makefile.in gnome-session-2.26.2/tools/Makefile.in
--- gnome-session-2.26.2.orig/tools/Makefile.in	2009-07-01 08:39:04.000000000 -0400
+++ gnome-session-2.26.2/tools/Makefile.in	2010-02-07 14:27:34.000000000 -0500
@@ -294,7 +294,7 @@
 	$(ICE_LIBS)				\
 	$(GNOME_SESSION_LIBS)			\
 	$(DBUS_GLIB_LIBS)			\
-	$(GCONF_LIBS)
+	$(GCONF_LIBS) $(INTLLIBS)
 
 gnome_session_save_SOURCES = \
 	gnome-session-save.c
