diff -Nurd -x'*~' libgnomeprint-2.18.3.orig/configure libgnomeprint-2.18.3/configure
--- libgnomeprint-2.18.3.orig/configure	2008-01-28 11:07:08.000000000 -0500
+++ libgnomeprint-2.18.3/configure	2008-01-30 23:37:20.000000000 -0500
@@ -23957,11 +23957,6 @@
   ;;
 esac
 
-if test "x$LIBM" = "x"; then
-	{ { echo "$as_me:$LINENO: error: You need -lm(mathematic library) for this version of libgnomeprint." >&5
-echo "$as_me: error: You need -lm(mathematic library) for this version of libgnomeprint." >&2;}
-   { (exit 1); exit 1; }; }
-fi
 LDFLAGS="$LDFLAGS $LIBM"
 
 FREETYPE_LIBS=
@@ -24915,8 +24910,8 @@
    { (exit 1); exit 1; }; }
 fi
 
-LIBGNOMEPRINT_CFLAGS="$GP_CFLAGS $FREETYPE_CFLAGS"
-LIBGNOMEPRINT_LIBS="$GP_LIBS $FREETYPE_LIBS $Z_LIBS"
+LIBGNOMEPRINT_CFLAGS="$FREETYPE_CFLAGS $GP_CFLAGS"
+LIBGNOMEPRINT_LIBS="$FREETYPE_LIBS $GP_LIBS $Z_LIBS"
 
 
 
