Package: file-roller
Version: 2.26.3
Revision: 1
Depends: <<
	atk1-shlibs (>= 1.26.0-1),
	cairo-shlibs (>= 1.8-1),
	dbus-shlibs (>= 1.2.1-1),
	desktop-file-utils,
	expat1-shlibs,
	fontconfig2-shlibs (>= 2.4.1-1),
	freetype219-shlibs (>= 2.3.8-2),
	gconf2 (>= 2.26.0-1),
	gconf2-shlibs (>= 2.26.0-1),
	glib2-shlibs (>= 2.20.0-1),
	glitz-shlibs,
	gtk+2 (>= 2.16.0-1),
	gtk+2-shlibs (>= 2.16.0-1),
	libgettext3-shlibs,
	libiconv,
	libpng3-shlibs,
	libnautilus-ext2-shlibs (>= 2.26.0-1),
	orbit2 (>= 2.14.17-1),
	pango1-xft2-ft219 (>= 1.24.0-1),
	pixman-shlibs (>= 0.14.0-1),
	rarian-compat,
	x11-shlibs
<<
BuildDepends: <<
	atk1 (>= 1.26.0-1),
	cairo (>= 1.8-1),
	dbus-dev (>= 1.2.1-1),
	expat1,
	fink (>= 0.24.12-1),
	fontconfig2-dev (>= 2.4.1-1),
	freetype219 (>= 2.3.8-2),
	gconf2-dev (>= 2.26.0-1),
	gettext-tools,
	gettext-bin,
	glib2-dev (>= 2.20.0-1),
	glitz,
	gnome-doc-utils (>= 0.16.0-1),
	gtk+2-dev (>= 2.16.0-1),
	gtk-doc,
	intltool40,
	libgettext3-dev,
	libiconv-dev,
	libnautilus-ext2-dev (>= 2.26.0-1),
	libpng3,
	orbit2-dev (>= 2.14.17-1),
	pango1-xft2-ft219-dev (>= 1.24.0-1),
	pixman (>= 0.14.0-1),
	pkgconfig (>= 0.23),
	x11-dev,
	xft2-dev
<<
Source: mirror:gnome:sources/%n/2.26/%n-%v.tar.bz2
Source-MD5: 1876e9807a12c37f9bb2d00807905755
SetCFLAGS: -Os
ConfigureParams: --libexecdir=%p/lib/%n --mandir=%p/share/man --disable-scrollkeeper --enable-nautilus-actions --disable-schemas-install --disable-dependency-tracking PKG_CONFIG_PATH="%p/lib/fontconfig2/lib/pkgconfig:$PKG_CONFIG_PATH" --disable-static
PatchScript: touch src/typedefs.h
InstallScript: make install DESTDIR=%d
PostInstScript: <<
	. %p/bin/init.sh
	if [ configure = "$1" ]; then
		scrollkeeper-update
		export GCONF_CONFIG_SOURCE=`gconftool-2 --get-default-source`
		gconftool-2 --makefile-install-rule %p/etc/gconf/schemas/file-roller.schemas >/dev/null
		gtk-update-icon-cache -f -t %p/share/icons/hicolor
	fi
	[ -x %p/bin/update-desktop-database ] && update-desktop-database -q %p/share/applications || :
<<
PostRmScript: <<
	. %p/bin/init.sh
	if [ upgrade != "$1" ]; then
		scrollkeeper-update
	fi
	[ -x %p/bin/update-desktop-database ] && update-desktop-database -q %p/share/applications || :
<<
ConfFiles: %p/etc/gconf/schemas/file-roller.schemas
DocFiles: AUTHORS COPYING ChangeLog po/ChangeLog:ChangeLog.po README TODO
DescPort: <<
	Update timestamp of typedefs.h to force regeneration of
	fr-enum-types.[ch] that shipped with incorrect relative
	pathname in an #include statement. See:
	http://bugzilla.gnome.org/show_bug.cgi?id=587491
<<
Description: GNOME archive manager
DescDetail: <<
File Roller is an archive manager for the GNOME environment.  This means that
you can create and modify archives, view the content of an archive, view a
file contained in the archive, and extract files from the archive.
File Roller is only a front-end (a graphical interface) to archiving programs
like tar and zip.
<<
DescUsage: <<
	To open/create some archives types, you may have to install the 
	corresponding tools.
	The tool for rpm files is not available via Fink.
	If you read the help file from the file-roller help menu, you may get
	an old version depending on your locale; hence if you can read English,
	click on the home icon in the help browser, then on Applications, and
	finally on System Tools, you will get the newest available version.
<<
License: GPL
Maintainer: The Gnome Core Team <fink-gnome-core@lists.sourceforge.net>
Homepage: http://www.gnome.org/
