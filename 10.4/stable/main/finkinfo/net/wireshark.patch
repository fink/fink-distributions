diff -ruN wireshark-1.0.0/configure wireshark-1.0.0-patched/configure
--- wireshark-1.0.0/configure	2008-03-31 20:22:39.000000000 +0200
+++ wireshark-1.0.0-patched/configure	2008-03-31 20:24:37.000000000 +0200
@@ -20591,7 +20591,7 @@
        LIBS="$ac_save_LIBS"
   fi
 
-  if test "x$no_libgnutls" = x ; then
+  if test "x$no_libgnutls" = xXXXX ; then
      { echo "$as_me:$LINENO: result: yes" >&5
 echo "${ECHO_T}yes" >&6; }
 
@@ -20841,7 +20841,7 @@
         fi
      fi
   fi
-  if test $ok = yes; then
+  if test $ok = XXXX; then
     LIBGCRYPT_CFLAGS=`$LIBGCRYPT_CONFIG --cflags`
     LIBGCRYPT_LIBS=`$LIBGCRYPT_CONFIG --libs`
 
