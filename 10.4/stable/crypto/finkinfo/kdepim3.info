Package: kdepim3
Version: 3.5.9
Revision: 2
Description: KDE - tools for managing personal information
License: GPL/LGPL
Maintainer: Benjamin Reed <kdepim3@fink.racoonfink.com>

Depends: <<
	%N-ioslaves (>= %v-%r),
	%N-kfile-plugins (>= %v-%r),
	%N-wizards (>= %v-%r),
	akregator (>= %v-%r),
	kleopatra (>= %v-%r),
	kaddressbook (>= %v-%r),
	kalarm (>= %v-%r),
	kandy (>= %v-%r),
	karm (>= %v-%r),
	kitchensync (>= %v-%r),
	kmail (>= %v-%r),
	knode (>= %v-%r),
	knotes (>= %v-%r),
	konsolekalendar (>= %v-%r),
	kontact (>= %v-%r),
	korganizer (>= %v-%r),
	korn (>= %v-%r),
	kpilot (>= %v-%r),
	ktnef (>= %v-%r)
<<
BuildDepends: <<
	arts-dev (>= 1.5.9-1),
	audiofile (>= 0.2.6-11),
	autoconf (>= 2.60-1),
	cctools-single-module,
	cyrus-sasl2-dev,
	cyrus-sasl2,
	esound,
	expat1,
	fink (>= 0.28),
	freetype219 (>= 2.2.1-5),
	gtk+ (>= 1.2.10-12),
	glib (>= 1.2.10-8),
	glib2-dev (>= 2.12.0-1),
	gnokii3-dev,
	gpgme11 (>= 1.0.3-2),
	ktts (>= %v-1),
	kdelibs3-unified-dev (>= %v-1),
	libart2 (>= 2.3.19-1),
	libiconv-dev,
	libgettext3-dev,
	gettext-tools,
	libgpg-error,
	libidn,
	libjpeg,
	libmad,
	libmal10 (>= 0.40-5),
	libpng3 (>= 1.2.8-1),
	libncurses5,
	libogg,
	libusb,
	libvorbis0,
	libxml2,
	opensync-dev,
	pcre,
	pkgconfig (>= 0.21-1),
	pilot-link10,
	readline5,
	qt3 (>= 3.3.8-1021),
	sqlite3-dev,
	unsermake (>= 0.4-0.20060316.1),
	x11-dev,
	xmkmf
<<
Replaces: %N-dev (<< %v-1)
GCC: 4.0

CustomMirror: <<
	Primary: http://ranger.users.finkproject.org/distfiles
	eur-NL: http://ranger.users.finkproject.org/distfiles
	nam-US: http://ranger.befunk.com/fink
	nam-US: http://astrange.ithinksw.net/~astrange/ranger-fink
	nam-CA: http://www.southofheaven.net/befunk
<<
Source: mirror:kde:stable/%v/src/kdepim-%v.tar.bz2
Source-MD5: ba27b06599556c572a26f03608471ee2
Source2: mirror:custom:kde-admindir/kde-admindir-1182.tar.bz2
Source2-MD5: b37c9ce91682d7972da7770d6bc085fb
Source2ExtractDir: kdepim-%v
SourceDirectory: kdepim-%v
PatchScript: <<
	sed -e 's,@FINKPREFIX@,%p,g' %a/%n.patch | patch -p1
	perl -pi -e 's,-O2,-Os,g; s,doc/HTML,doc/kde,g; s,/usr/share/doc/packages/qt3/html,%p/share/doc/qt3/html,g;' admin/*
	perl -pi -e 's,#include <malloc.h>,,g' libkcal/versit/vcc*
	perl -pi -e 's/-Xlinker -framework -Xlinker (\S*)/-Wl,-framework,$1/g; s/-framework (\S*)/-Wl,-framework,$1/g;' admin/*
<<

NoSetCPPFLAGS: true
NoSetLDFLAGS: true
ConfigureParams: --disable-indexlib --with-distribution='Fink/Mac OS X' --with-pilot-link=%p/lib/pilot-link10
CompileScript: <<
#!/bin/sh -ev

	export PREFIX="%p" USE_UNSERMAKE=1
	. ./environment-helper.sh
	export ac_cv_path_GPGME_CONFIG="%p/bin/gpgme-config --thread=pthread"

	export CC=gcc CXX=g++
	export PKG_CONFIG_PATH="%p/lib/pilot-link10/lib/pkgconfig:$PKG_CONFIG_PATH"
	export CPPFLAGS="-I%p/lib/pilot-link10/include $CPPFLAGS"
	export LDFLAGS="-L%p/lib/pilot-link10/lib $LDFLAGS"

	ranlib %p/lib/libgpg*.a >/dev/null 2>&1
	./build-helper.sh cvs       %N %v %r make -f admin/Makefile.common cvs
	./build-helper.sh configure %N %v %r ./configure %c $CONFIGURE_PARAMS

#  perl -pi -e 's,yytext_ptr,libical_ptr,g' libical/src/libical/icallexer.c
#  perl -pi -e 's,yytext_ptr,libicalss_ptr,g' libical/src/libicalss/icalsslexer.c
#  perl -pi -e 's, holidays , ,g' korganizer/plugins/Makefile

	./build-helper.sh make      %N %v %r unsermake $UNSERMAKEFLAGS
<<

InstallScript: <<
#!/bin/sh -ev

	export PREFIX="%p" USE_UNSERMAKE=1
	. ./environment-helper.sh

	./build-helper.sh install %N %v %r unsermake -p -j1 install DESTDIR=%d

	mkdir -p %i/share/doc/installed-packages
	touch %i/share/doc/installed-packages/%N
	touch %i/share/doc/installed-packages/%N-base
<<
DocFiles: README*
SplitOff: <<
	Package: %N-dev
	Description: KDE - kdepim development headers and libraries
	Depends: <<
		%N (>= %v-%r),
		%N-base (>= %v-%r)
	<<
# note: some files were moved from %N-common and kitchensync into here
# in 3.5.9-2 which is why Replaces has changed
	Replaces: <<
		%N (<< %v-1),
		%N-common (<< %v-%r),
		kdenetwork3-common,
		kdenetwork3-dev (<< %v-1),
		kitchensync (<< %v-%r),
		kmail (<< %v-%r),
		knode (<< %v-%r)
	<<
	Recommends: <<
		arts-dev (>= 1.5.3-1),
		fink (>= 0.26.0-1),
		gtk+ (>= 1.2.10-12),
		glib (>= 1.2.10-8),
		kdelibs3-unified-dev (>= %v-1),
		libart2,
		libjpeg,
		libpng3 (>= 1.2.8-1),
		pilot-link10,
		readline5,
		qt3 (>= 3.3.8-1021)
	<<
	BuildDependsOnly: true
	Files: <<
		include
		lib/*.la
		lib/libakregatorprivate.dylib
		lib/libgpgme++.dylib
		lib/libgwsoap.dylib
		lib/libindex.dylib
		lib/libkabc_groupdav.dylib
		lib/libkabc_groupwise.dylib
		lib/libkabc_newexchange.dylib
		lib/libkabc_slox.dylib
		lib/libkabc_xmlrpc.dylib
		lib/libkabckolab.dylib
		lib/libkabcscalix.dylib
		lib/libkabinterfaces.dylib
		lib/libkaddressbook.dylib
		lib/libkcal.dylib
		lib/libkcal_groupdav.dylib
		lib/libkcal_groupwise.dylib
		lib/libkcal_newexchange.dylib
		lib/libkcal_resourcefeatureplan.dylib
		lib/libkcal_resourceremote.dylib
		lib/libkcal_slox.dylib
		lib/libkcal_xmlrpc.dylib
		lib/libkcalkolab.dylib
		lib/libkcalscalix.dylib
		lib/libkdepim.dylib
		lib/libkgantt.dylib
		lib/libkgroupwarebase.dylib
		lib/libkgroupwaredav.dylib
		lib/libkholidays.dylib
		lib/libkitchensync.dylib
		lib/libkleopatra.dylib
		lib/libkmailprivate.dylib
		lib/libkmime.dylib
		lib/libknodecommon.dylib
		lib/libknotes_xmlrpc.dylib
		lib/libknoteskolab.dylib
		lib/libknotesscalix.dylib
		lib/libkode.dylib
		lib/libkontact.dylib
		lib/libkocorehelper.dylib
		lib/libkorg_stdprinting.dylib
		lib/libkorganizer.dylib
		lib/libkorganizer_calendar.dylib
		lib/libkorganizer_eventviewer.dylib
		lib/libkpgp.dylib
		lib/libkpilot.dylib
		lib/libkpimexchange.dylib
		lib/libkpimidentities.dylib
		lib/libkpinterfaces.dylib
		lib/libqopensync.dylib
		lib/libksieve.dylib
		lib/libkslox.dylib
		lib/libktnef.dylib
		lib/libmimelib.dylib
		lib/libqgpgme.dylib
		lib/kde3/plugins/designer
	<<
	DescUsage: <<
---[ Installation ]---------------------------------------------------

To install all of the official KDE packages, install "bundle-kde" or
"bundle-kde-ssl" (depending on whether crypotgraphic laws permit
downloading strong cryptography).  Unless you have a specific reason
not to, "bundle-kde-ssl" is suggested.

---[ Configuration ]--------------------------------------------------

To use KDE as your windowing system in XDarwin, create a file called
".xinitrc" in your home directory, containing the following line:

  /sw/bin/startkde >/tmp/kde.log 2>&1

See http://fink.sourceforge.net/doc/x11/run-xfree86.php#xinitrc
for more information on the xinitrc files and how they work.

If you want to use Apple's X11 instead of the KDE window manager,
put the following line before the startkde line in .xinitrc:

  export KDEWM=quartz-wm

If you want to disable quartz-wm even for clipboard synchronization
(proxying), put the following line before the startkde line in
.xinitrc:

  export DISABLE_PROXY=1

---[ Startup ]--------------------------------------------------------

Once you have created an ~/.xinitrc file, you should be able to start
KDE by clicking on the "XDarwin" icon in your Applications folder.  

NOTE: By default, X11 on Mac OS X runs in "rootless" mode, generally.
If you run KDE in rootless mode, it will take over your desktop with
a window that covers everything up.  You can remove this by disabling
desktop icons in the KDE control center.  Open the control center
(either from the "K" menu bar, or by typing "kcontrol" in an xterm)
then expand the "Desktop" list, click "Behavior", and uncheck the
"Show icons on desktop" checkbox.
   
---[ Removal ]--------------------------------------------------------
   
To remove all of the official KDE packages, you can remove the KDE
libraries and anything that depends on them by running, in a terminal:
   
  sudo apt-get remove kdelibs3-universal-shlibs

To remove an individual KDE package collection (like kdenetwork3 or
kdesdk3) you can generally remove [package]-base.  For example, to
remove all of the kdenetwork3 packages, run:

  sudo apt-get remove kdenetwork3-base
	<<
<<
SplitOff10: <<
	Package: kitchensync
	Description: KDE - sync tool
	Depends: <<
		%N-common (>= %v-%r),
		ktnef (>= %v-%r)
	<<
	Replaces: <<
		%N (<< %v-%r),
		%N-dev (<< %v-%r),
		%N-common,
		ksync (<< 3.5.7-1)
	<<
	Recommends: %N (>= %v-%r)
	Files: <<
		bin/kitchensync
		lib/kde3/*kitchensync*
		lib/libkdeinit_kitchensync.*
		lib/libkitchensync*
		lib/libqopensync*
		share/applications/kde/kitchensync*
		share/apps/kitchensync
		share/icons/hicolor/16x16/apps/kitchensync.png
		share/icons/hicolor/22x22/apps/kitchensync.png
		share/icons/hicolor/32x32/apps/kitchensync.png
		share/icons/hicolor/48x48/apps/kitchensync.png
	<<
	Shlibs: <<
		%p/lib/libkitchensync.0.dylib 1.0.0 kitchensync (>= 3.5.7-1)
		%p/lib/libqopensync.0.dylib   1.0.0 kitchensync (>= 3.5.7-1)
		!%p/lib/libkdeinit_kitchensync.dylib
	<<
	Homepage: http://www.opensync.org/wiki/kitchensync
	DescDetail: This package contains a synchronization framework, still under heavy development.
	DescUsage: <<
---[ Installation ]---------------------------------------------------

To install all of the official KDE packages, install "bundle-kde" or
"bundle-kde-ssl" (depending on whether crypotgraphic laws permit
downloading strong cryptography).  Unless you have a specific reason
not to, "bundle-kde-ssl" is suggested.

---[ Configuration ]--------------------------------------------------

To use KDE as your windowing system in XDarwin, create a file called
".xinitrc" in your home directory, containing the following line:

  /sw/bin/startkde >/tmp/kde.log 2>&1

See http://fink.sourceforge.net/doc/x11/run-xfree86.php#xinitrc
for more information on the xinitrc files and how they work.

If you want to use Apple's X11 instead of the KDE window manager,
put the following line before the startkde line in .xinitrc:

  export KDEWM=quartz-wm

If you want to disable quartz-wm even for clipboard synchronization
(proxying), put the following line before the startkde line in
.xinitrc:

  export DISABLE_PROXY=1

---[ Startup ]--------------------------------------------------------

Once you have created an ~/.xinitrc file, you should be able to start
KDE by clicking on the "XDarwin" icon in your Applications folder.  

NOTE: By default, X11 on Mac OS X runs in "rootless" mode, generally.
If you run KDE in rootless mode, it will take over your desktop with
a window that covers everything up.  You can remove this by disabling
desktop icons in the KDE control center.  Open the control center
(either from the "K" menu bar, or by typing "kcontrol" in an xterm)
then expand the "Desktop" list, click "Behavior", and uncheck the
"Show icons on desktop" checkbox.
   
---[ Removal ]--------------------------------------------------------
   
To remove all of the official KDE packages, you can remove the KDE
libraries and anything that depends on them by running, in a terminal:
   
  sudo apt-get remove kdelibs3-universal-shlibs

To remove an individual KDE package collection (like kdenetwork3 or
kdesdk3) you can generally remove [package]-base.  For example, to
remove all of the kdenetwork3 packages, run:

  sudo apt-get remove kdenetwork3-base
	<<
<<
SplitOff11: <<
	Package: kmail
	Description: KDE - e-mail reader
	Depends: <<
		%N-common (>= %v-%r),
		%N-kfile-plugins (>= %v-%r),
		%N-ioslaves (>= %v-%r),
		kleopatra (>= %v-%r),
		ktnef (>= %v-%r)
	<<
	Replaces: <<
		%N (<< %v-%r),
		%N-dev (<< %v-%r),
		%N-common
	<<
	Recommends: %N (>= %v-%r)
	Suggests: klamav
	Files: <<
		bin/kmail*
		lib/kde3/kcm_kmail.*
		lib/kde3/kmail.*
		lib/kde3/kmailcvt.*
		lib/kde3/libkmail*
		lib/libkdeinit_kmail.*
		lib/libkdeinit_kmailcvt.*
		lib/libkmailprivate.*.dylib
		share/applications/kde/KMail.desktop
		share/applications/kde/kmail_view.desktop
		share/applnk/Utilities/kmailcvt.desktop
		share/apps/kconf_update/kmail*
		share/apps/kconf_update/upgrade-signature.pl
		share/apps/kconf_update/upgrade-transport.pl
		share/apps/kmail*
		share/config.kcfg/custommimeheader.kcfg
		share/config.kcfg/kmail.kcfg
		share/config.kcfg/replyphrases.kcfg
		share/config/kmail.*
		share/doc/kde/en/kmail
		share/icons/crystalsvg/16x16/apps/kmail*
		share/icons/crystalsvg/22x22/apps/kmail*
		share/icons/crystalsvg/32x32/apps/kmail*
		share/icons/crystalsvg/48x48/apps/kmail*
		share/icons/hicolor/128x128/apps/kmail*
		share/icons/hicolor/16x16/apps/kmail*
		share/icons/hicolor/22x22/apps/kmail*
		share/icons/hicolor/32x32/apps/kmail*
		share/icons/hicolor/48x48/apps/kmail*
		share/icons/hicolor/64x64/apps/kmail*
		share/icons/hicolor/scalable/apps/kmail*
		share/services/kmail_*
		share/servicetypes/dcopimap.*
		share/servicetypes/dcopmail.*
	<<
	Shlibs: <<
		%p/lib/libkmailprivate.0.dylib 1.0.0 kmail (>= 3.5.0-1)
		!%p/lib/libkdeinit_kmail.dylib
		!%p/lib/libkdeinit_kmailcvt.dylib
	<<
	Homepage: http://pim.kde.org/components/kmail.php
	DescDetail: <<
KMail is a fully-featured email client that fits nicely into the K
Desktop Environment.  It has features such as support for IMAP,
POP3, multiple accounts, powerful filters, PGP/GnuPG privacy and
much more.
	<<
	DescUsage: <<
---[ Installation ]---------------------------------------------------

To install all of the official KDE packages, install "bundle-kde" or
"bundle-kde-ssl" (depending on whether crypotgraphic laws permit
downloading strong cryptography).  Unless you have a specific reason
not to, "bundle-kde-ssl" is suggested.

---[ Configuration ]--------------------------------------------------

To use KDE as your windowing system in XDarwin, create a file called
".xinitrc" in your home directory, containing the following line:

  /sw/bin/startkde >/tmp/kde.log 2>&1

See http://fink.sourceforge.net/doc/x11/run-xfree86.php#xinitrc
for more information on the xinitrc files and how they work.

If you want to use Apple's X11 instead of the KDE window manager,
put the following line before the startkde line in .xinitrc:

  export KDEWM=quartz-wm

If you want to disable quartz-wm even for clipboard synchronization
(proxying), put the following line before the startkde line in
.xinitrc:

  export DISABLE_PROXY=1

---[ Startup ]--------------------------------------------------------

Once you have created an ~/.xinitrc file, you should be able to start
KDE by clicking on the "XDarwin" icon in your Applications folder.  

NOTE: By default, X11 on Mac OS X runs in "rootless" mode, generally.
If you run KDE in rootless mode, it will take over your desktop with
a window that covers everything up.  You can remove this by disabling
desktop icons in the KDE control center.  Open the control center
(either from the "K" menu bar, or by typing "kcontrol" in an xterm)
then expand the "Desktop" list, click "Behavior", and uncheck the
"Show icons on desktop" checkbox.
   
---[ Removal ]--------------------------------------------------------
   
To remove all of the official KDE packages, you can remove the KDE
libraries and anything that depends on them by running, in a terminal:
   
  sudo apt-get remove kdelibs3-universal-shlibs

To remove an individual KDE package collection (like kdenetwork3 or
kdesdk3) you can generally remove [package]-base.  For example, to
remove all of the kdenetwork3 packages, run:

  sudo apt-get remove kdenetwork3-base
	<<
<<
SplitOff12: <<
	Package: knode
	Description: KDE - usenet news reader
	Depends: <<
		%N-common (>= %v-%r),
		%N-kfile-plugins (>= %v-%r),
		%N-ioslaves (>= %v-%r),
		ktnef (>= %v-%r)
	<<
	Replaces: <<
		%N (<< %v-%r),
		%N-dev (<< %v-%r),
		%N-common
	<<
	Recommends: %N (>= %v-%r)
	Files: <<
		bin/knode
		lib/kde3/kcm_knode.*
		lib/kde3/knode.*
		lib/kde3/libknodepart.*
		lib/libkdeinit_knode.*
		lib/libknodecommon.*.dylib
		share/applications/kde/KNode.desktop
		share/apps/knode
		share/doc/kde/en/knode
		share/icons/hicolor/128x128/apps/knode*
		share/icons/hicolor/16x16/apps/knode*
		share/icons/hicolor/32x32/apps/knode*
		share/icons/hicolor/48x48/apps/knode*
		share/icons/hicolor/64x64/apps/knode*
		share/services/knewsservice.*
		share/services/knode_*
	<<
	Shlibs: <<
		%p/lib/libknodecommon.0.dylib 1.0.0 knode (>= 3.5.0-1)
		!%p/lib/libkdeinit_knode.dylib
	<<
	Homepage: http://knode.sourceforge.net/
	DescDetail: <<
KNode is a newsreader for the K Desktop Environment.  It is GNKSA compliant
(unfortunally a review is still pending), and has support for MIME and
multiple servers.

It is a online-reader, but in combination with a local newsserver like
leafnode also usable with dial-up connections.
	<<
	DescUsage: <<
---[ Installation ]---------------------------------------------------

To install all of the official KDE packages, install "bundle-kde" or
"bundle-kde-ssl" (depending on whether crypotgraphic laws permit
downloading strong cryptography).  Unless you have a specific reason
not to, "bundle-kde-ssl" is suggested.

---[ Configuration ]--------------------------------------------------

To use KDE as your windowing system in XDarwin, create a file called
".xinitrc" in your home directory, containing the following line:

  /sw/bin/startkde >/tmp/kde.log 2>&1

See http://fink.sourceforge.net/doc/x11/run-xfree86.php#xinitrc
for more information on the xinitrc files and how they work.

If you want to use Apple's X11 instead of the KDE window manager,
put the following line before the startkde line in .xinitrc:

  export KDEWM=quartz-wm

If you want to disable quartz-wm even for clipboard synchronization
(proxying), put the following line before the startkde line in
.xinitrc:

  export DISABLE_PROXY=1

---[ Startup ]--------------------------------------------------------

Once you have created an ~/.xinitrc file, you should be able to start
KDE by clicking on the "XDarwin" icon in your Applications folder.  

NOTE: By default, X11 on Mac OS X runs in "rootless" mode, generally.
If you run KDE in rootless mode, it will take over your desktop with
a window that covers everything up.  You can remove this by disabling
desktop icons in the KDE control center.  Open the control center
(either from the "K" menu bar, or by typing "kcontrol" in an xterm)
then expand the "Desktop" list, click "Behavior", and uncheck the
"Show icons on desktop" checkbox.
   
---[ Removal ]--------------------------------------------------------
   
To remove all of the official KDE packages, you can remove the KDE
libraries and anything that depends on them by running, in a terminal:
   
  sudo apt-get remove kdelibs3-universal-shlibs

To remove an individual KDE package collection (like kdenetwork3 or
kdesdk3) you can generally remove [package]-base.  For example, to
remove all of the kdenetwork3 packages, run:

  sudo apt-get remove kdenetwork3-base
	<<
<<
SplitOff13: <<
	Package: knotes
	Description: KDE - post-it note tool
	Depends: <<
		%N-common (>= %v-%r),
		ktnef (>= %v-%r)
	<<
	Replaces: <<
		%N (<< %v-%r),
		%N-dev (<< %v-%r),
		%N-common
	<<
	Recommends: %N (>= %v-%r)
	Files: <<
		bin/knotes
		lib/kde3/knotes*
		lib/libkdeinit_knotes.*
		share/applications/kde/knotes.desktop
		share/apps/knotes
		share/config.kcfg/knote*config.kcfg
		share/doc/kde/en/knotes
		share/icons/hicolor/128x128/apps/knotes*
		share/icons/hicolor/16x16/apps/knotes*
		share/icons/hicolor/22x22/apps/knotes*
		share/icons/hicolor/32x32/apps/knotes*
		share/icons/hicolor/48x48/apps/knotes*
		share/icons/hicolor/64x64/apps/knotes*
		share/services/kresources/knotes*
	<<
	Shlibs: !%p/lib/libkdeinit_knotes.dylib
	Homepage: http://pim.kde.org/components/knotes.php
	DescDetail: KNotes is a nice and small tool to scribble down some notes.
	DescUsage: <<
---[ Installation ]---------------------------------------------------

To install all of the official KDE packages, install "bundle-kde" or
"bundle-kde-ssl" (depending on whether crypotgraphic laws permit
downloading strong cryptography).  Unless you have a specific reason
not to, "bundle-kde-ssl" is suggested.

---[ Configuration ]--------------------------------------------------

To use KDE as your windowing system in XDarwin, create a file called
".xinitrc" in your home directory, containing the following line:

  /sw/bin/startkde >/tmp/kde.log 2>&1

See http://fink.sourceforge.net/doc/x11/run-xfree86.php#xinitrc
for more information on the xinitrc files and how they work.

If you want to use Apple's X11 instead of the KDE window manager,
put the following line before the startkde line in .xinitrc:

  export KDEWM=quartz-wm

If you want to disable quartz-wm even for clipboard synchronization
(proxying), put the following line before the startkde line in
.xinitrc:

  export DISABLE_PROXY=1

---[ Startup ]--------------------------------------------------------

Once you have created an ~/.xinitrc file, you should be able to start
KDE by clicking on the "XDarwin" icon in your Applications folder.  

NOTE: By default, X11 on Mac OS X runs in "rootless" mode, generally.
If you run KDE in rootless mode, it will take over your desktop with
a window that covers everything up.  You can remove this by disabling
desktop icons in the KDE control center.  Open the control center
(either from the "K" menu bar, or by typing "kcontrol" in an xterm)
then expand the "Desktop" list, click "Behavior", and uncheck the
"Show icons on desktop" checkbox.
   
---[ Removal ]--------------------------------------------------------
   
To remove all of the official KDE packages, you can remove the KDE
libraries and anything that depends on them by running, in a terminal:
   
  sudo apt-get remove kdelibs3-universal-shlibs

To remove an individual KDE package collection (like kdenetwork3 or
kdesdk3) you can generally remove [package]-base.  For example, to
remove all of the kdenetwork3 packages, run:

  sudo apt-get remove kdenetwork3-base
	<<
<<
SplitOff14: <<
	Package: konsolekalendar
	Description: KDE - command-line calendar
	Depends: <<
		%N-common (>= %v-%r),
		ktnef (>= %v-%r)
	<<
	Replaces: <<
		%N (<< %v-%r),
		%N-dev (<< %v-%r),
		%N-common
	<<
	Recommends: %N (>= %v-%r)
	Files: <<
		bin/konsolekalendar
		lib/kde3/konsolekalendar.*
		lib/libkdeinit_konsolekalendar.*
		share/applications/kde/konsolekalendar.*
		share/doc/kde/en/konsolekalendar
		share/icons/crystalsvg/16x16/apps/konsolekalendar*
		share/icons/crystalsvg/22x22/apps/konsolekalendar*
		share/icons/crystalsvg/32x32/apps/konsolekalendar*
	<<
	Shlibs: !%p/lib/libkdeinit_konsolekalendar.dylib
	Homepage: http://pim.kde.org/components/konsolekalendar.php
	DescDetail: <<
KonsoleKalendar is a command line interface to KDE calendars.  It lets you
view, insert, remove, or modify calendar events by way of the command line
or from a scripting language.  Additionally, KonsoleKalendar can export a
KDE calendar to a variety of other formats.
	<<
	DescUsage: <<
---[ Installation ]---------------------------------------------------

To install all of the official KDE packages, install "bundle-kde" or
"bundle-kde-ssl" (depending on whether crypotgraphic laws permit
downloading strong cryptography).  Unless you have a specific reason
not to, "bundle-kde-ssl" is suggested.

---[ Configuration ]--------------------------------------------------

To use KDE as your windowing system in XDarwin, create a file called
".xinitrc" in your home directory, containing the following line:

  /sw/bin/startkde >/tmp/kde.log 2>&1

See http://fink.sourceforge.net/doc/x11/run-xfree86.php#xinitrc
for more information on the xinitrc files and how they work.

If you want to use Apple's X11 instead of the KDE window manager,
put the following line before the startkde line in .xinitrc:

  export KDEWM=quartz-wm

If you want to disable quartz-wm even for clipboard synchronization
(proxying), put the following line before the startkde line in
.xinitrc:

  export DISABLE_PROXY=1

---[ Startup ]--------------------------------------------------------

Once you have created an ~/.xinitrc file, you should be able to start
KDE by clicking on the "XDarwin" icon in your Applications folder.  

NOTE: By default, X11 on Mac OS X runs in "rootless" mode, generally.
If you run KDE in rootless mode, it will take over your desktop with
a window that covers everything up.  You can remove this by disabling
desktop icons in the KDE control center.  Open the control center
(either from the "K" menu bar, or by typing "kcontrol" in an xterm)
then expand the "Desktop" list, click "Behavior", and uncheck the
"Show icons on desktop" checkbox.
   
---[ Removal ]--------------------------------------------------------
   
To remove all of the official KDE packages, you can remove the KDE
libraries and anything that depends on them by running, in a terminal:
   
  sudo apt-get remove kdelibs3-universal-shlibs

To remove an individual KDE package collection (like kdenetwork3 or
kdesdk3) you can generally remove [package]-base.  For example, to
remove all of the kdenetwork3 packages, run:

  sudo apt-get remove kdenetwork3-base
	<<
<<
SplitOff15: <<
	Package: kontact
	Description: KDE - integrated PIM
	Depends: <<
		%N-common (>= %v-%r),
		%N-kfile-plugins (>= %v-%r),
		%N-ioslaves (>= %v-%r),
		%N-wizards,
		akregator (>= %v-%r),
		kaddressbook (>= %v-%r),
		kleopatra (>= %v-%r),
		kmail (>= %v-%r),
		knode (>= %v-%r),
		knotes (>= %v-%r),
		korganizer (>= %v-%r),
		kpilot (>= %v-%r)
	<<
	Replaces: <<
		%N (<< %v-%r),
		%N-dev (<< %v-%r),
		%N-common,
		korganizer (<< 3.4.0-1)
	<<
	Recommends: %N (>= %v-%r)
	Files: <<
		bin/kontact
		lib/kde3/kcm_*summary.*
		lib/kde3/kcm_kontact*
		lib/kde3/kontact.*
		lib/kde3/libkontact_*
		lib/libkdeinit_kontact.*
		lib/libkontact.*.dylib
		lib/libkpinterfaces.*.dylib
		share/applications/kde/Kontact.desktop
		share/applications/kde/kontactdcop.desktop
		share/apps/kontact*
		share/config.kcfg/kontact.kcfg
		share/doc/kde/en/kontact
		share/icons/crystalsvg/16x16/actions/kontact*
		share/icons/crystalsvg/22x22/actions/kontact*
		share/icons/crystalsvg/32x32/actions/kontact*
		share/icons/crystalsvg/48x48/actions/kontact*
		share/icons/crystalsvg/64x64/actions/kontact*
		share/icons/hicolor/128x128/apps/kontact*
		share/icons/hicolor/16x16/apps/kontact*
		share/icons/hicolor/22x22/apps/kontact*
		share/icons/hicolor/32x32/apps/kontact*
		share/icons/hicolor/48x48/apps/kontact*
		share/icons/hicolor/64x64/apps/kontact*
		share/services/kcm*summary.*
		share/services/kcmkontact*
		share/services/kontact*
		share/servicetypes/kontact*
	<<
	Shlibs: <<
		%p/lib/libkontact.1.dylib 2.0.0 kontact (>= 3.5.0-1)
		%p/lib/libkpinterfaces.1.dylib 2.0.0 kontact (>= 3.5.0-1)
		!%p/lib/libkdeinit_kontact.dylib
	<<
	Homepage: http://www.kontact.org/
	DescDetail: <<
The KDE Kontact Personal Information Management suite unites mature
and proven KDE applications (called components in the context) under
one roof.  Thanks to the powerful KParts technology, all components
are seamlessly integrated into one application.

The components of KDE Kontact are all shaped to work nice with each
other.  This results in features like intuitive drag 'n' dropping
between components and appointment handling, task lists and contacts.
KDE Kontact also extends its components with support for various
groupware servers.  When using these servers your workgroup will
have access to features like shared email folders, group task lists,
calendar sharing, central addressbooks and meeting scheduling.
	<<
	DescUsage: <<
---[ Installation ]---------------------------------------------------

To install all of the official KDE packages, install "bundle-kde" or
"bundle-kde-ssl" (depending on whether crypotgraphic laws permit
downloading strong cryptography).  Unless you have a specific reason
not to, "bundle-kde-ssl" is suggested.

---[ Configuration ]--------------------------------------------------

To use KDE as your windowing system in XDarwin, create a file called
".xinitrc" in your home directory, containing the following line:

  /sw/bin/startkde >/tmp/kde.log 2>&1

See http://fink.sourceforge.net/doc/x11/run-xfree86.php#xinitrc
for more information on the xinitrc files and how they work.

If you want to use Apple's X11 instead of the KDE window manager,
put the following line before the startkde line in .xinitrc:

  export KDEWM=quartz-wm

If you want to disable quartz-wm even for clipboard synchronization
(proxying), put the following line before the startkde line in
.xinitrc:

  export DISABLE_PROXY=1

---[ Startup ]--------------------------------------------------------

Once you have created an ~/.xinitrc file, you should be able to start
KDE by clicking on the "XDarwin" icon in your Applications folder.  

NOTE: By default, X11 on Mac OS X runs in "rootless" mode, generally.
If you run KDE in rootless mode, it will take over your desktop with
a window that covers everything up.  You can remove this by disabling
desktop icons in the KDE control center.  Open the control center
(either from the "K" menu bar, or by typing "kcontrol" in an xterm)
then expand the "Desktop" list, click "Behavior", and uncheck the
"Show icons on desktop" checkbox.
   
---[ Removal ]--------------------------------------------------------
   
To remove all of the official KDE packages, you can remove the KDE
libraries and anything that depends on them by running, in a terminal:
   
  sudo apt-get remove kdelibs3-universal-shlibs

To remove an individual KDE package collection (like kdenetwork3 or
kdesdk3) you can generally remove [package]-base.  For example, to
remove all of the kdenetwork3 packages, run:

  sudo apt-get remove kdenetwork3-base
	<<
<<
SplitOff16: <<
	Package: korganizer
	Description: KDE - calendar and scheduling
	Depends: <<
		%N-common (>= %v-%r),
		%N-kfile-plugins (>= %v-%r),
		%N-ioslaves (>= %v-%r),
		kleopatra (>= %v-%r),
		ktnef (>= %v-%r)
	<<
	Replaces: <<
		%N (<< %v-%r),
		%N-dev (<< %v-%r),
		%N-common,
		kmail (<< %v-%r)
	<<
	Recommends: %N (>= %v-%r)
	Files: <<
		bin/ical2vcal
		bin/korgac
		bin/korganizer
		lib/kde3/kcm_korganizer.*
		lib/kde3/korgac.*
		lib/kde3/korganizer.*
		lib/kde3/libkorg*
		lib/libkdeinit_korgac.*
		lib/libkdeinit_korganizer.*
		lib/libkocorehelper.*.dylib
		lib/libkorg_stdprinting.*.dylib
		lib/libkorganizer.*.dylib
		lib/libkorganizer_calendar.*.dylib
		lib/libkorganizer_eventviewer.*.dylib
		share/applications/kde/korganizer.*
		share/apps/kconf_update/korganizer*
		share/apps/korgac*
		share/apps/korganizer*
		share/autostart/korgac.*
		share/config.kcfg/korganizer.*
		share/doc/kde/en/korganizer
		share/icons/hicolor/128x128/apps/korganizer*
		share/icons/hicolor/16x16/apps/korganizer*
		share/icons/hicolor/32x32/apps/korganizer*
		share/icons/hicolor/48x48/apps/korganizer*
		share/icons/hicolor/64x64/apps/korganizer*
		share/services/korganizer*
		share/services/webcal.*
		share/servicetypes/calendardecoration.*
		share/servicetypes/calendarplugin.*
		share/servicetypes/dcopcalendar.*
		share/servicetypes/korganizerpart.*
		share/servicetypes/korgprintplugin.*
	<<
	Shlibs: <<
		%p/lib/libkocorehelper.1.dylib 2.0.0 korganizer (>= 3.5.0-1)
		%p/lib/libkorg_stdprinting.1.dylib 2.0.0 korganizer (>= 3.5.0-1)
		%p/lib/libkorganizer.1.dylib 2.0.0 korganizer (>= 3.5.0-1)
		%p/lib/libkorganizer_calendar.1.dylib 2.0.0 korganizer (>= 3.5.0-1)
		%p/lib/libkorganizer_eventviewer.1.dylib 2.0.0 korganizer (>= 3.5.0-1)
		!%p/lib/libkdeinit_korgac.dylib
		!%p/lib/libkdeinit_korganizer.dylib
	<<
	Homepage: http://pim.kde.org/korganizer/index.php
	DescDetail: <<
KOrganizer is the calendar and scheduling application of the popular
K Desktop Environment.  KDE is a powerful Open Source graphical
desktop environment for Unix workstations.  It combines ease of use,
contemporary functionality, and outstanding graphical design with the
technological superiority of the Unix operating system.

KOrganizer provides management of events and tasks, alarm notification,
web export, network transparent handling of data, group scheduling,
import and export of calendar files and more.
	<<
	DescUsage: <<
---[ Installation ]---------------------------------------------------

To install all of the official KDE packages, install "bundle-kde" or
"bundle-kde-ssl" (depending on whether crypotgraphic laws permit
downloading strong cryptography).  Unless you have a specific reason
not to, "bundle-kde-ssl" is suggested.

---[ Configuration ]--------------------------------------------------

To use KDE as your windowing system in XDarwin, create a file called
".xinitrc" in your home directory, containing the following line:

  /sw/bin/startkde >/tmp/kde.log 2>&1

See http://fink.sourceforge.net/doc/x11/run-xfree86.php#xinitrc
for more information on the xinitrc files and how they work.

If you want to use Apple's X11 instead of the KDE window manager,
put the following line before the startkde line in .xinitrc:

  export KDEWM=quartz-wm

If you want to disable quartz-wm even for clipboard synchronization
(proxying), put the following line before the startkde line in
.xinitrc:

  export DISABLE_PROXY=1

---[ Startup ]--------------------------------------------------------

Once you have created an ~/.xinitrc file, you should be able to start
KDE by clicking on the "XDarwin" icon in your Applications folder.  

NOTE: By default, X11 on Mac OS X runs in "rootless" mode, generally.
If you run KDE in rootless mode, it will take over your desktop with
a window that covers everything up.  You can remove this by disabling
desktop icons in the KDE control center.  Open the control center
(either from the "K" menu bar, or by typing "kcontrol" in an xterm)
then expand the "Desktop" list, click "Behavior", and uncheck the
"Show icons on desktop" checkbox.
   
---[ Removal ]--------------------------------------------------------
   
To remove all of the official KDE packages, you can remove the KDE
libraries and anything that depends on them by running, in a terminal:
   
  sudo apt-get remove kdelibs3-universal-shlibs

To remove an individual KDE package collection (like kdenetwork3 or
kdesdk3) you can generally remove [package]-base.  For example, to
remove all of the kdenetwork3 packages, run:

  sudo apt-get remove kdenetwork3-base
	<<
<<
SplitOff17: <<
	Package: korn
	Description: KDE - mailbox update notifier
	Depends: %N-common (>= %v-%r)
	Replaces: <<
		%N (<< %v-%r),
		%N-dev (<< %v-%r),
		%N-common
	<<
	Recommends: %N (>= %v-%r)
	Files: <<
		bin/korn
		lib/kconf_update_bin/korn-*
		lib/kde3/korn.*
		lib/libkdeinit_korn.*
		share/applications/kde/KOrn.desktop
		share/apps/kconf_update/korn-*
		share/doc/kde/en/korn
		share/icons/hicolor/16x16/apps/korn*
		share/icons/hicolor/32x32/apps/korn*
		share/icons/hicolor/48x48/apps/korn*
	<<
	Shlibs: !%p/lib/libkdeinit_korn.dylib
	Homepage: http://www.ph.unimelb.edu.au/~ssk/kde/korn/
	DescDetail: <<
KOrn is a KDE-compliant X11 program that monitors UNIX mailbox
files for incoming and unread messsages.  It periodically scans
each mailbox and displays the number of unread messages it
contains in a small on-screen button.
	<<
	DescUsage: <<
---[ Installation ]---------------------------------------------------

To install all of the official KDE packages, install "bundle-kde" or
"bundle-kde-ssl" (depending on whether crypotgraphic laws permit
downloading strong cryptography).  Unless you have a specific reason
not to, "bundle-kde-ssl" is suggested.

---[ Configuration ]--------------------------------------------------

To use KDE as your windowing system in XDarwin, create a file called
".xinitrc" in your home directory, containing the following line:

  /sw/bin/startkde >/tmp/kde.log 2>&1

See http://fink.sourceforge.net/doc/x11/run-xfree86.php#xinitrc
for more information on the xinitrc files and how they work.

If you want to use Apple's X11 instead of the KDE window manager,
put the following line before the startkde line in .xinitrc:

  export KDEWM=quartz-wm

If you want to disable quartz-wm even for clipboard synchronization
(proxying), put the following line before the startkde line in
.xinitrc:

  export DISABLE_PROXY=1

---[ Startup ]--------------------------------------------------------

Once you have created an ~/.xinitrc file, you should be able to start
KDE by clicking on the "XDarwin" icon in your Applications folder.  

NOTE: By default, X11 on Mac OS X runs in "rootless" mode, generally.
If you run KDE in rootless mode, it will take over your desktop with
a window that covers everything up.  You can remove this by disabling
desktop icons in the KDE control center.  Open the control center
(either from the "K" menu bar, or by typing "kcontrol" in an xterm)
then expand the "Desktop" list, click "Behavior", and uncheck the
"Show icons on desktop" checkbox.
   
---[ Removal ]--------------------------------------------------------
   
To remove all of the official KDE packages, you can remove the KDE
libraries and anything that depends on them by running, in a terminal:
   
  sudo apt-get remove kdelibs3-universal-shlibs

To remove an individual KDE package collection (like kdenetwork3 or
kdesdk3) you can generally remove [package]-base.  For example, to
remove all of the kdenetwork3 packages, run:

  sudo apt-get remove kdenetwork3-base
	<<
<<
SplitOff18: <<
	Package: kpilot
	Description: KDE - PalmOS tool
	Depends: <<
		%N-common (>= %v-%r),
		%N-kfile-plugins (>= %v-%r),
		%N-ioslaves (>= %v-%r),
		kaddressbook (>= %v-%r),
		ktnef (>= %v-%r),
		libmal10-shlibs (>= 0.40-5)
	<<
	Replaces: <<
		%N (<< %v-%r),
		%N-dev (<< %v-%r),
		%N-common
	<<
	Recommends: %N (>= %v-%r)
	Files: <<
		bin/kpalmdoc
		bin/kpilot
		bin/kpilot*
		lib/kde3/conduit_*
		lib/kde3/kcm_kpilot.*
		lib/kde3/kpalmdoc.*
		lib/kde3/kpilot.*
		lib/kde3/kpilotDaemon.*
		lib/libkdeinit_kpalmdoc.*
		lib/libkdeinit_kpilot.*
		lib/libkdeinit_kpilotDaemon.*
		lib/libkpilot.*.dylib
		share/applications/kde/kpalmdoc.desktop
		share/applications/kde/kpilot*
		share/apps/kconf_update/kpalmdoc.upd
		share/apps/kconf_update/kpilot.upd
		share/apps/kpilot
		share/config.kcfg/*conduit*
		share/config.kcfg/kpalmdoc.kcfg
		share/config.kcfg/kpilot.kcfg
		share/config.kcfg/kpilotlib.kcfg
		share/config.kcfg/popmail.kcfg
		share/doc/kde/en/kpilot
		share/icons/crystalsvg/16x16/apps/kpalmdoc*
		share/icons/crystalsvg/16x16/apps/kpilot*
		share/icons/crystalsvg/22x22/apps/kpalmdoc*
		share/icons/crystalsvg/22x22/apps/kpilot*
		share/icons/crystalsvg/32x32/apps/kpalmdoc*
		share/icons/crystalsvg/32x32/apps/kpilot*
		share/icons/crystalsvg/48x48/apps/kpalmdoc*
		share/icons/crystalsvg/48x48/apps/kpilot*
		share/icons/hicolor/16x16/apps/kpilot*
		share/icons/hicolor/22x22/apps/kpilot*
		share/icons/hicolor/32x32/apps/kpilot*
		share/icons/hicolor/48x48/apps/kpilot*
		share/icons/locolor/16x16/apps/kpilot*
		share/services/*conduit.desktop
		share/services/kpilot_config.desktop
		share/servicetypes/kpilotconduit.desktop
	<<
	Shlibs: <<
		%p/lib/libkpilot.0.dylib 1.0.0 kpilot (>= 3.5.0-1)
		!%p/lib/libkdeinit_kpalmdoc.dylib
		!%p/lib/libkdeinit_kpilot.dylib
		!%p/lib/libkdeinit_kpilotDaemon.dylib
	<<
	Homepage: http://www.slac.com/pilone/kpilot_home/
	DescDetail: <<
KPilot is a replacement for the Palm Desktop software from
PalmSource Inc., which makes your PalmOS powered handheld (e.g.,
a Palm Zire 31) capable of exchanging information with your KDE
powered computer.  KPilot doesn't replace the Palm Desktop all
by itself.  It connects and integrates a number of fine KDE 3.x
applications into a package that can do everything the Palm
Desktop can, and more.
	<<
	DescUsage: <<
---[ Installation ]---------------------------------------------------

To install all of the official KDE packages, install "bundle-kde" or
"bundle-kde-ssl" (depending on whether crypotgraphic laws permit
downloading strong cryptography).  Unless you have a specific reason
not to, "bundle-kde-ssl" is suggested.

---[ Configuration ]--------------------------------------------------

To use KDE as your windowing system in XDarwin, create a file called
".xinitrc" in your home directory, containing the following line:

  /sw/bin/startkde >/tmp/kde.log 2>&1

See http://fink.sourceforge.net/doc/x11/run-xfree86.php#xinitrc
for more information on the xinitrc files and how they work.

If you want to use Apple's X11 instead of the KDE window manager,
put the following line before the startkde line in .xinitrc:

  export KDEWM=quartz-wm

If you want to disable quartz-wm even for clipboard synchronization
(proxying), put the following line before the startkde line in
.xinitrc:

  export DISABLE_PROXY=1

---[ Startup ]--------------------------------------------------------

Once you have created an ~/.xinitrc file, you should be able to start
KDE by clicking on the "XDarwin" icon in your Applications folder.  

NOTE: By default, X11 on Mac OS X runs in "rootless" mode, generally.
If you run KDE in rootless mode, it will take over your desktop with
a window that covers everything up.  You can remove this by disabling
desktop icons in the KDE control center.  Open the control center
(either from the "K" menu bar, or by typing "kcontrol" in an xterm)
then expand the "Desktop" list, click "Behavior", and uncheck the
"Show icons on desktop" checkbox.
   
---[ Removal ]--------------------------------------------------------
   
To remove all of the official KDE packages, you can remove the KDE
libraries and anything that depends on them by running, in a terminal:
   
  sudo apt-get remove kdelibs3-universal-shlibs

To remove an individual KDE package collection (like kdenetwork3 or
kdesdk3) you can generally remove [package]-base.  For example, to
remove all of the kdenetwork3 packages, run:

  sudo apt-get remove kdenetwork3-base
	<<
<<
SplitOff2: <<
	Package: akregator
	Description: KDE - RSS reader
	Depends: <<
		%N-common (>= %v-%r),
		ktnef (>= %v-%r)
	<<
	Replaces: <<
		%N (<< %v-%r),
		%N-dev (<< %v-%r),
		%N-common
	<<
	Recommends: %N (>= %v-%r)
	Files: <<
		bin/akregator
		lib/kde3/*akregator*
		lib/libakregatorprivate.*.dylib
		lib/libkdeinit_akregator.*
		share/applications/kde/akregator.desktop
		share/apps/akregator
		share/config.kcfg/akregator.kcfg
		share/config.kcfg/mk4config.kcfg
		share/doc/kde/en/akregator
		share/icons/crystalsvg/16x16/actions/rss_tag*
		share/icons/crystalsvg/16x16/apps/akregator_empty*
		share/icons/crystalsvg/22x22/actions/rss_tag*
		share/icons/crystalsvg/32x32/actions/rss_tag*
		share/icons/crystalsvg/48x48/actions/rss_tag*
		share/icons/crystalsvg/64x64/actions/rss_tag*
		share/icons/hicolor/128x128/apps/akregator*
		share/icons/hicolor/16x16/apps/akregator*
		share/icons/hicolor/22x22/apps/akregator*
		share/icons/hicolor/32x32/apps/akregator*
		share/icons/hicolor/48x48/apps/akregator*
		share/icons/hicolor/64x64/apps/akregator*
		share/icons/hicolor/scalable/apps/akregator*
		share/services/akregator*
		share/services/feed.protocol
		share/servicetypes/akregator*
	<<
	Shlibs: <<
		%p/lib/libakregatorprivate.0.dylib 1.0.0 akregator (>= 3.5.0-1)
		!%p/lib/libkdeinit_akregator.dylib
	<<
	Homepage: http://akregator.sourceforge.net/
	DescDetail: <<
Akregator allows you to browse through thousands of internet feeds without
the hassle of using a web browser.
	<<
	DescUsage: <<
---[ Installation ]---------------------------------------------------

To install all of the official KDE packages, install "bundle-kde" or
"bundle-kde-ssl" (depending on whether crypotgraphic laws permit
downloading strong cryptography).  Unless you have a specific reason
not to, "bundle-kde-ssl" is suggested.

---[ Configuration ]--------------------------------------------------

To use KDE as your windowing system in XDarwin, create a file called
".xinitrc" in your home directory, containing the following line:

  /sw/bin/startkde >/tmp/kde.log 2>&1

See http://fink.sourceforge.net/doc/x11/run-xfree86.php#xinitrc
for more information on the xinitrc files and how they work.

If you want to use Apple's X11 instead of the KDE window manager,
put the following line before the startkde line in .xinitrc:

  export KDEWM=quartz-wm

If you want to disable quartz-wm even for clipboard synchronization
(proxying), put the following line before the startkde line in
.xinitrc:

  export DISABLE_PROXY=1

---[ Startup ]--------------------------------------------------------

Once you have created an ~/.xinitrc file, you should be able to start
KDE by clicking on the "XDarwin" icon in your Applications folder.  

NOTE: By default, X11 on Mac OS X runs in "rootless" mode, generally.
If you run KDE in rootless mode, it will take over your desktop with
a window that covers everything up.  You can remove this by disabling
desktop icons in the KDE control center.  Open the control center
(either from the "K" menu bar, or by typing "kcontrol" in an xterm)
then expand the "Desktop" list, click "Behavior", and uncheck the
"Show icons on desktop" checkbox.
   
---[ Removal ]--------------------------------------------------------
   
To remove all of the official KDE packages, you can remove the KDE
libraries and anything that depends on them by running, in a terminal:
   
  sudo apt-get remove kdelibs3-universal-shlibs

To remove an individual KDE package collection (like kdenetwork3 or
kdesdk3) you can generally remove [package]-base.  For example, to
remove all of the kdenetwork3 packages, run:

  sudo apt-get remove kdenetwork3-base
	<<
<<
SplitOff20: <<
	Package: ktnef
	Description: KDE - TNEF viewer/extractor
	Depends: %N-common (>= %v-%r)
	Replaces: <<
		%N (<< %v-%r),
		%N-dev (<< %v-%r),
		%N-common
	<<
	Recommends: %N (>= %v-%r)
	Files: <<
		bin/ktnef
		lib/kde3/ktnef.*
		lib/libkdeinit_ktnef.*
		lib/libktnef.*.dylib
		share/applications/kde/ktnef.desktop
		share/apps/ktnef
		share/doc/kde/en/ktnef
		share/icons/hicolor/48x48/apps/ktnef*
		share/icons/locolor/16x16/apps/ktnef*
		share/icons/locolor/32x32/apps/ktnef*
		share/mimelnk/application/ms-tnef.desktop
	<<
	Shlibs: <<
		%p/lib/libktnef.1.dylib 2.0.0 ktnef (>= 3.5.0-1)
		!%p/lib/libkdeinit_ktnef.dylib
	<<
	Homepage: http://sourceforge.net/projects/ktnef
	DescDetail: <<
A TNEF viewer/extractor.  It fully integrates with the KDE environment
(mime types, ...) and can be registered as opening application for
e-mail clients.
	<<
	DescUsage: <<
---[ Installation ]---------------------------------------------------

To install all of the official KDE packages, install "bundle-kde" or
"bundle-kde-ssl" (depending on whether crypotgraphic laws permit
downloading strong cryptography).  Unless you have a specific reason
not to, "bundle-kde-ssl" is suggested.

---[ Configuration ]--------------------------------------------------

To use KDE as your windowing system in XDarwin, create a file called
".xinitrc" in your home directory, containing the following line:

  /sw/bin/startkde >/tmp/kde.log 2>&1

See http://fink.sourceforge.net/doc/x11/run-xfree86.php#xinitrc
for more information on the xinitrc files and how they work.

If you want to use Apple's X11 instead of the KDE window manager,
put the following line before the startkde line in .xinitrc:

  export KDEWM=quartz-wm

If you want to disable quartz-wm even for clipboard synchronization
(proxying), put the following line before the startkde line in
.xinitrc:

  export DISABLE_PROXY=1

---[ Startup ]--------------------------------------------------------

Once you have created an ~/.xinitrc file, you should be able to start
KDE by clicking on the "XDarwin" icon in your Applications folder.  

NOTE: By default, X11 on Mac OS X runs in "rootless" mode, generally.
If you run KDE in rootless mode, it will take over your desktop with
a window that covers everything up.  You can remove this by disabling
desktop icons in the KDE control center.  Open the control center
(either from the "K" menu bar, or by typing "kcontrol" in an xterm)
then expand the "Desktop" list, click "Behavior", and uncheck the
"Show icons on desktop" checkbox.
   
---[ Removal ]--------------------------------------------------------
   
To remove all of the official KDE packages, you can remove the KDE
libraries and anything that depends on them by running, in a terminal:
   
  sudo apt-get remove kdelibs3-universal-shlibs

To remove an individual KDE package collection (like kdenetwork3 or
kdesdk3) you can generally remove [package]-base.  For example, to
remove all of the kdenetwork3 packages, run:

  sudo apt-get remove kdenetwork3-base
	<<
<<
SplitOff21: <<
	Package: %N-wizards
	Description: KDE - groupware configuration wizards
	Depends: <<
		%N-common (>= %v-%r),
		kaddressbook (>= %v-1),
		kleopatra (>= %v-%r),
		ktnef (>= %v-%r)
	<<
	Replaces: <<
		%N (<< %v-%r),
		%N-dev (<< %v-%r),
		%N-common
	<<
	Recommends: %N (>= %v-%r)
	Files: <<
		bin/*wizard
		lib/libegroupwarewizard.dylib
		lib/libexchangewizard.dylib
		lib/libgroupwisewizard.dylib
		lib/libkolabwizard.dylib
		lib/libsloxwizard.dylib
		share/applications/kde/groupwarewizard.desktop
		share/config.kcfg/egroupware*
		share/config.kcfg/groupwise*
		share/config.kcfg/kolab*
		share/config.kcfg/slox*
	<<
	Shlibs: <<
		!%p/lib/libegroupwarewizard.dylib
		!%p/lib/libexchangewizard.dylib
		!%p/lib/libgroupwisewizard.dylib
		!%p/lib/libkolabwizard.dylib
		!%p/lib/libsloxwizard.dylib
	<<
	DescUsage: <<
---[ Installation ]---------------------------------------------------

To install all of the official KDE packages, install "bundle-kde" or
"bundle-kde-ssl" (depending on whether crypotgraphic laws permit
downloading strong cryptography).  Unless you have a specific reason
not to, "bundle-kde-ssl" is suggested.

---[ Configuration ]--------------------------------------------------

To use KDE as your windowing system in XDarwin, create a file called
".xinitrc" in your home directory, containing the following line:

  /sw/bin/startkde >/tmp/kde.log 2>&1

See http://fink.sourceforge.net/doc/x11/run-xfree86.php#xinitrc
for more information on the xinitrc files and how they work.

If you want to use Apple's X11 instead of the KDE window manager,
put the following line before the startkde line in .xinitrc:

  export KDEWM=quartz-wm

If you want to disable quartz-wm even for clipboard synchronization
(proxying), put the following line before the startkde line in
.xinitrc:

  export DISABLE_PROXY=1

---[ Startup ]--------------------------------------------------------

Once you have created an ~/.xinitrc file, you should be able to start
KDE by clicking on the "XDarwin" icon in your Applications folder.  

NOTE: By default, X11 on Mac OS X runs in "rootless" mode, generally.
If you run KDE in rootless mode, it will take over your desktop with
a window that covers everything up.  You can remove this by disabling
desktop icons in the KDE control center.  Open the control center
(either from the "K" menu bar, or by typing "kcontrol" in an xterm)
then expand the "Desktop" list, click "Behavior", and uncheck the
"Show icons on desktop" checkbox.
   
---[ Removal ]--------------------------------------------------------
   
To remove all of the official KDE packages, you can remove the KDE
libraries and anything that depends on them by running, in a terminal:
   
  sudo apt-get remove kdelibs3-universal-shlibs

To remove an individual KDE package collection (like kdenetwork3 or
kdesdk3) you can generally remove [package]-base.  For example, to
remove all of the kdenetwork3 packages, run:

  sudo apt-get remove kdenetwork3-base
	<<
<<
SplitOff22: <<
	Package: %N-common
	Description: KDE - kdepim common files
	Depends: <<
		%N-base (>= %v-%r),
		cyrus-sasl2-shlibs,
		libgettext3-shlibs,
		gnokii3-shlibs,
		gpgme11-shlibs (>= 1.0.3-1),
		opensync-shlibs,
		readline5-shlibs
	<<
	Replaces: <<
		%N (<< %v-%r),
		%N-dev (<< %v-%r),
		kdenetwork3-common,
		kdenetwork3-dev (<< %v-1),
		kmail (<< %v-%r)
	<<
	Recommends: %N (>= %v-%r)
	Files: <<
		bin
		lib
		share/apps
		share/config.kcfg
		share/doc/kde
		share/icons
		share/mimelnk
		share/services
		share/servicetypes
	<<
	Shlibs: <<
		%p/lib/libgpgme++.0.dylib 4.0.0 kdepim3-common (>= 3.5.0-1)
		%p/lib/libindex.0.dylib 1.0.0 kdepim3-common (>= 3.5.0-1)
		%p/lib/libkabc_groupdav.1.dylib 2.0.0 kdepim3-common (>= 3.5.0-1)
		%p/lib/libkabc_groupwise.1.dylib 2.0.0 kdepim3-common (>= 3.5.0-1)
		%p/lib/libkabc_newexchange.1.dylib 2.0.0 kdepim3-common (>= 3.5.0-1)
		%p/lib/libkabc_slox.0.dylib 1.0.0 kdepim3-common (>= 3.5.0-1)
		%p/lib/libkabc_xmlrpc.1.dylib 2.0.0 kdepim3-common (>= 3.5.0-1)
		%p/lib/libkabckolab.0.dylib 1.0.0 kdepim3-common (>= 3.5.0-1)
		%p/lib/libkcal.2.dylib 3.0.0 kdepim3-common (>= 3.5.0-1)
		%p/lib/libkcal_groupdav.1.dylib 2.0.0 kdepim3-common (>= 3.5.0-1)
		%p/lib/libkcal_groupwise.1.dylib 2.0.0 kdepim3-common (>= 3.5.0-1)
		%p/lib/libkcal_newexchange.1.dylib 2.0.0 kdepim3-common (>= 3.5.0-1)
		%p/lib/libkcal_resourcefeatureplan.1.dylib 2.0.0 kdepim3-common (>= 3.5.0-1)
		%p/lib/libkcal_resourceremote.1.dylib 2.0.0 kdepim3-common (>= 3.5.0-1)
		%p/lib/libkcal_slox.0.dylib 1.0.0 kdepim3-common (>= 3.5.0-1)
		%p/lib/libkcal_xmlrpc.1.dylib 2.0.0 kdepim3-common (>= 3.5.0-1)
		%p/lib/libkcalkolab.0.dylib 1.0.0 kdepim3-common (>= 3.5.0-1)
		%p/lib/libkdepim.1.dylib 2.0.0 kdepim3-common (>= 3.5.0-1)
		%p/lib/libkgantt.0.dylib 1.0.0 kdepim3-common (>= 3.5.0-1)
		%p/lib/libkgroupwarebase.0.dylib 1.0.0 kdepim3-common (>= 3.5.0-1)
		%p/lib/libkgroupwaredav.0.dylib 1.0.0 kdepim3-common (>= 3.5.0-1)
		%p/lib/libkholidays.1.dylib 2.0.0 kdepim3-common (>= 3.5.0-1)
		%p/lib/libkmime.2.dylib 5.0.0 kdepim3-common (>= 3.5.0-1)
		%p/lib/libknotes_xmlrpc.1.dylib 2.0.0 kdepim3-common (>= 3.5.0-1)
		%p/lib/libknoteskolab.0.dylib 1.0.0 kdepim3-common (>= 3.5.0-1)
		%p/lib/libkode.1.dylib 2.0.0 kdepim3-common (>= 3.5.0-1)
		%p/lib/libkpgp.2.dylib 5.0.0 kdepim3-common (>= 3.5.0-1)
		%p/lib/libkpimexchange.1.dylib 2.0.0 kdepim3-common (>= 3.5.0-1)
		%p/lib/libkpimidentities.1.dylib 2.0.0 kdepim3-common (>= 3.5.0-1)
		%p/lib/libksieve.0.dylib 1.0.0 kdepim3-common (>= 3.5.0-1)
		%p/lib/libkslox.0.dylib 1.0.0 kdepim3-common (>= 3.5.0-1)
		%p/lib/libmimelib.1.dylib 2.0.0 kdepim3-common (>= 3.5.0-1)
		%p/lib/libqgpgme.0.dylib 1.0.0 kdepim3-common (>= 3.5.0-1)
		%p/lib/libgwsoap.0.dylib 1.0.0 kdepim3-common (>= 3.5.9-2)
		%p/lib/libkabcscalix.0.dylib 1.0.0 kdepim3-common (>= 3.5.9-2)
		%p/lib/libkcalscalix.0.dylib 1.0.0 kdepim3-common (>= 3.5.9-2)
		%p/lib/libknotesscalix.0.dylib 1.0.0 kdepim3-common (>= 3.5.9-2)
		!%p/lib/libkdeinit_kode.dylib
		!%p/lib/libkdeinit_networkstatustestservice.dylib
		!%p/lib/libscalixwizard.dylib
	<<
	DescUsage: <<
---[ Installation ]---------------------------------------------------

To install all of the official KDE packages, install "bundle-kde" or
"bundle-kde-ssl" (depending on whether crypotgraphic laws permit
downloading strong cryptography).  Unless you have a specific reason
not to, "bundle-kde-ssl" is suggested.

---[ Configuration ]--------------------------------------------------

To use KDE as your windowing system in XDarwin, create a file called
".xinitrc" in your home directory, containing the following line:

  /sw/bin/startkde >/tmp/kde.log 2>&1

See http://fink.sourceforge.net/doc/x11/run-xfree86.php#xinitrc
for more information on the xinitrc files and how they work.

If you want to use Apple's X11 instead of the KDE window manager,
put the following line before the startkde line in .xinitrc:

  export KDEWM=quartz-wm

If you want to disable quartz-wm even for clipboard synchronization
(proxying), put the following line before the startkde line in
.xinitrc:

  export DISABLE_PROXY=1

---[ Startup ]--------------------------------------------------------

Once you have created an ~/.xinitrc file, you should be able to start
KDE by clicking on the "XDarwin" icon in your Applications folder.  

NOTE: By default, X11 on Mac OS X runs in "rootless" mode, generally.
If you run KDE in rootless mode, it will take over your desktop with
a window that covers everything up.  You can remove this by disabling
desktop icons in the KDE control center.  Open the control center
(either from the "K" menu bar, or by typing "kcontrol" in an xterm)
then expand the "Desktop" list, click "Behavior", and uncheck the
"Show icons on desktop" checkbox.
   
---[ Removal ]--------------------------------------------------------
   
To remove all of the official KDE packages, you can remove the KDE
libraries and anything that depends on them by running, in a terminal:
   
  sudo apt-get remove kdelibs3-universal-shlibs

To remove an individual KDE package collection (like kdenetwork3 or
kdesdk3) you can generally remove [package]-base.  For example, to
remove all of the kdenetwork3 packages, run:

  sudo apt-get remove kdenetwork3-base
	<<
<<
SplitOff23: <<
	Package: %N-base
	Description: KDE - kdepim base package
	Depends: kdebase3-unified (>= %v-1)
	Replaces: <<
		%N (<< %v-%r),
		%N-dev (<< %v-%r)
	<<
	Recommends: %N (>= %v-%r)
	Files: share/doc/installed-packages/%N-base
	DescUsage: <<
---[ Installation ]---------------------------------------------------

To install all of the official KDE packages, install "bundle-kde" or
"bundle-kde-ssl" (depending on whether crypotgraphic laws permit
downloading strong cryptography).  Unless you have a specific reason
not to, "bundle-kde-ssl" is suggested.

---[ Configuration ]--------------------------------------------------

To use KDE as your windowing system in XDarwin, create a file called
".xinitrc" in your home directory, containing the following line:

  /sw/bin/startkde >/tmp/kde.log 2>&1

See http://fink.sourceforge.net/doc/x11/run-xfree86.php#xinitrc
for more information on the xinitrc files and how they work.

If you want to use Apple's X11 instead of the KDE window manager,
put the following line before the startkde line in .xinitrc:

  export KDEWM=quartz-wm

If you want to disable quartz-wm even for clipboard synchronization
(proxying), put the following line before the startkde line in
.xinitrc:

  export DISABLE_PROXY=1

---[ Startup ]--------------------------------------------------------

Once you have created an ~/.xinitrc file, you should be able to start
KDE by clicking on the "XDarwin" icon in your Applications folder.  

NOTE: By default, X11 on Mac OS X runs in "rootless" mode, generally.
If you run KDE in rootless mode, it will take over your desktop with
a window that covers everything up.  You can remove this by disabling
desktop icons in the KDE control center.  Open the control center
(either from the "K" menu bar, or by typing "kcontrol" in an xterm)
then expand the "Desktop" list, click "Behavior", and uncheck the
"Show icons on desktop" checkbox.
   
---[ Removal ]--------------------------------------------------------
   
To remove all of the official KDE packages, you can remove the KDE
libraries and anything that depends on them by running, in a terminal:
   
  sudo apt-get remove kdelibs3-universal-shlibs

To remove an individual KDE package collection (like kdenetwork3 or
kdesdk3) you can generally remove [package]-base.  For example, to
remove all of the kdenetwork3 packages, run:

  sudo apt-get remove kdenetwork3-base
	<<
<<
SplitOff3: <<
	Package: kleopatra
	Description: KDE - manage X.509 certificates
	Depends: <<
		%N-common (>= %v-%r),
		gpgme11-shlibs,
		libgpg-error-shlibs
	<<
	Replaces: <<
		%N (<< %v-%r),
		%N-dev (<< %v-%r),
		%N-common
	<<
	Recommends: %N (>= %v-%r)
	Files: <<
		bin/kleopatra
		bin/kwatchgnupg
		lib/kde3/*kleopatra*
		lib/kde3/kwatchgnupg*
		lib/libkdeinit_kleopatra.*
		lib/libkdeinit_kwatchgnupg.*
		lib/libkleopatra.*.dylib
		share/applications/kde/kleopatra*
		share/apps/kleopatra
		share/apps/kwatchgnupg
		share/apps/libkleopatra
		share/config/libkleopatrarc
		share/doc/kde/en/kleopatra
		share/doc/kde/en/kwatchgnupg
		share/icons/crystalsvg/16x16/apps/gpg*
		share/icons/crystalsvg/22x22/apps/gpg*
		share/icons/crystalsvg/32x32/apps/gpg*
		share/services/kleopatra*
	<<
	Shlibs: <<
		%p/lib/libkleopatra.1.dylib 2.0.0 kleopatra (>= 3.5.0-1)
		!%p/lib/libkdeinit_kleopatra.dylib
		!%p/lib/libkdeinit_kwatchgnupg.dylib
	<<
	DescDetail: <<
Kleopatra is the KDE tool for managing X.509 certificates in the
gpgsm keybox and for retrieving certificates from LDAP servers.
	<<
	DescUsage: <<
---[ Installation ]---------------------------------------------------

To install all of the official KDE packages, install "bundle-kde" or
"bundle-kde-ssl" (depending on whether crypotgraphic laws permit
downloading strong cryptography).  Unless you have a specific reason
not to, "bundle-kde-ssl" is suggested.

---[ Configuration ]--------------------------------------------------

To use KDE as your windowing system in XDarwin, create a file called
".xinitrc" in your home directory, containing the following line:

  /sw/bin/startkde >/tmp/kde.log 2>&1

See http://fink.sourceforge.net/doc/x11/run-xfree86.php#xinitrc
for more information on the xinitrc files and how they work.

If you want to use Apple's X11 instead of the KDE window manager,
put the following line before the startkde line in .xinitrc:

  export KDEWM=quartz-wm

If you want to disable quartz-wm even for clipboard synchronization
(proxying), put the following line before the startkde line in
.xinitrc:

  export DISABLE_PROXY=1

---[ Startup ]--------------------------------------------------------

Once you have created an ~/.xinitrc file, you should be able to start
KDE by clicking on the "XDarwin" icon in your Applications folder.  

NOTE: By default, X11 on Mac OS X runs in "rootless" mode, generally.
If you run KDE in rootless mode, it will take over your desktop with
a window that covers everything up.  You can remove this by disabling
desktop icons in the KDE control center.  Open the control center
(either from the "K" menu bar, or by typing "kcontrol" in an xterm)
then expand the "Desktop" list, click "Behavior", and uncheck the
"Show icons on desktop" checkbox.
   
---[ Removal ]--------------------------------------------------------
   
To remove all of the official KDE packages, you can remove the KDE
libraries and anything that depends on them by running, in a terminal:
   
  sudo apt-get remove kdelibs3-universal-shlibs

To remove an individual KDE package collection (like kdenetwork3 or
kdesdk3) you can generally remove [package]-base.  For example, to
remove all of the kdenetwork3 packages, run:

  sudo apt-get remove kdenetwork3-base
	<<
<<
SplitOff4: <<
	Package: kaddressbook
	Description: KDE - address book and contact manager
	Depends: <<
		%N-common (>= %v-%r),
		kleopatra (>= %v-%r),
		ktnef (>= %v-%r)
	<<
	Replaces: <<
		%N (<< %v-%r),
		%N-dev (<< %v-%r),
		%N-common
	<<
	Recommends: %N (>= %v-%r)
	Files: <<
		bin/kabc2mutt
		bin/kaddressbook
		lib/kde3/kabc2mutt*
		lib/kde3/kaddressbook*
		lib/kde3/kcm_kab*
		lib/kde3/ldifvcardthumbnail.*
		lib/kde3/libkaddr*
		lib/libkabinterfaces.*.dylib
		lib/libkaddressbook.*.dylib
		lib/libkdeinit_kabc2mutt.*
		lib/libkdeinit_kaddressbook.*
		share/applications/kde/kaddressbook.desktop
		share/apps/kaddressbook
		share/doc/kde/en/kaddressbook
		share/icons/hicolor/128x128/apps/kaddressbook*
		share/icons/hicolor/16x16/apps/kaddressbook*
		share/icons/hicolor/32x32/apps/kaddressbook*
		share/icons/hicolor/48x48/apps/kaddressbook*
		share/icons/hicolor/64x64/apps/kaddressbook*
		share/services/kab*
		share/services/kaddressbook
		share/services/ldifvcardthumbnail.*
		share/servicetypes/dcopaddressbook.*
		share/servicetypes/kaddressbook*
	<<
	Shlibs: <<
		%p/lib/libkabinterfaces.1.dylib 2.0.0 kaddressbook (>= 3.5.0-1)
		%p/lib/libkaddressbook.0.dylib 1.0.0 kaddressbook (>= 3.5.0-1)
		!%p/lib/libkdeinit_kabc2mutt.dylib
		!%p/lib/libkdeinit_kaddressbook.dylib
	<<
	Homepage: http://pim.kde.org/kaddressbook/index.php
	DescDetail: <<
KAddressBook is a comfortable frontend to organize the addresses of your friends,
business partners, etc.

KAddressBook allows you to import and export adressbook files, offers a function
to search for adresses and - which is the most important feature: it is fully
integrated into the K Desktop Enviroment and interacts with other KDE applications
like e.g. KMail.
	<<
	DescUsage: <<
---[ Installation ]---------------------------------------------------

To install all of the official KDE packages, install "bundle-kde" or
"bundle-kde-ssl" (depending on whether crypotgraphic laws permit
downloading strong cryptography).  Unless you have a specific reason
not to, "bundle-kde-ssl" is suggested.

---[ Configuration ]--------------------------------------------------

To use KDE as your windowing system in XDarwin, create a file called
".xinitrc" in your home directory, containing the following line:

  /sw/bin/startkde >/tmp/kde.log 2>&1

See http://fink.sourceforge.net/doc/x11/run-xfree86.php#xinitrc
for more information on the xinitrc files and how they work.

If you want to use Apple's X11 instead of the KDE window manager,
put the following line before the startkde line in .xinitrc:

  export KDEWM=quartz-wm

If you want to disable quartz-wm even for clipboard synchronization
(proxying), put the following line before the startkde line in
.xinitrc:

  export DISABLE_PROXY=1

---[ Startup ]--------------------------------------------------------

Once you have created an ~/.xinitrc file, you should be able to start
KDE by clicking on the "XDarwin" icon in your Applications folder.  

NOTE: By default, X11 on Mac OS X runs in "rootless" mode, generally.
If you run KDE in rootless mode, it will take over your desktop with
a window that covers everything up.  You can remove this by disabling
desktop icons in the KDE control center.  Open the control center
(either from the "K" menu bar, or by typing "kcontrol" in an xterm)
then expand the "Desktop" list, click "Behavior", and uncheck the
"Show icons on desktop" checkbox.
   
---[ Removal ]--------------------------------------------------------
   
To remove all of the official KDE packages, you can remove the KDE
libraries and anything that depends on them by running, in a terminal:
   
  sudo apt-get remove kdelibs3-universal-shlibs

To remove an individual KDE package collection (like kdenetwork3 or
kdesdk3) you can generally remove [package]-base.  For example, to
remove all of the kdenetwork3 packages, run:

  sudo apt-get remove kdenetwork3-base
	<<
<<
SplitOff5: <<
	Package: kalarm
	Description: KDE - alarm clock
	Depends: <<
		%N-common (>= %v-%r),
		ktts (>= %v-1),
		kleopatra (>= %v-%r),
		ktnef (>= %v-%r)
	<<
	Replaces: <<
		%N (<< %v-%r),
		%N-dev (<< %v-%r),
		%N-common
	<<
	Recommends: %N (>= %v-%r)
	Files: <<
		bin/kalarm*
		share/applications/kde/kalarm.desktop
		share/applnk/.hidden/kalarmd.desktop
		share/applnk/Applications/kalarm.desktop
		share/apps/kalarm
		share/autostart/kalarm*
		share/doc/kde/en/kalarm
		share/icons/crystalsvg/16x16/actions/kalarm*
		share/icons/crystalsvg/22x22/actions/kalarm*
		share/icons/hicolor/16x16/apps/kalarm*
		share/icons/hicolor/32x32/apps/kalarm*
		share/icons/hicolor/48x48/apps/kalarm*
	<<
	Homepage: http://pim.kde.org/components/kalarm.php
	DescDetail: <<
KAlarm is a personal alarm message, command and email scheduler.  It provides
a graphical interface to set up personal alarm messages which pop up on the
screen at the chosen time, or you can schedule commands to be executed or
emails to be sent.
	<<
	DescUsage: <<
---[ Installation ]---------------------------------------------------

To install all of the official KDE packages, install "bundle-kde" or
"bundle-kde-ssl" (depending on whether crypotgraphic laws permit
downloading strong cryptography).  Unless you have a specific reason
not to, "bundle-kde-ssl" is suggested.

---[ Configuration ]--------------------------------------------------

To use KDE as your windowing system in XDarwin, create a file called
".xinitrc" in your home directory, containing the following line:

  /sw/bin/startkde >/tmp/kde.log 2>&1

See http://fink.sourceforge.net/doc/x11/run-xfree86.php#xinitrc
for more information on the xinitrc files and how they work.

If you want to use Apple's X11 instead of the KDE window manager,
put the following line before the startkde line in .xinitrc:

  export KDEWM=quartz-wm

If you want to disable quartz-wm even for clipboard synchronization
(proxying), put the following line before the startkde line in
.xinitrc:

  export DISABLE_PROXY=1

---[ Startup ]--------------------------------------------------------

Once you have created an ~/.xinitrc file, you should be able to start
KDE by clicking on the "XDarwin" icon in your Applications folder.  

NOTE: By default, X11 on Mac OS X runs in "rootless" mode, generally.
If you run KDE in rootless mode, it will take over your desktop with
a window that covers everything up.  You can remove this by disabling
desktop icons in the KDE control center.  Open the control center
(either from the "K" menu bar, or by typing "kcontrol" in an xterm)
then expand the "Desktop" list, click "Behavior", and uncheck the
"Show icons on desktop" checkbox.
   
---[ Removal ]--------------------------------------------------------
   
To remove all of the official KDE packages, you can remove the KDE
libraries and anything that depends on them by running, in a terminal:
   
  sudo apt-get remove kdelibs3-universal-shlibs

To remove an individual KDE package collection (like kdenetwork3 or
kdesdk3) you can generally remove [package]-base.  For example, to
remove all of the kdenetwork3 packages, run:

  sudo apt-get remove kdenetwork3-base
	<<
<<
SplitOff6: <<
	Package: kandy
	Description: KDE - phone/organizer/etc. synchronization
	Depends: <<
		%N-common (>= %v-%r),
		ktnef (>= %v-%r)
	<<
	Replaces: <<
		%N (<< %v-%r),
		%N-dev (<< %v-%r),
		%N-common
	<<
	Recommends: %N (>= %v-%r)
	Files: <<
		bin/kandy*
		lib/kde3/kandy*
		lib/libkdeinit_kandy*
		share/applications/kde/kandy.desktop
		share/applnk/Utilities/kandy.desktop
		share/apps/kandy
		share/config.kcfg/kandy.kcfg
		share/doc/kde/en/kandy
	<<
	Shlibs: <<
		!%p/lib/libkdeinit_kandy.dylib
		!%p/lib/libkdeinit_kandy_client.dylib
	<<
	Homepage: http://pim.kde.org/components/kandy.php
	DescDetail: <<
Kandy aims at providing synchronisation of phonebook, organizer and other
data on your mobile phone with the data stored on the desktop.
	<<
	DescUsage: <<
---[ Installation ]---------------------------------------------------

To install all of the official KDE packages, install "bundle-kde" or
"bundle-kde-ssl" (depending on whether crypotgraphic laws permit
downloading strong cryptography).  Unless you have a specific reason
not to, "bundle-kde-ssl" is suggested.

---[ Configuration ]--------------------------------------------------

To use KDE as your windowing system in XDarwin, create a file called
".xinitrc" in your home directory, containing the following line:

  /sw/bin/startkde >/tmp/kde.log 2>&1

See http://fink.sourceforge.net/doc/x11/run-xfree86.php#xinitrc
for more information on the xinitrc files and how they work.

If you want to use Apple's X11 instead of the KDE window manager,
put the following line before the startkde line in .xinitrc:

  export KDEWM=quartz-wm

If you want to disable quartz-wm even for clipboard synchronization
(proxying), put the following line before the startkde line in
.xinitrc:

  export DISABLE_PROXY=1

---[ Startup ]--------------------------------------------------------

Once you have created an ~/.xinitrc file, you should be able to start
KDE by clicking on the "XDarwin" icon in your Applications folder.  

NOTE: By default, X11 on Mac OS X runs in "rootless" mode, generally.
If you run KDE in rootless mode, it will take over your desktop with
a window that covers everything up.  You can remove this by disabling
desktop icons in the KDE control center.  Open the control center
(either from the "K" menu bar, or by typing "kcontrol" in an xterm)
then expand the "Desktop" list, click "Behavior", and uncheck the
"Show icons on desktop" checkbox.
   
---[ Removal ]--------------------------------------------------------
   
To remove all of the official KDE packages, you can remove the KDE
libraries and anything that depends on them by running, in a terminal:
   
  sudo apt-get remove kdelibs3-universal-shlibs

To remove an individual KDE package collection (like kdenetwork3 or
kdesdk3) you can generally remove [package]-base.  For example, to
remove all of the kdenetwork3 packages, run:

  sudo apt-get remove kdenetwork3-base
	<<
<<
SplitOff7: <<
	Package: karm
	Description: KDE - time tracker
	Depends: <<
		%N-common (>= %v-%r),
		ktnef (>= %v-%r)
	<<
	Replaces: <<
		%N (<< %v-%r),
		%N-dev (<< %v-%r),
		%N-common
	<<
	Recommends: %N (>= %v-%r)
	Files: <<
		bin/karm
		lib/kde3/libkarmpart*
		share/applications/kde/karm.desktop
		share/applnk/Utilities/karm.desktop
		share/apps/karm
		share/apps/karmpart
		share/doc/kde/en/karm
		share/icons/hicolor/128x128/apps/karm*
		share/icons/hicolor/16x16/apps/karm*
		share/icons/hicolor/22x22/apps/karm*
		share/icons/hicolor/32x32/apps/karm*
		share/icons/hicolor/48x48/apps/karm*
		share/icons/hicolor/64x64/apps/karm*
		share/services/karm_part*
	<<
	Homepage: http://pim.kde.org/components/karm.php
	DescDetail: <<
KArm is a time tracker that integrates with the KOrganizer to do list.
It features a simple user interface, idle detection, desktop tracking,
CSV export, and a time card report.
	<<
	DescUsage: <<
---[ Installation ]---------------------------------------------------

To install all of the official KDE packages, install "bundle-kde" or
"bundle-kde-ssl" (depending on whether crypotgraphic laws permit
downloading strong cryptography).  Unless you have a specific reason
not to, "bundle-kde-ssl" is suggested.

---[ Configuration ]--------------------------------------------------

To use KDE as your windowing system in XDarwin, create a file called
".xinitrc" in your home directory, containing the following line:

  /sw/bin/startkde >/tmp/kde.log 2>&1

See http://fink.sourceforge.net/doc/x11/run-xfree86.php#xinitrc
for more information on the xinitrc files and how they work.

If you want to use Apple's X11 instead of the KDE window manager,
put the following line before the startkde line in .xinitrc:

  export KDEWM=quartz-wm

If you want to disable quartz-wm even for clipboard synchronization
(proxying), put the following line before the startkde line in
.xinitrc:

  export DISABLE_PROXY=1

---[ Startup ]--------------------------------------------------------

Once you have created an ~/.xinitrc file, you should be able to start
KDE by clicking on the "XDarwin" icon in your Applications folder.  

NOTE: By default, X11 on Mac OS X runs in "rootless" mode, generally.
If you run KDE in rootless mode, it will take over your desktop with
a window that covers everything up.  You can remove this by disabling
desktop icons in the KDE control center.  Open the control center
(either from the "K" menu bar, or by typing "kcontrol" in an xterm)
then expand the "Desktop" list, click "Behavior", and uncheck the
"Show icons on desktop" checkbox.
   
---[ Removal ]--------------------------------------------------------
   
To remove all of the official KDE packages, you can remove the KDE
libraries and anything that depends on them by running, in a terminal:
   
  sudo apt-get remove kdelibs3-universal-shlibs

To remove an individual KDE package collection (like kdenetwork3 or
kdesdk3) you can generally remove [package]-base.  For example, to
remove all of the kdenetwork3 packages, run:

  sudo apt-get remove kdenetwork3-base
	<<
<<
SplitOff8: <<
	Package: %N-kfile-plugins
	Description: KDE - file plugins for %N
	Depends: <<
		%N-common (>= %v-%r),
		ktnef (>= %v-%r),
		pilot-link10-shlibs
	<<
	Replaces: <<
		%N (<< %v-%r),
		%N-dev (<< %v-%r),
		%N-common,
		kpilot (<< 3.4.0-1),
		kmail (<< 3.4.0-1)
	<<
	Recommends: %N (>= %v-%r)
	Files: <<
		lib/kde3/kfile_ics.*
		lib/kde3/kfile_palm.*
		lib/kde3/kfile_vcf.*
		share/services/kfile_ics.*
		share/services/kfile_palm.*
		share/services/kfile_vcf.*
	<<
	DescUsage: <<
---[ Installation ]---------------------------------------------------

To install all of the official KDE packages, install "bundle-kde" or
"bundle-kde-ssl" (depending on whether crypotgraphic laws permit
downloading strong cryptography).  Unless you have a specific reason
not to, "bundle-kde-ssl" is suggested.

---[ Configuration ]--------------------------------------------------

To use KDE as your windowing system in XDarwin, create a file called
".xinitrc" in your home directory, containing the following line:

  /sw/bin/startkde >/tmp/kde.log 2>&1

See http://fink.sourceforge.net/doc/x11/run-xfree86.php#xinitrc
for more information on the xinitrc files and how they work.

If you want to use Apple's X11 instead of the KDE window manager,
put the following line before the startkde line in .xinitrc:

  export KDEWM=quartz-wm

If you want to disable quartz-wm even for clipboard synchronization
(proxying), put the following line before the startkde line in
.xinitrc:

  export DISABLE_PROXY=1

---[ Startup ]--------------------------------------------------------

Once you have created an ~/.xinitrc file, you should be able to start
KDE by clicking on the "XDarwin" icon in your Applications folder.  

NOTE: By default, X11 on Mac OS X runs in "rootless" mode, generally.
If you run KDE in rootless mode, it will take over your desktop with
a window that covers everything up.  You can remove this by disabling
desktop icons in the KDE control center.  Open the control center
(either from the "K" menu bar, or by typing "kcontrol" in an xterm)
then expand the "Desktop" list, click "Behavior", and uncheck the
"Show icons on desktop" checkbox.
   
---[ Removal ]--------------------------------------------------------
   
To remove all of the official KDE packages, you can remove the KDE
libraries and anything that depends on them by running, in a terminal:
   
  sudo apt-get remove kdelibs3-universal-shlibs

To remove an individual KDE package collection (like kdenetwork3 or
kdesdk3) you can generally remove [package]-base.  For example, to
remove all of the kdenetwork3 packages, run:

  sudo apt-get remove kdenetwork3-base
	<<
<<
SplitOff9: <<
	Package: %N-ioslaves
	Description: KDE - IOslaves for %N
	Depends: <<
		%N-common (>= %v-%r),
		cyrus-sasl2-shlibs
	<<
	Replaces: <<
		%N (<< %v-%r),
		%N-dev (<< %v-%r),
		%N-common,
		kdebase3 (<< 3.5.0-1),
		kdebase3-ssl (<< 3.5.0-1),
		kdebase3-unified (<< 3.5.0-1)
	<<
	Recommends: %N (>= %v-%r)
	Files: <<
		lib/kde3/kio_imap*
		lib/kde3/kio_mbox*
		lib/kde3/kio_sieve*
		share/services/imap*
		share/services/mbox*
		share/services/sieve*
	<<
	DescUsage: <<
---[ Installation ]---------------------------------------------------

To install all of the official KDE packages, install "bundle-kde" or
"bundle-kde-ssl" (depending on whether crypotgraphic laws permit
downloading strong cryptography).  Unless you have a specific reason
not to, "bundle-kde-ssl" is suggested.

---[ Configuration ]--------------------------------------------------

To use KDE as your windowing system in XDarwin, create a file called
".xinitrc" in your home directory, containing the following line:

  /sw/bin/startkde >/tmp/kde.log 2>&1

See http://fink.sourceforge.net/doc/x11/run-xfree86.php#xinitrc
for more information on the xinitrc files and how they work.

If you want to use Apple's X11 instead of the KDE window manager,
put the following line before the startkde line in .xinitrc:

  export KDEWM=quartz-wm

If you want to disable quartz-wm even for clipboard synchronization
(proxying), put the following line before the startkde line in
.xinitrc:

  export DISABLE_PROXY=1

---[ Startup ]--------------------------------------------------------

Once you have created an ~/.xinitrc file, you should be able to start
KDE by clicking on the "XDarwin" icon in your Applications folder.  

NOTE: By default, X11 on Mac OS X runs in "rootless" mode, generally.
If you run KDE in rootless mode, it will take over your desktop with
a window that covers everything up.  You can remove this by disabling
desktop icons in the KDE control center.  Open the control center
(either from the "K" menu bar, or by typing "kcontrol" in an xterm)
then expand the "Desktop" list, click "Behavior", and uncheck the
"Show icons on desktop" checkbox.
   
---[ Removal ]--------------------------------------------------------
   
To remove all of the official KDE packages, you can remove the KDE
libraries and anything that depends on them by running, in a terminal:
   
  sudo apt-get remove kdelibs3-universal-shlibs

To remove an individual KDE package collection (like kdenetwork3 or
kdesdk3) you can generally remove [package]-base.  For example, to
remove all of the kdenetwork3 packages, run:

  sudo apt-get remove kdenetwork3-base
	<<
<<

PostInstScript: <<
	if [ -x "%p/bin/kde-update-caches.sh" ]; then
		%p/bin/kde-update-caches.sh >/dev/null 2>&1 || :
	fi
<<

DescDetail: <<
Various tools for KDE Personal Information Management

Install the kdepim3 package if you want to install all of the
kdepim packages.
<<
DescUsage: <<
---[ Installation ]---------------------------------------------------

To install all of the official KDE packages, install "bundle-kde" or
"bundle-kde-ssl" (depending on whether crypotgraphic laws permit
downloading strong cryptography).  Unless you have a specific reason
not to, "bundle-kde-ssl" is suggested.

---[ Configuration ]--------------------------------------------------

To use KDE as your windowing system in XDarwin, create a file called
".xinitrc" in your home directory, containing the following line:

  /sw/bin/startkde >/tmp/kde.log 2>&1

See http://fink.sourceforge.net/doc/x11/run-xfree86.php#xinitrc
for more information on the xinitrc files and how they work.

If you want to use Apple's X11 instead of the KDE window manager,
put the following line before the startkde line in .xinitrc:

  export KDEWM=quartz-wm

If you want to disable quartz-wm even for clipboard synchronization
(proxying), put the following line before the startkde line in
.xinitrc:

  export DISABLE_PROXY=1

---[ Startup ]--------------------------------------------------------

Once you have created an ~/.xinitrc file, you should be able to start
KDE by clicking on the "XDarwin" icon in your Applications folder.  

NOTE: By default, X11 on Mac OS X runs in "rootless" mode, generally.
If you run KDE in rootless mode, it will take over your desktop with
a window that covers everything up.  You can remove this by disabling
desktop icons in the KDE control center.  Open the control center
(either from the "K" menu bar, or by typing "kcontrol" in an xterm)
then expand the "Desktop" list, click "Behavior", and uncheck the
"Show icons on desktop" checkbox.
   
---[ Removal ]--------------------------------------------------------
   
To remove all of the official KDE packages, you can remove the KDE
libraries and anything that depends on them by running, in a terminal:
   
  sudo apt-get remove kdelibs3-universal-shlibs

To remove an individual KDE package collection (like kdenetwork3 or
kdesdk3) you can generally remove [package]-base.  For example, to
remove all of the kdenetwork3 packages, run:

  sudo apt-get remove kdenetwork3-base
<<

