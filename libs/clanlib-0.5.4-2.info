Package: clanlib
Version: 0.5.4
Revision: 2
Depends: zlib, x11, freetype, libmikmod, tolua, hermes-shlibs, libvorbis-shlibs, libogg-shlibs, libjpeg-shlibs, libpng-shlibs, %N-shlibs (= %v-%r)
BuildDepends: fink (>= 0.9.9), hermes, libvorbis, libogg, libjpeg, libpng, freetype | freetype-hinting
Replaces: %N (<< %v-3)
Source: http://dark.x.dtu.dk/~mbn/%n/download/download-sphair/ClanLib-%v-1.tar.gz
SourceDirectory: ClanLib-%v
Patch: %f.patch
ConfigureParams: --enable-lua --enable-opengl --with-extra-includes=%p/include --with-extra-libs=%p/lib --with-x --enable-dyn --enable-x11 --enable-network --enable-gui --enable-mikmod --enable-vorbis --enable-png --enable-jpeg --enable-ttf --mandir=%p/share/man --infodir=%p/share/info --libexecdir=%p/lib
InstallScript: <<
  make install prefix=%i
  make docs_install prefix=%i
<<
SetCXX: c++
SplitOff: <<
  Package: %N-shlibs
  Replaces: %N (<< %v-3)
  Files: <<
    lib/libclanApp.*.dylib
    lib/libclanCore.*.dylib
    lib/libclanDisplay.*.dylib
    lib/libclanGL.*.dylib
    lib/libclanGUI.*.dylib
    lib/libclanJPEG.*.dylib
    lib/libclanLua.*.dylib
    lib/libclanMikMod.*.dylib
    lib/libclanNetwork.*.dylib
    lib/libclanPNG.*.dylib
    lib/libclanSmallJPEG.*.dylib
    lib/libclanSound.*.dylib
    lib/libclanTTF.*.dylib
    lib/libclanVorbis.*.dylib
  <<
  DocFiles: BUGS CODING_STYLE COPYING CREDITS HARDWARE INSTALL* NEWS PATCHES PORTING README* ROADMAP
<<
SplitOff2: <<
  Package: %N-doc
  Files: share/%N/html
  DocFiles: COPYING CREDITS
<<
DocFiles: COPYING CREDITS
Description: The platform-independent game SDK
DescDetail: <<
ClanLib is a cross-platform game SDK currently supporting Windows, Linux,
and BeOS. It provides easy-to-use interfaces for 2D graphics, sound,
resources, networking, and OpenGL. The goal is to provide a game SDK that
does all the dirty work that all modern games need to implement anyway,
thus making the game developer concentrate on the gameplay instead of
reinventing the wheel over and over again.
<<
DescPort: <<
Anything that uses the clanlib headers will need to add -D__DARWIN__
to the compile line.

If the port uses clan-config to set the cflags, there is no need to add
-D__DARWIN__ manually.

libclanSound was hacked up to use esd, this may cause problems with sound,
currently untested.
<<
License: LGPL
Maintainer: Justin F. Hallett <thesin@southofheaven.net>
Homepage: http://www.clanlib.org/
