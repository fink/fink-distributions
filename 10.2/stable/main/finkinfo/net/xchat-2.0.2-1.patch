diff -ru xchat-2.0.2/configure xchat-2.0.2-patched/configure
--- xchat-2.0.2/configure	2003-04-21 19:10:14.000000000 +0200
+++ xchat-2.0.2-patched/configure	2003-04-22 15:36:05.000000000 +0200
@@ -12607,7 +12607,7 @@
 echo "${ECHO_T}ok" >&6
 
 		oldLIBS=$LIBS
-		LIBS="$LIBS $PERL_LDFLAGS"
+		LIBS="$PERL_LDFLAGS $LIBS"
 
 for ac_func in eval_pv
 do
@@ -12808,7 +12808,7 @@
                         PY_PREFIX=`$pythonpath -c 'import sys; print sys.prefix'`
                         PY_EXEC_PREFIX=`$pythonpath -c 'import sys; print sys.exec_prefix'`
                         if test -f $PY_PREFIX/include/python$PY_VERSION/Python.h; then
-                                PY_LIBS="-L$PY_EXEC_PREFIX/lib/python$PY_VERSION/config -lpython$PY_VERSION -lpthread -lutil"
+                                PY_LIBS="-L$PY_EXEC_PREFIX/lib/python$PY_VERSION/config -lpython$PY_VERSION"
                                 PY_CFLAGS="-I$PY_PREFIX/include/python$PY_VERSION"
                                 echo "$as_me:$LINENO: result: ok" >&5
 echo "${ECHO_T}ok" >&6
diff -ru xchat-2.0.2/ltmain.sh xchat-2.0.2-patched/ltmain.sh
--- xchat-2.0.2/ltmain.sh	2002-11-28 11:29:05.000000000 +0100
+++ xchat-2.0.2-patched/ltmain.sh	2003-04-22 15:36:47.000000000 +0200
@@ -1493,18 +1493,7 @@
 	  fi
 	  case $linkmode in
 	  lib)
-	    if test "$deplibs_check_method" != pass_all; then
-	      echo
-	      echo "*** Warning: This library needs some functionality provided by $deplib."
-	      echo "*** I have the capability to make that library automatically link in when"
-	      echo "*** you link to this library.  But I can only do this if you have a"
-	      echo "*** shared version of the library, which you do not appear to have."
-	    else
-	      echo
-	      echo "*** Warning: Linking the shared library $output against the"
-	      echo "*** static library $deplib is not portable!"
-	      deplibs="$deplib $deplibs"
-	    fi
+	    deplibs="$deplib $deplibs"
 	    continue
 	    ;;
 	  prog)
