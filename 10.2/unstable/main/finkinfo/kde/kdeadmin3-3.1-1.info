Package: kdeadmin3
Version: 3.1
Revision: 1
Description: KDE - administrative tools
License: GPL/LGPL
Maintainer: Benjamin Reed <ranger@befunk.com>

Depends: kcron (>= %v-%r), kpackage (>= %v-%r), kwuftpd (>= %v-%r)
BuildDepends: dlcompat-dev (>= 20021117), kdelibs3-ssl-dev (>= %v-1) | kdelibs3-dev (>= %v-1), kdebase3-ssl-dev (>= %v-1) | kdebase3-dev (>= %v-1), libart2 (>= 2.3.9), libpng3 (>= 1.2.4), qt3-dev (>= 3.1.0), x11
GCC: 3.1

CustomMirror: <<
  nam-US: http://ranger.befunk.com/fink
  nam-CA: http://www.southofheaven.net/befunk
<<
Source: mirror:sourceforge:fink/kdeadmin-%vrc5.tar.bz2
Source-MD5: 8cb805dd11e3415ea8364c23e644276c
Source2: mirror:custom:kde-admindir-20021214.tar.bz2
Source2-MD5: c09e25ed693826dfa497d505979e636a
Source2ExtractDir: kdeadmin-%vrc5

PatchScript: sed 's|@PREFIX@|%p|g' <%a/%f.patch | patch -p0

SetCFLAGS: -fno-common
SetCPPFLAGS: -no-cpp-precomp -Ddlsym=dlsym_auto_underscore -isystem %p/include -I%p/include
SetLIBS: -L%p/lib
SetMACOSX_DEPLOYMENT_TARGET: 10.2
ConfigureParams: --prefix=%p --with-qt-dir=%p --with-qt-includes=%p/include/qt --with-extra-libs=%p/lib --with-extra-includes=%p/include --enable-mt --with-pic --enable-rpath --enable-shared=yes --enable-static=no --mandir=%i/share/man --with-xinerama --with-pam=yes --without-quota --with-homeprefix=/Users --enable-final --disable-dependency-tracking `cat /tmp/kde-build-options`
CompileScript: <<
#!/bin/sh
  export KDEDIR=%p
  make -f Makefile.cvs
  [ $? = 0 ] && sh ./configure %c 
  [ $? = 0 ] && make -j2
<<

InstallScript: <<
#!/bin/sh

  make install DESTDIR=%d
  mkdir -p %i/share/doc/kde-installed-packages
  touch %i/share/doc/kde-installed-packages/%N
  touch %i/share/doc/kde-installed-packages/%N-base
  rm -rf %i/share/applnk/System/More/kwuftpd.desktop
  rm -rf %i/share/doc/HTML/en/kdat
  rm -rf %i/share/doc/HTML/en/ksysv
  rm -rf %i/share/doc/HTML/en/kuser
  rm -rf %i/share/doc/HTML/en/kxconfig
<<
DocFiles: AUTHORS COPYING INSTALL README
SplitOff: <<
  Package: kcron
  Description: KDE - crontab editor
  Depends: dlcompat-shlibs (>= 20021117), kdelibs3-ssl (>= %v-1) | kdelibs3 (>= %v-1), libart2-shlibs (>= 2.3.9), libpng3-shlibs (>= 1.2.4), qt3-shlibs (>= 3.1.0), x11, %N-base (>= %v-%r)
  BuildDependsOnly: true
  Files: <<
    bin/kcron
    share/applnk/System/kcron.desktop
    share/doc/HTML/en/kcron
    share/icons/crystalsvg/16x16/apps/kcron.png
    share/icons/crystalsvg/32x32/apps/kcron.png
  <<
<<
SplitOff2: <<
  Package: kpackage
  Description: KDE - binary (debian, RPM) package manager
  Depends: dlcompat-shlibs (>= 20021117), kdelibs3-ssl (>= %v-1) | kdelibs3 (>= %v-1), libart2-shlibs (>= 2.3.9), libpng3-shlibs (>= 1.2.4), qt3-shlibs (>= 3.1.0), x11, %N-base (>= %v-%r)
  BuildDependsOnly: true
  Files: <<
    bin/kpackage
    lib/kde3/kfile_deb*
    lib/kde3/kfile_rpm*
    share/applnk/System/kpackage.desktop
    share/apps/kpackage
    share/doc/HTML/en/kpackage
    share/icons/crystalsvg/128x128/apps/kpackage.png
    share/icons/crystalsvg/16x16/apps/kpackage.png
    share/icons/crystalsvg/32x32/apps/kpackage.png
    share/icons/crystalsvg/48x48/apps/kpackage.png
    share/icons/crystalsvg/64x64/apps/kpackage.png
    share/mimelnk/application/x-debian-package.desktop
    share/services/kfile_deb.desktop
    share/services/kfile_rpm.desktop
  <<
<<
SplitOff3: <<
  Package: kwuftpd
  Description: KDE - WU-FTP daemon management
  Depends: dlcompat-shlibs (>= 20021117), kdelibs3-ssl (>= %v-1) | kdelibs3 (>= %v-1), libart2-shlibs (>= 2.3.9), libpng3-shlibs (>= 1.2.4), qt3-shlibs (>= 3.1.0), x11, %N-base (>= %v-%r)
  BuildDependsOnly: true
  Files: <<
    bin/kwuftpd
    share/applnk/System/kwuftpd.desktop
    share/doc/HTML/en/kwuftpd
  <<
<<
SplitOff4: <<
  Package: %N-base
  Description: KDE - kdeadmin base package
  BuildDependsOnly: true
  Files: <<
    share/doc/kde-installed-packages/%N-base
  <<
<<

DescDetail: <<
Install kdeadmin3 if you would like to install all of the kdeadmin
packages.  kdeadmin3 contains:

  kcron
  kpackage
  kwuftpd
<<
DescUsage: <<
---[ Installation ]---------------------------------------------------

To install all of the official KDE packages, install "bundle-kde" or
"bundle-kde-ssl" (depending on whether crypotgraphic laws permit
downloading strong cryptography).

---[ Configuration ]--------------------------------------------------

To use KDE as your windowing system in XDarwin, create a file called
".xinitrc" in your home directory, with the following commands (it is
safe to cut and paste these):

  echo "source /sw/bin/init.sh" > ~/.xinitrc
  echo "/sw/bin/startkde >/tmp/kde.log 2>&1" >> ~/.xinitrc

Note that this will overwrite an existing .xinitrc file!

---[ Startup ]--------------------------------------------------------

Once you have created an ~/.xinitrc file, you should be able to start
KDE by clicking on the "XDarwin" icon in your Applications folder.  
   
NOTE: If you plan on running in rootless mode, you will likely want to
disable desktop icons, or else the root window desktop will cover up
your Aqua destkop.  You can do this by starting the KDE control
center, expanding the "Desktop" list, click "Behavior", and uncheck   
the "Enable icons on desktop" checkbox.
   
---[ Removal ]--------------------------------------------------------
   
To remove all of the official KDE packages, you can remove aRts and
anything that depends on it, by running (in a terminal):
   
  sudo apt-get remove arts

To remove just the kdeadmin packages, run:

  sudo apt-get remove kdeadmin3-base
<<
