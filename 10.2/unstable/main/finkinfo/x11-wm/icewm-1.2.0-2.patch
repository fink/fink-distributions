--- icewm-1.2.0pre1/src/Makefile.orig	Fri Dec 28 07:01:33 2001
+++ icewm-1.2.0pre1/src/Makefile	Fri Dec 28 07:01:51 2001
@@ -20,7 +20,7 @@
 	-DEXEEXT='"$(EXEEXT)"' \
 	-DICEWMEXE='"icewm"EXEEXT' \
 	-DICEHELPEXE='"icehelp"EXEEXT' \
-	-DICEHELPIDX='"$(DOCDIR)/icewm-$(VERSION)/icewm.html"' \
+	-DICEHELPIDX='"$(DOCDIR)/icewm/icewm.html"' \
         $(SYS_CFLAGS) $(SYS_INCDIRS)
 LFLAGS = $(SYS_LIBDIRS) 
 LIBS = $(SYS_LIBS)
@@ -143,10 +143,10 @@
 
 %.p: %.cc
 	@echo Analysing dependencies for $^...
-	@$(CXX) $(CXXFLAGS) -M -c $< > $@
+	@$(CXX) $(CXXFLAGS) $(CPPFLAGS) -M -c $< > $@
 
 %.o: %.cc
 	@echo Compiling $@...
-	@$(CXX) $(CXXFLAGS) -c $<
+	@$(CXX) $(CXXFLAGS) $(CPPFLAGS) -c $<
 	
 ################################################################################
--- icewm-1.2.0pre1/Makefile.in.orig	Fri Dec 28 06:51:13 2001
+++ icewm-1.2.0pre1/Makefile.in	Fri Dec 28 06:51:59 2001
@@ -129,11 +129,11 @@
 
 install-docs: docs $(LSM)
 	@echo ------------------------------------------
-	@rm -fr $(DESTDIR)$(DOCDIR)/icewm-$(VERSION)
-	@$(INSTALLDIR) $(DESTDIR)$(DOCDIR)/icewm-$(VERSION)
+	@rm -fr $(DESTDIR)$(DOCDIR)/icewm
+	@$(INSTALLDIR) $(DESTDIR)$(DOCDIR)/icewm
 	@echo Installing documentation in $(DESTDIR)$(DOCDIR); \
-	cp $(DOCFILES) $(DESTDIR)$(DOCDIR)/icewm-$(VERSION); \
-	cp -r doc/*.sgml doc/*.html $(DESTDIR)$(DOCDIR)/icewm-$(VERSION)
+	cp $(DOCFILES) $(DESTDIR)$(DOCDIR)/icewm
+	cp -r doc/*.sgml doc/*.html $(DESTDIR)$(DOCDIR)/icewm
 	@echo ------------------------------------------
 
 install-nls: nls
