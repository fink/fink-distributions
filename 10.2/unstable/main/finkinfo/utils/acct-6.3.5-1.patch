diff -ru acct-6.3.5/configure acct-6.3.5_patched/configure
--- acct-6.3.5/configure	Wed May 27 07:02:44 1998
+++ acct-6.3.5_patched/configure	Wed Mar  5 17:01:50 2003
@@ -2713,7 +2713,7 @@
 #include <utmp.h>
 
 #ifndef WTMP_FILE
-#  if defined(__FreeBSD__) || defined (__NetBSD__) || defined(__linux__)
+#  if defined(__FreeBSD__) || defined (__NetBSD__) || defined(__linux__) || defined(__APPLE__)
 #    define WTMP_FILE "/var/log/wtmp"
 #  else
 #    if defined(sun) || defined(AMIX)
@@ -2732,7 +2732,7 @@
 #  if defined(__FreeBSD__) || defined(__linux__)
 #    define ACCT_FILE "/var/account/pacct"
 #  else
-#    if defined(__NetBSD__)
+#    if defined(__NetBSD__) || defined(__APPLE__)
 #      define ACCT_FILE "/var/account/acct"
 #    else
 #      if defined(sun) || defined(AMIX)
@@ -2749,7 +2749,7 @@
 #endif
 
 #ifndef SAVACCT_FILE
-#  if defined(__FreeBSD__) || defined(__NetBSD__) || defined(__linux__)
+#  if defined(__FreeBSD__) || defined(__NetBSD__) || defined(__linux__) || defined(__APPLE__)
 #    define SAVACCT_FILE "/var/account/savacct"
 #  else
 #    if defined(sun) || defined(AMIX)
@@ -2765,7 +2765,7 @@
 #endif
 
 #ifndef USRACCT_FILE
-#  if defined(__FreeBSD__) || defined(__NetBSD__) || defined(__linux__)
+#  if defined(__FreeBSD__) || defined(__NetBSD__) || defined(__linux__) || defined(__APPLE__)
 #    define USRACCT_FILE "/var/account/usracct"
 #  else
 #    if defined(sun) || defined(AMIX)
diff -ru acct-6.3.5/lastcomm.c acct-6.3.5_patched/lastcomm.c
--- acct-6.3.5/lastcomm.c	Thu Apr  9 01:24:49 1998
+++ acct-6.3.5_patched/lastcomm.c	Wed Mar  5 10:41:37 2003
@@ -22,10 +22,6 @@
 #include "config.h"
 #include <stdio.h>
 
-#ifdef HAVE_STDLIB_H
-#include <stdlib.h>
-#endif
-
 #include <ctype.h>
 
 #ifdef HAVE_STRING_H