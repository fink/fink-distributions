diff -r -u -P wml-2.0.8/README.Fink wml-2.0.8-new/README.Fink
--- wml-2.0.8/README.Fink	Wed Dec 31 19:00:00 1969
+++ wml-2.0.8-new/README.Fink	Mon Mar 11 16:50:43 2002
@@ -0,0 +1,19 @@
+Overview of patches needed for Fink/Darwin/OS X
+
+* wml_aux/iselect does not find -lncurses or ncurses.h
+
+  Must add  --with-incdir=/sw/include --with-libdir=/sw/lib
+
+* wml_common/gd does not find -lpng or png.h
+
+  configure.in patched to check --with-incdir and --with-libdir.
+
+* wml_aux/tidy  platform.h:72: redefinition of `uint'
+
+  defined __FreeBSD__ in Makefile
+  (should check architecture in configure script)
+
+* Makefile.in
+
+  mandir set to $(prefix)/share/man
+
diff -r -u -P wml-2.0.8/wml_aux/tidy/Makefile.in wml-2.0.8-new/wml_aux/tidy/Makefile.in
--- wml-2.0.8/wml_aux/tidy/Makefile.in	Sun Mar 12 15:57:00 2000
+++ wml-2.0.8-new/wml_aux/tidy/Makefile.in	Mon Mar 11 16:52:36 2002
@@ -2,7 +2,7 @@
 
 @SET_MAKE@
 CC= @CC@
-CFLAGS= @CFLAGS@
+CFLAGS= -D__FreeBSD__ @CFLAGS@
 
 OFILES = \
    attrs.o         istack.o        parser.o        tags.o \
diff -r -u -P wml-2.0.8/wml_common/gd/Makefile.PL.in wml-2.0.8-new/wml_common/gd/Makefile.PL.in
--- wml-2.0.8/wml_common/gd/Makefile.PL.in	Sun Mar 12 11:22:30 2000
+++ wml-2.0.8-new/wml_common/gd/Makefile.PL.in	Mon Mar 11 16:54:11 2002
@@ -14,6 +14,8 @@
     # having to do with missing math functions (like floor)
     'LIBS'      =>  ["-lm @LIBPNG@"],
     'DEFINE'	=> '@DEFS@'
+   ,'CC'    => '@CC@ @INCDIR@'
+   ,'LDFLAGS'   => '@LDFLAGS@'
     # Uncomment the line below if you're using a perl compiled
     # for fastCGI (http://www.fastcgi.com/).
     # 'DEFINE'	=> '-DFCGI'
diff -r -u -P wml-2.0.8/wml_common/gd/configure wml-2.0.8-new/wml_common/gd/configure
--- wml-2.0.8/wml_common/gd/configure	Thu May  4 15:19:05 2000
+++ wml-2.0.8-new/wml_common/gd/configure	Mon Mar 11 17:02:44 2002
@@ -17,6 +17,10 @@
   --with-cc=PATH          use a specific ANSI C compiler"
 ac_help="$ac_help
   --with-forced-cc=PATH   force the global usage of a specific ANSI C compiler"
+ac_help="$ac_help
+  --with-incdir=DIR       add DIR to the include path"
+ac_help="$ac_help
+  --with-libdir=DIR       add DIR to the library path"
 
 # Initialize some variables set by options.
 # The variables have the same names as the options, with
@@ -916,11 +920,43 @@
 fi
 
 
+echo $ac_n "checking for additional include dir""... $ac_c" 1>&6
+echo "configure:925: checking for additional include dir" >&5
+
+# Check whether --with-incdir or --without-incdir was given.
+if test "${with_incdir+set}" = set; then
+  withval="$with_incdir"
+  CFLAGS="$CFLAGS -I$with_incdir"
+CPPFLAGS="$CPPFLAGS -I$with_incdir"
+INCDIR="-I$with_incdir"
+x="$with_incdir"
+
+else
+  x="none particular"
+
+fi
+echo "$ac_t""$x" 1>&6
+echo $ac_n "checking for additional library dir""... $ac_c" 1>&6
+echo "configure:941: checking for additional library dir" >&5
+
+# Check whether --with-libdir or --without-libdir was given.
+if test "${with_libdir+set}" = set; then
+  withval="$with_libdir"
+  LDFLAGS="$LDFLAGS -L$with_libdir"
+LIBDIR="-L$with_libdir"
+x="$with_libdir"
+
+else
+  x="none particular"
+
+fi
+echo "$ac_t""$x" 1>&6
+
 echo "$ac_t""" 1>&6
 echo "$ac_t""CHECK: PNG Support" 1>&6
 
 echo $ac_n "checking for png_write_image in -lpng""... $ac_c" 1>&6
-echo "configure:924: checking for png_write_image in -lpng" >&5
+echo "configure:960: checking for png_write_image in -lpng" >&5
 ac_lib_var=`echo png'_'png_write_image | sed 'y%./+-%__p_%'`
 if eval "test \"`echo '$''{'ac_cv_lib_$ac_lib_var'+set}'`\" = set"; then
   echo $ac_n "(cached) $ac_c" 1>&6
@@ -1158,6 +1194,8 @@
 s%@CPP@%$CPP%g
 s%@RANLIB@%$RANLIB%g
 s%@SET_MAKE@%$SET_MAKE%g
+s%@INCDIR@%$INCDIR%g
+s%@LIBDIR@%$LIBDIR%g
 s%@LIBPNG@%$LIBPNG%g
 
 CEOF
diff -r -u -P wml-2.0.8/wml_common/gd/configure.in wml-2.0.8-new/wml_common/gd/configure.in
--- wml-2.0.8/wml_common/gd/configure.in	Thu May  4 15:19:05 2000
+++ wml-2.0.8-new/wml_common/gd/configure.in	Mon Mar 11 17:00:21 2002
@@ -36,6 +36,30 @@
 AC_PROG_RANLIB
 AC_PROG_MAKE_SET
 
+AC_MSG_CHECKING(for additional include dir)
+AC_SUBST(INCDIR)
+AC_ARG_WITH(incdir,dnl
+[  --with-incdir=DIR       add DIR to the include path],
+CFLAGS="$CFLAGS -I$with_incdir"
+CPPFLAGS="$CPPFLAGS -I$with_incdir"
+INCDIR="-I$with_incdir"
+x="$with_incdir"
+,
+x="none particular"
+)dnl
+AC_MSG_RESULT([$x])
+AC_MSG_CHECKING(for additional library dir)
+AC_SUBST(LIBDIR)
+AC_ARG_WITH(libdir,dnl
+[  --with-libdir=DIR       add DIR to the library path],
+LDFLAGS="$LDFLAGS -L$with_libdir"
+LIBDIR="-L$with_libdir"
+x="$with_libdir"
+,
+x="none particular"
+)dnl
+AC_MSG_RESULT([$x])
+
 dnl ##  PNG support
 AC_CONFIGURE_PART(CHECK: PNG Support)
 AC_CHECK_LIB(png, png_write_image)
diff -r -u -P wml-2.0.8/wml_common/gd/libgd/Makefile.PL.in wml-2.0.8-new/wml_common/gd/libgd/Makefile.PL.in
--- wml-2.0.8/wml_common/gd/libgd/Makefile.PL.in	Tue Jan 11 18:45:56 2000
+++ wml-2.0.8-new/wml_common/gd/libgd/Makefile.PL.in	Mon Mar 11 17:03:24 2002
@@ -3,6 +3,7 @@
 	'NAME'     =>   'libgd',
         'LINKTYPE'  => 'static',
         'DEFINE'    => '@DEFS@',
+        'CC'        => '@CC@ @INCDIR@',
         'SKIP'      => [qw(static dynamic test)],
 	'H'         => [qw(gd.h gd_io.h)],
         'C'         => [qw(gd.c gd_gif.c gd_png.c gd_io.c gd_io_dp.c)],
