diff -ruN gengetopt-2.8.orig/Makefile.in gengetopt-2.8/Makefile.in
--- gengetopt-2.8.orig/Makefile.in	2002-12-31 05:34:11.000000000 -0700
+++ gengetopt-2.8/Makefile.in	2003-01-02 16:21:43.000000000 -0700
@@ -83,6 +83,7 @@
 PATH_SEPARATOR = @PATH_SEPARATOR@
 AMTAR = @AMTAR@
 AWK = @AWK@
+SED = @SED@
 CC = @CC@
 CPP2HTML = @CPP2HTML@
 CXX = @CXX@
@@ -173,7 +174,7 @@
 	$(mkinstalldirs) $(DESTDIR)$(gengetoptdocdir)
 	@list='$(gengetoptdoc_DATA)'; for p in $$list; do \
 	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
-	  f="`echo $$p | sed -e 's|^.*/||'`"; \
+	  f="`echo $$p | $(SED) -e 's|^.*/||'`"; \
 	  echo " $(gengetoptdocDATA_INSTALL) $$d$$p $(DESTDIR)$(gengetoptdocdir)/$$f"; \
 	  $(gengetoptdocDATA_INSTALL) $$d$$p $(DESTDIR)$(gengetoptdocdir)/$$f; \
 	done
@@ -181,7 +182,7 @@
 uninstall-gengetoptdocDATA:
 	@$(NORMAL_UNINSTALL)
 	@list='$(gengetoptdoc_DATA)'; for p in $$list; do \
-	  f="`echo $$p | sed -e 's|^.*/||'`"; \
+	  f="`echo $$p | $(SED) -e 's|^.*/||'`"; \
 	  echo " rm -f $(DESTDIR)$(gengetoptdocdir)/$$f"; \
 	  rm -f $(DESTDIR)$(gengetoptdocdir)/$$f; \
 	done
@@ -195,7 +196,7 @@
 $(RECURSIVE_TARGETS):
 	@set fnord $$MAKEFLAGS; amf=$$2; \
 	dot_seen=no; \
-	target=`echo $@ | sed s/-recursive//`; \
+	target=`echo $@ | $(SED) s/-recursive//`; \
 	list='$(SUBDIRS)'; for subdir in $$list; do \
 	  echo "Making $$target in $$subdir"; \
 	  if test "$$subdir" = "."; then \
@@ -225,7 +226,7 @@
 	  fi; \
 	done; \
 	rev="$$rev ."; \
-	target=`echo $@ | sed s/-recursive//`; \
+	target=`echo $@ | $(SED) s/-recursive//`; \
 	for subdir in $$rev; do \
 	  echo "Making $$target in $$subdir"; \
 	  if test "$$subdir" = "."; then \
@@ -300,7 +301,7 @@
 	$(mkinstalldirs) $(distdir)/. $(distdir)/doc $(distdir)/tests
 	@list='$(DISTFILES)'; for file in $$list; do \
 	  if test -f $$file || test -d $$file; then d=.; else d=$(srcdir); fi; \
-	  dir=`echo "$$file" | sed -e 's,/[^/]*$$,,'`; \
+	  dir=`echo "$$file" | $(SED) -e 's,/[^/]*$$,,'`; \
 	  if test "$$dir" != "$$file" && test "$$dir" != "."; then \
 	    dir="/$$dir"; \
 	    $(mkinstalldirs) "$(distdir)$$dir"; \
@@ -373,7 +374,7 @@
 	  && $(MAKE) $(AM_MAKEFLAGS) distcleancheck
 	$(am__remove_distdir)
 	@echo "$(distdir).tar.gz is ready for distribution" | \
-	  sed 'h;s/./=/g;p;x;p;x'
+	  $(SED) 'h;s/./=/g;p;x;p;x'
 distcleancheck: distclean
 	if test '$(srcdir)' = . ; then \
 	  echo "ERROR: distcleancheck can only run from a VPATH build" ; \
diff -ruN gengetopt-2.8.orig/configure gengetopt-2.8/configure
--- gengetopt-2.8.orig/configure	2002-12-31 05:34:22.000000000 -0700
+++ gengetopt-2.8/configure	2003-01-02 16:15:35.000000000 -0700
@@ -59,6 +59,11 @@
     { $as_unset LC_MESSAGES || test "${LC_MESSAGES+set}" != set; } ||
       { LC_MESSAGES=C; export LC_MESSAGES; }
 
+if test -f /usr/bin/sed; then
+  SED=/usr/bin/sed
+else
+  SED=sed
+fi
 
 # Name of the executable.
 as_me=`(basename "$0") 2>/dev/null ||
@@ -67,7 +72,7 @@
 	 X"$0" : 'X\(/\)$' \| \
 	 .     : '\(.\)' 2>/dev/null ||
 echo X/"$0" |
-    sed '/^.*\/\([^/][^/]*\)\/*$/{ s//\1/; q; }
+    $SED '/^.*\/\([^/][^/]*\)\/*$/{ s//\1/; q; }
   	  /^X\/\(\/\/\)$/{ s//\1/; q; }
   	  /^X\/\(\/\).*/{ s//\1/; q; }
   	  s/.*/./; q'`
@@ -156,8 +161,8 @@
   # substitution so that $LINENO is not a special case at line end.
   # (Raja R Harinath suggested sed '=', and Paul Eggert wrote the
   # second 'sed' script.  Blame Lee E. McMahon for sed's syntax.  :-)
-  sed '=' <$as_myself |
-    sed '
+  $SED '=' <$as_myself |
+    $SED '
       N
       s,$,-,
       : loop
@@ -213,10 +218,10 @@
 as_executable_p="test -f"
 
 # Sed expression to map a string onto a valid CPP name.
-as_tr_cpp="sed y%*$as_cr_letters%P$as_cr_LETTERS%;s%[^_$as_cr_alnum]%_%g"
+as_tr_cpp="$SED y%*$as_cr_letters%P$as_cr_LETTERS%;s%[^_$as_cr_alnum]%_%g"
 
 # Sed expression to map a string onto a valid variable name.
-as_tr_sh="sed y%*+%pp%;s%[^_$as_cr_alnum]%_%g"
+as_tr_sh="$SED y%*+%pp%;s%[^_$as_cr_alnum]%_%g"
 
 
 # IFS
@@ -232,7 +237,7 @@
 # Name of the host.
 # hostname on some systems (SVR3.2, Linux) returns a bogus exit status,
 # so uname gets run too.
-ac_hostname=`(hostname || uname -n) 2>/dev/null | sed 1q`
+ac_hostname=`(hostname || uname -n) 2>/dev/null | $SED 1q`
 
 exec 6>&1
 
@@ -383,7 +388,7 @@
     expr "x$ac_feature" : ".*[^-_$as_cr_alnum]" >/dev/null &&
       { echo "$as_me: error: invalid feature name: $ac_feature" >&2
    { (exit 1); exit 1; }; }
-    ac_feature=`echo $ac_feature | sed 's/-/_/g'`
+    ac_feature=`echo $ac_feature | $SED 's/-/_/g'`
     eval "enable_$ac_feature=no" ;;
 
   -enable-* | --enable-*)
@@ -392,9 +397,9 @@
     expr "x$ac_feature" : ".*[^-_$as_cr_alnum]" >/dev/null &&
       { echo "$as_me: error: invalid feature name: $ac_feature" >&2
    { (exit 1); exit 1; }; }
-    ac_feature=`echo $ac_feature | sed 's/-/_/g'`
+    ac_feature=`echo $ac_feature | $SED 's/-/_/g'`
     case $ac_option in
-      *=*) ac_optarg=`echo "$ac_optarg" | sed "s/'/'\\\\\\\\''/g"`;;
+      *=*) ac_optarg=`echo "$ac_optarg" | $SED "s/'/'\\\\\\\\''/g"`;;
       *) ac_optarg=yes ;;
     esac
     eval "enable_$ac_feature='$ac_optarg'" ;;
@@ -574,9 +579,9 @@
     expr "x$ac_package" : ".*[^-_$as_cr_alnum]" >/dev/null &&
       { echo "$as_me: error: invalid package name: $ac_package" >&2
    { (exit 1); exit 1; }; }
-    ac_package=`echo $ac_package| sed 's/-/_/g'`
+    ac_package=`echo $ac_package| $SED 's/-/_/g'`
     case $ac_option in
-      *=*) ac_optarg=`echo "$ac_optarg" | sed "s/'/'\\\\\\\\''/g"`;;
+      *=*) ac_optarg=`echo "$ac_optarg" | $SED "s/'/'\\\\\\\\''/g"`;;
       *) ac_optarg=yes ;;
     esac
     eval "with_$ac_package='$ac_optarg'" ;;
@@ -587,7 +592,7 @@
     expr "x$ac_package" : ".*[^-_$as_cr_alnum]" >/dev/null &&
       { echo "$as_me: error: invalid package name: $ac_package" >&2
    { (exit 1); exit 1; }; }
-    ac_package=`echo $ac_package | sed 's/-/_/g'`
+    ac_package=`echo $ac_package | $SED 's/-/_/g'`
     eval "with_$ac_package=no" ;;
 
   --x)
@@ -619,7 +624,7 @@
     expr "x$ac_envvar" : ".*[^_$as_cr_alnum]" >/dev/null &&
       { echo "$as_me: error: invalid variable name: $ac_envvar" >&2
    { (exit 1); exit 1; }; }
-    ac_optarg=`echo "$ac_optarg" | sed "s/'/'\\\\\\\\''/g"`
+    ac_optarg=`echo "$ac_optarg" | $SED "s/'/'\\\\\\\\''/g"`
     eval "$ac_envvar='$ac_optarg'"
     export $ac_envvar ;;
 
@@ -635,7 +640,7 @@
 done
 
 if test -n "$ac_prev"; then
-  ac_option=--`echo $ac_prev | sed 's/_/-/g'`
+  ac_option=--`echo $ac_prev | $SED 's/_/-/g'`
   { echo "$as_me: error: missing argument to $ac_option" >&2
    { (exit 1); exit 1; }; }
 fi
@@ -698,7 +703,7 @@
          X"$0" : 'X\(/\)' \| \
          .     : '\(.\)' 2>/dev/null ||
 echo X"$0" |
-    sed '/^X\(.*[^/]\)\/\/*[^/][^/]*\/*$/{ s//\1/; q; }
+    $SED '/^X\(.*[^/]\)\/\/*[^/][^/]*\/*$/{ s//\1/; q; }
   	  /^X\(\/\/\)[^/].*/{ s//\1/; q; }
   	  /^X\(\/\/\)$/{ s//\1/; q; }
   	  /^X\(\/\).*/{ s//\1/; q; }
@@ -719,7 +724,7 @@
    { (exit 1); exit 1; }; }
   fi
 fi
-srcdir=`echo "$srcdir" | sed 's%\([^\\/]\)[\\/]*$%\1%'`
+srcdir=`echo "$srcdir" | $SED 's%\([^\\/]\)[\\/]*$%\1%'`
 ac_env_build_alias_set=${build_alias+set}
 ac_env_build_alias_value=$build_alias
 ac_cv_env_build_alias_set=${build_alias+set}
@@ -864,9 +869,9 @@
     ac_builddir=.
 
 if test "$ac_dir" != .; then
-  ac_dir_suffix=/`echo "$ac_dir" | sed 's,^\.[\\/],,'`
+  ac_dir_suffix=/`echo "$ac_dir" | $SED 's,^\.[\\/],,'`
   # A "../" for each directory in $ac_dir_suffix.
-  ac_top_builddir=`echo "$ac_dir_suffix" | sed 's,/[^\\/]*,../,g'`
+  ac_top_builddir=`echo "$ac_dir_suffix" | $SED 's,/[^\\/]*,../,g'`
 else
   ac_dir_suffix= ac_top_builddir=
 fi
@@ -877,7 +882,7 @@
     if test -z "$ac_top_builddir"; then
        ac_top_srcdir=.
     else
-       ac_top_srcdir=`echo $ac_top_builddir | sed 's,/$,,'`
+       ac_top_srcdir=`echo $ac_top_builddir | $SED 's,/$,,'`
     fi ;;
   [\\/]* | ?:[\\/]* )  # Absolute path.
     ac_srcdir=$srcdir$ac_dir_suffix;
@@ -940,7 +945,7 @@
 ## Platform. ##
 ## --------- ##
 
-hostname = `(hostname || uname -n) 2>/dev/null | sed 1q`
+hostname = `(hostname || uname -n) 2>/dev/null | $SED 1q`
 uname -m = `(uname -m) 2>/dev/null || echo unknown`
 uname -r = `(uname -r) 2>/dev/null || echo unknown`
 uname -s = `(uname -s) 2>/dev/null || echo unknown`
@@ -993,7 +998,7 @@
   | --no-recurs | --no-recur | --no-recu | --no-rec | --no-re | --no-r)
     continue ;;
   *" "*|*"	"*|*[\[\]\~\#\$\^\&\*\(\)\{\}\\\|\;\<\>\?\"\']*)
-    ac_arg=`echo "$ac_arg" | sed "s/'/'\\\\\\\\''/g"` ;;
+    ac_arg=`echo "$ac_arg" | $SED "s/'/'\\\\\\\\''/g"` ;;
   esac
   case " $ac_configure_args " in
     *" '$ac_arg' "*) ;; # Avoid dups.  Use of quotes ensures accuracy.
@@ -1023,12 +1028,12 @@
   (set) 2>&1 |
     case `(ac_space='"'"' '"'"'; set | grep ac_space) 2>&1` in
     *ac_space=\ *)
-      sed -n \
+      $SED -n \
         "s/'"'"'/'"'"'\\\\'"'"''"'"'/g;
     	  s/^\\([_$as_cr_alnum]*_cv_[_$as_cr_alnum]*\\)=\\(.*\\)/\\1='"'"'\\2'"'"'/p"
       ;;
     *)
-      sed -n \
+      $SED -n \
         "s/^\\([_$as_cr_alnum]*_cv_[_$as_cr_alnum]*\\)=\\(.*\\)/\\1=\\2/p"
       ;;
     esac;
@@ -1041,7 +1046,7 @@
 ## ----------- ##
 _ASBOX
       echo
-      sed "/^$/d" confdefs.h
+      $SED "/^$/d" confdefs.h
       echo
     fi
     test "$ac_signal" != 0 &&
@@ -1102,7 +1107,7 @@
   if test -r "$ac_site_file"; then
     { echo "$as_me:$LINENO: loading site script $ac_site_file" >&5
 echo "$as_me: loading site script $ac_site_file" >&6;}
-    sed 's/^/| /' "$ac_site_file" >&5
+    $SED 's/^/| /' "$ac_site_file" >&5
     . "$ac_site_file"
   fi
 done
@@ -1128,7 +1133,7 @@
 # value.
 ac_cache_corrupted=false
 for ac_var in `(set) 2>&1 |
-               sed -n 's/^ac_env_\([a-zA-Z_0-9]*\)_set=.*/\1/p'`; do
+               $SED -n 's/^ac_env_\([a-zA-Z_0-9]*\)_set=.*/\1/p'`; do
   eval ac_old_set=\$ac_cv_env_${ac_var}_set
   eval ac_new_set=\$ac_env_${ac_var}_set
   eval ac_old_val="\$ac_cv_env_${ac_var}_value"
@@ -1158,7 +1163,7 @@
   if test "$ac_new_set" = set; then
     case $ac_new_val in
     *" "*|*"	"*|*[\[\]\~\#\$\^\&\*\(\)\{\}\\\|\;\<\>\?\"\']*)
-      ac_arg=$ac_var=`echo "$ac_new_val" | sed "s/'/'\\\\\\\\''/g"` ;;
+      ac_arg=$ac_var=`echo "$ac_new_val" | $SED "s/'/'\\\\\\\\''/g"` ;;
     *) ac_arg=$ac_var=$ac_new_val ;;
     esac
     case " $ac_configure_args " in
@@ -1364,7 +1369,7 @@
 cat <<\_ACEOF >conftest.sed
 s/[\\$]/&&/g;s/;s,x,x,$//
 _ACEOF
-program_transform_name=`echo $program_transform_name | sed -f conftest.sed`
+program_transform_name=`echo $program_transform_name | $SED -f conftest.sed`
 rm conftest.sed
 
 
@@ -1423,7 +1428,7 @@
 
 echo "$as_me:$LINENO: checking whether ${MAKE-make} sets \${MAKE}" >&5
 echo $ECHO_N "checking whether ${MAKE-make} sets \${MAKE}... $ECHO_C" >&6
-set dummy ${MAKE-make}; ac_make=`echo "$2" | sed 'y,./+-,__p_,'`
+set dummy ${MAKE-make}; ac_make=`echo "$2" | $SED 'y,./+-,__p_,'`
 if eval "test \"\${ac_cv_prog_make_${ac_make}_set+set}\" = set"; then
   echo $ECHO_N "(cached) $ECHO_C" >&6
 else
@@ -1953,7 +1958,7 @@
 # of exeext.
 echo "$as_me:$LINENO: checking for C compiler default output" >&5
 echo $ECHO_N "checking for C compiler default output... $ECHO_C" >&6
-ac_link_default=`echo "$ac_link" | sed 's/ -o *conftest[^ ]*//'`
+ac_link_default=`echo "$ac_link" | $SED 's/ -o *conftest[^ ]*//'`
 if { (eval echo "$as_me:$LINENO: \"$ac_link_default\"") >&5
   (eval $ac_link_default) 2>&5
   ac_status=$?
@@ -2445,7 +2450,7 @@
 
   am_cv_CC_dependencies_compiler_type=none
   if test "$am_compiler_list" = ""; then
-     am_compiler_list=`sed -n 's/^#*\([a-zA-Z0-9]*\))$/\1/p' < ./depcomp`
+     am_compiler_list=`$SED -n 's/^#*\([a-zA-Z0-9]*\))$/\1/p' < ./depcomp`
   fi
   for depmode in $am_compiler_list; do
     # We need to recreate these files for each test, as the compiler may
@@ -3517,7 +3522,7 @@
 
   am_cv_CXX_dependencies_compiler_type=none
   if test "$am_compiler_list" = ""; then
-     am_compiler_list=`sed -n 's/^#*\([a-zA-Z0-9]*\))$/\1/p' < ./depcomp`
+     am_compiler_list=`$SED -n 's/^#*\([a-zA-Z0-9]*\))$/\1/p' < ./depcomp`
   fi
   for depmode in $am_compiler_list; do
     # We need to recreate these files for each test, as the compiler may
@@ -5269,18 +5274,18 @@
     *ac_space=\ *)
       # `set' does not quote correctly, so add quotes (double-quote
       # substitution turns \\\\ into \\, and sed turns \\ into \).
-      sed -n \
+      $SED -n \
         "s/'/'\\\\''/g;
     	  s/^\\([_$as_cr_alnum]*_cv_[_$as_cr_alnum]*\\)=\\(.*\\)/\\1='\\2'/p"
       ;;
     *)
       # `set' quotes correctly as required by POSIX, so do not add quotes.
-      sed -n \
+      $SED -n \
         "s/^\\([_$as_cr_alnum]*_cv_[_$as_cr_alnum]*\\)=\\(.*\\)/\\1=\\2/p"
       ;;
     esac;
 } |
-  sed '
+  $SED '
      t clear
      : clear
      s/^\([^=]*\)=\(.*[{}].*\)$/test "${\1+set}" = set || &/
@@ -5417,6 +5422,11 @@
     { $as_unset LC_MESSAGES || test "${LC_MESSAGES+set}" != set; } ||
       { LC_MESSAGES=C; export LC_MESSAGES; }
 
+if test -f /usr/bin/sed; then
+  SED=/usr/bin/sed
+else
+  SED=sed
+fi
 
 # Name of the executable.
 as_me=`(basename "$0") 2>/dev/null ||
@@ -5425,7 +5435,7 @@
 	 X"$0" : 'X\(/\)$' \| \
 	 .     : '\(.\)' 2>/dev/null ||
 echo X/"$0" |
-    sed '/^.*\/\([^/][^/]*\)\/*$/{ s//\1/; q; }
+    $SED '/^.*\/\([^/][^/]*\)\/*$/{ s//\1/; q; }
   	  /^X\/\(\/\/\)$/{ s//\1/; q; }
   	  /^X\/\(\/\).*/{ s//\1/; q; }
   	  s/.*/./; q'`
@@ -5515,8 +5525,8 @@
   # substitution so that $LINENO is not a special case at line end.
   # (Raja R Harinath suggested sed '=', and Paul Eggert wrote the
   # second 'sed' script.  Blame Lee E. McMahon for sed's syntax.  :-)
-  sed '=' <$as_myself |
-    sed '
+  $SED '=' <$as_myself |
+    $SED '
       N
       s,$,-,
       : loop
@@ -5573,10 +5583,10 @@
 as_executable_p="test -f"
 
 # Sed expression to map a string onto a valid CPP name.
-as_tr_cpp="sed y%*$as_cr_letters%P$as_cr_LETTERS%;s%[^_$as_cr_alnum]%_%g"
+as_tr_cpp="$SED y%*$as_cr_letters%P$as_cr_LETTERS%;s%[^_$as_cr_alnum]%_%g"
 
 # Sed expression to map a string onto a valid variable name.
-as_tr_sh="sed y%*+%pp%;s%[^_$as_cr_alnum]%_%g"
+as_tr_sh="$SED y%*+%pp%;s%[^_$as_cr_alnum]%_%g"
 
 
 # IFS
@@ -5596,7 +5606,7 @@
 exec 5>>config.log
 {
   echo
-  sed 'h;s/./-/g;s/^.../## /;s/...$/ ##/;p;x;p;x' <<_ASBOX
+  $SED 'h;s/./-/g;s/^.../## /;s/...$/ ##/;p;x;p;x' <<_ASBOX
 ## Running $as_me. ##
 _ASBOX
 } >&5
@@ -5612,7 +5622,7 @@
   $ $0 $@
 
 _CSEOF
-echo "on `(hostname || uname -n) 2>/dev/null | sed 1q`" >&5
+echo "on `(hostname || uname -n) 2>/dev/null | $SED 1q`" >&5
 echo >&5
 _ACEOF
 
@@ -5666,7 +5676,7 @@
 ac_cs_version="\\
 config.status
 configured by $0, generated by GNU Autoconf 2.53,
-  with options \\"`echo "$ac_configure_args" | sed 's/[\\""\`\$]/\\\\&/g'`\\"
+  with options \\"`echo "$ac_configure_args" | $SED 's/[\\""\`\$]/\\\\&/g'`\\"
 
 Copyright 1992, 1993, 1994, 1995, 1996, 1998, 1999, 2000, 2001
 Free Software Foundation, Inc.
@@ -5827,7 +5837,7 @@
 # This happens for instance when ./config.status config.h
 if test -n "\$CONFIG_FILES"; then
   # Protect against being on the right side of a sed subst in config.status.
-  sed 's/,@/@@/; s/@,/@@/; s/,;t t\$/@;t t/; /@;t t\$/s/[\\\\&,]/\\\\&/g;
+  $SED 's/,@/@@/; s/@,/@@/; s/,;t t\$/@;t t/; /@;t t\$/s/[\\\\&,]/\\\\&/g;
    s/@@/,@/; s/@@/@,/; s/@;t t\$/,;t t/' >\$tmp/subs.sed <<\\CEOF
 s,@SHELL@,$SHELL,;t t
 s,@PATH_SEPARATOR@,$PATH_SEPARATOR,;t t
@@ -5875,6 +5885,7 @@
 s,@ac_ct_STRIP@,$ac_ct_STRIP,;t t
 s,@INSTALL_STRIP_PROGRAM@,$INSTALL_STRIP_PROGRAM,;t t
 s,@AWK@,$AWK,;t t
+s,@SED@,$SED,;t t
 s,@SET_MAKE@,$SET_MAKE,;t t
 s,@gengetoptdocdir@,$gengetoptdocdir,;t t
 s,@gengetoptexamplesdir@,$gengetoptexamplesdir,;t t
@@ -5932,9 +5943,9 @@
   ac_sed_cmds=
   while $ac_more_lines; do
     if test $ac_beg -gt 1; then
-      sed "1,${ac_beg}d; ${ac_end}q" $tmp/subs.sed >$tmp/subs.frag
+      $SED "1,${ac_beg}d; ${ac_end}q" $tmp/subs.sed >$tmp/subs.frag
     else
-      sed "${ac_end}q" $tmp/subs.sed >$tmp/subs.frag
+      $SED "${ac_end}q" $tmp/subs.sed >$tmp/subs.frag
     fi
     if test ! -s $tmp/subs.frag; then
       ac_more_lines=false
@@ -5946,9 +5957,9 @@
       (echo ':t
   /@[a-zA-Z_][a-zA-Z_0-9]*@/!b' && cat $tmp/subs.frag) >$tmp/subs-$ac_sed_frag.sed
       if test -z "$ac_sed_cmds"; then
-  	ac_sed_cmds="sed -f $tmp/subs-$ac_sed_frag.sed"
+  	ac_sed_cmds="$SED -f $tmp/subs-$ac_sed_frag.sed"
       else
-  	ac_sed_cmds="$ac_sed_cmds | sed -f $tmp/subs-$ac_sed_frag.sed"
+  	ac_sed_cmds="$ac_sed_cmds | $SED -f $tmp/subs-$ac_sed_frag.sed"
       fi
       ac_sed_frag=`expr $ac_sed_frag + 1`
       ac_beg=$ac_end
@@ -5967,10 +5978,10 @@
   case $ac_file in
   - | *:- | *:-:* ) # input from stdin
         cat >$tmp/stdin
-        ac_file_in=`echo "$ac_file" | sed 's,[^:]*:,,'`
-        ac_file=`echo "$ac_file" | sed 's,:.*,,'` ;;
-  *:* ) ac_file_in=`echo "$ac_file" | sed 's,[^:]*:,,'`
-        ac_file=`echo "$ac_file" | sed 's,:.*,,'` ;;
+        ac_file_in=`echo "$ac_file" | $SED 's,[^:]*:,,'`
+        ac_file=`echo "$ac_file" | $SED 's,:.*,,'` ;;
+  *:* ) ac_file_in=`echo "$ac_file" | $SED 's,[^:]*:,,'`
+        ac_file=`echo "$ac_file" | $SED 's,:.*,,'` ;;
   * )   ac_file_in=$ac_file.in ;;
   esac
 
@@ -5982,7 +5993,7 @@
          X"$ac_file" : 'X\(/\)' \| \
          .     : '\(.\)' 2>/dev/null ||
 echo X"$ac_file" |
-    sed '/^X\(.*[^/]\)\/\/*[^/][^/]*\/*$/{ s//\1/; q; }
+    $SED '/^X\(.*[^/]\)\/\/*[^/][^/]*\/*$/{ s//\1/; q; }
   	  /^X\(\/\/\)[^/].*/{ s//\1/; q; }
   	  /^X\(\/\/\)$/{ s//\1/; q; }
   	  /^X\(\/\).*/{ s//\1/; q; }
@@ -6010,9 +6021,9 @@
   ac_builddir=.
 
 if test "$ac_dir" != .; then
-  ac_dir_suffix=/`echo "$ac_dir" | sed 's,^\.[\\/],,'`
+  ac_dir_suffix=/`echo "$ac_dir" | $SED 's,^\.[\\/],,'`
   # A "../" for each directory in $ac_dir_suffix.
-  ac_top_builddir=`echo "$ac_dir_suffix" | sed 's,/[^\\/]*,../,g'`
+  ac_top_builddir=`echo "$ac_dir_suffix" | $SED 's,/[^\\/]*,../,g'`
 else
   ac_dir_suffix= ac_top_builddir=
 fi
@@ -6023,7 +6034,7 @@
     if test -z "$ac_top_builddir"; then
        ac_top_srcdir=.
     else
-       ac_top_srcdir=`echo $ac_top_builddir | sed 's,/$,,'`
+       ac_top_srcdir=`echo $ac_top_builddir | $SED 's,/$,,'`
     fi ;;
   [\\/]* | ?:[\\/]* )  # Absolute path.
     ac_srcdir=$srcdir$ac_dir_suffix;
@@ -6059,7 +6070,7 @@
     configure_input="$ac_file.  "
   fi
   configure_input=$configure_input"Generated from `echo $ac_file_in |
-                                     sed 's,.*/,,'` by configure."
+                                     $SED 's,.*/,,'` by configure."
 
   # First look for the input files in the build tree, otherwise in the
   # src tree.
@@ -6090,7 +6101,7 @@
     done` || { (exit 1); exit 1; }
 _ACEOF
 cat >>$CONFIG_STATUS <<_ACEOF
-  sed "$ac_vpsub
+  $SED "$ac_vpsub
 $extrasub
 _ACEOF
 cat >>$CONFIG_STATUS <<\_ACEOF
@@ -6142,10 +6153,10 @@
   case $ac_file in
   - | *:- | *:-:* ) # input from stdin
         cat >$tmp/stdin
-        ac_file_in=`echo "$ac_file" | sed 's,[^:]*:,,'`
-        ac_file=`echo "$ac_file" | sed 's,:.*,,'` ;;
-  *:* ) ac_file_in=`echo "$ac_file" | sed 's,[^:]*:,,'`
-        ac_file=`echo "$ac_file" | sed 's,:.*,,'` ;;
+        ac_file_in=`echo "$ac_file" | $SED 's,[^:]*:,,'`
+        ac_file=`echo "$ac_file" | $SED 's,:.*,,'` ;;
+  *:* ) ac_file_in=`echo "$ac_file" | $SED 's,[^:]*:,,'`
+        ac_file=`echo "$ac_file" | $SED 's,:.*,,'` ;;
   * )   ac_file_in=$ac_file.in ;;
   esac
 
@@ -6180,7 +6191,7 @@
       esac
     done` || { (exit 1); exit 1; }
   # Remove the trailing spaces.
-  sed 's/[ 	]*$//' $ac_file_inputs >$tmp/in
+  $SED 's/[ 	]*$//' $ac_file_inputs >$tmp/in
 
 _ACEOF
 
@@ -6211,8 +6222,8 @@
 # If some macros were called several times there might be several times
 # the same #defines, which is useless.  Nevertheless, we may not want to
 # sort them, since we want the *last* AC-DEFINE to be honored.
-uniq confdefs.h | sed -n -f confdef2sed.sed >conftest.defines
-sed 's/ac_d/ac_u/g' conftest.defines >conftest.undefs
+uniq confdefs.h | $SED -n -f confdef2sed.sed >conftest.defines
+$SED 's/ac_d/ac_u/g' conftest.defines >conftest.undefs
 rm -f confdef2sed.sed
 
 # This sed command replaces #undef with comments.  This is necessary, for
@@ -6238,13 +6249,13 @@
   # Work around the forget-to-reset-the-flag bug.
   echo 't clr' >>$CONFIG_STATUS
   echo ': clr' >>$CONFIG_STATUS
-  sed ${ac_max_here_lines}q conftest.defines >>$CONFIG_STATUS
+  $SED ${ac_max_here_lines}q conftest.defines >>$CONFIG_STATUS
   echo 'CEOF
-  sed -f $tmp/defines.sed $tmp/in >$tmp/out
+  $SED -f $tmp/defines.sed $tmp/in >$tmp/out
   rm -f $tmp/in
   mv $tmp/out $tmp/in
 ' >>$CONFIG_STATUS
-  sed 1,${ac_max_here_lines}d conftest.defines >conftest.tail
+  $SED 1,${ac_max_here_lines}d conftest.defines >conftest.tail
   rm -f conftest.defines
   mv conftest.tail conftest.defines
 done
@@ -6265,13 +6276,13 @@
   # Work around the forget-to-reset-the-flag bug.
   echo 't clr' >>$CONFIG_STATUS
   echo ': clr' >>$CONFIG_STATUS
-  sed ${ac_max_here_lines}q conftest.undefs >>$CONFIG_STATUS
+  $SED ${ac_max_here_lines}q conftest.undefs >>$CONFIG_STATUS
   echo 'CEOF
-  sed -f $tmp/undefs.sed $tmp/in >$tmp/out
+  $SED -f $tmp/undefs.sed $tmp/in >$tmp/out
   rm -f $tmp/in
   mv $tmp/out $tmp/in
 ' >>$CONFIG_STATUS
-  sed 1,${ac_max_here_lines}d conftest.undefs >conftest.tail
+  $SED 1,${ac_max_here_lines}d conftest.undefs >conftest.tail
   rm -f conftest.undefs
   mv conftest.tail conftest.undefs
 done
@@ -6300,7 +6311,7 @@
          X"$ac_file" : 'X\(/\)' \| \
          .     : '\(.\)' 2>/dev/null ||
 echo X"$ac_file" |
-    sed '/^X\(.*[^/]\)\/\/*[^/][^/]*\/*$/{ s//\1/; q; }
+    $SED '/^X\(.*[^/]\)\/\/*[^/][^/]*\/*$/{ s//\1/; q; }
   	  /^X\(\/\/\)[^/].*/{ s//\1/; q; }
   	  /^X\(\/\/\)$/{ s//\1/; q; }
   	  /^X\(\/\).*/{ s//\1/; q; }
@@ -6346,8 +6357,8 @@
 # CONFIG_COMMANDS section.
 #
 for ac_file in : $CONFIG_COMMANDS; do test "x$ac_file" = x: && continue
-  ac_dest=`echo "$ac_file" | sed 's,:.*,,'`
-  ac_source=`echo "$ac_file" | sed 's,[^:]*:,,'`
+  ac_dest=`echo "$ac_file" | $SED 's,:.*,,'`
+  ac_source=`echo "$ac_file" | $SED 's,[^:]*:,,'`
   ac_dir=`(dirname "$ac_dest") 2>/dev/null ||
 $as_expr X"$ac_dest" : 'X\(.*[^/]\)//*[^/][^/]*/*$' \| \
          X"$ac_dest" : 'X\(//\)[^/]' \| \
@@ -6355,7 +6366,7 @@
          X"$ac_dest" : 'X\(/\)' \| \
          .     : '\(.\)' 2>/dev/null ||
 echo X"$ac_dest" |
-    sed '/^X\(.*[^/]\)\/\/*[^/][^/]*\/*$/{ s//\1/; q; }
+    $SED '/^X\(.*[^/]\)\/\/*[^/][^/]*\/*$/{ s//\1/; q; }
   	  /^X\(\/\/\)[^/].*/{ s//\1/; q; }
   	  /^X\(\/\/\)$/{ s//\1/; q; }
   	  /^X\(\/\).*/{ s//\1/; q; }
@@ -6363,9 +6374,9 @@
   ac_builddir=.
 
 if test "$ac_dir" != .; then
-  ac_dir_suffix=/`echo "$ac_dir" | sed 's,^\.[\\/],,'`
+  ac_dir_suffix=/`echo "$ac_dir" | $SED 's,^\.[\\/],,'`
   # A "../" for each directory in $ac_dir_suffix.
-  ac_top_builddir=`echo "$ac_dir_suffix" | sed 's,/[^\\/]*,../,g'`
+  ac_top_builddir=`echo "$ac_dir_suffix" | $SED 's,/[^\\/]*,../,g'`
 else
   ac_dir_suffix= ac_top_builddir=
 fi
@@ -6376,7 +6387,7 @@
     if test -z "$ac_top_builddir"; then
        ac_top_srcdir=.
     else
-       ac_top_srcdir=`echo $ac_top_builddir | sed 's,/$,,'`
+       ac_top_srcdir=`echo $ac_top_builddir | $SED 's,/$,,'`
     fi ;;
   [\\/]* | ?:[\\/]* )  # Absolute path.
     ac_srcdir=$srcdir$ac_dir_suffix;
@@ -6398,7 +6409,7 @@
   case $ac_dest in
     depfiles ) test x"$AMDEP_TRUE" != x"" || for mf in $CONFIG_FILES; do
   # Strip MF so we end up with the name of the file.
-  mf=`echo "$mf" | sed -e 's/:.*$//'`
+  mf=`echo "$mf" | $SED -e 's/:.*$//'`
   # Check whether this is an Automake generated Makefile or not.
   # We used to match only the files named `Makefile.in', but
   # some people rename them; so instead we look at the file content.
@@ -6413,7 +6424,7 @@
          X"$mf" : 'X\(/\)' \| \
          .     : '\(.\)' 2>/dev/null ||
 echo X"$mf" |
-    sed '/^X\(.*[^/]\)\/\/*[^/][^/]*\/*$/{ s//\1/; q; }
+    $SED '/^X\(.*[^/]\)\/\/*[^/][^/]*\/*$/{ s//\1/; q; }
   	  /^X\(\/\/\)[^/].*/{ s//\1/; q; }
   	  /^X\(\/\/\)$/{ s//\1/; q; }
   	  /^X\(\/\).*/{ s//\1/; q; }
@@ -6424,14 +6435,14 @@
   grep '^DEP_FILES *= *[^ #]' < "$mf" > /dev/null || continue
   # Extract the definition of DEP_FILES from the Makefile without
   # running `make'.
-  DEPDIR=`sed -n -e '/^DEPDIR = / s///p' < "$mf"`
+  DEPDIR=`$SED -n -e '/^DEPDIR = / s///p' < "$mf"`
   test -z "$DEPDIR" && continue
   # When using ansi2knr, U may be empty or an underscore; expand it
-  U=`sed -n -e '/^U = / s///p' < "$mf"`
+  U=`$SED -n -e '/^U = / s///p' < "$mf"`
   test -d "$dirpart/$DEPDIR" || mkdir "$dirpart/$DEPDIR"
   # We invoke sed twice because it is the simplest approach to
   # changing $(DEPDIR) to its actual value in the expansion.
-  for file in `sed -n -e '
+  for file in `$SED -n -e '
     /^DEP_FILES = .*\\\\$/ {
       s/^DEP_FILES = //
       :loop
@@ -6442,7 +6453,7 @@
       p
     }
     /^DEP_FILES = / s/^DEP_FILES = //p' < "$mf" | \
-       sed -e 's/\$(DEPDIR)/'"$DEPDIR"'/g' -e 's/\$U/'"$U"'/g'`; do
+       $SED -e 's/\$(DEPDIR)/'"$DEPDIR"'/g' -e 's/\$U/'"$U"'/g'`; do
     # Make sure the directory exists.
     test -f "$dirpart/$file" && continue
     fdir=`(dirname "$file") 2>/dev/null ||
@@ -6452,7 +6463,7 @@
          X"$file" : 'X\(/\)' \| \
          .     : '\(.\)' 2>/dev/null ||
 echo X"$file" |
-    sed '/^X\(.*[^/]\)\/\/*[^/][^/]*\/*$/{ s//\1/; q; }
+    $SED '/^X\(.*[^/]\)\/\/*[^/][^/]*\/*$/{ s//\1/; q; }
   	  /^X\(\/\/\)[^/].*/{ s//\1/; q; }
   	  /^X\(\/\/\)$/{ s//\1/; q; }
   	  /^X\(\/\).*/{ s//\1/; q; }
diff -ruN gengetopt-2.8.orig/doc/Makefile.in gengetopt-2.8/doc/Makefile.in
--- gengetopt-2.8.orig/doc/Makefile.in	2002-12-31 05:36:26.000000000 -0700
+++ gengetopt-2.8/doc/Makefile.in	2003-01-02 16:30:56.000000000 -0700
@@ -83,6 +83,7 @@
 PATH_SEPARATOR = @PATH_SEPARATOR@
 AMTAR = @AMTAR@
 AWK = @AWK@
+SED = @SED@
 CC = @CC@
 CPP2HTML = @CPP2HTML@
 CXX = @CXX@
@@ -304,14 +305,14 @@
 	for i in $$list; do \
 	  if test -f $(srcdir)/$$i; then file=$(srcdir)/$$i; \
 	  else file=$$i; fi; \
-	  ext=`echo $$i | sed -e 's/^.*\\.//'`; \
+	  ext=`echo $$i | $(SED) -e 's/^.*\\.//'`; \
 	  case "$$ext" in \
 	    1*) ;; \
 	    *) ext='1' ;; \
 	  esac; \
-	  inst=`echo $$i | sed -e 's/\\.[0-9a-z]*$$//'`; \
-	  inst=`echo $$inst | sed -e 's/^.*\///'`; \
-	  inst=`echo $$inst | sed '$(transform)'`.$$ext; \
+	  inst=`echo $$i | $(SED) -e 's/\\.[0-9a-z]*$$//'`; \
+	  inst=`echo $$inst | $(SED) -e 's/^.*\///'`; \
+	  inst=`echo $$inst | $(SED) '$(transform)'`.$$ext; \
 	  echo " $(INSTALL_DATA) $$file $(DESTDIR)$(man1dir)/$$inst"; \
 	  $(INSTALL_DATA) $$file $(DESTDIR)$(man1dir)/$$inst; \
 	done
@@ -325,10 +326,10 @@
 	  esac; \
 	done; \
 	for i in $$list; do \
-	  ext=`echo $$i | sed -e 's/^.*\\.//'`; \
-	  inst=`echo $$i | sed -e 's/\\.[0-9a-z]*$$//'`; \
-	  inst=`echo $$inst | sed -e 's/^.*\///'`; \
-	  inst=`echo $$inst | sed '$(transform)'`.$$ext; \
+	  ext=`echo $$i | $(SED) -e 's/^.*\\.//'`; \
+	  inst=`echo $$i | $(SED) -e 's/\\.[0-9a-z]*$$//'`; \
+	  inst=`echo $$inst | $(SED) -e 's/^.*\///'`; \
+	  inst=`echo $$inst | $(SED) '$(transform)'`.$$ext; \
 	  echo " rm -f $(DESTDIR)$(man1dir)/$$inst"; \
 	  rm -f $(DESTDIR)$(man1dir)/$$inst; \
 	done
@@ -338,7 +339,7 @@
 	$(mkinstalldirs) $(DESTDIR)$(gengetoptdocdir)
 	@list='$(gengetoptdoc_DATA)'; for p in $$list; do \
 	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
-	  f="`echo $$p | sed -e 's|^.*/||'`"; \
+	  f="`echo $$p | $(SED) -e 's|^.*/||'`"; \
 	  echo " $(gengetoptdocDATA_INSTALL) $$d$$p $(DESTDIR)$(gengetoptdocdir)/$$f"; \
 	  $(gengetoptdocDATA_INSTALL) $$d$$p $(DESTDIR)$(gengetoptdocdir)/$$f; \
 	done
@@ -346,7 +347,7 @@
 uninstall-gengetoptdocDATA:
 	@$(NORMAL_UNINSTALL)
 	@list='$(gengetoptdoc_DATA)'; for p in $$list; do \
-	  f="`echo $$p | sed -e 's|^.*/||'`"; \
+	  f="`echo $$p | $(SED) -e 's|^.*/||'`"; \
 	  echo " rm -f $(DESTDIR)$(gengetoptdocdir)/$$f"; \
 	  rm -f $(DESTDIR)$(gengetoptdocdir)/$$f; \
 	done
@@ -356,7 +357,7 @@
 	$(mkinstalldirs) $(DESTDIR)$(gengetoptexamplesdir)
 	@list='$(gengetoptexamples_DATA)'; for p in $$list; do \
 	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
-	  f="`echo $$p | sed -e 's|^.*/||'`"; \
+	  f="`echo $$p | $(SED) -e 's|^.*/||'`"; \
 	  echo " $(gengetoptexamplesDATA_INSTALL) $$d$$p $(DESTDIR)$(gengetoptexamplesdir)/$$f"; \
 	  $(gengetoptexamplesDATA_INSTALL) $$d$$p $(DESTDIR)$(gengetoptexamplesdir)/$$f; \
 	done
@@ -364,7 +365,7 @@
 uninstall-gengetoptexamplesDATA:
 	@$(NORMAL_UNINSTALL)
 	@list='$(gengetoptexamples_DATA)'; for p in $$list; do \
-	  f="`echo $$p | sed -e 's|^.*/||'`"; \
+	  f="`echo $$p | $(SED) -e 's|^.*/||'`"; \
 	  echo " rm -f $(DESTDIR)$(gengetoptexamplesdir)/$$f"; \
 	  rm -f $(DESTDIR)$(gengetoptexamplesdir)/$$f; \
 	done
@@ -412,7 +413,7 @@
 distdir: $(DISTFILES)
 	@list='$(DISTFILES)'; for file in $$list; do \
 	  if test -f $$file || test -d $$file; then d=.; else d=$(srcdir); fi; \
-	  dir=`echo "$$file" | sed -e 's,/[^/]*$$,,'`; \
+	  dir=`echo "$$file" | $(SED) -e 's,/[^/]*$$,,'`; \
 	  if test "$$dir" != "$$file" && test "$$dir" != "."; then \
 	    dir="/$$dir"; \
 	    $(mkinstalldirs) "$(distdir)$$dir"; \
diff -ruN gengetopt-2.8.orig/src/Makefile.in gengetopt-2.8/src/Makefile.in
--- gengetopt-2.8.orig/src/Makefile.in	2002-12-31 05:34:53.000000000 -0700
+++ gengetopt-2.8/src/Makefile.in	2003-01-02 16:28:24.000000000 -0700
@@ -83,6 +83,7 @@
 PATH_SEPARATOR = @PATH_SEPARATOR@
 AMTAR = @AMTAR@
 AWK = @AWK@
+SED = @SED@
 CC = @CC@
 CPP2HTML = @CPP2HTML@
 CXX = @CXX@
@@ -211,11 +212,11 @@
 	@$(NORMAL_INSTALL)
 	$(mkinstalldirs) $(DESTDIR)$(bindir)
 	@list='$(bin_PROGRAMS)'; for p in $$list; do \
-	  p1=`echo $$p|sed 's/$(EXEEXT)$$//'`; \
+	  p1=`echo $$p|$(SED) 's/$(EXEEXT)$$//'`; \
 	  if test -f $$p \
 	  ; then \
-	    p1=`echo "$$p1" | sed -e 's,^.*/,,'`; \
-	    f=`echo $$p1|sed '$(transform);s/$$/$(EXEEXT)/'`; \
+	    p1=`echo "$$p1" | $(SED) -e 's,^.*/,,'`; \
+	    f=`echo $$p1|$(SED) '$(transform);s/$$/$(EXEEXT)/'`; \
 	   echo " $(INSTALL_PROGRAM_ENV) $(binPROGRAMS_INSTALL) $$p $(DESTDIR)$(bindir)/$$f"; \
 	   $(INSTALL_PROGRAM_ENV) $(binPROGRAMS_INSTALL) $$p $(DESTDIR)$(bindir)/$$f; \
 	  else :; fi; \
@@ -224,8 +225,8 @@
 uninstall-binPROGRAMS:
 	@$(NORMAL_UNINSTALL)
 	@list='$(bin_PROGRAMS)'; for p in $$list; do \
-	  f=`echo $$p|sed 's/$(EXEEXT)$$//;$(transform);s/$$/$(EXEEXT)/'`; \
-	  f=`echo "$$f" | sed -e 's,^.*/,,'`; \
+	  f=`echo $$p|$(SED0 's/$(EXEEXT)$$//;$(transform);s/$$/$(EXEEXT)/'`; \
+	  f=`echo "$$f" | $(SED) -e 's,^.*/,,'`; \
 	  echo " rm -f $(DESTDIR)$(bindir)/$$f"; \
 	  rm -f $(DESTDIR)$(bindir)/$$f; \
 	done
@@ -286,18 +287,18 @@
 
 .ll.cc:
 	$(LEXCOMPILE) `test -f $< || echo '$(srcdir)/'`$<
-	sed '/^#/ s|$(LEX_OUTPUT_ROOT)\.c|$@|' $(LEX_OUTPUT_ROOT).c >$@
+	$(SED) '/^#/ s|$(LEX_OUTPUT_ROOT)\.c|$@|' $(LEX_OUTPUT_ROOT).c >$@
 	rm -f $(LEX_OUTPUT_ROOT).c
 
 .yy.cc:
 	$(YACCCOMPILE) `test -f '$<' || echo '$(srcdir)/'`$<
-	sed '/^#/ s|y\.tab\.c|$@|' y.tab.c >$@
+	$(SED) '/^#/ s|y\.tab\.c|$@|' y.tab.c >$@
 	rm -f y.tab.c
 	if test -f y.tab.h; then \
-	  to=`echo "$*_H" | sed \
+	  to=`echo "$*_H" | $(SED) \
                 -e 'y/abcdefghijklmnopqrstuvwxyz/ABCDEFGHIJKLMNOPQRSTUVWXYZ/' \
                 -e 's/[^ABCDEFGHIJKLMNOPQRSTUVWXYZ]/_/g'`; \
-	  sed "/^#/ s/Y_TAB_H/$$to/g" y.tab.h >$*.ht; \
+	  $(SED) "/^#/ s/Y_TAB_H/$$to/g" y.tab.h >$*.ht; \
 	  rm -f y.tab.h; \
 	  if cmp -s $*.ht $*.h; then \
 	    rm -f $*.ht ;\
@@ -315,7 +316,7 @@
 	$(mkinstalldirs) $(DESTDIR)$(pkgdatadir)
 	@list='$(pkgdata_DATA)'; for p in $$list; do \
 	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
-	  f="`echo $$p | sed -e 's|^.*/||'`"; \
+	  f="`echo $$p | $(SED) -e 's|^.*/||'`"; \
 	  echo " $(pkgdataDATA_INSTALL) $$d$$p $(DESTDIR)$(pkgdatadir)/$$f"; \
 	  $(pkgdataDATA_INSTALL) $$d$$p $(DESTDIR)$(pkgdatadir)/$$f; \
 	done
@@ -323,7 +324,7 @@
 uninstall-pkgdataDATA:
 	@$(NORMAL_UNINSTALL)
 	@list='$(pkgdata_DATA)'; for p in $$list; do \
-	  f="`echo $$p | sed -e 's|^.*/||'`"; \
+	  f="`echo $$p | $(SED) -e 's|^.*/||'`"; \
 	  echo " rm -f $(DESTDIR)$(pkgdatadir)/$$f"; \
 	  rm -f $(DESTDIR)$(pkgdatadir)/$$f; \
 	done
@@ -337,7 +338,7 @@
 $(RECURSIVE_TARGETS):
 	@set fnord $$MAKEFLAGS; amf=$$2; \
 	dot_seen=no; \
-	target=`echo $@ | sed s/-recursive//`; \
+	target=`echo $@ | $(SED) s/-recursive//`; \
 	list='$(SUBDIRS)'; for subdir in $$list; do \
 	  echo "Making $$target in $$subdir"; \
 	  if test "$$subdir" = "."; then \
@@ -367,7 +368,7 @@
 	  fi; \
 	done; \
 	rev="$$rev ."; \
-	target=`echo $@ | sed s/-recursive//`; \
+	target=`echo $@ | $(SED) s/-recursive//`; \
 	for subdir in $$rev; do \
 	  echo "Making $$target in $$subdir"; \
 	  if test "$$subdir" = "."; then \
@@ -431,7 +432,7 @@
 distdir: $(DISTFILES)
 	@list='$(DISTFILES)'; for file in $$list; do \
 	  if test -f $$file || test -d $$file; then d=.; else d=$(srcdir); fi; \
-	  dir=`echo "$$file" | sed -e 's,/[^/]*$$,,'`; \
+	  dir=`echo "$$file" | $(SED) -e 's,/[^/]*$$,,'`; \
 	  if test "$$dir" != "$$file" && test "$$dir" != "."; then \
 	    dir="/$$dir"; \
 	    $(mkinstalldirs) "$(distdir)$$dir"; \
@@ -574,7 +575,7 @@
 # strdup().
 gengetopt_strdup.text: strdup.c
 	echo "/* gengetopt_strdup(): automatically generated from strdup.c. */" > $@
-	cat $< | sed -e 's/^strdup (/gengetopt_&/' \
+	cat $< | $(SED) -e 's/^strdup (/gengetopt_&/' \
                -e 's/^char \* *$$/static &/' \
                -e '/^#include.*$$/D' -e '/^$$/D' >> $@
 # Tell versions [3.59,3.63) of GNU make to not export all variables.
diff -ruN gengetopt-2.8.orig/src/includes/Makefile.in gengetopt-2.8/src/includes/Makefile.in
--- gengetopt-2.8.orig/src/includes/Makefile.in	2002-12-31 05:34:57.000000000 -0700
+++ gengetopt-2.8/src/includes/Makefile.in	2003-01-02 16:29:06.000000000 -0700
@@ -65,6 +65,7 @@
 PATH_SEPARATOR = @PATH_SEPARATOR@
 AMTAR = @AMTAR@
 AWK = @AWK@
+SED = @SED@
 CC = @CC@
 CPP2HTML = @CPP2HTML@
 CXX = @CXX@
@@ -114,7 +115,7 @@
 distdir: $(DISTFILES)
 	@list='$(DISTFILES)'; for file in $$list; do \
 	  if test -f $$file || test -d $$file; then d=.; else d=$(srcdir); fi; \
-	  dir=`echo "$$file" | sed -e 's,/[^/]*$$,,'`; \
+	  dir=`echo "$$file" | $(SED) -e 's,/[^/]*$$,,'`; \
 	  if test "$$dir" != "$$file" && test "$$dir" != "."; then \
 	    dir="/$$dir"; \
 	    $(mkinstalldirs) "$(distdir)$$dir"; \
diff -ruN gengetopt-2.8.orig/src/skels/Makefile.in gengetopt-2.8/src/skels/Makefile.in
--- gengetopt-2.8.orig/src/skels/Makefile.in	2002-12-31 05:35:02.000000000 -0700
+++ gengetopt-2.8/src/skels/Makefile.in	2003-01-02 16:26:33.000000000 -0700
@@ -78,6 +78,7 @@
 PATH_SEPARATOR = @PATH_SEPARATOR@
 AMTAR = @AMTAR@
 AWK = @AWK@
+SED = @SED@
 CC = @CC@
 CPP2HTML = @CPP2HTML@
 CXX = @CXX@
@@ -333,7 +334,7 @@
 distdir: $(DISTFILES)
 	@list='$(DISTFILES)'; for file in $$list; do \
 	  if test -f $$file || test -d $$file; then d=.; else d=$(srcdir); fi; \
-	  dir=`echo "$$file" | sed -e 's,/[^/]*$$,,'`; \
+	  dir=`echo "$$file" | $(SED) -e 's,/[^/]*$$,,'`; \
 	  if test "$$dir" != "$$file" && test "$$dir" != "."; then \
 	    dir="/$$dir"; \
 	    $(mkinstalldirs) "$(distdir)$$dir"; \
diff -ruN gengetopt-2.8.orig/tests/Makefile.in gengetopt-2.8/tests/Makefile.in
--- gengetopt-2.8.orig/tests/Makefile.in	2002-12-31 05:36:32.000000000 -0700
+++ gengetopt-2.8/tests/Makefile.in	2003-01-02 16:26:13.000000000 -0700
@@ -86,6 +86,7 @@
 PATH_SEPARATOR = @PATH_SEPARATOR@
 AMTAR = @AMTAR@
 AWK = @AWK@
+SED = @SED@
 CC = @CC@
 CPP2HTML = @CPP2HTML@
 CXX = @CXX@
@@ -601,7 +602,7 @@
 	      banner="$$failed of $$all tests did not behave as expected ($$xpass unexpected passes)"; \
 	    fi; \
 	  fi; \
-	  dashes=`echo "$$banner" | sed s/./=/g`; \
+	  dashes=`echo "$$banner" | $(SED) s/./=/g`; \
 	  echo "$$dashes"; \
 	  echo "$$banner"; \
 	  echo "$$dashes"; \
@@ -615,7 +616,7 @@
 distdir: $(DISTFILES)
 	@list='$(DISTFILES)'; for file in $$list; do \
 	  if test -f $$file || test -d $$file; then d=.; else d=$(srcdir); fi; \
-	  dir=`echo "$$file" | sed -e 's,/[^/]*$$,,'`; \
+	  dir=`echo "$$file" | $(SED) -e 's,/[^/]*$$,,'`; \
 	  if test "$$dir" != "$$file" && test "$$dir" != "."; then \
 	    dir="/$$dir"; \
 	    $(mkinstalldirs) "$(distdir)$$dir"; \
diff -ruN gengetopt-2.8.orig/txtc.sh.in gengetopt-2.8/txtc.sh.in
--- gengetopt-2.8.orig/txtc.sh.in	2001-03-20 16:16:38.000000000 -0700
+++ gengetopt-2.8/txtc.sh.in	2003-01-02 16:38:47.000000000 -0700
@@ -34,6 +34,7 @@
 
 CC="@CC@"
 AWK="@AWK@"
+SED="@SED@"
 
 # --------------------
 # Initialize the data 
@@ -73,12 +74,12 @@
 for file in $text_files
 do
   # Get the filenames for C, object and pic object files
-  c_file="`echo $file  | sed 's/\.[a-zA-Z]*$/.c/g'  | $AWK -F/ '{ print $NF }'`"
-  o_file="`echo $file  | sed 's/\.[a-zA-Z]*$/.o/g'  | $AWK -F/ '{ print $NF }'`"
-  lo_file="`echo $file | sed 's/\.[a-zA-Z]*$/.lo/g' | $AWK -F/ '{ print $NF }'`"
+  c_file="`echo $file  | $SED 's/\.[a-zA-Z]*$/.c/g'  | $AWK -F/ '{ print $NF }'`"
+  o_file="`echo $file  | $SED 's/\.[a-zA-Z]*$/.o/g'  | $AWK -F/ '{ print $NF }'`"
+  lo_file="`echo $file | $SED 's/\.[a-zA-Z]*$/.lo/g' | $AWK -F/ '{ print $NF }'`"
 
   # Get the symbol name for the file and the length
-  symbol_name="`echo $file | sed 's/\./_/g' | $AWK -F/ '{ print $NF }'`"
+  symbol_name="`echo $file | $SED 's/\./_/g' | $AWK -F/ '{ print $NF }'`"
   file_length="`cat $file | wc -l`"
 
   # Create the C file
@@ -89,7 +90,7 @@
   # Then pipe through awk to make the C program
   # FIXME: perhaps another substitution needed for tabs?
   rm -f $c_file
-  cat $file | sed 's/\\/\\\\/g
+  cat $file | $SED 's/\\/\\\\/g
                    s/\"/\\\"/g
                    s/[[:blank:]]*$//g' | 
               $AWK -v symbol_name="$symbol_name" -v file_length="$file_length" \
