diff -ruN jakarta-ant-1.5/build.xml jakarta-ant-1.5-patched/build.xml
--- jakarta-ant-1.5/build.xml	Tue Jul  9 17:29:12 2002
+++ jakarta-ant-1.5-patched/build.xml	Fri Sep 13 15:10:45 2002
@@ -763,8 +763,6 @@
 
     <chmod perm="ugo+rx" dir="${dist.dir}" type="dir" includes="**"
            failonerror="${chmod.fail}" />
-    <chmod perm="ugo+r" dir="${dist.dir}" type="file" includes="**"
-           failonerror="${chmod.fail}" />
     <chmod perm="ugo+x" type="file" failonerror="${chmod.fail}">
        <fileset dir="${dist.bin}">
          <include name="**/ant" />
@@ -832,8 +830,6 @@
     </copy>
 
     <chmod perm="ugo+rx" dir="${dist.dir}" type="dir" includes="**"
-           failonerror="${chmod.fail}" />
-    <chmod perm="ugo+r" dir="${dist.dir}" type="file" includes="**"
            failonerror="${chmod.fail}" />
     <chmod perm="ugo+x" type="file" failonerror="${chmod.fail}">
        <fileset dir="${dist.bin}">
