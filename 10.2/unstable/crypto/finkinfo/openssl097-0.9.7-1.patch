diff -ru openssl-0.9.7/Makefile openssl-0.9.7-patched/Makefile
--- openssl-0.9.7/Makefile	2002-12-31 01:00:03.000000000 +0100
+++ openssl-0.9.7-patched/Makefile	2003-01-30 17:15:27.000000000 +0100
@@ -287,7 +287,7 @@
 	libs='-L. ${SHLIBDEPS}'; for i in ${SHLIBDIRS}; do \
 	( set -x ; ${CC} --verbose -dynamiclib -o lib$$i${SHLIB_EXT} \
 		lib$$i.a $$libs -all_load -current_version ${SHLIB_MAJOR}.${SHLIB_MINOR} \
-		-compatibility_version ${SHLIB_MAJOR}.`echo ${SHLIB_MINOR} | cut -d. -f1` \
+		-compatibility_version ${SHLIB_MAJOR}.${SHLIB_MINOR} \
 		-install_name ${INSTALLTOP}/lib/lib$$i${SHLIB_EXT} ) || exit 1; \
 	libs="$$libs -l`basename $$i${SHLIB_EXT} .dylib`"; \
 	echo "" ; \
diff -ru openssl-0.9.7/Makefile.org openssl-0.9.7-patched/Makefile.org
--- openssl-0.9.7/Makefile.org	2002-12-30 00:03:16.000000000 +0100
+++ openssl-0.9.7-patched/Makefile.org	2003-01-30 17:15:22.000000000 +0100
@@ -285,7 +285,7 @@
 	libs='-L. ${SHLIBDEPS}'; for i in ${SHLIBDIRS}; do \
 	( set -x ; ${CC} --verbose -dynamiclib -o lib$$i${SHLIB_EXT} \
 		lib$$i.a $$libs -all_load -current_version ${SHLIB_MAJOR}.${SHLIB_MINOR} \
-		-compatibility_version ${SHLIB_MAJOR}.`echo ${SHLIB_MINOR} | cut -d. -f1` \
+		-compatibility_version ${SHLIB_MAJOR}.${SHLIB_MINOR} \
 		-install_name ${INSTALLTOP}/lib/lib$$i${SHLIB_EXT} ) || exit 1; \
 	libs="$$libs -l`basename $$i${SHLIB_EXT} .dylib`"; \
 	echo "" ; \
diff -ru openssl-0.9.7/Makefile.ssl openssl-0.9.7-patched/Makefile.ssl
--- openssl-0.9.7/Makefile.ssl	2002-12-31 01:00:03.000000000 +0100
+++ openssl-0.9.7-patched/Makefile.ssl	2003-01-30 17:15:27.000000000 +0100
@@ -287,7 +287,7 @@
 	libs='-L. ${SHLIBDEPS}'; for i in ${SHLIBDIRS}; do \
 	( set -x ; ${CC} --verbose -dynamiclib -o lib$$i${SHLIB_EXT} \
 		lib$$i.a $$libs -all_load -current_version ${SHLIB_MAJOR}.${SHLIB_MINOR} \
-		-compatibility_version ${SHLIB_MAJOR}.`echo ${SHLIB_MINOR} | cut -d. -f1` \
+		-compatibility_version ${SHLIB_MAJOR}.${SHLIB_MINOR} \
 		-install_name ${INSTALLTOP}/lib/lib$$i${SHLIB_EXT} ) || exit 1; \
 	libs="$$libs -l`basename $$i${SHLIB_EXT} .dylib`"; \
 	echo "" ; \
diff -ru openssl-0.9.7/apps/Makefile openssl-0.9.7-patched/apps/Makefile
--- openssl-0.9.7/apps/Makefile	2002-12-24 11:50:16.000000000 +0100
+++ openssl-0.9.7-patched/apps/Makefile	2003-01-29 14:43:24.000000000 +0100
@@ -31,8 +31,8 @@
 
 DLIBCRYPTO=../libcrypto.a
 DLIBSSL=../libssl.a
-LIBCRYPTO=-L.. -lcrypto
-LIBSSL=-L.. -lssl
+LIBCRYPTO=$(DLIBCRYPTO)
+LIBSSL=$(DLIBSSL)
 
 PROGRAM= openssl
 
diff -ru openssl-0.9.7/apps/Makefile.ssl openssl-0.9.7-patched/apps/Makefile.ssl
--- openssl-0.9.7/apps/Makefile.ssl	2002-12-24 11:50:16.000000000 +0100
+++ openssl-0.9.7-patched/apps/Makefile.ssl	2003-01-29 14:43:24.000000000 +0100
@@ -31,8 +31,8 @@
 
 DLIBCRYPTO=../libcrypto.a
 DLIBSSL=../libssl.a
-LIBCRYPTO=-L.. -lcrypto
-LIBSSL=-L.. -lssl
+LIBCRYPTO=$(DLIBCRYPTO)
+LIBSSL=$(DLIBSSL)
 
 PROGRAM= openssl
 
