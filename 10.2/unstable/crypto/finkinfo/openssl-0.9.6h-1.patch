diff -ru openssl-0.9.6g/Makefile.org openssl-0.9.6g-patched/Makefile.org
--- openssl-0.9.6g/Makefile.org	2002-08-09 13:43:56.000000000 +0200
+++ openssl-0.9.6g-patched/Makefile.org	2002-10-11 12:20:38.000000000 +0200
@@ -281,7 +281,7 @@
 	libs='-L. ${SHLIBDEPS}'; for i in ${SHLIBDIRS}; do \
 	( set -x ; ${CC} --verbose -dynamiclib -o lib$$i${SHLIB_EXT} \
 		lib$$i.a $$libs -all_load -current_version ${SHLIB_MAJOR}.${SHLIB_MINOR} \
-		-compatibility_version ${SHLIB_MAJOR}.`echo ${SHLIB_MINOR} | cut -d. -f1` \
+		-compatibility_version 0.9.6 \
 		-install_name ${INSTALLTOP}/lib/lib$$i${SHLIB_EXT} ) || exit 1; \
 	libs="$$libs -l`basename $$i${SHLIB_EXT} .dylib`"; \
 	echo "" ; \
diff -ru openssl-0.9.6g/Makefile.ssl openssl-0.9.6g-patched/Makefile.ssl
--- openssl-0.9.6g/Makefile.ssl	2002-08-09 13:45:26.000000000 +0200
+++ openssl-0.9.6g-patched/Makefile.ssl	2002-10-11 12:20:38.000000000 +0200
@@ -283,7 +283,7 @@
 	libs='-L. ${SHLIBDEPS}'; for i in ${SHLIBDIRS}; do \
 	( set -x ; ${CC} --verbose -dynamiclib -o lib$$i${SHLIB_EXT} \
 		lib$$i.a $$libs -all_load -current_version ${SHLIB_MAJOR}.${SHLIB_MINOR} \
-		-compatibility_version ${SHLIB_MAJOR}.`echo ${SHLIB_MINOR} | cut -d. -f1` \
+		-compatibility_version 0.9.6 \
 		-install_name ${INSTALLTOP}/lib/lib$$i${SHLIB_EXT} ) || exit 1; \
 	libs="$$libs -l`basename $$i${SHLIB_EXT} .dylib`"; \
 	echo "" ; \
diff -ru openssl-0.9.6g/apps/Makefile.ssl openssl-0.9.6g-patched/apps/Makefile.ssl
--- openssl-0.9.6g/apps/Makefile.ssl	2002-08-08 23:13:36.000000000 +0200
+++ openssl-0.9.6g-patched/apps/Makefile.ssl	2002-10-11 12:22:04.000000000 +0200
@@ -26,8 +26,8 @@
 
 DLIBCRYPTO=../libcrypto.a
 DLIBSSL=../libssl.a
-LIBCRYPTO=-L.. -lcrypto
-LIBSSL=-L.. -lssl
+LIBCRYPTO=$(DLIBCRYPTO)
+LIBSSL=$(DLIBSSL)
 
 PROGRAM= openssl
 
